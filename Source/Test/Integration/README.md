# Source/Test/Integration/

Tests d'**intégration** : interaction correcte entre plusieurs modules assemblés.

Exemples : `Core` ↔ `Elements` (chargement de niveaux), `Core` ↔ `HMI` (état affiché conforme à l'état de jeu).
