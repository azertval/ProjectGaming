# Elements/Audio/

Bruitages (`LOT-60`). Pas de musique (hors MVP, cf. `vision.md`).

- `sounds.json` : catalogue événement → fichier, lu par `hmi::SoundCatalog`.
- `*.wav` : PCM 16 bits, courts, libres de droit (CC0, Kenney — voir `CREDITS.md`).
- `CREDITS.md` : auteur, source et licence de chaque fichier.

Consommés par `HMI/Audio`. Assets binaires — candidats à Git LFS.
