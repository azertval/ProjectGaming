# Core/Resources/

Gestion des ressources par **handle** (`ResourceManager`), chargement à la demande.

Fait le lien entre les assets de `Source/Elements` et la simulation (niveaux, données).

À venir. Réf. specs : `EX-ARCH-080`.
