# Core/Level/

Modèle de niveau, chargement et validation.

- Format **hybride ASCII + JSON** (grille de tuiles + mécanismes).
- Validation (entrée/sortie présentes, dimensions cohérentes, liaisons valides).

À venir. Dépendance à prévoir : parseur JSON (ex. `nlohmann/json`) dans `External/`.
Réf. specs : `EX-LVL-001`…`EX-LVL-004`.
