# Core/Diagnostics/

Outils transverses de diagnostic.

- Assertions projet : `PROJECTGAMING_ASSERT` (active en Debug, signale un bug).
- Journalisation : niveaux `trace` / `info` / `warning` / `error` (pas de `std::cout` en production).

À venir. Réf. : guide de conventions §10.
