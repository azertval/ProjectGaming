# Modules de code source
#
# Core     : logique / moteur (bibliothèque)
# HMI      : présentation + exécutable du jeu
# Elements : assets statiques (non compilés)

add_subdirectory(Core)
add_subdirectory(HMI)
