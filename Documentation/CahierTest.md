# Cahier de test {#cahiertest}

**535 cas de test**, générés depuis les blocs `\castest{...}` du code par `scripts/generate_cahier_test.py` (ne pas éditer directement — modifier le commentaire du test concerné puis relancer le script). Organisés ici selon l'arborescence de `Source/Test/` pour rester lisibles page par page.

## Tests unitaires (453)

### Core

**`test_core.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **EngineTest.VersionNonVide** (Majeur)<br/><sub>`Source/Test/Unit/Core/test_core.cpp:12`</sub> | Vérifie que la version du moteur n'est pas vide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `engine.version().empty()` est faux. |

#### Diagnostics (14)

**`test_assert.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **AssertTest.ConditionVraieNInvoquePasLeHandler** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_assert.cpp:14`</sub> | Une condition vraie n'invoque pas le gestionnaire d'assertion. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `invoked` est faux. |
| **AssertTest.ConditionFausseInvoqueLeHandler** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_assert.cpp:37`</sub> | Une condition fausse invoque le gestionnaire une fois, avec le message (Debug uniquement). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `count` vaut `1`.<br/>Vérifie que `capturedMessage` vaut `"echec attendu"`. |

**`test_log_format.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LogFormatTest.LigneContientTousLesChamps** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_log_format.cpp:14`</sub> | La ligne formatée contient horodatage, niveau, catégorie, position source et message. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `line.find("12:34:56")` diffère de `std::string::npos`.<br/>Vérifie que `line.find("WARNING")` diffère de `std::string::npos`.<br/>Vérifie que `line.find("HMI")` diffère de `std::string::npos`.<br/>Vérifie que `line.find("Foo.cpp:42")` diffère de `std::string::npos`.<br/>Vérifie que `line.find("message test")` diffère de `std::string::npos`. |
| **LogFormatTest.CheminReduitAuNomDeFichier** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_log_format.cpp:37`</sub> | Le chemin source est réduit à son nom de fichier dans la ligne. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `line.find("Foo.cpp:7")` diffère de `std::string::npos`.<br/>Vérifie que `line.find("ProjectGaming")` vaut `std::string::npos`.<br/>Vérifie que `line.find("Source")` vaut `std::string::npos`. |
| **LogFormatTest.FileNameIsoleLeNom** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_log_format.cpp:57`</sub> | fileName isole le nom de fichier des chemins Windows et POSIX, ou renvoie l'entrée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::fileName("a/b/c/File.cpp")` vaut `"File.cpp"`.<br/>Vérifie que `core::fileName("a\\\\b\\\\File.cpp")` vaut `"File.cpp"`.<br/>Vérifie que `core::fileName("File.cpp")` vaut `"File.cpp"`. |
| **LogFormatTest.HorodatageFormatHeure** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_log_format.cpp:74`</sub> | L'horodatage courant respecte le format HH:MM:SS (longueur 8). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `timestamp.size()` vaut `8u`.<br/>Vérifie que `timestamp[2]` vaut `':'`.<br/>Vérifie que `timestamp[5]` vaut `':'`. |

**`test_log_level_parse.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LogLevelParseTest.NiveauxReconnus** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_log_level_parse.cpp:12`</sub> | Les noms de niveaux reconnus sont convertis (y compris l'alias « warn »). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::parseLogLevel("trace")` vaut `core::LogLevel::Trace`.<br/>Vérifie que `core::parseLogLevel("info")` vaut `core::LogLevel::Info`.<br/>Vérifie que `core::parseLogLevel("warning")` vaut `core::LogLevel::Warning`.<br/>Vérifie que `core::parseLogLevel("warn")` vaut `core::LogLevel::Warning`.<br/>Vérifie que `core::parseLogLevel("error")` vaut `core::LogLevel::Error`. |
| **LogLevelParseTest.InsensibleALaCasse** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_log_level_parse.cpp:30`</sub> | L'analyse est insensible à la casse. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::parseLogLevel("INFO")` vaut `core::LogLevel::Info`.<br/>Vérifie que `core::parseLogLevel("Warning")` vaut `core::LogLevel::Warning`.<br/>Vérifie que `core::parseLogLevel("Error")` vaut `core::LogLevel::Error`. |
| **LogLevelParseTest.ValeurInconnue** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_log_level_parse.cpp:46`</sub> | Une valeur inconnue ou vide n'est pas convertie. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::parseLogLevel("verbose").has_value()` est faux.<br/>Vérifie que `core::parseLogLevel("").has_value()` est faux. |

**`test_logger.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LoggerTest.FiltreParNiveauMinimal** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_logger.cpp:15`</sub> | Un message au-dessus du niveau minimal est diffusé ; en dessous, il est filtré. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `observed->entries().size()` vaut `1u`.<br/>Vérifie que `observed->entries()[0].level` vaut `core::LogLevel::Error`.<br/>Vérifie que `observed->entries()[0].message` vaut `"garde"`. |
| **LoggerTest.DiffuseAPlusieursSinks** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_logger.cpp:41`</sub> | Le même message atteint tous les sinks enregistrés. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `firstObserved->entries().size()` vaut `1u`.<br/>Vérifie que `secondObserved->entries().size()` vaut `1u`. |
| **LoggerTest.ClearSinksArreteLaDiffusion** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_logger.cpp:66`</sub> | clearSinks retire les destinations : plus rien n'est diffusé ensuite. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `observed->entries().empty()` est vrai. |

**`test_sinks.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **MemoryLogSinkTest.ConserveNiveauEtTexteDansLOrdre** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_sinks.cpp:12`</sub> | Le sink mémoire conserve fidèlement niveau et texte, dans l'ordre. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `sink.entries().size()` vaut `2u`.<br/>Vérifie que `sink.entries()[0].level` vaut `core::LogLevel::Info`.<br/>Vérifie que `sink.entries()[0].message` vaut `"premier"`.<br/>Vérifie que `sink.entries()[1].level` vaut `core::LogLevel::Error`.<br/>Vérifie que `sink.entries()[1].message` vaut `"second"`. |
| **MemoryLogSinkTest.ClearVideLesMessages** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_sinks.cpp:34`</sub> | clear vide les messages mémorisés. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `sink.entries().empty()` est vrai. |

#### Ecs (34)

**`test_component_pool.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **ComponentPoolTest.AjoutPuisAcces** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_component_pool.cpp:25`</sub> | `add` puis `get` renvoie la valeur stockée ; `has` est cohérent. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `pool.has(entity)` est faux.<br/>Vérifie que `pool.has(entity)` est vrai.<br/>Vérifie que `pool.get(entity).x` vaut `3`.<br/>Vérifie que `pool.get(entity).y` vaut `4`.<br/>Vérifie que `pool.size()` vaut `1u`. |
| **ComponentPoolTest.GetRenvoieReferenceModifiable** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_component_pool.cpp:49`</sub> | `get` renvoie une référence modifiable sur le composant stocké. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `pool.get(entity).x` vaut `42`. |
| **ComponentPoolTest.RemoveAuMilieuSwapAndPop** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_component_pool.cpp:71`</sub> | `remove` d'un élément au milieu (swap-and-pop) laisse les autres composants accessibles et corrects, et le stockage dense reste contigu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `pool.has(middle)` est faux.<br/>Vérifie que `pool.has(first)` est vrai.<br/>Vérifie que `pool.has(last)` est vrai.<br/>Vérifie que `pool.get(first)` vaut `10`.<br/>Vérifie que `pool.get(last)` vaut `30`.<br/>Vérifie que `pool.size()` vaut `2u`.<br/>Vérifie que `pool.components().size()` vaut `2u`.<br/>Vérifie que `pool.entities().size()` vaut `2u`. |
| **ComponentPoolTest.RemoveDernierElement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_component_pool.cpp:107`</sub> | Retirer le dernier élément ne perturbe pas les précédents. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `pool.has(a)` est vrai.<br/>Vérifie que `pool.has(b)` est faux.<br/>Vérifie que `pool.get(a)` vaut `1`.<br/>Vérifie que `pool.size()` vaut `1u`. |
| **ComponentPoolTest.HandlePerimeNePossedePasLeComposant** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_component_pool.cpp:134`</sub> | Un handle périmé (index recyclé, génération différente) ne possède pas le composant de l'ancienne entité. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `pool.has(original)` est vrai.<br/>Vérifie que `pool.has(recycled)` est faux. |
| **ComponentPoolTest.RemoveIfPresent** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_component_pool.cpp:161`</sub> | `removeIfPresent` retire si le composant existe, sinon ne fait rien. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `pool.removeIfPresent(entity)` est faux.<br/>Vérifie que `pool.removeIfPresent(entity)` est vrai.<br/>Vérifie que `pool.has(entity)` est faux. |
| **ComponentPoolTest.GetSurEntiteAbsenteViolePrecondition** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_component_pool.cpp:182`</sub> | `get` sur une entité absente viole une précondition (assertion en Debug). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que l'opération lève bien une exception `std::runtime_error`. |

**`test_entity_manager.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **EntityManagerTest.CreeEntitesVivantesEtDistinctes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_entity_manager.cpp:13`</sub> | `create` renvoie des entités vivantes et distinctes. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `manager.isAlive(a)` est vrai.<br/>Vérifie que `manager.isAlive(b)` est vrai.<br/>Vérifie que `a` diffère de `b`.<br/>Vérifie que `manager.aliveCount()` vaut `2u`. |
| **EntityManagerTest.DestructionInvalideLeHandle** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_entity_manager.cpp:34`</sub> | Après destruction, l'ancien handle n'est plus vivant. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `manager.isAlive(entity)` est faux.<br/>Vérifie que `manager.aliveCount()` vaut `0u`. |
| **EntityManagerTest.RecyclageChangeLaGeneration** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_entity_manager.cpp:55`</sub> | Un index recyclé produit une génération différente : l'ancien handle reste invalide, le nouveau est valide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `recycled.index` vaut `first.index`.<br/>Vérifie que `recycled.generation` diffère de `first.generation`.<br/>Vérifie que `manager.isAlive(first)` est faux.<br/>Vérifie que `manager.isAlive(recycled)` est vrai. |
| **EntityManagerTest.DestructionHandlePerimeSansEffet** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_entity_manager.cpp:83`</sub> | Détruire un handle périmé est sans effet (idempotent) et ne touche pas l'entité vivante qui occupe désormais le même index. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `manager.isAlive(live)` est vrai.<br/>Vérifie que `manager.aliveCount()` vaut `1u`. |
| **EntityManagerTest.EntiteInvalideJamaisVivante** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_entity_manager.cpp:107`</sub> | L'entité invalide conventionnelle n'est jamais vivante. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `manager.isAlive(core::INVALID_ENTITY)` est faux.<br/>Vérifie que `manager.isAlive(created)` est vrai.<br/>Vérifie que `manager.isAlive(core::INVALID_ENTITY)` est faux. |
| **EntityManagerTest.EgaliteHandleInvalide** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_entity_manager.cpp:125`</sub> | Le handle invalide se compare comme tel. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::INVALID_ENTITY` vaut `core::INVALID_ENTITY`.<br/>Vérifie que `core::INVALID_ENTITY.index` vaut `core::Entity::INVALID_INDEX`. |

**`test_player_components.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **PlayerComponentsTest.ColliderParDefautEstNul** (Mineur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_player_components.cpp:17`</sub> | Collider par défaut nul | 1. Construire un `core::Collider` par défaut.<br/>2. Lire `size.x` et `size.y`. | Vérifie que `collider.size.x` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `collider.size.y` vaut `0.0f` (comparaison flottante). |
| **PlayerComponentsTest.PlayerParDefautPasAuSol** (Mineur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_player_components.cpp:31`</sub> | Player par défaut (en l'air, budgets illimités) | 1. Construire un `core::Player` par défaut.<br/>2. Lire ses champs. | Vérifie que `player.grounded` est faux.<br/>Vérifie que `player.coyoteTimer` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `player.jumpBufferTimer` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `player.airJumpsRemaining` vaut `0`.<br/>Vérifie que `player.facing` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `player.wallDirection` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `player.wallJumpLockTimer` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `player.dashAvailable` est faux.<br/>Vérifie que `player.dashTimer` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `player.jumpsRemaining` vaut `-1`.<br/>Vérifie que `player.dashesRemaining` vaut `-1`.<br/>Vérifie que `player.mass` vaut `1.0f` (comparaison flottante). |
| **PlayerComponentsTest.PlayerInputParDefautImmobile** (Mineur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_player_components.cpp:56`</sub> | PlayerInput par défaut neutre | 1. Construire un `core::PlayerInput` par défaut.<br/>2. Lire ses champs. | Vérifie que `input.moveX` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `input.moveY` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `input.jumpPressed` est faux.<br/>Vérifie que `input.jumpHeld` est faux.<br/>Vérifie que `input.dashPressed` est faux. |
| **PlayerComponentsTest.PhysicsConfigParDefautPlausible** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_player_components.cpp:73`</sub> | PhysicsConfig par défaut plausible | 1. Construire un `core::PhysicsConfig` par défaut.<br/>2. Vérifier chaque réglage. | Vérifie que `config.moveSpeed` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.gravity` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.fallDragCoefficient` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.jumpSpeed` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.coyoteTime` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.jumpBufferTime` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.jumpCutFactor` est supérieur ou égal à `0.0f`.<br/>Vérifie que `config.jumpCutFactor` est inférieur ou égal à `1.0f`.<br/>Vérifie que `config.airJumps` est supérieur ou égal à `1`.<br/>Vérifie que `config.wallSlideSpeed` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.wallJumpSpeedX` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.wallJumpSpeedY` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.dashSpeed` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.dashDuration` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.fallGravityMultiplier` est strictement supérieur à `1.0f`.<br/>Vérifie que `config.apexThreshold` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.apexGravityMultiplier` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.apexGravityMultiplier` est strictement inférieur à `1.0f`.<br/>Vérifie que `config.fastFallMultiplier` est strictement supérieur à `1.0f`. |
| **PlayerComponentsTest.TailleEtSpawnHumanoide** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_player_components.cpp:108`</sub> | Taille humanoïde et spawn centré | 1. Lire `playerSize()`.<br/>2. Calculer `playerSpawnPosition(3, 5)`. | Vérifie que `core::playerSize().x` vaut `0.4f` (comparaison flottante).<br/>Vérifie que `core::playerSize().y` vaut `0.8f` (comparaison flottante).<br/>Vérifie que `spawn.x` vaut `3.3f` (comparaison flottante).<br/>Vérifie que `spawn.y` vaut `5.1f` (comparaison flottante). |
| **PlayerComponentsTest.AggregationRenseigneLesChamps** (Mineur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_player_components.cpp:126`</sub> | Agrégation des composants | 1. Initialiser `Collider` et `PlayerInput` par accolades.<br/>2. Lire les champs. | Vérifie que `collider.size.x` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `collider.size.y` vaut `3.0f` (comparaison flottante).<br/>Vérifie que `input.moveX` vaut `-1.0f` (comparaison flottante). |

**`test_sprite.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **SpriteTest.ValeursParDefaut** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_sprite.cpp:12`</sub> | Un sprite par défaut a une teinte blanche opaque, la couche 0 et une région nulle. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `sprite.layer` vaut `0`.<br/>Vérifie que `sprite.region.x` vaut `0`.<br/>Vérifie que `sprite.region.y` vaut `0`.<br/>Vérifie que `sprite.region.width` vaut `0`.<br/>Vérifie que `sprite.region.height` vaut `0`.<br/>Vérifie que `sprite.tint.r` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `sprite.tint.g` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `sprite.tint.b` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `sprite.tint.a` vaut `1.0f` (comparaison flottante). |
| **SpriteTest.ChampsAssignables** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_sprite.cpp:37`</sub> | Les champs sont librement assignables (donnée pure). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `sprite.region.x` vaut `16`.<br/>Vérifie que `sprite.region.y` vaut `32`.<br/>Vérifie que `sprite.region.width` vaut `16`.<br/>Vérifie que `sprite.region.height` vaut `16`.<br/>Vérifie que `sprite.layer` vaut `2`.<br/>Vérifie que `sprite.tint.g` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `sprite.tint.a` vaut `0.8f` (comparaison flottante). |
| **SpriteTest.CopieValeur** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_sprite.cpp:62`</sub> | Le composant est utilisable comme un composant d'ECS (stockage/copie de données pures). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `copy.layer` vaut `5`.<br/>Vérifie que `copy.region.width` vaut `16`.<br/>Vérifie que `copy.tint.r` vaut `0.2f` (comparaison flottante). |

**`test_view.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **ViewTest.SelectionneUniquementLIntersection** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_view.cpp:27`</sub> | Une vue <A, B> itère exactement les entités possédant A et B. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `visited.size()` vaut `1u`.<br/>Vérifie que `visited.front()` vaut `both`. |
| **ViewTest.ComposantsCorrespondentALEntite** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_view.cpp:58`</sub> | Les composants fournis par la vue correspondent bien à l'entité itérée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `velocity.value` vaut `position.value * 10`.<br/>Vérifie que `position.value` vaut `1`.<br/>Vérifie que `position.value` vaut `2`. |
| **ViewTest.ModificationViaVueEstVisible** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_view.cpp:92`</sub> | La modification d'un composant via la vue est visible ensuite (référence). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `positions.get(entity).value` vaut `8`. |
| **ViewTest.VueVideNIterePas** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_view.cpp:118`</sub> | Une vue sans entité correspondante s'itère sans erreur (aucune visite). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `visits` vaut `0`. |
| **ViewTest.IterationForEtEachCoherentes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_view.cpp:145`</sub> | L'itération par `for` visite les mêmes entités que `each`. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `fromEach.size()` vaut `2u`.<br/>Vérifie que `fromFor` vaut `fromEach`. |

**`test_world.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **WorldTest.CycleComposant** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_world.cpp:53`</sub> | Le cycle add/has/get/remove d'un composant est cohérent via le `World`. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.hasComponent<Health>(entity)` est faux.<br/>Vérifie que `world.hasComponent<Health>(entity)` est vrai.<br/>Vérifie que `world.getComponent<Health>(entity).value` vaut `100`.<br/>Vérifie que `world.getComponent<Health>(entity).value` vaut `60`.<br/>Vérifie que `world.hasComponent<Health>(entity)` est faux. |
| **WorldTest.HasComponentSansPool** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_world.cpp:80`</sub> | `hasComponent` est faux quand aucune pool du type n'existe encore. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.hasComponent<Health>(entity)` est faux. |
| **WorldTest.DestroyEntityRetireTousLesComposants** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_world.cpp:96`</sub> | `destroyEntity` retire l'entité de toutes les pools et la rend non vivante. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.isAlive(entity)` est faux.<br/>Vérifie que `world.hasComponent<Health>(entity)` est faux.<br/>Vérifie que `world.hasComponent<Position>(entity)` est faux. |
| **WorldTest.SystemesExecutesDansLOrdre** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_world.cpp:119`</sub> | Les systèmes enregistrés s'exécutent dans l'ordre d'enregistrement. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.systemCount()` vaut `3u`.<br/>Vérifie que `log` vaut `(std::vector<int>{1, 2, 3})`. |
| **WorldTest.UpdateNFoisExecuteNFois** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_world.cpp:142`</sub> | `update` appelé N fois exécute chaque système N fois (cadencement déterministe). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `log.size()` vaut `static_cast<std::size_t>(calls)`. |
| **WorldTest.FixedDeltaTransmisAuxSystemes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_world.cpp:166`</sub> | Le `fixedDelta` passé à `update` est transmis tel quel aux systèmes. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `observed` vaut `0.25f` (comparaison flottante). |
| **WorldTest.ViewViaWorld** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_world.cpp:186`</sub> | La vue exposée par le `World` itère l'intersection des composants. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `visited.size()` vaut `1u`.<br/>Vérifie que `visited.front()` vaut `both`. |

#### Gameplay (29)

**`test_block_controller.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **BlockControllerTest.PositionInitiale** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:42`</sub> | À la construction, le bloc est à sa position du niveau, et cette case est solide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions().size()` vaut `1u`.<br/>Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 1})`.<br/>Vérifie que `controller.collisionMap(level.tileMap()).isSolid(2, 1)` est vrai. |
| **BlockControllerTest.PousseVersLaDroiteSiLibre** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:63`</sub> | Toucher un bloc à droite en se déplaçant vers la droite le pousse d'une case, si la case suivante est libre. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions().size()` vaut `1u`.<br/>Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{3, 1})`. |
| **BlockControllerTest.PousseeRefuseeContreUnMur** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:86`</sub> | Un bloc ne peut pas être poussé à travers un mur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 1})`. |
| **BlockControllerTest.PousseeRefuseeContreUnAutreBloc** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:112`</sub> | Un bloc ne peut pas être poussé sur un autre bloc. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 1})`.<br/>Vérifie que `controller.positions()[1]` vaut `(core::GridPosition{3, 1})`. |
| **BlockControllerTest.ContactSansIntentionNePousseRien** (Mineur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:139`</sub> | Sans intention de déplacement horizontal, un bloc touché ne bouge pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 1})`. |
| **BlockControllerTest.BlocSoutenuNeTombePas** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:158`</sub> | Un bloc posé sur le sol, non poussé, ne tombe jamais. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 1})`. |
| **BlockControllerTest.BlocNonSoutenuTombe** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:179`</sub> | Un bloc sans support tombe d'une case toutes les FALL_INTERVAL_STEPS mises à jour. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 0})`.<br/>Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 1})`.<br/>Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 1})`. |
| **BlockControllerTest.CollisionMapEffaceLAncienneCase** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:216`</sub> | collisionMap efface la case d'origine d'un bloc déplacé (pas de mur fantôme). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `collision.isSolid(2, 1)` est faux.<br/>Vérifie que `collision.isSolid(3, 1)` est vrai. |
| **BlockControllerTest.ReconnaitLesTroisFacteursDeTaille** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:239`</sub> | Les trois types de bloc sont reconnus avec le facteur de taille attendu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions().size()` vaut `3u`.<br/>Vérifie que `controller.scales().size()` vaut `3u`.<br/>Vérifie que `controller.scales()[0]` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `controller.scales()[1]` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `controller.scales()[2]` vaut `0.25f` (comparaison flottante). |
| **BlockControllerTest.BoxAtCentreEtReduitSelonLaTaille** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:270`</sub> | boxAt renvoie une boîte centrée et réduite pour un bloc réduit, pleine case pour un bloc plein. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `fullBox.min.x` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `fullBox.min.y` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `fullBox.max.x` vaut `3.0f` (comparaison flottante).<br/>Vérifie que `fullBox.max.y` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `halfBox.min.x` vaut `3.25f` (comparaison flottante).<br/>Vérifie que `halfBox.min.y` vaut `1.25f` (comparaison flottante).<br/>Vérifie que `halfBox.max.x` vaut `3.75f` (comparaison flottante).<br/>Vérifie que `halfBox.max.y` vaut `1.75f` (comparaison flottante). |
| **BlockControllerTest.CollisionMapNeSolidifiePasLesBlocsReduits** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:304`</sub> | collisionMap ne rend jamais solide la case d'un bloc réduit, à la différence d'un bloc plein. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `collision.isSolid(1, 1)` est vrai.<br/>Vérifie que `collision.isSolid(2, 1)` est faux.<br/>Vérifie que `collision.isSolid(3, 1)` est faux. |
| **BlockControllerTest.BlocReduitPousseSelonSaBoiteReelle** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:333`</sub> | Un bloc réduit se pousse comme un bloc plein, mais le contact se teste contre sa boîte réduite, pas la case entière. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 1})`.<br/>Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{3, 1})`. |
| **BlockControllerTest.BlocNeTombePasATraversUnePente** (Bloquant)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:370`</sub> | Un bloc suspendu au-dessus d'une pente ne tombe pas au travers. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 0})`. |
| **BlockControllerTest.PousseeRefuseeContreUnePente** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:403`</sub> | Un bloc ne peut pas être poussé sur une pente. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 1})`. |

**`test_danger_controller.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **DangerControllerTest.MobileDemarreASaPositionDeDepart** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_danger_controller.cpp:44`</sub> | Au chargement, un danger mobile est à sa position de départ. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.moverCount()` vaut `1u`.<br/>Vérifie que `box.min.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `box.min.y` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `box.max.x` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `box.max.y` vaut `2.0f` (comparaison flottante). |
| **DangerControllerTest.MobileHorizontalAllerRetourDeterministe** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_danger_controller.cpp:65`</sub> | Un danger mobile horizontal fait un aller-retour déterministe. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.moverBox(0).min.x` vaut `2.0f`, à `1e-4f` près.<br/>Vérifie que `controller.moverBox(0).min.x` vaut `3.0f`, à `1e-4f` près.<br/>Vérifie que `controller.moverBox(0).min.x` vaut `2.0f`, à `1e-4f` près.<br/>Vérifie que `controller.moverBox(0).min.x` vaut `1.0f`, à `1e-4f` près.<br/>Vérifie que `controller.moverBox(0).min.y` vaut `1.0f` (comparaison flottante). |
| **DangerControllerTest.MobileVerticalDeplaceLaLigne** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_danger_controller.cpp:100`</sub> | Un danger mobile vertical déplace sa ligne, jamais sa colonne. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `box.min.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `box.min.y` vaut `3.0f`, à `1e-4f` près. |
| **DangerControllerTest.PorteeNulleImmobiliseLeMobile** (Mineur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_danger_controller.cpp:120`</sub> | Une portée nulle immobilise le danger mobile. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `box.min.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `box.min.y` vaut `1.0f` (comparaison flottante). |
| **DangerControllerTest.TemporiseAlterneSelonSaPeriode** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_danger_controller.cpp:141`</sub> | Un danger temporisé alterne actif/inactif selon sa période. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.isBlinkActive(position)` est vrai.<br/>Vérifie que `controller.isBlinkActive(position)` est vrai.<br/>Vérifie que `controller.isBlinkActive(position)` est faux.<br/>Vérifie que `controller.isBlinkActive(position)` est faux.<br/>Vérifie que `controller.isBlinkActive(position)` est vrai. |
| **DangerControllerTest.DephasageDecaleLeMotif** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_danger_controller.cpp:167`</sub> | Un déphasage décale le motif actif/inactif. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.isBlinkActive(position)` est faux.<br/>Vérifie que `controller.isBlinkActive(position)` est vrai. |
| **DangerControllerTest.TemporiseFauxSansConfiguration** (Mineur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_danger_controller.cpp:187`</sub> | isBlinkActive renvoie faux pour une position sans configuration. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.isBlinkActive(core::GridPosition{0, 0})` est faux. |

**`test_mechanism_controller.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **MechanismControllerTest.ContactOuvreLaPorte** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_mechanism_controller.cpp:75`</sub> | Au départ, la porte est **fermée** (solide) ; toucher l'interrupteur l'**ouvre**. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.collisionMap().isSolid(4, 1)` est vrai.<br/>Vérifie que `controller.isDoorOpen(0)` est faux.<br/>Vérifie que `controller.collisionMap().isSolid(4, 1)` est faux.<br/>Vérifie que `controller.isDoorOpen(0)` est vrai. |
| **MechanismControllerTest.BasculeSurFront** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_mechanism_controller.cpp:97`</sub> | La bascule est **sur front** : rester sur l'interrupteur ne re-bascule pas ; revenir bascule. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.isDoorOpen(0)` est vrai.<br/>Vérifie que `controller.isDoorOpen(0)` est vrai.<br/>Vérifie que `controller.isDoorOpen(0)` est vrai.<br/>Vérifie que `controller.isDoorOpen(0)` est faux.<br/>Vérifie que `controller.collisionMap().isSolid(4, 1)` est vrai. |
| **MechanismControllerTest.SansContactRienNeChange** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_mechanism_controller.cpp:124`</sub> | Loin de l'interrupteur, rien ne change (la porte reste fermée). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.collisionMap().isSolid(4, 1)` est vrai.<br/>Vérifie que `controller.isDoorOpen(0)` est faux. |
| **MechanismControllerTest.PlaqueDePressionActivationContinue** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_mechanism_controller.cpp:142`</sub> | Une plaque de pression ouvre la porte tant que le poids y repose, et la referme dès qu'il en part. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.collisionMap().isSolid(4, 1)` est vrai.<br/>Vérifie que `controller.isDoorOpen(0)` est vrai.<br/>Vérifie que `controller.collisionMap().isSolid(4, 1)` est faux.<br/>Vérifie que `controller.isDoorOpen(0)` est vrai.<br/>Vérifie que `controller.isDoorOpen(0)` est faux.<br/>Vérifie que `controller.collisionMap().isSolid(4, 1)` est vrai.<br/>Vérifie que `controller.isDoorOpen(0)` est vrai. |
| **MechanismControllerTest.PlaqueDePressionPoidsInsuffisant** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_mechanism_controller.cpp:173`</sub> | Un poids insuffisant sur la plaque de pression n'ouvre pas la porte. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.isDoorOpen(0)` est faux.<br/>Vérifie que `controller.collisionMap().isSolid(4, 1)` est vrai. |
| **MechanismControllerTest.DangerCommuteActiveParInterrupteur** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_mechanism_controller.cpp:191`</sub> | Un danger commuté devient actif au contact de son interrupteur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.isDangerActive(core::GridPosition{4, 1})` est faux.<br/>Vérifie que `controller.isDangerActive(core::GridPosition{4, 1})` est vrai.<br/>Vérifie que `controller.isDangerActive(core::GridPosition{4, 1})` est vrai.<br/>Vérifie que `controller.isDangerActive(core::GridPosition{4, 1})` est vrai.<br/>Vérifie que `controller.isDangerActive(core::GridPosition{4, 1})` est faux. |
| **MechanismControllerTest.DangerCommuteActivationContinuePlaqueDePression** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_mechanism_controller.cpp:219`</sub> | Un danger commuté lié à une plaque de pression est actif tant que le poids y repose. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.isDangerActive(core::GridPosition{4, 1})` est vrai.<br/>Vérifie que `controller.isDangerActive(core::GridPosition{4, 1})` est faux. |
| **MechanismControllerTest.DangerActiveFauxSansLiaison** (Mineur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_mechanism_controller.cpp:241`</sub> | isDangerActive renvoie faux pour une position sans liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.isDangerActive(core::GridPosition{4, 1})` est faux. |

#### Levels (115)

**`test_danger_geometry.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **DangerGeometryTest.DangerClassiqueOccupeLaCaseEntiere** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_danger_geometry.cpp:13`</sub> | Danger classique occupe toute la case. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `box.min.x` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `box.min.y` vaut `3.0f` (comparaison flottante).<br/>Vérifie que `box.max.x` vaut `3.0f` (comparaison flottante).<br/>Vérifie que `box.max.y` vaut `4.0f` (comparaison flottante). |
| **DangerGeometryTest.DangerUpBordeLeHaut** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_danger_geometry.cpp:32`</sub> | DangerUp réduit sa zone mortelle au bord haut. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `box.min.x` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `box.max.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `box.min.y` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `box.max.y` vaut `core::kDangerEdgeThickness` (comparaison flottante). |
| **DangerGeometryTest.DangerDownBordeLeBas** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_danger_geometry.cpp:51`</sub> | DangerDown réduit sa zone mortelle au bord bas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `box.min.x` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `box.max.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `box.min.y` vaut `1.0f - core::kDangerEdgeThickness` (comparaison flottante).<br/>Vérifie que `box.max.y` vaut `1.0f` (comparaison flottante). |
| **DangerGeometryTest.DangerLeftBordeLaGauche** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_danger_geometry.cpp:70`</sub> | DangerLeft réduit sa zone mortelle au bord gauche. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `box.min.y` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `box.max.y` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `box.min.x` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `box.max.x` vaut `core::kDangerEdgeThickness` (comparaison flottante). |
| **DangerGeometryTest.DangerRightBordeLaDroite** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_danger_geometry.cpp:89`</sub> | DangerRight réduit sa zone mortelle au bord droit. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `box.min.y` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `box.max.y` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `box.min.x` vaut `1.0f - core::kDangerEdgeThickness` (comparaison flottante).<br/>Vérifie que `box.max.x` vaut `1.0f` (comparaison flottante). |
| **DangerGeometryTest.DangersTemporelsOccupentLaCaseEntiere** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_danger_geometry.cpp:108`</sub> | Les dangers temporels occupent toute la case. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `box.min.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `box.min.y` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `box.max.x` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `box.max.y` vaut `3.0f` (comparaison flottante). |

**`test_level.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **TileMapTest.GrilleNeuveVideAuxBonnesDimensions** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level.cpp:14`</sub> | Une grille neuve a les bonnes dimensions et n'est composée que de cases Empty. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `map.width()` vaut `4`.<br/>Vérifie que `map.height()` vaut `3`.<br/>Vérifie que `map.tile(column, row)` vaut `core::TileType::Empty`. |
| **TileMapTest.EcritureLectureDUneTuile** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level.cpp:37`</sub> | Écrire puis lire une tuile restitue le type posé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `map.tile(2, 1)` vaut `core::TileType::Solid`.<br/>Vérifie que `map.tile(0, 0)` vaut `core::TileType::Empty`. |
| **TileMapTest.Bornes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level.cpp:55`</sub> | Les bornes de la grille sont correctement détectées. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `map.inBounds(0, 0)` est vrai.<br/>Vérifie que `map.inBounds(3, 2)` est vrai.<br/>Vérifie que `map.inBounds(4, 2)` est faux.<br/>Vérifie que `map.inBounds(3, 3)` est faux.<br/>Vérifie que `map.inBounds(-1, 0)` est faux. |
| **TileMapTest.Solidite** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level.cpp:75`</sub> | Seules les tuiles solides bloquent statiquement. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `map.isSolid(0, 0)` est vrai.<br/>Vérifie que `map.isSolid(1, 0)` est faux.<br/>Vérifie que `map.isSolid(2, 0)` est faux. |
| **TileMapTest.IsSolidParType** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level.cpp:96`</sub> | `isSolid(TileType)` : vrai seulement pour Solid. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::isSolid(core::TileType::Solid)` est vrai.<br/>Vérifie que `core::isSolid(core::TileType::Empty)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::Door)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::Danger)` est faux. |
| **TileMapTest.IsSolidFauxPourLesDangersAvances** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level.cpp:114`</sub> | Les sept nouvelles variantes de danger ne sont jamais solides. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::isSolid(core::TileType::DangerUp)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::DangerDown)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::DangerLeft)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::DangerRight)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::DangerMover)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::DangerSwitched)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::DangerBlink)` est faux. |
| **LevelTest.RestitueSesComposantes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level.cpp:134`</sub> | Un Level restitue ses composantes (nom, grille, entrée/sortie, mécanismes). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `level.name()` vaut `"Tutoriel"`.<br/>Vérifie que `level.tileMap().width()` vaut `5`.<br/>Vérifie que `level.entry()` vaut `(core::GridPosition{1, 1})`.<br/>Vérifie que `level.exit()` vaut `(core::GridPosition{3, 2})`.<br/>Vérifie que `level.mechanisms().size()` vaut `1u`.<br/>Vérifie que `level.mechanisms().front().switchPosition` vaut `(core::GridPosition{2, 1})`.<br/>Vérifie que `level.mechanisms().front().doorPosition` vaut `(core::GridPosition{4, 2})`. |
| **LevelTest.RestitueSesDangersAvances** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level.cpp:165`</sub> | Un Level restitue ses liaisons et configurations de dangers avancés. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `level.dangerLinks().size()` vaut `1u`.<br/>Vérifie que `level.dangerLinks().front().triggerPosition` vaut `(core::GridPosition{0, 0})`.<br/>Vérifie que `level.dangerLinks().front().dangerPosition` vaut `(core::GridPosition{4, 0})`.<br/>Vérifie que `level.moverConfigs().size()` vaut `1u`.<br/>Vérifie que `level.moverConfigs().front().axis` vaut `core::DangerMoverAxis::Vertical`.<br/>Vérifie que `level.moverConfigs().front().range` vaut `3`.<br/>Vérifie que `level.blinkConfigs().size()` vaut `1u`.<br/>Vérifie que `level.blinkConfigs().front().period` vaut `90`.<br/>Vérifie que `level.blinkConfigs().front().phase` vaut `10`.<br/>Vérifie que `level.blinkConfigs().front().activeDuration` vaut `30`. |

**`test_level_draft.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LevelDraftTest.BrouillonVierge** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:23`</sub> | Un brouillon vierge a une grille entièrement vide, sans entrée ni sortie. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.name()` vaut `"Nouveau"`.<br/>Vérifie que `draft.tileMap().width()` vaut `5`.<br/>Vérifie que `draft.tileMap().height()` vaut `4`.<br/>Vérifie que `draft.entry().has_value()` est faux.<br/>Vérifie que `draft.exit().has_value()` est faux.<br/>Vérifie que `draft.mechanisms().empty()` est vrai.<br/>Vérifie que `draft.jumpBudget()` vaut `-1`.<br/>Vérifie que `draft.dashBudget()` vaut `-1`.<br/>Vérifie que `draft.tileMap().tile(0, 0)` vaut `TileType::Empty`. |
| **LevelDraftTest.PaintTilePoseLeType** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:46`</sub> | paintTile pose le type demandé sur la case visée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Solid`. |
| **LevelDraftTest.SetEntryDeplaceLEntreeExistante** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:62`</sub> | Poser une seconde entrée déplace la première (unicité, EX-EDIT-004). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.entry().has_value()` est vrai.<br/>Vérifie que `*draft.entry()` vaut `(GridPosition{0, 0})`.<br/>Vérifie que `*draft.entry()` vaut `(GridPosition{2, 2})`.<br/>Vérifie que `draft.tileMap().tile(0, 0)` vaut `TileType::Empty`.<br/>Vérifie que `draft.tileMap().tile(2, 2)` vaut `TileType::Entry`. |
| **LevelDraftTest.SetExitDeplaceLaSortieExistante** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:84`</sub> | Poser une seconde sortie déplace la première (unicité, EX-EDIT-004). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `*draft.exit()` vaut `(GridPosition{3, 3})`.<br/>Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Empty`.<br/>Vérifie que `draft.tileMap().tile(3, 3)` vaut `TileType::Exit`. |
| **LevelDraftTest.PeindrePardessusLEntreeLInvalide** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:103`</sub> | Peindre par-dessus l'entrée invalide la position d'entrée mémorisée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.entry().has_value()` est faux.<br/>Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Solid`. |
| **LevelDraftTest.LierPuisDelierUnMecanisme** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:121`</sub> | Lier un interrupteur à une porte crée un mécanisme ; le délier le retire. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.mechanisms().size()` vaut `1u`.<br/>Vérifie que `draft.mechanisms().front().switchPosition` vaut `(GridPosition{0, 0})`.<br/>Vérifie que `draft.mechanisms().front().doorPosition` vaut `(GridPosition{3, 3})`.<br/>Vérifie que `draft.mechanisms().empty()` est vrai. |
| **LevelDraftTest.LierUnePlaqueDePression** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:146`</sub> | Lier une plaque de pression à une porte crée un mécanisme. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.mechanisms().size()` vaut `1u`.<br/>Vérifie que `draft.mechanisms().front().switchPosition` vaut `(GridPosition{0, 0})`.<br/>Vérifie que `draft.mechanisms().front().doorPosition` vaut `(GridPosition{3, 3})`. |
| **LevelDraftTest.RelierUnePorteRemplaceLaLiaisonPrecedente** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:167`</sub> | Relier une porte déjà liée remplace la liaison précédente (une porte, un interrupteur). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.mechanisms().size()` vaut `1u`.<br/>Vérifie que `draft.mechanisms().front().switchPosition` vaut `(GridPosition{1, 0})`. |
| **LevelDraftTest.UnInterrupteurPeutOuvrirPlusieursPortes** (Mineur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:192`</sub> | Un interrupteur peut ouvrir plusieurs portes. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.mechanisms().size()` vaut `2u`. |
| **LevelDraftTest.PeindrePardessusUnInterrupteurRetireSesLiaisons** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:214`</sub> | Peindre par-dessus un interrupteur retire les liaisons qui le référencent. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.mechanisms().empty()` est vrai. |
| **LevelDraftTest.AgrandirConserveLeContenu** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:234`</sub> | Agrandir la grille conserve le contenu existant et complète en cases vides. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.tileMap().width()` vaut `5`.<br/>Vérifie que `draft.tileMap().height()` vaut `5`.<br/>Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Entry`.<br/>Vérifie que `*draft.entry()` vaut `(GridPosition{1, 1})`.<br/>Vérifie que `draft.tileMap().tile(4, 4)` vaut `TileType::Empty`. |
| **LevelDraftTest.ReduireTronqueEtInvalideLEntreePerdue** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:257`</sub> | Réduire la grille tronque le contenu hors bornes et invalide l'entrée perdue. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.tileMap().width()` vaut `3`.<br/>Vérifie que `draft.tileMap().height()` vaut `3`.<br/>Vérifie que `draft.entry().has_value()` est faux.<br/>Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Solid`. |
| **LevelDraftTest.ReduireRetireLesMecanismesHorsBornes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:281`</sub> | Réduire la grille retire les mécanismes dont une extrémité sort des bornes. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.mechanisms().empty()` est vrai. |
| **LevelDraftTest.ToLevelSurBrouillonValideReussit** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:302`</sub> | toLevel() sur un brouillon complet et valide produit un niveau conforme. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.level->name()` vaut `"Niveau test"`.<br/>Vérifie que `result.level->entry()` vaut `(GridPosition{0, 0})`.<br/>Vérifie que `result.level->exit()` vaut `(GridPosition{3, 3})`.<br/>Vérifie que `result.level->jumpBudget()` vaut `2`. |
| **LevelDraftTest.ToLevelSansSortieEchoueProprement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:326`</sub> | toLevel() sur un brouillon sans sortie échoue avec un message récupérable (EX-EDIT-007). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.error.empty()` est faux. |
| **LevelDraftTest.FromLevelRestitueLeContenu** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:347`</sub> | Un brouillon reconstruit depuis un niveau existant restitue son contenu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `draft.name()` vaut `"Depart"`.<br/>Vérifie que `*draft.entry()` vaut `(GridPosition{0, 0})`.<br/>Vérifie que `*draft.exit()` vaut `(GridPosition{2, 2})`. |
| **LevelDraftTest.BrouillonNeufSansHistorique** (Mineur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:369`</sub> | Un brouillon neuf ne peut ni annuler ni refaire. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.canUndo()` est faux.<br/>Vérifie que `draft.canRedo()` est faux. |
| **LevelDraftTest.UndoApresPeintureRestitueLEtatPrecedent** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:385`</sub> | undo() après une peinture restitue l'état exact précédent. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.canUndo()` est vrai.<br/>Vérifie que `undone` est vrai.<br/>Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Empty`.<br/>Vérifie que `draft.canUndo()` est faux.<br/>Vérifie que `draft.canRedo()` est vrai. |
| **LevelDraftTest.RedoApresUndoRestitueLEtatMute** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:408`</sub> | redo() après un undo() restitue l'état muté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `redone` est vrai.<br/>Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Solid`.<br/>Vérifie que `draft.canUndo()` est vrai.<br/>Vérifie que `draft.canRedo()` est faux. |
| **LevelDraftTest.SequenceDeMutationsPuisUndoRestitueLEtatInitial** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:431`</sub> | Une séquence de N mutations suivie de N undo() restitue l'état initial exact. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.canUndo()` est faux.<br/>Vérifie que `draft.tileMap().tile(0, 0)` vaut `TileType::Empty`.<br/>Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Empty`.<br/>Vérifie que `draft.tileMap().tile(2, 2)` vaut `TileType::Empty`.<br/>Vérifie que `draft.tileMap().tile(4, 4)` vaut `TileType::Empty`.<br/>Vérifie que `draft.entry().has_value()` est faux.<br/>Vérifie que `draft.exit().has_value()` est faux. |
| **LevelDraftTest.MutationApresUndoInvalideLeRefaire** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:463`</sub> | Une nouvelle mutation après un undo() invalide la branche de refaire. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.canRedo()` est vrai.<br/>Vérifie que `draft.canRedo()` est faux.<br/>Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Danger`. |
| **LevelDraftTest.UndoRedoSurPileVideSansEffet** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:485`</sub> | undo()/redo() sur une pile vide est sans effet (pas de plantage). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.undo()` est faux.<br/>Vérifie que `draft.redo()` est faux.<br/>Vérifie que `draft.tileMap().tile(0, 0)` vaut `TileType::Empty`. |
| **LevelDraftTest.UndoApresLiaisonMecanismeRestitueLAbsenceDeLiaison** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:502`</sub> | L'annulation d'une liaison de mécanisme restitue la liaison précédente. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.mechanisms().size()` vaut `1u`.<br/>Vérifie que `draft.mechanisms().empty()` est vrai. |
| **LevelDraftTest.PaintRegionAppliqueLeBlocEntier** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:524`</sub> | paintRegion applique un bloc homogène comme une succession de paintTile équivalente. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Solid`.<br/>Vérifie que `draft.tileMap().tile(2, 1)` vaut `TileType::Solid`.<br/>Vérifie que `draft.tileMap().tile(1, 2)` vaut `TileType::Solid`.<br/>Vérifie que `draft.tileMap().tile(2, 2)` vaut `TileType::Solid`.<br/>Vérifie que `draft.tileMap().tile(0, 0)` vaut `TileType::Empty`. |
| **LevelDraftTest.PaintRegionUnSeulSnapshotUndo** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:550`</sub> | paintRegion ne pousse qu'un seul snapshot undo pour tout le bloc. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.canUndo()` est faux.<br/>Vérifie que `draft.tileMap().tile(0, 0)` vaut `TileType::Empty`.<br/>Vérifie que `draft.tileMap().tile(1, 0)` vaut `TileType::Empty`.<br/>Vérifie que `draft.tileMap().tile(2, 0)` vaut `TileType::Empty`. |
| **LevelDraftTest.PaintRegionDecoupeAuxBords** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:574`</sub> | paintRegion découpe silencieusement le bloc aux bords de la grille. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.tileMap().tile(1, 2)` vaut `TileType::Solid`.<br/>Vérifie que `draft.tileMap().tile(2, 2)` vaut `TileType::Solid`. |
| **LevelDraftTest.PaintRegionDeplaceLEntree** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:597`</sub> | paintRegion qui inclut une position d'entrée déplace l'entrée existante. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.tileMap().tile(0, 0)` vaut `TileType::Empty`.<br/>Vérifie que `*draft.entry()` vaut `(GridPosition{2, 2})`. |
| **LevelDraftTest.PaintRegionRetireLesLiaisonsRecouvertes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:617`</sub> | paintRegion qui recouvre un interrupteur retire les liaisons qui le référencent. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.mechanisms().empty()` est vrai. |
| **LevelDraftTest.PaintRegionBlocVideSansEffet** (Mineur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:640`</sub> | paintRegion avec un bloc vide est sans effet. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.canUndo()` est faux. |
| **LevelDraftTest.WouldResizeDropContentDetecteLaPerte** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:656`</sub> | wouldResizeDropContent détecte la perte de l'entrée, de la sortie ou d'une liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.wouldResizeDropContent(3, 3)` est vrai.<br/>Vérifie que `draft.wouldResizeDropContent(2, 5)` est vrai.<br/>Vérifie que `draft.wouldResizeDropContent(10, 10)` est faux.<br/>Vérifie que `draft.wouldResizeDropContent(5, 5)` est faux. |
| **LevelDraftTest.WouldResizeDropContentFauxSurBrouillonVierge** (Mineur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:685`</sub> | wouldResizeDropContent est faux sur un brouillon vierge, quelle que soit la taille visee. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.wouldResizeDropContent(1, 1)` est faux. |
| **LevelDraftTest.UndoApresRedimensionnementRestitueLesDimensions** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:702`</sub> | L'annulation d'un redimensionnement restitue les dimensions et le contenu précédents. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.entry().has_value()` est faux.<br/>Vérifie que `draft.tileMap().width()` vaut `5`.<br/>Vérifie que `draft.tileMap().height()` vaut `5`.<br/>Vérifie que `draft.entry().has_value()` est vrai.<br/>Vérifie que `*draft.entry()` vaut `(GridPosition{4, 4})`. |
| **LevelDraftTest.PaintTilePoseUnDangerDirectionnel** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:729`</sub> | Peindre un danger directionnel pose le type demandé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::DangerUp`. |
| **LevelDraftTest.LierUnDangerCommute** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:746`</sub> | Lier un interrupteur à un danger commuté crée une liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.mechanisms().empty()` est vrai.<br/>Vérifie que `draft.dangerLinks().size()` vaut `1u`.<br/>Vérifie que `draft.dangerLinks().front().triggerPosition` vaut `(GridPosition{0, 0})`.<br/>Vérifie que `draft.dangerLinks().front().dangerPosition` vaut `(GridPosition{3, 3})`. |
| **LevelDraftTest.DelierUnDangerCommute** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:769`</sub> | Délier un danger commuté retire sa liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.dangerLinks().empty()` est vrai. |
| **LevelDraftTest.PeindrePardessusUnDangerCommuteRetireSaLiaison** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:791`</sub> | Peindre par-dessus un danger commuté lié retire sa liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.dangerLinks().empty()` est vrai. |
| **LevelDraftTest.SetMoverConfigDefinitLaConfiguration** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:812`</sub> | setMoverConfig définit l'axe et la portée d'un danger mobile. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.moverConfigs().size()` vaut `1u`.<br/>Vérifie que `draft.moverConfigs().front().axis` vaut `core::DangerMoverAxis::Vertical`.<br/>Vérifie que `draft.moverConfigs().front().range` vaut `3`. |
| **LevelDraftTest.PeindrePardessusUnDangerMobileRetireSaConfiguration** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:833`</sub> | Peindre par-dessus un danger mobile configuré retire sa configuration. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.moverConfigs().empty()` est vrai. |
| **LevelDraftTest.SetBlinkConfigDefinitLaConfiguration** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:854`</sub> | setBlinkConfig définit la configuration d'un danger temporisé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.blinkConfigs().size()` vaut `1u`.<br/>Vérifie que `draft.blinkConfigs().front().period` vaut `90`.<br/>Vérifie que `draft.blinkConfigs().front().phase` vaut `15`.<br/>Vérifie que `draft.blinkConfigs().front().activeDuration` vaut `30`. |
| **LevelDraftTest.FromLevelRestitueLesDangersAvances** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:878`</sub> | fromLevel restitue les dangers avancés d'un niveau chargé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `draft.dangerLinks().size()` vaut `1u`.<br/>Vérifie que `draft.dangerLinks().front().dangerPosition` vaut `(GridPosition{2, 1})`.<br/>Vérifie que `draft.moverConfigs().size()` vaut `1u`.<br/>Vérifie que `draft.moverConfigs().front().range` vaut `1`.<br/>Vérifie que `draft.blinkConfigs().size()` vaut `1u`.<br/>Vérifie que `draft.blinkConfigs().front().period` vaut `90`. |
| **LevelDraftTest.ReduireRetireLesDangersAvancesHorsBornes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:914`</sub> | Réduire la grille retire les dangers avancés hors bornes. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.dangerLinks().empty()` est vrai.<br/>Vérifie que `draft.moverConfigs().empty()` est vrai.<br/>Vérifie que `draft.blinkConfigs().empty()` est vrai. |

**`test_level_loader.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LevelLoaderTest.ChargeUnNiveauValide** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:33`</sub> | Un niveau valide est chargé avec ses dimensions, tuiles, entrée/sortie et mécanismes. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::None`.<br/>Vérifie que `level.name()` vaut `"Tutoriel"`.<br/>Vérifie que `level.tileMap().width()` vaut `4`.<br/>Vérifie que `level.tileMap().height()` vaut `3`.<br/>Vérifie que `level.tileMap().tile(0, 0)` vaut `core::TileType::Solid`.<br/>Vérifie que `level.tileMap().tile(1, 1)` vaut `core::TileType::Entry`.<br/>Vérifie que `level.entry()` vaut `(core::GridPosition{1, 1})`.<br/>Vérifie que `level.exit()` vaut `(core::GridPosition{3, 2})`.<br/>Vérifie que `level.mechanisms().size()` vaut `1u`.<br/>Vérifie que `level.mechanisms().front().switchPosition` vaut `(core::GridPosition{2, 0})`.<br/>Vérifie que `level.mechanisms().front().doorPosition` vaut `(core::GridPosition{3, 0})`. |
| **LevelLoaderTest.ChargeUnePlaqueDePression** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:64`</sub> | Une plaque de pression se charge comme un interrupteur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(2, 0)` vaut `core::TileType::PressurePlate`.<br/>Vérifie que `level.mechanisms().size()` vaut `1u`.<br/>Vérifie que `level.mechanisms().front().switchPosition` vaut `(core::GridPosition{2, 0})`.<br/>Vérifie que `level.mechanisms().front().doorPosition` vaut `(core::GridPosition{3, 0})`. |
| **LevelLoaderTest.ChargeUnBlocPoussable** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:98`</sub> | Un bloc poussable se charge comme une simple tuile, sans identifiant ni liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(2, 0)` vaut `core::TileType::Block`.<br/>Vérifie que `level.mechanisms().empty()` est vrai. |
| **LevelLoaderTest.ChargeLesBlocsATailleReduite** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:129`</sub> | Les deux blocs à taille réduite se chargent comme de simples tuiles, sans identifiant ni liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(0, 0)` vaut `core::TileType::BlockHalf`.<br/>Vérifie que `level.tileMap().tile(1, 0)` vaut `core::TileType::BlockQuarter`.<br/>Vérifie que `level.mechanisms().empty()` est vrai. |
| **LevelLoaderTest.ChargeLesDeuxPentes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:163`</sub> | Les deux orientations de pente se chargent comme de simples tuiles, sans identifiant ni liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(0, 0)` vaut `core::TileType::SlopeUpRight`.<br/>Vérifie que `level.tileMap().tile(1, 0)` vaut `core::TileType::SlopeUpLeft`.<br/>Vérifie que `level.mechanisms().empty()` est vrai. |
| **LevelLoaderTest.ChargeLesDeuxArrondis** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:197`</sub> | Les deux orientations d'arrondi se chargent comme de simples tuiles, sans identifiant ni liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(0, 0)` vaut `core::TileType::RoundedUpRight`.<br/>Vérifie que `level.tileMap().tile(1, 0)` vaut `core::TileType::RoundedUpLeft`.<br/>Vérifie que `level.mechanisms().empty()` est vrai. |
| **LevelLoaderTest.ChargeLesQuatrePentesEtArrondisDePlafond** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:231`</sub> | Les quatre pentes/arrondis de plafond se chargent comme de simples tuiles, sans identifiant ni liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(0, 0)` vaut `core::TileType::SlopeDownRight`.<br/>Vérifie que `level.tileMap().tile(1, 0)` vaut `core::TileType::SlopeDownLeft`.<br/>Vérifie que `level.tileMap().tile(2, 0)` vaut `core::TileType::RoundedDownRight`.<br/>Vérifie que `level.tileMap().tile(3, 0)` vaut `core::TileType::RoundedDownLeft`.<br/>Vérifie que `level.mechanisms().empty()` est vrai. |
| **LevelLoaderTest.ChargeLesQuatreArrondisConcaves** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:269`</sub> | Les quatre arrondis concaves se chargent comme de simples tuiles, sans identifiant ni liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(0, 0)` vaut `core::TileType::ConcaveUpRight`.<br/>Vérifie que `level.tileMap().tile(1, 0)` vaut `core::TileType::ConcaveUpLeft`.<br/>Vérifie que `level.tileMap().tile(2, 0)` vaut `core::TileType::ConcaveDownRight`.<br/>Vérifie que `level.tileMap().tile(3, 0)` vaut `core::TileType::ConcaveDownLeft`.<br/>Vérifie que `level.mechanisms().empty()` est vrai. |
| **LevelLoaderTest.BudgetsOptionnels** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:307`</sub> | Les budgets de mouvements (EX-GP-024) sont chargés s'ils sont présents, illimités (-1) sinon. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `withBudget.ok()` est vrai.<br/>Vérifie que `withBudget.level->jumpBudget()` vaut `2`.<br/>Vérifie que `withBudget.level->dashBudget()` vaut `1`.<br/>Vérifie que `noBudget.ok()` est vrai.<br/>Vérifie que `noBudget.level->jumpBudget()` vaut `-1`.<br/>Vérifie que `noBudget.level->dashBudget()` vaut `-1`. |
| **LevelLoaderTest.JsonMalformeRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:335`</sub> | Un JSON syntaxiquement invalide est rejeté sans plantage. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.error.empty()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::ParseError`. |
| **LevelLoaderTest.ChampManquantRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:352`</sub> | Un champ obligatoire manquant est rejeté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::ParseError`. |
| **LevelLoaderTest.TypeDeTuileInconnuRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:369`</sub> | Un type de tuile inconnu est rejeté avec un message. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.error.empty()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::UnknownTileType`. |
| **LevelLoaderTest.TuileHorsBornesRejetee** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:387`</sub> | Une tuile hors des bornes est rejetée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::OutOfBounds`. |
| **LevelLoaderTest.LiaisonMecanismeNonResolueRejetee** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:404`</sub> | Une porte liée à un interrupteur inexistant est rejetée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.error.empty()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::UnresolvedMechanism`. |
| **LevelLoaderTest.PlusieursEntreesRejetees** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:428`</sub> | Plusieurs entrées sont rejetées (une seule attendue). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::InvalidEntryCount`. |
| **LevelLoaderTest.PlusieursSortiesRejetees** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:451`</sub> | Plusieurs sorties sont rejetées (une seule attendue). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::InvalidExitCount`. |
| **LevelLoaderTest.PositionEnDoubleRejetee** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:474`</sub> | Deux tuiles à la même position sont rejetées. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::DuplicatePosition`. |
| **LevelLoaderTest.TilesNonListeRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:498`</sub> | Un champ 'tiles' qui n'est pas une liste est rejeté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::ParseError`. |
| **LevelLoaderTest.DimensionsNonPositivesRejetees** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:515`</sub> | Des dimensions non positives sont rejetées. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::ParseError`. |
| **LevelLoaderTest.InterrupteurSansIdRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:532`</sub> | Un interrupteur sans 'id' est rejeté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::MissingSwitchId`. |
| **LevelLoaderTest.IdentifiantInterrupteurEnDoubleRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:555`</sub> | Deux interrupteurs avec le même identifiant sont rejetés. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::DuplicateSwitchId`. |
| **LevelLoaderTest.NiveauSansEntreeRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:579`</sub> | Un niveau sans entrée est rejeté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::InvalidEntryCount`. |
| **LevelLoaderTest.NiveauSansSortieRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:596`</sub> | Un niveau sans sortie est rejeté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::InvalidExitCount`. |
| **LevelLoaderTest.FichierIntrouvableRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:613`</sub> | Charger un fichier inexistant échoue proprement (récupérable). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.error.empty()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::FileNotFound`. |
| **LevelLoaderTest.PorteSansLiaisonEstValide** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:631`</sub> | Une porte sans 'opensWith' est une simple tuile : chargement valide, aucun mécanisme. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.level->mechanisms().empty()` est vrai. |
| **LevelLoaderTest.NiveauDeDemoLivreValide** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:655`</sub> | Le niveau de démonstration livré (Source/Elements/Levels) se charge et se valide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai. |
| **LevelLoaderTest.ChargeLesQuatreDangersDirectionnels** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:674`</sub> | Les quatre dangers directionnels se chargent comme de simples tuiles, sans identifiant ni liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(0, 0)` vaut `core::TileType::DangerUp`.<br/>Vérifie que `level.tileMap().tile(1, 0)` vaut `core::TileType::DangerDown`.<br/>Vérifie que `level.tileMap().tile(2, 0)` vaut `core::TileType::DangerLeft`.<br/>Vérifie que `level.tileMap().tile(3, 0)` vaut `core::TileType::DangerRight`.<br/>Vérifie que `level.mechanisms().empty()` est vrai. |
| **LevelLoaderTest.ChargeUnDangerMobileValeursParDefaut** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:712`</sub> | Un danger mobile sans champs explicites reçoit les valeurs par défaut. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(1, 1)` vaut `core::TileType::DangerMover`.<br/>Vérifie que `level.moverConfigs().size()` vaut `1u`.<br/>Vérifie que `level.moverConfigs().front().startPosition` vaut `(core::GridPosition{1, 1})`.<br/>Vérifie que `level.moverConfigs().front().axis` vaut `core::DangerMoverAxis::Horizontal`.<br/>Vérifie que `level.moverConfigs().front().range` vaut `2`. |
| **LevelLoaderTest.ChargeUnDangerMobileValeursExplicites** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:742`</sub> | Un danger mobile avec axe et portée explicites les préserve. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.level->moverConfigs().size()` vaut `1u`.<br/>Vérifie que `result.level->moverConfigs().front().axis` vaut `core::DangerMoverAxis::Vertical`.<br/>Vérifie que `result.level->moverConfigs().front().range` vaut `3`. |
| **LevelLoaderTest.PorteeDangerMobileHorsBornesRejetee** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:769`</sub> | Une portée de danger mobile qui sortirait de la grille est rejetée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::OutOfBounds`. |
| **LevelLoaderTest.ChargeUnDangerCommuteLie** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:794`</sub> | Un danger commuté lié à un interrupteur est résolu au chargement. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(3, 0)` vaut `core::TileType::DangerSwitched`.<br/>Vérifie que `level.mechanisms().empty()` est vrai.<br/>Vérifie que `level.dangerLinks().size()` vaut `1u`.<br/>Vérifie que `level.dangerLinks().front().triggerPosition` vaut `(core::GridPosition{2, 0})`.<br/>Vérifie que `level.dangerLinks().front().dangerPosition` vaut `(core::GridPosition{3, 0})`. |
| **LevelLoaderTest.DangerCommuteSansLiaisonEstValide** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:826`</sub> | Un danger commuté sans 'opensWith' est une simple tuile inerte. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.level->dangerLinks().empty()` est vrai. |
| **LevelLoaderTest.DangerCommuteLieAInterrupteurInexistantRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:851`</sub> | Un danger commuté lié à un interrupteur inexistant est rejeté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::UnresolvedMechanism`. |
| **LevelLoaderTest.ChargeUnDangerTemporiseValeursParDefaut** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:876`</sub> | Un danger temporisé sans champs explicites reçoit les valeurs par défaut. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.level->blinkConfigs().size()` vaut `1u`.<br/>Vérifie que `config.position` vaut `(core::GridPosition{2, 0})`.<br/>Vérifie que `config.period` vaut `120`.<br/>Vérifie que `config.phase` vaut `0`.<br/>Vérifie que `config.activeDuration` vaut `60`. |
| **LevelLoaderTest.ChargeUnDangerTemporiseValeursExplicites** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:908`</sub> | Un danger temporisé avec valeurs explicites les préserve. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.level->blinkConfigs().size()` vaut `1u`.<br/>Vérifie que `config.period` vaut `90`.<br/>Vérifie que `config.phase` vaut `15`.<br/>Vérifie que `config.activeDuration` vaut `30`. |

**`test_level_outcome.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LevelOutcomeTest.ZoneLibreEstEnCours** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_outcome.cpp:36`</sub> | En zone libre (ni sortie, ni danger, dans les limites) : partie en cours. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::evaluateOutcome(unitBox(1.0f, 1.0f), level)` vaut `core::LevelOutcome::Playing`. |
| **LevelOutcomeTest.SurLaSortieEstGagne** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_outcome.cpp:51`</sub> | La boîte recouvrant la case de sortie : niveau gagné. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::evaluateOutcome(unitBox(8.0f, 8.0f), level)` vaut `core::LevelOutcome::Won`. |
| **LevelOutcomeTest.SurUnDangerEstPerdu** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_outcome.cpp:66`</sub> | La boîte recouvrant une tuile Danger : niveau perdu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::evaluateOutcome(unitBox(5.0f, 5.0f), level)` vaut `core::LevelOutcome::Lost`. |
| **LevelOutcomeTest.SousLaLimiteBasseEstPerdu** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_outcome.cpp:81`</sub> | La boîte sous la limite basse (chute dans le vide) : niveau perdu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::evaluateOutcome(unitBox(2.0f, 10.0f), level)` vaut `core::LevelOutcome::Lost`. |
| **LevelOutcomeTest.EchecPrioritaireSurSucces** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_outcome.cpp:96`</sub> | Recouvrement simultané sortie + danger : l'échec est prioritaire (déterminisme). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::evaluateOutcome(unitBox(5.0f, 5.0f), level)` vaut `core::LevelOutcome::Lost`. |
| **LevelOutcomeTest.DangerDirectionnelPerduSeulementSurSaBande** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_outcome.cpp:114`</sub> | Un danger directionnel ne provoque l'échec que sur la bande de son bord désigné. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::evaluateOutcome(safeBox, level)` vaut `core::LevelOutcome::Playing`.<br/>Vérifie que `core::evaluateOutcome(dangerBox, level)` vaut `core::LevelOutcome::Lost`. |
| **LevelOutcomeTest.BoiteSupplementaireProvoqueLEchec** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_outcome.cpp:143`</sub> | Une boîte supplémentaire fournie par l'appelant provoque l'échec. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::evaluateOutcome(unitBox(1.0f, 1.0f), level, extraDangerBoxes)` vaut `core::LevelOutcome::Playing`.<br/>Vérifie que `core::evaluateOutcome(unitBox(3.0f, 3.0f), level, extraDangerBoxes)` vaut `core::LevelOutcome::Lost`. |
| **LevelOutcomeTest.SansBoiteSupplementaireComportementInchange** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_outcome.cpp:164`</sub> | Sans boîte supplémentaire, le comportement est inchangé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::evaluateOutcome(unitBox(5.0f, 5.0f), level)` vaut `core::LevelOutcome::Lost`.<br/>Vérifie que `core::evaluateOutcome(unitBox(5.0f, 5.0f), level, {})` vaut `core::LevelOutcome::Lost`. |

**`test_level_writer.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LevelWriterTest.RoundTripPreserveLeContenu** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:36`</sub> | Sérialiser puis recharger un niveau produit un niveau équivalent (round-trip). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `roundTripped.name()` vaut `original.name()`.<br/>Vérifie que `roundTripped.tileMap().width()` vaut `original.tileMap().width()`.<br/>Vérifie que `roundTripped.tileMap().height()` vaut `original.tileMap().height()`.<br/>Vérifie que `roundTripped.tileMap().tile(column, row)` vaut `original.tileMap().tile(column, row)`.<br/>Vérifie que `roundTripped.entry()` vaut `original.entry()`.<br/>Vérifie que `roundTripped.exit()` vaut `original.exit()`.<br/>Vérifie que `roundTripped.jumpBudget()` vaut `original.jumpBudget()`.<br/>Vérifie que `roundTripped.dashBudget()` vaut `original.dashBudget()`.<br/>Vérifie que `roundTripped.mechanisms().size()` vaut `original.mechanisms().size()`.<br/>Vérifie que `roundTripped.mechanisms().front().switchPosition` vaut `original.mechanisms().front().switchPosition`.<br/>Vérifie que `roundTripped.mechanisms().front().doorPosition` vaut `original.mechanisms().front().doorPosition`. |
| **LevelWriterTest.PlaqueDePressionSurvitAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:81`</sub> | Une plaque de pression survit au round-trip. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(2, 0)` vaut `core::TileType::PressurePlate`.<br/>Vérifie que `reloaded.level->mechanisms().size()` vaut `1u`.<br/>Vérifie que `reloaded.level->mechanisms().front().switchPosition` vaut `(core::GridPosition{2, 0})`.<br/>Vérifie que `reloaded.level->mechanisms().front().doorPosition` vaut `(core::GridPosition{3, 0})`. |
| **LevelWriterTest.BlocPoussableSurvitAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:117`</sub> | Un bloc poussable survit au round-trip. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(2, 0)` vaut `core::TileType::Block`.<br/>Vérifie que `reloaded.level->mechanisms().empty()` est vrai. |
| **LevelWriterTest.BlocsATailleReduiteSurviventAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:150`</sub> | Les deux blocs à taille réduite survivent à un aller-retour JSON. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(0, 0)` vaut `core::TileType::BlockHalf`.<br/>Vérifie que `reloaded.level->tileMap().tile(1, 0)` vaut `core::TileType::BlockQuarter`. |
| **LevelWriterTest.PentesSurviventAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:183`</sub> | Les deux orientations de pente survivent au round-trip. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(0, 0)` vaut `core::TileType::SlopeUpRight`.<br/>Vérifie que `reloaded.level->tileMap().tile(1, 0)` vaut `core::TileType::SlopeUpLeft`. |
| **LevelWriterTest.ArrondisSurviventAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:217`</sub> | Les deux orientations d'arrondi survivent à un aller-retour JSON. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(0, 0)` vaut `core::TileType::RoundedUpRight`.<br/>Vérifie que `reloaded.level->tileMap().tile(1, 0)` vaut `core::TileType::RoundedUpLeft`. |
| **LevelWriterTest.PentesEtArrondisDePlafondSurviventAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:250`</sub> | Les quatre pentes/arrondis de plafond survivent à un aller-retour JSON. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(0, 0)` vaut `core::TileType::SlopeDownRight`.<br/>Vérifie que `reloaded.level->tileMap().tile(1, 0)` vaut `core::TileType::SlopeDownLeft`.<br/>Vérifie que `reloaded.level->tileMap().tile(2, 0)` vaut `core::TileType::RoundedDownRight`.<br/>Vérifie que `reloaded.level->tileMap().tile(3, 0)` vaut `core::TileType::RoundedDownLeft`. |
| **LevelWriterTest.ArrondisConcavesSurviventAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:288`</sub> | Les quatre arrondis concaves survivent à un aller-retour JSON. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(0, 0)` vaut `core::TileType::ConcaveUpRight`.<br/>Vérifie que `reloaded.level->tileMap().tile(1, 0)` vaut `core::TileType::ConcaveUpLeft`.<br/>Vérifie que `reloaded.level->tileMap().tile(2, 0)` vaut `core::TileType::ConcaveDownRight`.<br/>Vérifie que `reloaded.level->tileMap().tile(3, 0)` vaut `core::TileType::ConcaveDownLeft`. |
| **LevelWriterTest.BudgetsIllimitesOmisDuJson** (Mineur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:325`</sub> | Les budgets illimités (-1) ne sont pas écrits dans le JSON produit. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `json.find("jumpBudget")` vaut `std::string::npos`.<br/>Vérifie que `json.find("dashBudget")` vaut `std::string::npos`.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->jumpBudget()` vaut `-1`.<br/>Vérifie que `reloaded.level->dashBudget()` vaut `-1`. |
| **LevelWriterTest.InterrupteurNonRelieRegenereUnIdentifiant** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:351`</sub> | Un interrupteur non relié à une porte est tout de même sérialisé, avec un identifiant. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(1, 1)` vaut `core::TileType::Switch`.<br/>Vérifie que `reloaded.level->mechanisms().empty()` est vrai. |
| **LevelWriterTest.NiveauPuzzleLivreSurvieAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:377`</sub> | Le niveau puzzle livré (demo-interrupteur.json) survit à un round-trip. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->mechanisms().size()` vaut `loaded.level->mechanisms().size()`.<br/>Vérifie que `reloaded.level->entry()` vaut `loaded.level->entry()`.<br/>Vérifie que `reloaded.level->exit()` vaut `loaded.level->exit()`. |
| **LevelWriterTest.SaveToFileEcritUnFichierRechargeable** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:401`</sub> | saveToFile écrit un fichier qui se recharge à l'identique (round-trip disque). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `core::LevelWriter::saveToFile(*loaded.level, path)` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->name()` vaut `loaded.level->name()`.<br/>Vérifie que `reloaded.level->entry()` vaut `loaded.level->entry()`. |
| **LevelWriterTest.SaveToFileVersDossierInexistantEchoueProprement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:428`</sub> | saveToFile vers un dossier inexistant échoue proprement (récupérable, EX-NFR-040). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `core::LevelWriter::saveToFile(*loaded.level, path)` est faux. |
| **LevelWriterTest.DangersDirectionnelsSurviventAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:447`</sub> | Les quatre dangers directionnels survivent à un aller-retour JSON. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(0, 0)` vaut `core::TileType::DangerUp`.<br/>Vérifie que `reloaded.level->tileMap().tile(1, 0)` vaut `core::TileType::DangerDown`.<br/>Vérifie que `reloaded.level->tileMap().tile(2, 0)` vaut `core::TileType::DangerLeft`.<br/>Vérifie que `reloaded.level->tileMap().tile(3, 0)` vaut `core::TileType::DangerRight`. |
| **LevelWriterTest.DangerMobileSurvitAuRoundTripAvecSaConfiguration** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:482`</sub> | Un danger mobile survit au round-trip avec sa configuration. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(1, 1)` vaut `core::TileType::DangerMover`.<br/>Vérifie que `reloaded.level->moverConfigs().size()` vaut `1u`.<br/>Vérifie que `reloaded.level->moverConfigs().front().axis` vaut `core::DangerMoverAxis::Vertical`.<br/>Vérifie que `reloaded.level->moverConfigs().front().range` vaut `3`. |
| **LevelWriterTest.DangerCommuteSurvitAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:514`</sub> | Un danger commuté survit au round-trip avec sa liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(3, 0)` vaut `core::TileType::DangerSwitched`.<br/>Vérifie que `reloaded.level->dangerLinks().size()` vaut `1u`.<br/>Vérifie que `reloaded.level->dangerLinks().front().triggerPosition` vaut `(core::GridPosition{2, 0})`.<br/>Vérifie que `reloaded.level->dangerLinks().front().dangerPosition` vaut `(core::GridPosition{3, 0})`. |
| **LevelWriterTest.DangerTemporiseSurvitAuRoundTripAvecSaConfiguration** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:548`</sub> | Un danger temporisé survit au round-trip avec sa configuration. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->blinkConfigs().size()` vaut `1u`.<br/>Vérifie que `config.period` vaut `90`.<br/>Vérifie que `config.phase` vaut `15`.<br/>Vérifie que `config.activeDuration` vaut `30`. |

#### Math (20)

**`test_math_utils.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **MathUtilsTest.EgaliteExacte** (Mineur)<br/><sub>`Source/Test/Unit/Core/Math/test_math_utils.cpp:12`</sub> | approximatelyEqual : égalité exacte | 1. Comparer des valeurs identiques (1, 0, -2,5). | Vérifie que `core::approximatelyEqual(1.0f, 1.0f)` est vrai.<br/>Vérifie que `core::approximatelyEqual(0.0f, 0.0f)` est vrai.<br/>Vérifie que `core::approximatelyEqual(-2.5f, -2.5f)` est vrai. |
| **MathUtilsTest.ToleranceParDefaut** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_math_utils.cpp:26`</sub> | approximatelyEqual : tolérance par défaut | 1. Comparer 1 et 1+1e-6.<br/>2. Comparer 1 et 1,1. | Vérifie que `core::approximatelyEqual(1.0f, 1.0f + 1e-6f)` est vrai.<br/>Vérifie que `core::approximatelyEqual(1.0f, 1.1f)` est faux. |
| **MathUtilsTest.MiseALEchelleGrandesMagnitudes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_math_utils.cpp:39`</sub> | approximatelyEqual : mise à l'échelle des grandes magnitudes | 1. Comparer 1e6 et 1e6+1.<br/>2. Comparer 1e6 et 1e6+1000. | Vérifie que `core::approximatelyEqual(1.0e6f, 1.0e6f + 1.0f)` est vrai.<br/>Vérifie que `core::approximatelyEqual(1.0e6f, 1.0e6f + 1000.0f)` est faux. |
| **MathUtilsTest.SignesOpposes** (Mineur)<br/><sub>`Source/Test/Unit/Core/Math/test_math_utils.cpp:52`</sub> | approximatelyEqual : signes opposés | 1. Comparer 2 et -2. | Vérifie que `core::approximatelyEqual(2.0f, -2.0f)` est faux. |
| **MathUtilsTest.ToleranceExplicite** (Mineur)<br/><sub>`Source/Test/Unit/Core/Math/test_math_utils.cpp:64`</sub> | approximatelyEqual : tolérance explicite | 1. Comparer 1 et 1,5 sans tolérance.<br/>2. Recomparer avec une tolérance de 1. | Vérifie que `core::approximatelyEqual(1.0f, 1.5f)` est faux.<br/>Vérifie que `core::approximatelyEqual(1.0f, 1.5f, 1.0f)` est vrai. |

**`test_rect.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **RectTest.Bords** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_rect.cpp:17`</sub> | Les bords exposés découlent de la position et de la taille. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `rect.left()` vaut `10.0f`, à `TOLERANCE` près.<br/>Vérifie que `rect.top()` vaut `20.0f`, à `TOLERANCE` près.<br/>Vérifie que `rect.right()` vaut `40.0f`, à `TOLERANCE` près.<br/>Vérifie que `rect.bottom()` vaut `60.0f`, à `TOLERANCE` près. |
| **RectTest.ContientPointInterieur** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_rect.cpp:35`</sub> | Un point strictement intérieur est contenu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `rect.contains(core::Vector2{5.0f, 5.0f})` est vrai. |
| **RectTest.ContientBords** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_rect.cpp:50`</sub> | Contenance inclusive haut/gauche, exclusive bas/droit. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `rect.contains(core::Vector2{0.0f, 0.0f})` est vrai.<br/>Vérifie que `rect.contains(core::Vector2{10.0f, 5.0f})` est faux.<br/>Vérifie que `rect.contains(core::Vector2{5.0f, 10.0f})` est faux. |
| **RectTest.NeContientPasExterieur** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_rect.cpp:67`</sub> | Un point extérieur n'est pas contenu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `rect.contains(core::Vector2{-1.0f, 5.0f})` est faux.<br/>Vérifie que `rect.contains(core::Vector2{5.0f, 100.0f})` est faux. |
| **RectTest.IntersectionRecouvrement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_rect.cpp:83`</sub> | Deux rectangles qui se recouvrent s'intersectent (relation symétrique). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `a.intersects(b)` est vrai.<br/>Vérifie que `b.intersects(a)` est vrai. |
| **RectTest.ContactBordSansIntersection** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_rect.cpp:100`</sub> | Un simple contact par un bord ne compte pas comme intersection. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `a.intersects(b)` est faux. |
| **RectTest.Disjonction** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_rect.cpp:116`</sub> | Deux rectangles disjoints ne s'intersectent pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `a.intersects(b)` est faux. |

**`test_vector2.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **Vector2Test.AdditionSoustraction** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_vector2.cpp:16`</sub> | Vector2 : addition et soustraction | 1. Poser deux vecteurs.<br/>2. Calculer a+b et a-b. | Vérifie que `a + b` vaut `core::Vector2(4.0f, -2.0f)`.<br/>Vérifie que `a - b` vaut `core::Vector2(-2.0f, 6.0f)`. |
| **Vector2Test.EchelleScalaire** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_vector2.cpp:31`</sub> | Vector2 : échelle par un scalaire | 1. Poser un vecteur.<br/>2. Multiplier/diviser par un scalaire, prendre l'opposé. | Vérifie que `v * 2.0f` vaut `core::Vector2(4.0f, -6.0f)`.<br/>Vérifie que `2.0f * v` vaut `core::Vector2(4.0f, -6.0f)`.<br/>Vérifie que `v / 2.0f` vaut `core::Vector2(1.0f, -1.5f)`.<br/>Vérifie que `-v` vaut `core::Vector2(-2.0f, 3.0f)`. |
| **Vector2Test.OperateursComposes** (Mineur)<br/><sub>`Source/Test/Unit/Core/Math/test_vector2.cpp:47`</sub> | Vector2 : opérateurs composés | 1. Appliquer +=, -=, *=, /= à un vecteur. | Vérifie que `v` vaut `core::Vector2(3.0f, 4.0f)`.<br/>Vérifie que `v` vaut `core::Vector2(2.0f, 3.0f)`.<br/>Vérifie que `v` vaut `core::Vector2(4.0f, 6.0f)`.<br/>Vérifie que `v` vaut `core::Vector2(2.0f, 3.0f)`. |
| **Vector2Test.ProduitScalaire** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_vector2.cpp:67`</sub> | Vector2 : produit scalaire | 1. Calculer a·b.<br/>2. Calculer le produit de deux vecteurs orthogonaux. | Vérifie que `a.dot(b)` vaut `11.0f`, à `TOLERANCE` près.<br/>Vérifie que `core::Vector2(1.0f, 0.0f).dot(core::Vector2(0.0f, 1.0f))` vaut `0.0f`, à `TOLERANCE` près. |
| **Vector2Test.Longueur** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_vector2.cpp:83`</sub> | Vector2 : longueur | 1. Calculer `lengthSquared` et `length` de (3, 4). | Vérifie que `v.lengthSquared()` vaut `25.0f`, à `TOLERANCE` près.<br/>Vérifie que `v.length()` vaut `5.0f`, à `TOLERANCE` près. |
| **Vector2Test.NormalisationNonNulle** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_vector2.cpp:97`</sub> | Vector2 : normalisation (non nul) | 1. Normaliser (3, 4). | Vérifie que `normalized.length()` vaut `1.0f`, à `TOLERANCE` près.<br/>Vérifie que `normalized` vaut `core::Vector2(0.6f, 0.8f)`. |
| **Vector2Test.NormalisationVecteurNul** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_vector2.cpp:111`</sub> | Vector2 : normalisation (vecteur nul) | 1. Normaliser le vecteur nul. | Vérifie que `core::Vector2{}.normalized()` vaut `core::Vector2(0.0f, 0.0f)`. |
| **Vector2Test.EgaliteApprochee** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_vector2.cpp:123`</sub> | Vector2 : égalité approchée | 1. Comparer (0,1+0,2 ; 1) à (0,3 ; 1).<br/>2. Comparer deux vecteurs distincts. | Vérifie que `a` vaut `core::Vector2(0.3f, 1.0f)`.<br/>Vérifie que `core::Vector2(0.0f, 0.0f)` diffère de `core::Vector2(1.0f, 0.0f)`. |

#### Physics (39)

**`test_aabb_vs_aabb.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **AabbVsAabbTest.TrajetLibreLoinDeLObstacle** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_aabb_vs_aabb.cpp:29`</sub> | Balayage boîte-boîte : trajet libre, loin de l'obstacle | 1. Placer un obstacle réduit en (2, 1).<br/>2. Balayer une boîte 1×1 partie de loin, dont le trajet ne le croise pas. | Vérifie que `r.hit` est faux.<br/>Vérifie que `r.position.x` vaut `11.0f` (comparaison flottante).<br/>Vérifie que `r.position.y` vaut `11.0f` (comparaison flottante). |
| **AabbVsAabbTest.ButeeHorizontaleContreUnObstacleReduit** (Bloquant)<br/><sub>`Source/Test/Unit/Core/Physics/test_aabb_vs_aabb.cpp:48`</sub> | Balayage boîte-boîte : butée horizontale contre un obstacle réduit | 1. Placer un obstacle réduit (0,5×0,5) centré en (2, 1).<br/>2. Balayer une boîte 1×1 vers la droite, alignée verticalement avec l'obstacle. | Vérifie que `r.hit` est vrai.<br/>Vérifie que `r.position.x` vaut `1.25f` (comparaison flottante).<br/>Vérifie que `r.position.y` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `r.normal.x` vaut `-1.0f` (comparaison flottante). |
| **AabbVsAabbTest.ButeeVerticaleSurUnObstacleReduit** (Bloquant)<br/><sub>`Source/Test/Unit/Core/Physics/test_aabb_vs_aabb.cpp:71`</sub> | Balayage boîte-boîte : butée verticale (dessus d'un obstacle réduit) | 1. Placer un obstacle réduit centré en (2, 1).<br/>2. Balayer une boîte 1×1 en chute verticale au-dessus. | Vérifie que `r.hit` est vrai.<br/>Vérifie que `r.position.y` vaut `0.25f` (comparaison flottante).<br/>Vérifie que `r.normal.y` vaut `-1.0f` (comparaison flottante). |
| **AabbVsAabbTest.EspaceAutourDeLObstacleReduitEstFranchissable** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_aabb_vs_aabb.cpp:93`</sub> | Balayage boîte-boîte : le vide autour d'un obstacle réduit reste franchissable | 1. Placer un obstacle réduit centré en (2, 1) (marge de 0,25 de chaque côté).<br/>2. Balayer une boîte étroite qui passe dans cette marge, sans jamais chevaucher l'obstacle. | Vérifie que `r.hit` est faux.<br/>Vérifie que `r.position.x` vaut `3.5f` (comparaison flottante). |
| **AabbVsAabbTest.GlissementDiagonalLeLongDUnObstacleReduit** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_aabb_vs_aabb.cpp:116`</sub> | Balayage boîte-boîte : glissement diagonal contre un obstacle réduit | 1. Placer un obstacle réduit centré en (2, 1).<br/>2. Balayer une boîte en diagonale (droite + bas), bloquée en X par l'obstacle mais pas en Y. | Vérifie que `r.normal.x` vaut `-1.0f` (comparaison flottante).<br/>Vérifie que `r.position.x` vaut `1.25f` (comparaison flottante).<br/>Vérifie que `r.normal.y` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `r.position.y` vaut `4.3f` (comparaison flottante). |
| **AabbVsAabbTest.DeplacementNulNeBougeRien** (Mineur)<br/><sub>`Source/Test/Unit/Core/Physics/test_aabb_vs_aabb.cpp:142`</sub> | Balayage boîte-boîte : déplacement nul sur un axe le laisse inchangé | 1. Placer un obstacle réduit.<br/>2. Balayer avec un déplacement nul sur les deux axes. | Vérifie que `r.hit` est faux.<br/>Vérifie que `r.position.x` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `r.position.y` vaut `0.0f` (comparaison flottante). |

**`test_slope_geometry.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **SlopeGeometryTest.SlopeUpRightMonteDeGaucheADroite** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:16`</sub> | SlopeUpRight monte de gauche à droite : haut au bord droit, bas au bord gauche. | 1. Évaluer `slopeSurfaceHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs attendues. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `*center` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `*right` vaut `0.0f`, à `1e-3f` près. |
| **SlopeGeometryTest.SlopeUpLeftMonteDeDroiteAGauche** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:39`</sub> | SlopeUpLeft monte de droite à gauche : haut au bord gauche, bas au bord droit. | 1. Évaluer `slopeSurfaceHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs attendues. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `0.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `*right` vaut `1.0f`, à `1e-3f` près. |
| **SlopeGeometryTest.TypeNonSuivableRenvoieNullopt** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:62`</sub> | Un type de tuile sans surface à suivre renvoie nullopt. | 1. Évaluer `slopeSurfaceHeight` pour Solid, Empty, Block.<br/>2. Vérifier l'absence de valeur. | Vérifie que `core::slopeSurfaceHeight(core::TileType::Solid, 0.5f).has_value()` est faux.<br/>Vérifie que `core::slopeSurfaceHeight(core::TileType::Empty, 0.5f).has_value()` est faux.<br/>Vérifie que `core::slopeSurfaceHeight(core::TileType::Block, 0.5f).has_value()` est faux. |
| **SlopeGeometryTest.PenteNestPasSolideStatiquement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:78`</sub> | isSolid renvoie false pour les deux orientations de pente. | 1. Appeler `core::isSolid` sur `SlopeUpRight` et `SlopeUpLeft`.<br/>2. Vérifier l'assertion. | Vérifie que `core::isSolid(core::TileType::SlopeUpRight)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::SlopeUpLeft)` est faux. |
| **SlopeGeometryTest.IsFollowableSurfaceDistingueLesTypes** (Mineur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:94`</sub> | isFollowableSurface distingue les tuiles suivables des autres. | 1. Appeler `isFollowableSurface` sur les deux pentes et sur `Solid`.<br/>2. Vérifier l'assertion. | Vérifie que `core::isFollowableSurface(core::TileType::SlopeUpRight)` est vrai.<br/>Vérifie que `core::isFollowableSurface(core::TileType::SlopeUpLeft)` est vrai.<br/>Vérifie que `core::isFollowableSurface(core::TileType::Solid)` est faux. |
| **SlopeGeometryTest.RoundedUpRightSuitUnQuartDeCercle** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:111`</sub> | RoundedUpRight suit un profil de quart de cercle, haut à droite. | 1. Évaluer `slopeSurfaceHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs calculées à la main. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `1.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `1.0f - std::sqrt(0.75f)`, à `1e-4f` près.<br/>Vérifie que `*right` vaut `0.0f`, à `1e-3f` près. |
| **SlopeGeometryTest.RoundedUpLeftSuitUnQuartDeCercle** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:135`</sub> | RoundedUpLeft suit un profil de quart de cercle, haut à gauche. | 1. Évaluer `slopeSurfaceHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs calculées à la main. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `0.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `1.0f - std::sqrt(0.75f)`, à `1e-4f` près.<br/>Vérifie que `*right` vaut `1.0f`, à `1e-6f` près. |
| **SlopeGeometryTest.ArrondiNestPasSolideStatiquement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:163`</sub> | isSolid renvoie false pour les deux orientations d'arrondi. | 1. Appeler `core::isSolid` sur `RoundedUpRight` et `RoundedUpLeft`.<br/>2. Vérifier l'assertion. | Vérifie que `core::isSolid(core::TileType::RoundedUpRight)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::RoundedUpLeft)` est faux. |
| **SlopeGeometryTest.IsFollowableSurfaceReconnaitLesArrondis** (Mineur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:178`</sub> | isFollowableSurface reconnaît les deux orientations d'arrondi. | 1. Appeler `isFollowableSurface` sur `RoundedUpRight` et `RoundedUpLeft`.<br/>2. Vérifier l'assertion. | Vérifie que `core::isFollowableSurface(core::TileType::RoundedUpRight)` est vrai.<br/>Vérifie que `core::isFollowableSurface(core::TileType::RoundedUpLeft)` est vrai. |
| **SlopeGeometryTest.PenteEtArrondiDePlafondNeSontPasSolidesStatiquement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:196`</sub> | Les pentes/arrondis de plafond ne sont pas solides pour la grille classique, comme leurs équivalents de sol. | 1. Appeler `core::isSolid` sur les quatre types de plafond.<br/>2. Vérifier l'assertion. | Vérifie que `core::isSolid(core::TileType::SlopeDownRight)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::SlopeDownLeft)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::RoundedDownRight)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::RoundedDownLeft)` est faux. |
| **SlopeGeometryTest.PenteEtArrondiDePlafondOntUneFaceDuHautPlate** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:219`</sub> | Les pentes/arrondis de plafond ont une face du haut plate (0,0), sans déplacement latéral calé. | 1. Appeler `isFollowableSurface` et `slopeSurfaceHeight` sur les quatre types de plafond.<br/>2. Vérifier les valeurs. | Vérifie que `core::isFollowableSurface(type)` est faux.<br/>Vérifie que `height.has_value()` est vrai.<br/>Vérifie que `*height` vaut `0.0f` (comparaison flottante). |
| **SlopeGeometryTest.IsCeilingSlopeReconnaitExactementLesQuatreTypes** (Mineur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:243`</sub> | isCeilingSlope reconnaît exactement les quatre types de plafond, aucun autre. | 1. Appeler `isCeilingSlope` sur les quatre types de plafond et sur `SlopeUpRight`/ `Solid`.<br/>2. Vérifier l'assertion. | Vérifie que `core::isCeilingSlope(core::TileType::SlopeDownRight)` est vrai.<br/>Vérifie que `core::isCeilingSlope(core::TileType::SlopeDownLeft)` est vrai.<br/>Vérifie que `core::isCeilingSlope(core::TileType::RoundedDownRight)` est vrai.<br/>Vérifie que `core::isCeilingSlope(core::TileType::RoundedDownLeft)` est vrai.<br/>Vérifie que `core::isCeilingSlope(core::TileType::SlopeUpRight)` est faux.<br/>Vérifie que `core::isCeilingSlope(core::TileType::Solid)` est faux. |
| **SlopeGeometryTest.CeilingSlopeDownRightEstLeMiroirDeSlopeUpRight** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:264`</sub> | ceilingSlopeHeight(SlopeDownRight) est le miroir vertical exact de slopeSurfaceHeight(SlopeUpRight). | 1. Évaluer `ceilingSlopeHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs attendues. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `0.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `*right` vaut `1.0f`, à `1e-3f` près. |
| **SlopeGeometryTest.CeilingSlopeDownLeftEstLeMiroirDeSlopeUpLeft** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:289`</sub> | ceilingSlopeHeight(SlopeDownLeft) est le miroir vertical exact de slopeSurfaceHeight(SlopeUpLeft). | 1. Évaluer `ceilingSlopeHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs attendues. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `1.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `*right` vaut `0.0f`, à `1e-3f` près. |
| **SlopeGeometryTest.CeilingRoundedDownRightEstLeMiroirDeRoundedUpRight** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:314`</sub> | ceilingSlopeHeight(RoundedDownRight) est le miroir vertical exact de slopeSurfaceHeight(RoundedUpRight). | 1. Évaluer `ceilingSlopeHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs calculées à la main. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `0.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `std::sqrt(0.75f)`, à `1e-4f` près.<br/>Vérifie que `*right` vaut `1.0f`, à `1e-6f` près. |
| **SlopeGeometryTest.CeilingRoundedDownLeftEstLeMiroirDeRoundedUpLeft** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:339`</sub> | ceilingSlopeHeight(RoundedDownLeft) est le miroir vertical exact de slopeSurfaceHeight(RoundedUpLeft). | 1. Évaluer `ceilingSlopeHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs calculées à la main. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `1.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `std::sqrt(0.75f)`, à `1e-4f` près.<br/>Vérifie que `*right` vaut `0.0f`, à `1e-6f` près. |
| **SlopeGeometryTest.CeilingSlopeHeightNulloptSiPasDePlafond** (Mineur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:366`</sub> | ceilingSlopeHeight renvoie nullopt pour un type sans silhouette de plafond. | 1. Évaluer `ceilingSlopeHeight` pour `Solid` et `SlopeUpRight` (variante de sol).<br/> 2. Vérifier l'absence de valeur. | Vérifie que `core::ceilingSlopeHeight(core::TileType::Solid, 0.5f).has_value()` est faux.<br/>Vérifie que `core::ceilingSlopeHeight(core::TileType::SlopeUpRight, 0.5f).has_value()` est faux. |
| **SlopeGeometryTest.ConcaveUpRightSuitUnQuartDeCercleConcave** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:384`</sub> | ConcaveUpRight suit un profil de quart de cercle concave, mêmes bords que RoundedUpRight, courbure inversée. | 1. Évaluer `slopeSurfaceHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs calculées à la main. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `1.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `std::sqrt(0.75f)`, à `1e-4f` près.<br/>Vérifie que `*right` vaut `0.0f`, à `1e-6f` près. |
| **SlopeGeometryTest.ConcaveUpLeftSuitUnQuartDeCercleConcave** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:410`</sub> | ConcaveUpLeft suit un profil de quart de cercle concave, haut à gauche. | 1. Évaluer `slopeSurfaceHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs calculées à la main. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `0.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `std::sqrt(0.75f)`, à `1e-4f` près.<br/>Vérifie que `*right` vaut `1.0f`, à `1e-6f` près. |
| **SlopeGeometryTest.ConcaveNestPasSolideStatiquement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:434`</sub> | isSolid renvoie false pour les deux orientations d'arrondi concave. | 1. Appeler `core::isSolid` sur `ConcaveUpRight` et `ConcaveUpLeft`.<br/>2. Vérifier l'assertion. | Vérifie que `core::isSolid(core::TileType::ConcaveUpRight)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::ConcaveUpLeft)` est faux. |
| **SlopeGeometryTest.IsFollowableSurfaceReconnaitLesConcaves** (Mineur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:449`</sub> | isFollowableSurface reconnaît les deux orientations d'arrondi concave. | 1. Appeler `isFollowableSurface` sur `ConcaveUpRight` et `ConcaveUpLeft`.<br/>2. Vérifier l'assertion. | Vérifie que `core::isFollowableSurface(core::TileType::ConcaveUpRight)` est vrai.<br/>Vérifie que `core::isFollowableSurface(core::TileType::ConcaveUpLeft)` est vrai. |
| **SlopeGeometryTest.ConcaveDePlafondNeSontPasSolidesEtOntUneFaceDuHautPlate** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:467`</sub> | Les arrondis concaves de plafond ne sont pas solides, n'offrent pas de déplacement latéral calé, et ont une face du haut plate. | 1. Appeler `isSolid`/`isFollowableSurface`/`slopeSurfaceHeight` sur les deux types.<br/> 2. Vérifier les valeurs. | Vérifie que `core::isSolid(type)` est faux.<br/>Vérifie que `core::isFollowableSurface(type)` est faux.<br/>Vérifie que `height.has_value()` est vrai.<br/>Vérifie que `*height` vaut `0.0f` (comparaison flottante). |
| **SlopeGeometryTest.IsCeilingSlopeReconnaitAussiLesConcaves** (Mineur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:490`</sub> | isCeilingSlope reconnaît les deux types de plafond concave. | 1. Appeler `isCeilingSlope` sur `ConcaveDownRight`/`ConcaveDownLeft` et sur `ConcaveUpRight`.<br/>2. Vérifier l'assertion. | Vérifie que `core::isCeilingSlope(core::TileType::ConcaveDownRight)` est vrai.<br/>Vérifie que `core::isCeilingSlope(core::TileType::ConcaveDownLeft)` est vrai.<br/>Vérifie que `core::isCeilingSlope(core::TileType::ConcaveUpRight)` est faux. |
| **SlopeGeometryTest.CeilingConcaveDownRightEstLeMiroirDeConcaveUpRight** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:507`</sub> | ceilingSlopeHeight(ConcaveDownRight) est le miroir vertical exact de slopeSurfaceHeight(ConcaveUpRight). | 1. Évaluer `ceilingSlopeHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs calculées à la main. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `0.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `1.0f - std::sqrt(0.75f)`, à `1e-4f` près.<br/>Vérifie que `*right` vaut `1.0f`, à `1e-6f` près. |
| **SlopeGeometryTest.CeilingConcaveDownLeftEstLeMiroirDeConcaveUpLeft** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:532`</sub> | ceilingSlopeHeight(ConcaveDownLeft) est le miroir vertical exact de slopeSurfaceHeight(ConcaveUpLeft). | 1. Évaluer `ceilingSlopeHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs calculées à la main. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `1.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `1.0f - std::sqrt(0.75f)`, à `1e-4f` près.<br/>Vérifie que `*right` vaut `0.0f`, à `1e-6f` près. |

**`test_swept_collision.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **SweptCollisionTest.TrajetLibre** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_swept_collision.cpp:30`</sub> | Balayage : trajet libre | 1. Grille sans tuile solide.<br/>2. Balayer une boîte 1×1 d'un déplacement (3, 2). | Vérifie que `r.hit` est faux.<br/>Vérifie que `r.position.x` vaut `3.0f` (comparaison flottante).<br/>Vérifie que `r.position.y` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `r.normal.x` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `r.normal.y` vaut `0.0f` (comparaison flottante). |
| **SweptCollisionTest.ButeeHorizontale** (Bloquant)<br/><sub>`Source/Test/Unit/Core/Physics/test_swept_collision.cpp:50`</sub> | Balayage : butée horizontale (mur à droite) | 1. Placer un mur solide à droite.<br/>2. Balayer la boîte vers la droite. | Vérifie que `r.hit` est vrai.<br/>Vérifie que `r.position.x` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `r.position.y` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `r.normal.x` vaut `-1.0f` (comparaison flottante). |
| **SweptCollisionTest.ButeeVerticale** (Bloquant)<br/><sub>`Source/Test/Unit/Core/Physics/test_swept_collision.cpp:70`</sub> | Balayage : butée verticale (sol) | 1. Placer un sol solide en dessous.<br/>2. Balayer la boîte vers le bas. | Vérifie que `r.hit` est vrai.<br/>Vérifie que `r.position.y` vaut `4.0f` (comparaison flottante).<br/>Vérifie que `r.position.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `r.normal.y` vaut `-1.0f` (comparaison flottante). |
| **SweptCollisionTest.NonTunneling** (Bloquant)<br/><sub>`Source/Test/Unit/Core/Physics/test_swept_collision.cpp:90`</sub> | Balayage : non-tunneling à vitesse élevée | 1. Mur à 5 tuiles.<br/>2. Balayer d'un déplacement de 10 (≫ une tuile) vers le mur. | Vérifie que `r.hit` est vrai.<br/>Vérifie que `r.position.x` vaut `4.0f` (comparaison flottante).<br/>Vérifie que `r.normal.x` vaut `-1.0f` (comparaison flottante). |
| **SweptCollisionTest.GlissementLeLongDuMur** (Bloquant)<br/><sub>`Source/Test/Unit/Core/Physics/test_swept_collision.cpp:110`</sub> | Balayage : glissement le long d'un mur | 1. Mur vertical à droite.<br/>2. Balayer d'un déplacement diagonal (droite + bas). | Vérifie que `r.hit` est vrai.<br/>Vérifie que `r.position.x` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `r.position.y` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `r.normal.x` vaut `-1.0f` (comparaison flottante). |
| **SweptCollisionTest.ButeeGauche** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_swept_collision.cpp:132`</sub> | Balayage : butée horizontale (mur à gauche) | 1. Mur solide à gauche.<br/>2. Balayer la boîte vers la gauche. | Vérifie que `r.hit` est vrai.<br/>Vérifie que `r.position.x` vaut `3.0f` (comparaison flottante).<br/>Vérifie que `r.normal.x` vaut `1.0f` (comparaison flottante). |
| **SweptCollisionTest.ButeePlafond** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_swept_collision.cpp:151`</sub> | Balayage : butée verticale (plafond) | 1. Plafond solide au-dessus.<br/>2. Balayer la boîte vers le haut. | Vérifie que `r.hit` est vrai.<br/>Vérifie que `r.position.y` vaut `3.0f` (comparaison flottante).<br/>Vérifie que `r.normal.y` vaut `1.0f` (comparaison flottante). |
| **SweptCollisionTest.MarcheSurLeSolSansBlocageHorizontal** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_swept_collision.cpp:171`</sub> | Balayage : marcher sur un sol sans blocage horizontal | 1. Sol continu ; boîte posée dessus.<br/>2. Balayer horizontalement le long du sol. | Vérifie que `r.hit` est faux.<br/>Vérifie que `r.position.x` vaut `3.0f` (comparaison flottante). |

#### Time (6)

**`test_fixed_timestep.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **FixedTimestepTest.UnPasExact** (Majeur)<br/><sub>`Source/Test/Unit/Core/Time/test_fixed_timestep.cpp:16`</sub> | Un temps écoulé égal au pas fixe produit exactement un pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `timestep.advance(STEP)` vaut `1`. |
| **FixedTimestepTest.TempsInsuffisant** (Majeur)<br/><sub>`Source/Test/Unit/Core/Time/test_fixed_timestep.cpp:31`</sub> | Un temps écoulé inférieur au pas ne produit aucun pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `timestep.advance(STEP * 0.5f)` vaut `0`. |
| **FixedTimestepTest.TempsNulOuNegatif** (Majeur)<br/><sub>`Source/Test/Unit/Core/Time/test_fixed_timestep.cpp:46`</sub> | Un temps écoulé nul ou négatif ne produit aucun pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `timestep.advance(0.0f)` vaut `0`.<br/>Vérifie que `timestep.advance(-1.0f)` vaut `0`. |
| **FixedTimestepTest.ResteConserve** (Majeur)<br/><sub>`Source/Test/Unit/Core/Time/test_fixed_timestep.cpp:62`</sub> | 2,5 pas donnent 2 pas, et le reste (0,5 pas) est conservé puis complété. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `timestep.advance(STEP * 2.5f)` vaut `2`.<br/>Vérifie que `timestep.interpolationAlpha()` vaut `0.5f`, à `1e-4f` près.<br/>Vérifie que `timestep.advance(STEP * 0.6f)` vaut `1`. |
| **FixedTimestepTest.PlafondAntiSpirale** (Majeur)<br/><sub>`Source/Test/Unit/Core/Time/test_fixed_timestep.cpp:82`</sub> | Un temps écoulé énorme est plafonné (anti-spirale de la mort). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `timestep.advance(STEP * 1000.0f)` vaut `maximum`.<br/>Vérifie que `timestep.advance(0.0f)` vaut `0`. |
| **FixedTimestepTest.PasFixeExpose** (Majeur)<br/><sub>`Source/Test/Unit/Core/Time/test_fixed_timestep.cpp:100`</sub> | Le pas fixe exposé correspond à la configuration. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `timestep.fixedDeltaSeconds()` vaut `STEP`, à `1e-6f` près. |

### HMI

#### Editor (57)

**`test_level_name_validation.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LevelNameValidationTest.NomSimpleValide** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_name_validation.cpp:12`</sub> | Un nom simple, sans caractère interdit, est valide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::isValidLevelName("Niveau 1")` est vrai.<br/>Vérifie que `hmi::isValidLevelName("Foret enchantee")` est vrai. |
| **LevelNameValidationTest.NomVideOuEspacesInvalide** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_name_validation.cpp:27`</sub> | Un nom vide ou composé uniquement d'espaces est invalide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::isValidLevelName("")` est faux.<br/>Vérifie que `hmi::isValidLevelName(" ")` est faux. |
| **LevelNameValidationTest.CaractereInterditInvalide** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_name_validation.cpp:42`</sub> | Un nom contenant un caractère interdit par le système de fichiers Windows est invalide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::isValidLevelName("Niveau:1")` est faux.<br/>Vérifie que `hmi::isValidLevelName("Niveau/1")` est faux.<br/>Vérifie que `hmi::isValidLevelName("Niveau\\\\1")` est faux.<br/>Vérifie que `hmi::isValidLevelName("Niveau*1")` est faux.<br/>Vérifie que `hmi::isValidLevelName("Niveau?1")` est faux.<br/>Vérifie que `hmi::isValidLevelName("Niveau\\"1")` est faux.<br/>Vérifie que `hmi::isValidLevelName("Niveau<1")` est faux.<br/>Vérifie que `hmi::isValidLevelName("Niveau>1")` est faux.<br/>Vérifie que `hmi::isValidLevelName("Niveau\|1")` est faux. |
| **LevelNameValidationTest.NomAccentueValide** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_name_validation.cpp:67`</sub> | Un nom accentué (Unicode) reste valide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::isValidLevelName("Fort\\xC3\\xA9resse")` est vrai. |
| **LevelNameValidationTest.TrimRetireLesEspacesDeBord** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_name_validation.cpp:81`</sub> | trimLevelName retire les espaces de bord sans toucher au contenu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::trimLevelName(" Niveau 1 ")` vaut `"Niveau 1"`.<br/>Vérifie que `hmi::trimLevelName("Niveau 1")` vaut `"Niveau 1"`.<br/>Vérifie que `hmi::trimLevelName(" ")` vaut `""`. |

**`test_level_picker.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LevelPickerTest.SelectionInitialeEstLePremierChoix** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_picker.cpp:53`</sub> | La sélection initiale pointe le premier choix (« Nouveau niveau »). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `picker.selected()` vaut `0`.<br/>Vérifie que `picker.choices()[0].path.has_value()` est faux. |
| **LevelPickerTest.FlechesDeplacentLaSelectionAvecBouclage** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_picker.cpp:69`</sub> | Bas puis Haut déplacent la sélection, avec bouclage aux extrémités. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `picker.selected()` vaut `expected`.<br/>Vérifie que `picker.selected()` vaut `2`. |
| **LevelPickerTest.EntreeConfirmeLaSelection** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_picker.cpp:103`</sub> | Entrée confirme l'indice actuellement sélectionné. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `picker.selected()` vaut `1`.<br/>Vérifie que `confirmed.has_value()` est vrai.<br/>Vérifie que `*confirmed` vaut `1`.<br/>Vérifie que `picker.choices()[*confirmed].path` vaut `std::filesystem::path("demo.json")`. |
| **LevelPickerTest.SansAppuiAucuneConfirmation** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_picker.cpp:132`</sub> | Sans appui, update() ne confirme rien. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `picker.update(input, VIEWPORT_HEIGHT).has_value()` est faux. |
| **LevelPickerTest.SurvolSourisDeplaceLaSelection** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_picker.cpp:150`</sub> | Survoler un choix à la souris déplace la sélection dessus (sans confirmer). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `picker.selected()` vaut `2`.<br/>Vérifie que `confirmed.has_value()` est faux. |
| **LevelPickerTest.ClicGaucheConfirmeLeChoixSurvole** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_picker.cpp:172`</sub> | Un clic gauche sur un choix survolé le confirme. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `confirmed.has_value()` est vrai.<br/>Vérifie que `*confirmed` vaut `1`.<br/>Vérifie que `picker.choices()[*confirmed].path` vaut `std::filesystem::path("demo.json")`. |
| **LevelPickerTest.SourisHorsChoixNeChangeRien** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_picker.cpp:196`</sub> | La souris hors de tout choix ne change ni la sélection ni la confirmation. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `picker.selected()` vaut `0`.<br/>Vérifie que `confirmed.has_value()` est faux. |
| **LevelPickerTest.ForDirectoryDossierInexistantNeProposeQueNouveauNiveau** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_picker.cpp:219`</sub> | forDirectory sur un dossier inexistant ne propose que « Nouveau niveau ». | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `picker.choices().size()` vaut `1u`.<br/>Vérifie que `picker.choices()[0].path.has_value()` est faux. |
| **LevelPickerTest.VisibleCountAuMoinsUn** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_picker.cpp:236`</sub> | visibleCount() renvoie au moins 1, même pour une hauteur de viewport minuscule. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::LevelPicker::visibleCount(0.0f)` vaut `1`.<br/>Vérifie que `hmi::LevelPicker::visibleCount(SMALL_VIEWPORT_HEIGHT)` vaut `3`. |
| **LevelPickerTest.DefilementSuitLaSelectionVersLeBas** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_picker.cpp:252`</sub> | Une liste plus longue que la fenêtre visible défile pour suivre la sélection vers le bas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `picker.scrollOffset()` vaut `0`.<br/>Vérifie que `picker.selected()` vaut `4`.<br/>Vérifie que `picker.scrollOffset()` vaut `2`. |
| **LevelPickerTest.DefilementSuitLaSelectionVersLeHaut** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_picker.cpp:285`</sub> | Depuis une position défilée, remonter la sélection jusqu'en haut ramène le défilement à zéro. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `picker.scrollOffset()` vaut `2`.<br/>Vérifie que `picker.selected()` vaut `0`.<br/>Vérifie que `picker.scrollOffset()` vaut `0`. |
| **LevelPickerTest.MoletteDefileSansChangerLaSelection** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_picker.cpp:323`</sub> | La molette défile la liste sans changer la sélection courante. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `picker.selected()` vaut `0`.<br/>Vérifie que `picker.scrollOffset()` vaut `1`. |
| **LevelPickerTest.ChoixDefileHorsFenetreNestPasCliquable** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_picker.cpp:345`</sub> | Un choix défilé hors de la fenêtre visible n'est pas cliquable à son ancienne position écran. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `picker.scrollOffset()` vaut `2`.<br/>Vérifie que `confirmed.has_value()` est vrai.<br/>Vérifie que `*confirmed` vaut `2`. |

**`test_level_size_validation.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LevelSizeValidationTest.FormatValideAnalyseCorrectement** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_size_validation.cpp:12`</sub> | Un format « largeur x hauteur » valide est analysé correctement. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.has_value()` est vrai.<br/>Vérifie que `result->first` vaut `40`.<br/>Vérifie que `result->second` vaut `30`. |
| **LevelSizeValidationTest.EspacesEtCasseTolerees** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_size_validation.cpp:29`</sub> | Les espaces autour du séparateur et une casse différente sont tolérés. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::parseLevelSize("40 x 30")` vaut `(std::make_pair(40, 30))`.<br/>Vérifie que `hmi::parseLevelSize("40X30")` vaut `(std::make_pair(40, 30))`.<br/>Vérifie que `hmi::parseLevelSize(" 40x30 ")` vaut `(std::make_pair(40, 30))`. |
| **LevelSizeValidationTest.SeparateurEtoileAccepte** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_size_validation.cpp:47`</sub> | Le séparateur `*` est accepté comme alternative à `x`/`X`. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::parseLevelSize("40*30")` vaut `(std::make_pair(40, 30))`.<br/>Vérifie que `hmi::parseLevelSize("40 * 30")` vaut `(std::make_pair(40, 30))`. |
| **LevelSizeValidationTest.SansSeparateurRefuse** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_size_validation.cpp:62`</sub> | Un texte sans séparateur est refusé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::parseLevelSize("4030").has_value()` est faux.<br/>Vérifie que `hmi::parseLevelSize("").has_value()` est faux. |
| **LevelSizeValidationTest.DimensionInvalideRefusee** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_size_validation.cpp:77`</sub> | Une dimension non numérique, nulle, négative ou partiellement numérique est refusée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::parseLevelSize("abcxdef").has_value()` est faux.<br/>Vérifie que `hmi::parseLevelSize("0x30").has_value()` est faux.<br/>Vérifie que `hmi::parseLevelSize("40x0").has_value()` est faux.<br/>Vérifie que `hmi::parseLevelSize("-5x30").has_value()` est faux.<br/>Vérifie que `hmi::parseLevelSize("12abcx30").has_value()` est faux. |
| **LevelSizeValidationTest.PlafondRespecte** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_size_validation.cpp:96`</sub> | Une dimension au-delà du plafond est refusée ; le plafond lui-même est accepté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::parseLevelSize(atCap).has_value()` est vrai.<br/>Vérifie que `hmi::parseLevelSize(overCap).has_value()` est faux. |
| **LevelSizeValidationTest.IsValidCoherentAvecParse** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_size_validation.cpp:116`</sub> | isValidLevelSize reste cohérent avec parseLevelSize. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::isValidLevelSize("14x8")` est vrai.<br/>Vérifie que `hmi::isValidLevelSize("quatorze x huit")` est faux. |

**`test_text_input_field.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **TextInputFieldTest.CaracteresTapesSAccumulent** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_text_input_field.cpp:13`</sub> | Les caractères tapés s'accumulent dans le texte du champ, dans l'ordre de saisie. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `field.text()` vaut `"N1"`.<br/>Vérifie que `field.confirmed()` est faux.<br/>Vérifie que `field.cancelled()` est faux. |
| **TextInputFieldTest.RetourArriereRetireLeDernierCaractere** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_text_input_field.cpp:38`</sub> | Retour arrière retire le dernier caractère du texte pré-rempli. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `field.text()` vaut `"Nivea"`. |
| **TextInputFieldTest.RetourArriereRetireUnCaractereAccentueEntier** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_text_input_field.cpp:59`</sub> | Retour arrière retire un caractère accentué entier (UTF-8 multi-octets), pas un octet. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `field.text()` vaut `"\\xC3\\xA9"`.<br/>Vérifie que `field.text().empty()` est vrai. |
| **TextInputFieldTest.EntreeConfirmeSansValidateur** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_text_input_field.cpp:87`</sub> | Sans validateur, Entrée confirme toujours la saisie courante. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `field.confirmed()` est vrai.<br/>Vérifie que `field.text()` vaut `"Niveau"`. |
| **TextInputFieldTest.EntreeRefuseeParLeValidateur** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_text_input_field.cpp:109`</sub> | Un validateur qui refuse le texte empêche la confirmation et marque un refus. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `field.confirmed()` est faux.<br/>Vérifie que `field.rejected()` est vrai. |
| **TextInputFieldTest.EditionApresRefusEffaceLeRefus** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_text_input_field.cpp:132`</sub> | Modifier le texte après un refus efface l'indicateur de refus. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `field.rejected()` est vrai.<br/>Vérifie que `field.rejected()` est faux.<br/>Vérifie que `field.text()` vaut `"A"`. |
| **TextInputFieldTest.EchapAnnule** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_text_input_field.cpp:159`</sub> | Échap annule la saisie sans modifier le texte du champ. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `field.cancelled()` est vrai.<br/>Vérifie que `field.text()` vaut `"Niveau"`. |
| **TextInputFieldTest.SansEffetApresConfirmation** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_text_input_field.cpp:181`</sub> | Une fois confirmé, update() n'a plus aucun effet sur le champ. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `field.confirmed()` est vrai.<br/>Vérifie que `field.text()` vaut `"Niveau"`. |

**`test_tile_palette.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **TilePaletteTest.EtatInitialCinqEntreesVisibles** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_palette.cpp:38`</sub> | À l'état initial, seules les catégories et entrées autonomes sont visibles. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `palette.entries().size()` vaut `5u`. |
| **TilePaletteTest.SelectionParDefautEstSolid** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_palette.cpp:53`</sub> | La sélection par défaut est Solid. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `palette.selected()` vaut `core::TileType::Solid`. |
| **TilePaletteTest.ClicSurEntreeAutonomeSelectionneSonType** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_palette.cpp:68`</sub> | Cliquer une entrée autonome sélectionne son type et signale le clic consommé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `target.has_value()` est vrai.<br/>Vérifie que `consumed` est vrai.<br/>Vérifie que `palette.selected()` vaut `core::TileType::Empty`. |
| **TilePaletteTest.CategoriePiegeDeplieeExposeSesFeuilles** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_palette.cpp:90`</sub> | La catégorie Piège dépliée expose Classique, Mobile, Commuté, Clignotant. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `classic.has_value()` est vrai.<br/>Vérifie que `findByLabel(palette, "> Directionnel").has_value()` est vrai.<br/>Vérifie que `findByLabel(palette, "Mobile").has_value()` est vrai.<br/>Vérifie que `findByLabel(palette, "Commute").has_value()` est vrai.<br/>Vérifie que `findByLabel(palette, "Clignotant").has_value()` est vrai.<br/>Vérifie que `consumed` est vrai.<br/>Vérifie que `palette.selected()` vaut `core::TileType::Danger`. |
| **TilePaletteTest.SousGroupeDirectionnelDeplieExposeSesBords** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_palette.cpp:119`</sub> | Le sous-groupe Directionnel déplié expose ses quatre bords. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `findByLabel(palette, "Haut").has_value()` est vrai.<br/>Vérifie que `findByLabel(palette, "Bas").has_value()` est vrai.<br/>Vérifie que `findByLabel(palette, "Gauche").has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `consumed` est vrai.<br/>Vérifie que `palette.selected()` vaut `core::TileType::DangerRight`. |
| **TilePaletteTest.ClicHorsPaletteNonConsomme** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_palette.cpp:146`</sub> | Cliquer hors de la palette ne change pas la sélection et n'est pas consommé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `consumed` est faux.<br/>Vérifie que `palette.selected()` vaut `before`. |
| **TilePaletteTest.ChaqueEntreePorteUnLibelle** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_palette.cpp:167`</sub> | Chaque entrée visible porte un libellé non vide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `entry.label.empty()` est faux. |
| **TilePaletteTest.ClicSurEnTeteDeCategorieLaDeplie** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_palette.cpp:184`</sub> | Cliquer l'en-tête d'une catégorie repliée la déplie sans changer la sélection. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `header.has_value()` est vrai.<br/>Vérifie que `consumed` est vrai.<br/>Vérifie que `palette.selected()` vaut `before`.<br/>Vérifie que `findByLabel(palette, "v Tuile").has_value()` est vrai.<br/>Vérifie que `findByLabel(palette, "Plein").has_value()` est vrai.<br/>Vérifie que `findByLabel(palette, "> Pente").has_value()` est vrai.<br/>Vérifie que `findByLabel(palette, "> Arrondi").has_value()` est vrai. |
| **TilePaletteTest.SecondClicSurEnTeteDeCategorieLaReplie** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_palette.cpp:213`</sub> | Cliquer à nouveau l'en-tête d'une catégorie dépliée la replie. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `palette.entries().size()` vaut `5u`.<br/>Vérifie que `findByLabel(palette, "> Tuile").has_value()` est vrai. |
| **TilePaletteTest.ClicSurFeuilleDirecteSelectionneSonType** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_palette.cpp:234`</sub> | Cliquer la feuille directe d'une catégorie dépliée sélectionne son type. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `door.has_value()` est vrai.<br/>Vérifie que `consumed` est vrai.<br/>Vérifie que `palette.selected()` vaut `core::TileType::Door`. |
| **TilePaletteTest.SousGroupeBlocPoussableDeplieExposeSesTailles** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_palette.cpp:257`</sub> | Le sous-groupe Bloc poussable déplié expose ses trois tailles. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `findByLabel(palette, "> Bloc poussable").has_value()` est vrai.<br/>Vérifie que `half.has_value()` est vrai.<br/>Vérifie que `consumed` est vrai.<br/>Vérifie que `palette.selected()` vaut `core::TileType::BlockHalf`. |
| **TilePaletteTest.SousGroupePenteDeplieExposeSesOrientations** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_palette.cpp:282`</sub> | Le sous-groupe Pente déplié expose ses quatre orientations. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `ceilingRight.has_value()` est vrai.<br/>Vérifie que `consumed` est vrai.<br/>Vérifie que `palette.selected()` vaut `core::TileType::SlopeDownRight`.<br/>Vérifie que `findByLabel(palette, "> Arrondi").has_value()` est vrai. |
| **TilePaletteTest.SousGroupeConcaveDeplieExposeSesOrientations** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_palette.cpp:310`</sub> | Le sous-groupe Concave déplié expose ses quatre orientations. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `floorRight.has_value()` est vrai.<br/>Vérifie que `consumed` est vrai.<br/>Vérifie que `palette.selected()` vaut `core::TileType::ConcaveUpRight`.<br/>Vérifie que `findByLabel(palette, "> Arrondi").has_value()` est vrai. |
| **TilePaletteTest.BottomSuitLEtatDeDepliage** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_palette.cpp:338`</sub> | `bottom()` suit l'état de dépliage courant. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `palette.bottom()` est strictement supérieur à `collapsedBottom`.<br/>Vérifie que `palette.bottom()` vaut `collapsedBottom` (comparaison flottante). |
| **TilePaletteTest.FenetreReduiteLimiteLesEntreesVisibles** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_palette.cpp:380`</sub> | Une fenêtre réduite limite le nombre d'entrées visibles simultanément. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `palette.totalRowCount()` vaut `30`.<br/>Vérifie que `palette.entries().size()` vaut `1u`.<br/>Vérifie que `palette.totalRowCount()` vaut `30`. |
| **TilePaletteTest.ScrollChangeLaFenetreSansChangerLaSelection** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_palette.cpp:403`</sub> | Le défilement change la fenêtre visible sans changer la sélection. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `palette.scrollOffset()` vaut `1`.<br/>Vérifie que `palette.entries().front().label` diffère de `firstLabel`.<br/>Vérifie que `palette.selected()` vaut `before`. |
| **TilePaletteTest.ScrollBorneAuxExtremites** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_palette.cpp:428`</sub> | Le défilement est borné aux deux extrémités de la liste. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `palette.scrollOffset()` vaut `palette.totalRowCount() - 1`.<br/>Vérifie que `palette.scrollOffset()` vaut `0`. |
| **TilePaletteTest.EnTeteDeplieResteVisibleDansUneFenetreEtroite** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_palette.cpp:453`</sub> | Un en-tête déplié reste visible même dans une fenêtre étroite. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::TilePalette::visibleRowCount(370.0f)` vaut `7`.<br/>Vérifie que `findByLabel(palette, "> Interactif").has_value()` est vrai.<br/>Vérifie que `findByLabel(palette, "v Interactif").has_value()` est vrai. |

**`test_tool_bar.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **ToolBarTest.SelectionParDefautEstPinceau** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tool_bar.cpp:13`</sub> | La sélection par défaut est l'outil Pinceau. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `toolBar.selected()` vaut `hmi::EditorTool::Paint`. |
| **ToolBarTest.ExposeTroisEntrees** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tool_bar.cpp:28`</sub> | La barre expose une entrée pour chacun des trois outils. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `toolBar.entries().size()` vaut `3u`. |
| **ToolBarTest.ClicSurUneEntreeSelectionneSonOutil** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tool_bar.cpp:43`</sub> | Cliquer une entrée de la barre sélectionne son outil et signale le clic consommé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `consumed` est vrai.<br/>Vérifie que `toolBar.selected()` vaut `target.tool`. |
| **ToolBarTest.ClicHorsBarreNonConsomme** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tool_bar.cpp:66`</sub> | Cliquer hors de la barre ne change pas la sélection et n'est pas consommé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `consumed` est faux.<br/>Vérifie que `toolBar.selected()` vaut `before`. |
| **ToolBarTest.SelectNextDefileEnBoucle** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tool_bar.cpp:87`</sub> | selectNext() fait défiler les outils dans l'ordre attendu, en boucle. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `toolBar.selected()` vaut `hmi::EditorTool::Paint`.<br/>Vérifie que `toolBar.selected()` vaut `hmi::EditorTool::Rectangle`.<br/>Vérifie que `toolBar.selected()` vaut `hmi::EditorTool::Selection`.<br/>Vérifie que `toolBar.selected()` vaut `hmi::EditorTool::Paint`. |
| **ToolBarTest.RelayoutRepositionneSansChangerLaSelection** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tool_bar.cpp:113`</sub> | `relayout(top)` repositionne les entrées sans changer la sélection. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `toolBar.entries().size()` vaut `countBefore`.<br/>Vérifie que `toolBar.selected()` vaut `selectedBefore`.<br/>Vérifie que `toolBar.entries().front().y` vaut `250.0f` (comparaison flottante). |

#### Graphics (9)

**`test_camera2d.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **Camera2DTest.CentreAuMilieuDeLEcran** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:21`</sub> | Le centre de la caméra se projette au centre de l'écran. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `screen.x` vaut `WIDTH * 0.5f`, à `TOLERANCE` près.<br/>Vérifie que `screen.y` vaut `HEIGHT * 0.5f`, à `TOLERANCE` près. |
| **Camera2DTest.EchelleEtAxeY** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:40`</sub> | Une unité monde vaut 16 pixels ; l'axe Y va vers le bas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `right.x` vaut `WIDTH * 0.5f + 16.0f`, à `TOLERANCE` près.<br/>Vérifie que `right.y` vaut `HEIGHT * 0.5f`, à `TOLERANCE` près.<br/>Vérifie que `down.y` vaut `HEIGHT * 0.5f + 16.0f`, à `TOLERANCE` près. |
| **Camera2DTest.Zoom** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:62`</sub> | Le zoom multiplie l'échelle en pixels. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `right.x` vaut `WIDTH * 0.5f + 32.0f`, à `TOLERANCE` près. |
| **Camera2DTest.ConversionsReciproques** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:80`</sub> | `screenToWorld` est la réciproque de `worldToScreen`. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `roundTrip.x` vaut `world.x`, à `TOLERANCE` près.<br/>Vérifie que `roundTrip.y` vaut `world.y`, à `TOLERANCE` près. |
| **Camera2DTest.ProjectionCentreVersOrigineClip** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:101`</sub> | La matrice de projection envoie le centre de la caméra à l'origine du clip space. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.x` vaut `0.0f`, à `TOLERANCE` près.<br/>Vérifie que `result.y` vaut `0.0f`, à `TOLERANCE` près.<br/>Vérifie que `result.w` vaut `1.0f`, à `TOLERANCE` près. |
| **Camera2DTest.BordEcranVersBordClip** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:128`</sub> | Un coin de l'écran correspond à un bord du clip space (±1). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.x` vaut `1.0f`, à `TOLERANCE` près. |
| **Camera2DTest.FitZoomEntierPourPetitNiveau** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:152`</sub> | fitZoom reste entier tant que le facteur brut est supérieur ou égal à 1 (petit niveau). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `zoom` est supérieur ou égal à `1.0f`.<br/>Vérifie que `zoom` vaut `std::floor(zoom)` (comparaison flottante). |
| **Camera2DTest.FitZoomFractionnairePourGrandNiveau** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:171`</sub> | fitZoom devient fractionnaire pour un niveau plus grand que la surface disponible. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `zoom` est strictement supérieur à `0.0f`.<br/>Vérifie que `zoom` est strictement inférieur à `1.0f`.<br/>Vérifie que `100.0f * hmi::Camera2D::PIXELS_PER_UNIT * zoom` est inférieur ou égal à `1280.0f + TOLERANCE`.<br/>Vérifie que `100.0f * hmi::Camera2D::PIXELS_PER_UNIT * zoom` est inférieur ou égal à `720.0f + TOLERANCE`. |
| **Camera2DTest.FitZoomAppliqueLaMarge** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:192`</sub> | fitZoom applique la marge avant l'arrondi. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `zoomSansMarge` vaut `5.0f` (comparaison flottante).<br/>Vérifie que `zoomAvecMarge` vaut `4.0f` (comparaison flottante). |

#### Input (62)

**`test_editor_key_bindings.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **EditorKeyBindingsTest.ValeursParDefautALaConstruction** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_editor_key_bindings.cpp:16`</sub> | Les neuf actions ont leurs valeurs par défaut (S/Z/Y/C/V/P/F10/F1/F2) à la construction. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::EditorAction::Save)` vaut `hmi::Key::S`.<br/>Vérifie que `bindings.key(hmi::EditorAction::Undo)` vaut `hmi::Key::Z`.<br/>Vérifie que `bindings.key(hmi::EditorAction::Redo)` vaut `hmi::Key::Y`.<br/>Vérifie que `bindings.key(hmi::EditorAction::Copy)` vaut `hmi::Key::C`.<br/>Vérifie que `bindings.key(hmi::EditorAction::Paste)` vaut `hmi::Key::V`.<br/>Vérifie que `bindings.key(hmi::EditorAction::Playtest)` vaut `hmi::Key::P`.<br/>Vérifie que `bindings.key(hmi::EditorAction::ToggleGrid)` vaut `hmi::Key::F10`.<br/>Vérifie que `bindings.key(hmi::EditorAction::ToggleHelp)` vaut `hmi::Key::F1`.<br/>Vérifie que `bindings.key(hmi::EditorAction::Rename)` vaut `hmi::Key::F2`. |
| **EditorKeyBindingsTest.SetKeyEchangeSurConflit** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_editor_key_bindings.cpp:41`</sub> | `setKey` sur une touche déjà liée à une autre action échange les deux touches. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::EditorAction::Rename)` vaut `hmi::Key::S`.<br/>Vérifie que `bindings.key(hmi::EditorAction::Save)` vaut `hmi::Key::F2`. |
| **EditorKeyBindingsTest.ResetToDefaultsRestaureLesDefauts** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_editor_key_bindings.cpp:59`</sub> | `resetToDefaults` restaure les valeurs par défaut après un ou plusieurs remaps. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::EditorAction::Playtest)` vaut `hmi::Key::P`. |
| **EditorKeyBindingsTest.SaveEtLoadAllerRetour** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_editor_key_bindings.cpp:77`</sub> | Sauvegarder puis recharger préserve un jeu de bindings personnalisé (aller-retour JSON). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `original.save(path)` est vrai.<br/>Vérifie que `reloaded.key(hmi::EditorAction::Playtest)` vaut `hmi::Key::F1`.<br/>Vérifie que `reloaded.key(hmi::EditorAction::Save)` vaut `hmi::Key::S`. |
| **EditorKeyBindingsTest.LoadFichierAbsentRenvoieLesDefauts** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_editor_key_bindings.cpp:105`</sub> | Charger depuis un fichier absent renvoie les valeurs par défaut, sans erreur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::EditorAction::Save)` vaut `hmi::Key::S`. |
| **EditorKeyBindingsTest.SavePreserveLaSectionJeu** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_editor_key_bindings.cpp:125`</sub> | Sauvegarder les touches d'éditeur préserve une section « jeu » déjà présente. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.save(path)` est vrai.<br/>Vérifie que `content.find("\\"jeu\\"")` diffère de `std::string::npos`.<br/>Vérifie que `content.find("sauter")` diffère de `std::string::npos`. |

**`test_game_key_bindings.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **GameKeyBindingsTest.ValeursParDefautALaConstruction** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_game_key_bindings.cpp:15`</sub> | Les six actions ont leurs valeurs par défaut (flèches/Espace/Maj) à la construction. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::GameAction::MoveLeft)` vaut `hmi::Key::Left`.<br/>Vérifie que `bindings.key(hmi::GameAction::MoveRight)` vaut `hmi::Key::Right`.<br/>Vérifie que `bindings.key(hmi::GameAction::AimUp)` vaut `hmi::Key::Up`.<br/>Vérifie que `bindings.key(hmi::GameAction::AimDown)` vaut `hmi::Key::Down`.<br/>Vérifie que `bindings.key(hmi::GameAction::Jump)` vaut `hmi::Key::Space`.<br/>Vérifie que `bindings.key(hmi::GameAction::Dash)` vaut `hmi::Key::Shift`. |
| **GameKeyBindingsTest.SetKeySurToucheLibre** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_game_key_bindings.cpp:36`</sub> | `setKey` sur une touche libre lie l'action sans toucher aux autres. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::GameAction::Jump)` vaut `hmi::Key::F1`.<br/>Vérifie que `bindings.key(hmi::GameAction::MoveLeft)` vaut `hmi::Key::Left`. |
| **GameKeyBindingsTest.SetKeyEchangeSurConflit** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_game_key_bindings.cpp:53`</sub> | `setKey` sur une touche déjà liée à une autre action échange les deux touches. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::GameAction::Jump)` vaut `hmi::Key::Left`.<br/>Vérifie que `bindings.key(hmi::GameAction::MoveLeft)` vaut `hmi::Key::Space`. |
| **GameKeyBindingsTest.ResetToDefaultsRestaureLesDefauts** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_game_key_bindings.cpp:72`</sub> | `resetToDefaults` restaure les valeurs par défaut après un ou plusieurs remaps. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::GameAction::Jump)` vaut `hmi::Key::Space`.<br/>Vérifie que `bindings.key(hmi::GameAction::Dash)` vaut `hmi::Key::Shift`. |
| **GameKeyBindingsTest.SaveEtLoadAllerRetour** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_game_key_bindings.cpp:92`</sub> | Sauvegarder puis recharger préserve un jeu de bindings personnalisé (aller-retour JSON). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `original.save(path)` est vrai.<br/>Vérifie que `reloaded.key(hmi::GameAction::Jump)` vaut `hmi::Key::F1`.<br/>Vérifie que `reloaded.key(hmi::GameAction::Dash)` vaut `hmi::Key::F2`.<br/>Vérifie que `reloaded.key(hmi::GameAction::MoveLeft)` vaut `hmi::Key::Left`. |
| **GameKeyBindingsTest.LoadFichierAbsentRenvoieLesDefauts** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_game_key_bindings.cpp:122`</sub> | Charger depuis un fichier absent renvoie les valeurs par défaut, sans erreur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::GameAction::MoveLeft)` vaut `hmi::Key::Left`.<br/>Vérifie que `bindings.key(hmi::GameAction::Jump)` vaut `hmi::Key::Space`. |
| **GameKeyBindingsTest.LoadJsonCorrompuRenvoieLesDefauts** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_game_key_bindings.cpp:143`</sub> | Charger un fichier JSON corrompu renvoie les valeurs par défaut, sans exception. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::GameAction::MoveLeft)` vaut `hmi::Key::Left`. |
| **GameKeyBindingsTest.LoadValeurHorsBornesIgnoree** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Input/test_game_key_bindings.cpp:168`</sub> | Une valeur hors bornes dans le fichier est ignorée : l'action garde sa valeur par défaut. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::GameAction::Jump)` vaut `hmi::Key::Space`. |
| **GameKeyBindingsTest.SavePreserveLaSectionEditeur** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_game_key_bindings.cpp:194`</sub> | Sauvegarder les touches de jeu préserve une section « editeur » déjà présente. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.save(path)` est vrai.<br/>Vérifie que `content.find("editeur")` diffère de `std::string::npos`.<br/>Vérifie que `content.find("sauvegarder")` diffère de `std::string::npos`. |

**`test_gamepad_bindings.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **GamepadBindingsTest.ValeursParDefautALaConstruction** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_bindings.cpp:16`</sub> | Les six actions ont leurs boutons par défaut à la construction. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.button(hmi::GameAction::MoveLeft)` vaut `hmi::GamepadButton::Left`.<br/>Vérifie que `bindings.button(hmi::GameAction::MoveRight)` vaut `hmi::GamepadButton::Right`.<br/>Vérifie que `bindings.button(hmi::GameAction::AimUp)` vaut `hmi::GamepadButton::Up`.<br/>Vérifie que `bindings.button(hmi::GameAction::AimDown)` vaut `hmi::GamepadButton::Down`.<br/>Vérifie que `bindings.button(hmi::GameAction::Jump)` vaut `hmi::GamepadButton::A`.<br/>Vérifie que `bindings.button(hmi::GameAction::Dash)` vaut `hmi::GamepadButton::RightShoulder`. |
| **GamepadBindingsTest.SetKeySurBoutonLibre** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_bindings.cpp:36`</sub> | `setKey` sur un bouton libre lie l'action sans toucher aux autres. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.button(hmi::GameAction::Jump)` vaut `hmi::GamepadButton::X`.<br/>Vérifie que `bindings.button(hmi::GameAction::MoveLeft)` vaut `hmi::GamepadButton::Left`. |
| **GamepadBindingsTest.SetKeyEchangeSurConflit** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_bindings.cpp:53`</sub> | `setKey` sur un bouton déjà lié à une autre action échange les deux boutons. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.button(hmi::GameAction::Jump)` vaut `hmi::GamepadButton::RightShoulder`.<br/>Vérifie que `bindings.button(hmi::GameAction::Dash)` vaut `hmi::GamepadButton::A`. |
| **GamepadBindingsTest.ResetToDefaultsRestaureLesDefauts** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_bindings.cpp:71`</sub> | `resetToDefaults` restaure les valeurs par défaut après un ou plusieurs remaps. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.button(hmi::GameAction::Jump)` vaut `hmi::GamepadButton::A`.<br/>Vérifie que `bindings.button(hmi::GameAction::Dash)` vaut `hmi::GamepadButton::RightShoulder`. |
| **GamepadBindingsTest.SaveEtLoadAllerRetour** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_bindings.cpp:91`</sub> | Sauvegarder puis recharger préserve un jeu de bindings personnalisé (aller-retour JSON). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `original.save(path)` est vrai.<br/>Vérifie que `reloaded.button(hmi::GameAction::Jump)` vaut `hmi::GamepadButton::X`.<br/>Vérifie que `reloaded.button(hmi::GameAction::Dash)` vaut `hmi::GamepadButton::Y`.<br/>Vérifie que `reloaded.button(hmi::GameAction::MoveLeft)` vaut `hmi::GamepadButton::Left`. |
| **GamepadBindingsTest.LoadFichierAbsentRenvoieLesDefauts** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_bindings.cpp:121`</sub> | Charger depuis un fichier absent renvoie les valeurs par défaut, sans erreur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.button(hmi::GameAction::Jump)` vaut `hmi::GamepadButton::A`. |
| **GamepadBindingsTest.LoadNomBoutonInconnuIgnore** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_bindings.cpp:142`</sub> | Un nom de bouton inconnu est ignoré : l'action garde sa valeur par défaut. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.button(hmi::GameAction::Jump)` vaut `hmi::GamepadButton::A`. |
| **GamepadBindingsTest.SavePreserveLesAutresSections** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_bindings.cpp:167`</sub> | Sauvegarder les bindings manette préserve les sections « jeu »/« editeur » déjà présentes. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.save(path)` est vrai.<br/>Vérifie que `content.find("\\"jeu\\"")` diffère de `std::string::npos`.<br/>Vérifie que `content.find("\\"editeur\\"")` diffère de `std::string::npos`.<br/>Vérifie que `content.find("\\"manette\\"")` diffère de `std::string::npos`. |

**`test_gamepad_button_name.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **GamepadButtonNameTest.ChaqueBoutonALibelleDedie** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_button_name.cpp:14`</sub> | Chaque bouton manette a un libellé lisible dédié. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::gamepadButtonDisplayName(hmi::GamepadButton::A)` vaut `"A"`.<br/>Vérifie que `hmi::gamepadButtonDisplayName(hmi::GamepadButton::Left)` vaut `"Gauche"`.<br/>Vérifie que `hmi::gamepadButtonDisplayName(hmi::GamepadButton::RightShoulder)` vaut `"Epaule droite"`. |
| **GamepadButtonNameTest.CapturedGamepadButtonRenvoieLeBoutonEnfonce** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_button_name.cpp:30`</sub> | `capturedGamepadButton` renvoie le bouton pressé à la frame courante. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `captured.has_value()` est vrai.<br/>Vérifie que `*captured` vaut `hmi::GamepadButton::X`. |
| **GamepadButtonNameTest.CapturedGamepadButtonVideSansBouton** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_button_name.cpp:48`</sub> | `capturedGamepadButton` renvoie vide quand aucun bouton n'est pressé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::capturedGamepadButton(input).has_value()` est faux. |

**`test_input_state.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **InputStateTest.FrontMontantClavier** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:12`</sub> | Une touche passée d'« absente » à « présente » est « pressée » exactement une frame. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.keyDown(hmi::Key::Enter)` est vrai.<br/>Vérifie que `input.keyPressed(hmi::Key::Enter)` est vrai.<br/>Vérifie que `input.keyReleased(hmi::Key::Enter)` est faux.<br/>Vérifie que `input.keyDown(hmi::Key::Enter)` est vrai.<br/>Vérifie que `input.keyPressed(hmi::Key::Enter)` est faux. |
| **InputStateTest.MaintienClavier** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:38`</sub> | Une touche restée enfoncée n'est « pressée » qu'à la première frame. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.keyPressed(hmi::Key::Down)` est vrai.<br/>Vérifie que `input.keyDown(hmi::Key::Down)` est vrai.<br/>Vérifie que `input.keyPressed(hmi::Key::Down)` est faux.<br/>Vérifie que `input.keyReleased(hmi::Key::Down)` est faux. |
| **InputStateTest.FrontDescendantClavier** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:63`</sub> | Le relâchement d'une touche est détecté « relâchée » pendant exactement une frame. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.keyDown(hmi::Key::Space)` est faux.<br/>Vérifie que `input.keyReleased(hmi::Key::Space)` est vrai.<br/>Vérifie que `input.keyReleased(hmi::Key::Space)` est faux. |
| **InputStateTest.TouchesIndependantes** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:90`</sub> | Les touches sont indépendantes : un front sur l'une n'affecte pas les autres. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.keyPressed(hmi::Key::Up)` est vrai.<br/>Vérifie que `input.keyDown(hmi::Key::Down)` est faux.<br/>Vérifie que `input.keyPressed(hmi::Key::Down)` est faux. |
| **InputStateTest.BoutonSouris** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:111`</sub> | Un bouton de souris suit la même logique pressé/cliqué/relâché que les touches. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.mouseButtonDown(hmi::MouseButton::Left)` est vrai.<br/>Vérifie que `input.mouseButtonPressed(hmi::MouseButton::Left)` est vrai.<br/>Vérifie que `input.mouseButtonDown(hmi::MouseButton::Left)` est vrai.<br/>Vérifie que `input.mouseButtonPressed(hmi::MouseButton::Left)` est faux.<br/>Vérifie que `input.mouseButtonDown(hmi::MouseButton::Left)` est faux.<br/>Vérifie que `input.mouseButtonReleased(hmi::MouseButton::Left)` est vrai. |
| **InputStateTest.PositionSouris** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:140`</sub> | La position de la souris reflète le dernier déplacement injecté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.mouseX()` vaut `42`.<br/>Vérifie que `input.mouseY()` vaut `99`.<br/>Vérifie que `input.mouseX()` vaut `-3`.<br/>Vérifie que `input.mouseY()` vaut `7`. |
| **InputStateTest.MoletteAccumuleEtSeReinitialise** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:162`</sub> | Les incréments de molette d'une frame s'additionnent et repartent de zéro ensuite. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.wheelDelta()` vaut `0`.<br/>Vérifie que `input.wheelDelta()` vaut `80`.<br/>Vérifie que `input.wheelDelta()` vaut `0`. |
| **InputStateTest.CaracteresTapesAccumulesEtVides** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:186`</sub> | Les caractères tapés s'accumulent dans l'ordre puis sont vidés à la frame suivante. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.typedCharacters().empty()` est vrai.<br/>Vérifie que `input.typedCharacters().size()` vaut `3u`.<br/>Vérifie que `input.typedCharacters()[0]` vaut `L'N'`.<br/>Vérifie que `input.typedCharacters()[1]` vaut `L'1'`.<br/>Vérifie que `input.typedCharacters()[2]` vaut `L'\\xE9'`.<br/>Vérifie que `input.typedCharacters().empty()` est vrai. |
| **InputStateTest.ManetteSeuleActiveLaTouche** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:215`</sub> | Un bouton manette seul rend `keyDown`/`keyPressed` vrais, comme au clavier. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.keyDown(hmi::Key::Enter)` est vrai.<br/>Vérifie que `input.keyPressed(hmi::Key::Enter)` est vrai.<br/>Vérifie que `input.keyDown(hmi::Key::Enter)` est vrai.<br/>Vérifie que `input.keyPressed(hmi::Key::Enter)` est faux.<br/>Vérifie que `input.keyDown(hmi::Key::Enter)` est faux.<br/>Vérifie que `input.keyReleased(hmi::Key::Enter)` est vrai. |
| **InputStateTest.ClavierEtManetteMemeToucheUnSeulFront** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:243`</sub> | Clavier et manette combinés sur la même touche ne produisent pas de double front. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.keyPressed(hmi::Key::Space)` est vrai.<br/>Vérifie que `input.keyPressed(hmi::Key::Space)` est faux.<br/>Vérifie que `input.keyDown(hmi::Key::Space)` est vrai.<br/>Vérifie que `input.keyReleased(hmi::Key::Space)` est faux.<br/>Vérifie que `input.keyDown(hmi::Key::Space)` est faux.<br/>Vérifie que `input.keyReleased(hmi::Key::Space)` est vrai. |
| **InputStateTest.ManetteRelacheeNeMasquePasLeClavier** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:280`</sub> | La manette relâchée ne masque jamais une touche clavier réellement maintenue. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.keyDown(hmi::Key::Left)` est vrai.<br/>Vérifie que `input.keyDown(hmi::Key::Left)` est vrai.<br/>Vérifie que `input.keyReleased(hmi::Key::Left)` est faux. |
| **InputStateTest.GamepadConnecteReecrasable** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:308`</sub> | `gamepadConnected` reflète le dernier `setGamepadConnected` appelé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.gamepadConnected()` est faux.<br/>Vérifie que `input.gamepadConnected()` est vrai.<br/>Vérifie que `input.gamepadConnected()` est faux. |
| **InputStateTest.FrontMontantBoutonManetteBrut** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:330`</sub> | Un bouton manette (piste brute) est « pressé » exactement une frame. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.gamepadButtonDown(hmi::GamepadButton::A)` est vrai.<br/>Vérifie que `input.gamepadButtonPressed(hmi::GamepadButton::A)` est vrai.<br/>Vérifie que `input.gamepadButtonDown(hmi::GamepadButton::A)` est vrai.<br/>Vérifie que `input.gamepadButtonPressed(hmi::GamepadButton::A)` est faux.<br/>Vérifie que `input.gamepadButtonDown(hmi::GamepadButton::A)` est faux. |
| **InputStateTest.PisteBrutIndependanteDeLaFusionKey** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:357`</sub> | La piste manette brute est indépendante de la fusion clavier/manette sur Key. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.keyDown(hmi::Key::Enter)` est faux.<br/>Vérifie que `input.gamepadButtonDown(hmi::GamepadButton::B)` est faux. |

**`test_key_name.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **KeyNameTest.ToucheNommeeLibelleDedie** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_key_name.cpp:13`</sub> | Une touche nommée de l'énumération a un libellé lisible dédié. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::keyDisplayName(hmi::Key::Left)` vaut `"Fleche gauche"`.<br/>Vérifie que `hmi::keyDisplayName(hmi::Key::Space)` vaut `"Espace"`.<br/>Vérifie que `hmi::keyDisplayName(hmi::Key::Shift)` vaut `"Maj"`.<br/>Vérifie que `hmi::keyDisplayName(hmi::Key::F10)` vaut `"F10"`. |
| **KeyNameTest.LettreNonNommeeAfficheSonCaractere** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Input/test_key_name.cpp:30`</sub> | Une lettre ou un chiffre non nommé explicitement s'affiche par son propre caractère. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::keyDisplayName(static_cast<hmi::Key>('B'))` vaut `"B"`.<br/>Vérifie que `hmi::keyDisplayName(static_cast<hmi::Key>('5'))` vaut `"5"`. |
| **KeyNameTest.ToucheInconnueRepliHexadecimal** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Input/test_key_name.cpp:47`</sub> | Une touche totalement inconnue s'affiche via un repli hexadécimal. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::keyDisplayName(static_cast<hmi::Key>(0x12))` vaut `"Touche 0x12"`. |
| **KeyNameTest.CapturedKeyRenvoieLaToucheEnfoncee** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_key_name.cpp:61`</sub> | `capturedKey` renvoie la touche pressée à la frame courante. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `captured.has_value()` est vrai.<br/>Vérifie que `*captured` vaut `hmi::Key::F1`. |
| **KeyNameTest.CapturedKeyVideSansTouche** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_key_name.cpp:79`</sub> | `capturedKey` renvoie vide quand aucune touche n'est pressée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::capturedKey(input).has_value()` est faux. |
| **KeyNameTest.EchapEtEntreeReserveesNonCapturees** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_key_name.cpp:94`</sub> | `Échap` et `Entrée` sont réservées : leur pression seule ne capture rien. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::capturedKey(escapeOnly).has_value()` est faux.<br/>Vérifie que `hmi::capturedKey(enterOnly).has_value()` est faux. |

**`test_player_input_mapper.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **PlayerInputMapperTest.FlecheGauche** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:43`</sub> | Flèche gauche seule → intention vers la gauche (-1). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mapWithDefaults(withKeys({hmi::Key::Left})).moveX` vaut `-1.0f` (comparaison flottante). |
| **PlayerInputMapperTest.FlecheDroite** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:57`</sub> | Flèche droite seule → intention vers la droite (+1). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mapWithDefaults(withKeys({hmi::Key::Right})).moveX` vaut `1.0f` (comparaison flottante). |
| **PlayerInputMapperTest.SansRemapQDWNeDeclenchentRien** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:71`</sub> | Sans remap, Q/D/W ne déclenchent rien (aucun alias fixe). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mapWithDefaults(withKeys({hmi::Key::Q})).moveX` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `mapWithDefaults(withKeys({hmi::Key::D})).moveX` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `mapWithDefaults(withKeys({hmi::Key::W})).jumpPressed` est faux. |
| **PlayerInputMapperTest.AucuneTouche** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:87`</sub> | Aucune touche → intention nulle (immobile). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mapWithDefaults(hmi::InputState{}).moveX` vaut `0.0f` (comparaison flottante). |
| **PlayerInputMapperTest.GaucheEtDroiteSeNeutralisent** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:101`</sub> | Gauche et droite simultanées → neutralisation (0). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mapWithDefaults(withKeys({hmi::Key::Left, hmi::Key::Right})).moveX` vaut `0.0f` (comparaison flottante). |
| **PlayerInputMapperTest.EspacePresseeDeclencheLeSaut** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:115`</sub> | Espace fraîchement enfoncée → saut **pressé** (front) et **maintenu**. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.jumpPressed` est vrai.<br/>Vérifie que `input.jumpHeld` est vrai. |
| **PlayerInputMapperTest.SautMaintenuN_estPasUnFront** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:131`</sub> | Saut **maintenu** sans nouveau front → `jumpHeld` vrai mais `jumpPressed` faux. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mapped.jumpPressed` est faux.<br/>Vérifie que `mapped.jumpHeld` est vrai. |
| **PlayerInputMapperTest.PasDeSaut** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:153`</sub> | Aucune touche de saut → ni pressé ni maintenu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.jumpPressed` est faux.<br/>Vérifie que `input.jumpHeld` est faux. |
| **PlayerInputMapperTest.DeplacementEtSautIndependants** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:169`</sub> | Déplacement et saut sont indépendants (axes distincts). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.moveX` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `input.jumpPressed` est vrai. |
| **PlayerInputMapperTest.MajDeclencheLeDash** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:185`</sub> | Maj fraîchement enfoncée → dash **pressé** (front). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mapWithDefaults(withKeys({hmi::Key::Shift})).dashPressed` est vrai.<br/>Vérifie que `mapWithDefaults(hmi::InputState{}).dashPressed` est faux. |
| **PlayerInputMapperTest.ViseeVerticale** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:200`</sub> | Visée verticale (y vers le bas) : Bas → +1, Haut → -1, les deux → 0. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mapWithDefaults(withKeys({hmi::Key::Down})).moveY` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `mapWithDefaults(withKeys({hmi::Key::Up})).moveY` vaut `-1.0f` (comparaison flottante).<br/>Vérifie que `mapWithDefaults(withKeys({hmi::Key::Up, hmi::Key::Down})).moveY` vaut `0.0f` (comparaison flottante). |
| **PlayerInputMapperTest.RemapperUneActionClavierUtiliseLaNouvelleTouche** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:216`</sub> | Une action remappée au clavier réagit à sa nouvelle touche. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::toPlayerInput(withKeys({hmi::Key::F1}), gameKeyBindings, hmi::GamepadBindings{}) .dashPressed` est vrai. |
| **PlayerInputMapperTest.RemapperGaucheEtDroiteSurDesTouchesDistinctesNeLesAnnulePas** (Bloquant)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:235`</sub> | Remapper Gauche et Droite sur des touches distinctes ne les neutralise pas l'une l'autre. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::toPlayerInput(withKeys({hmi::Key::D}), bindings, gamepadBindings).moveX` vaut `-1.0f` (comparaison flottante).<br/>Vérifie que `hmi::toPlayerInput(withKeys({hmi::Key::Q}), bindings, gamepadBindings).moveX` vaut `1.0f` (comparaison flottante). |
| **PlayerInputMapperTest.ActionDeclencheeParSonBoutonManetteParDefaut** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:262`</sub> | Une action est déclenchée par son bouton manette lié. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.jumpPressed` est vrai. |
| **PlayerInputMapperTest.RemapperUneActionManetteUtiliseLeNouveauBouton** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:279`</sub> | Une action remappée manette réagit à son nouveau bouton, le clavier reste indépendant. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::toPlayerInput(withGamepadButton(hmi::GamepadButton::X), hmi::GameKeyBindings{}, gamepadBindings) .jumpPressed` est vrai.<br/>Vérifie que `mapWithDefaults(withKeys({hmi::Key::Space})).jumpPressed` est vrai. |
| **PlayerInputMapperTest.RemapperGaucheEtDroiteManetteSurDesBoutonsDistinctsNeLesAnnulePas** (Bloquant)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:304`</sub> | Remapper deux actions manette sur des boutons distincts ne les neutralise pas l'une l'autre. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::toPlayerInput(withGamepadButton(hmi::GamepadButton::X), gameKeyBindings, gamepadBindings) .moveX` vaut `-1.0f` (comparaison flottante).<br/>Vérifie que `hmi::toPlayerInput(withGamepadButton(hmi::GamepadButton::Y), gameKeyBindings, gamepadBindings) .moveX` vaut `1.0f` (comparaison flottante). |

#### Interface (59)

**`test_editor_keybindings_model.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **EditorKeybindingsModelTest.SelectionParDefaut** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_editor_keybindings_model.cpp:55`</sub> | À l'ouverture, la première ligne (Sauvegarder) est sélectionnée, hors capture. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `model.selectedIndex()` vaut `0`.<br/>Vérifie que `model.isCapturing()` est faux.<br/>Vérifie que `model.rowLabel(0)` vaut `"Sauvegarder"`.<br/>Vérifie que `model.rowLabel(hmi::EditorKeybindingsModel::RESET_ROW)` vaut `"Reinitialiser"`.<br/>Vérifie que `model.rowLabel(hmi::EditorKeybindingsModel::BACK_ROW)` vaut `"Retour"`. |
| **EditorKeybindingsModelTest.ToucheCaptureeLieLActionEtRenvoieRebound** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_editor_keybindings_model.cpp:78`</sub> | Une touche pressée pendant la capture lie l'action, sauvegarde, et renvoie Rebound. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.has_value()` est vrai.<br/>Vérifie que `*action` vaut `hmi::EditorKeybindingsAction::Rebound`.<br/>Vérifie que `model.isCapturing()` est faux.<br/>Vérifie que `bindings.key(hmi::EditorAction::Save)` vaut `hmi::Key::A`. |
| **EditorKeybindingsModelTest.EchapPendantCaptureAnnule** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_editor_keybindings_model.cpp:108`</sub> | `Échap` pendant la capture l'annule sans modifier le binding. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `model.isCapturing()` est vrai.<br/>Vérifie que `action.has_value()` est faux.<br/>Vérifie que `model.isCapturing()` est faux.<br/>Vérifie que `bindings.key(hmi::EditorAction::Save)` vaut `hmi::Key::S`. |
| **EditorKeybindingsModelTest.ConfirmerReinitialiserRestaureLesDefauts** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_editor_keybindings_model.cpp:133`</sub> | Confirmer « Réinitialiser » restaure les défauts et renvoie Reset. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.has_value()` est vrai.<br/>Vérifie que `*action` vaut `hmi::EditorKeybindingsAction::Reset`.<br/>Vérifie que `bindings.key(hmi::EditorAction::Save)` vaut `hmi::Key::S`. |
| **EditorKeybindingsModelTest.ConfirmerRetourRenvoieBack** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_editor_keybindings_model.cpp:165`</sub> | Confirmer « Retour » renvoie l'action Back. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.has_value()` est vrai.<br/>Vérifie que `*action` vaut `hmi::EditorKeybindingsAction::Back`. |
| **EditorKeybindingsModelTest.RemapperSurConflitEchange** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_editor_keybindings_model.cpp:190`</sub> | Remapper une action sur une touche déjà liée à une autre échange les deux (conflit). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::EditorAction::Save)` vaut `hmi::Key::Z`.<br/>Vérifie que `bindings.key(hmi::EditorAction::Undo)` vaut `hmi::Key::S`. |

**`test_game_keybindings_model.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **GameKeybindingsModelTest.SelectionParDefaut** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_game_keybindings_model.cpp:50`</sub> | À l'ouverture, la première ligne (Aller à gauche) est sélectionnée, hors capture. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `model.selectedIndex()` vaut `0`.<br/>Vérifie que `model.isCapturing()` est faux.<br/>Vérifie que `model.rowLabel(0)` vaut `"Aller a gauche"`.<br/>Vérifie que `model.rowLabel(hmi::GameKeybindingsModel::RESET_ROW)` vaut `"Reinitialiser"`.<br/>Vérifie que `model.rowLabel(hmi::GameKeybindingsModel::BACK_ROW)` vaut `"Retour"`. |
| **GameKeybindingsModelTest.ConfirmerUneActionEntreEnCapture** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_game_keybindings_model.cpp:73`</sub> | Confirmer une ligne d'action entre en capture, sans modifier le binding immédiatement. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.has_value()` est faux.<br/>Vérifie que `model.isCapturing()` est vrai.<br/>Vérifie que `bindings.key(hmi::GameAction::MoveLeft)` vaut `hmi::Key::Left`. |
| **GameKeybindingsModelTest.ToucheCaptureeLieLActionEtRenvoieRebound** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_game_keybindings_model.cpp:96`</sub> | Une touche pressée pendant la capture lie l'action, sauvegarde, et renvoie Rebound. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.has_value()` est vrai.<br/>Vérifie que `*action` vaut `hmi::GameKeybindingsAction::Rebound`.<br/>Vérifie que `model.isCapturing()` est faux.<br/>Vérifie que `bindings.key(hmi::GameAction::MoveLeft)` vaut `hmi::Key::F1`. |
| **GameKeybindingsModelTest.EchapPendantCaptureAnnule** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_game_keybindings_model.cpp:126`</sub> | `Échap` pendant la capture l'annule sans modifier le binding. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `model.isCapturing()` est vrai.<br/>Vérifie que `action.has_value()` est faux.<br/>Vérifie que `model.isCapturing()` est faux.<br/>Vérifie que `bindings.key(hmi::GameAction::MoveLeft)` vaut `hmi::Key::Left`. |
| **GameKeybindingsModelTest.ConfirmerReinitialiserRestaureLesDefauts** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_game_keybindings_model.cpp:151`</sub> | Confirmer « Réinitialiser » restaure les défauts et renvoie Reset. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.has_value()` est vrai.<br/>Vérifie que `*action` vaut `hmi::GameKeybindingsAction::Reset`.<br/>Vérifie que `bindings.key(hmi::GameAction::MoveLeft)` vaut `hmi::Key::Left`. |
| **GameKeybindingsModelTest.ConfirmerRetourRenvoieBack** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_game_keybindings_model.cpp:183`</sub> | Confirmer « Retour » renvoie l'action Back. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.has_value()` est vrai.<br/>Vérifie que `*action` vaut `hmi::GameKeybindingsAction::Back`. |

**`test_gamepad_bindings_model.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **GamepadBindingsModelTest.SelectionParDefaut** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_gamepad_bindings_model.cpp:62`</sub> | À l'ouverture, la première ligne est sélectionnée, hors capture. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `model.selectedIndex()` vaut `0`.<br/>Vérifie que `model.isCapturing()` est faux.<br/>Vérifie que `model.rowLabel(0)` vaut `"Aller a gauche"`.<br/>Vérifie que `model.rowLabel(hmi::GamepadBindingsModel::RESET_ROW)` vaut `"Reinitialiser"`.<br/>Vérifie que `model.rowLabel(hmi::GamepadBindingsModel::BACK_ROW)` vaut `"Retour"`. |
| **GamepadBindingsModelTest.SansManetteConnecteeNEntrePasEnCapture** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_gamepad_bindings_model.cpp:84`</sub> | Sans manette connectée, confirmer une ligne d'action n'entre pas en capture. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.has_value()` est faux.<br/>Vérifie que `model.isCapturing()` est faux. |
| **GamepadBindingsModelTest.ManetteConnecteeConfirmerEntreEnCapture** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_gamepad_bindings_model.cpp:106`</sub> | Manette connectée, confirmer une ligne d'action entre en capture. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.has_value()` est faux.<br/>Vérifie que `model.isCapturing()` est vrai. |
| **GamepadBindingsModelTest.BoutonCaptureLieLActionEtRenvoieRebound** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_gamepad_bindings_model.cpp:132`</sub> | Un bouton pressé pendant la capture lie l'action et renvoie Rebound. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.has_value()` est vrai.<br/>Vérifie que `*action` vaut `hmi::GamepadBindingsAction::Rebound`.<br/>Vérifie que `model.isCapturing()` est faux.<br/>Vérifie que `bindings.button(hmi::GameAction::MoveLeft)` vaut `hmi::GamepadButton::X`. |
| **GamepadBindingsModelTest.EchapPendantCaptureAnnule** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_gamepad_bindings_model.cpp:166`</sub> | `Échap` pendant la capture l'annule sans modifier le binding. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `model.isCapturing()` est vrai.<br/>Vérifie que `action.has_value()` est faux.<br/>Vérifie que `model.isCapturing()` est faux.<br/>Vérifie que `bindings.button(hmi::GameAction::MoveLeft)` vaut `hmi::GamepadButton::Left`. |
| **GamepadBindingsModelTest.ConfirmerReinitialiserRestaureLesDefauts** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_gamepad_bindings_model.cpp:196`</sub> | Confirmer « Réinitialiser » restaure les défauts et renvoie Reset. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.has_value()` est vrai.<br/>Vérifie que `*action` vaut `hmi::GamepadBindingsAction::Reset`.<br/>Vérifie que `bindings.button(hmi::GameAction::MoveLeft)` vaut `hmi::GamepadButton::Left`. |
| **GamepadBindingsModelTest.ConfirmerRetourRenvoieBack** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_gamepad_bindings_model.cpp:227`</sub> | Confirmer « Retour » renvoie l'action Back. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.has_value()` est vrai.<br/>Vérifie que `*action` vaut `hmi::GamepadBindingsAction::Back`. |

**`test_language_selector.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LanguageSelectorTest.RectangleAncreBasDroite** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_language_selector.cpp:29`</sub> | Le bouton est ancré au coin bas-droit, marge comprise. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `rect.width` vaut `hmi::LanguageSelector::BUTTON_WIDTH` (comparaison flottante).<br/>Vérifie que `rect.height` vaut `hmi::LanguageSelector::BUTTON_HEIGHT` (comparaison flottante).<br/>Vérifie que `rect.x` vaut `VIEWPORT_WIDTH - hmi::LanguageSelector::MARGIN - hmi::LanguageSelector::BUTTON_WIDTH` (comparaison flottante).<br/>Vérifie que `rect.y` vaut `VIEWPORT_HEIGHT - hmi::LanguageSelector::MARGIN - hmi::LanguageSelector::BUTTON_HEIGHT` (comparaison flottante). |
| **LanguageSelectorTest.BasculeAlterneLesLangues** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_language_selector.cpp:51`</sub> | La bascule renvoie l'autre langue (français ↔ anglais). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::LanguageSelector::other("fr")` vaut `"en"`.<br/>Vérifie que `hmi::LanguageSelector::other("en")` vaut `"fr"`.<br/>Vérifie que `hmi::LanguageSelector::other("xx")` vaut `"en"`. |
| **LanguageSelectorTest.ClicDansLeBoutonBascule** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_language_selector.cpp:67`</sub> | Un clic dans le bouton demande la bascule vers l'autre langue. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `toggle.requested` est vrai.<br/>Vérifie que `toggle.next` vaut `"en"`. |
| **LanguageSelectorTest.ClicHorsBoutonNeBasculePas** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_language_selector.cpp:90`</sub> | Un clic hors du bouton ne demande aucune bascule. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `toggle.requested` est faux. |
| **LanguageSelectorTest.SansClicPasDeBascule** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_language_selector.cpp:108`</sub> | Sans clic, aucune bascule même si la souris est sur le bouton. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `toggle.requested` est faux. |

**`test_level_sequence.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LevelSequenceTest.SequenceVide** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_level_sequence.cpp:12`</sub> | Une séquence vide est signalée comme telle. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `sequence.empty()` est vrai.<br/>Vérifie que `sequence.size()` vaut `0u`. |
| **LevelSequenceTest.DemarreSurLePremier** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_level_sequence.cpp:28`</sub> | La séquence démarre sur le premier niveau, dans l'ordre fourni. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `sequence.empty()` est faux.<br/>Vérifie que `sequence.size()` vaut `3u`.<br/>Vérifie que `sequence.index()` vaut `0u`.<br/>Vérifie que `sequence.current()` vaut `std::filesystem::path("a.json")`.<br/>Vérifie que `sequence.hasNext()` est vrai. |
| **LevelSequenceTest.AvanceDansLOrdre** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_level_sequence.cpp:47`</sub> | `advance` parcourt les niveaux dans l'ordre jusqu'au dernier. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `sequence.index()` vaut `1u`.<br/>Vérifie que `sequence.current()` vaut `std::filesystem::path("b.json")`.<br/>Vérifie que `sequence.hasNext()` est vrai.<br/>Vérifie que `sequence.current()` vaut `std::filesystem::path("c.json")`.<br/>Vérifie que `sequence.hasNext()` est faux. |
| **LevelSequenceTest.NeDepassePasLeDernier** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_level_sequence.cpp:70`</sub> | Au-delà du dernier niveau, `advance` est sans effet (pas de dépassement). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `sequence.hasNext()` est faux.<br/>Vérifie que `sequence.index()` vaut `0u`.<br/>Vérifie que `sequence.current()` vaut `std::filesystem::path("seul.json")`. |

**`test_menu_model.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **MenuModelTest.SelectionParDefaut** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_menu_model.cpp:63`</sub> | À l'ouverture, la première option (Charger niveau) est sélectionnée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `menu.selectedIndex()` vaut `0`.<br/>Vérifie que `menu.optionLabel(0)` vaut `"Charger niveau"`.<br/>Vérifie que `menu.title()` vaut `"ProjectGaming"`. |
| **MenuModelTest.ValiderChargeNiveau** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_menu_model.cpp:82`</sub> | Valider par défaut (Entrée) bascule vers l'écran de jeu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `transition.kind` vaut `hmi::ScreenTransition::Kind::Switch`.<br/>Vérifie que `transition.target` vaut `hmi::ScreenId::Game`. |
| **MenuModelTest.FlecheBasPuisValider** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_menu_model.cpp:101`</sub> | La flèche bas déplace la sélection ; valider mène alors au Mode Edition. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `none.kind` vaut `hmi::ScreenTransition::Kind::None`.<br/>Vérifie que `menu.selectedIndex()` vaut `1`.<br/>Vérifie que `transition.kind` vaut `hmi::ScreenTransition::Kind::Switch`.<br/>Vérifie que `transition.target` vaut `hmi::ScreenId::Editor`. |
| **MenuModelTest.FlecheHautBoucleSurQuitter** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_menu_model.cpp:124`</sub> | La flèche haut depuis la première option boucle sur la dernière (Quitter). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `menu.selectedIndex()` vaut `3`.<br/>Vérifie que `transition.kind` vaut `hmi::ScreenTransition::Kind::Quit`. |
| **MenuModelTest.OptionOptionsBasculeVersEcranOptions** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_menu_model.cpp:145`</sub> | La troisième option (Options) bascule vers l'écran de réglages. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `menu.selectedIndex()` vaut `2`.<br/>Vérifie que `transition.kind` vaut `hmi::ScreenTransition::Kind::Switch`.<br/>Vérifie que `transition.target` vaut `hmi::ScreenId::Options`. |
| **MenuModelTest.BouclageBasComplet** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_menu_model.cpp:168`</sub> | Quatre flèches bas ramènent à la première option (bouclage déterministe). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `menu.selectedIndex()` vaut `1`.<br/>Vérifie que `menu.selectedIndex()` vaut `2`.<br/>Vérifie que `menu.selectedIndex()` vaut `3`.<br/>Vérifie que `menu.selectedIndex()` vaut `0`. |
| **MenuModelTest.SurvolSourisSelectionne** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_menu_model.cpp:192`</sub> | Le survol d'une option à la souris la sélectionne. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `transition.kind` vaut `hmi::ScreenTransition::Kind::None`.<br/>Vérifie que `menu.selectedIndex()` vaut `1`. |
| **MenuModelTest.ClicValideOption** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_menu_model.cpp:211`</sub> | Un clic gauche sur une option la valide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `menu.selectedIndex()` vaut `3`.<br/>Vérifie que `transition.kind` vaut `hmi::ScreenTransition::Kind::Quit`. |
| **MenuModelTest.ClicHorsOptionNeValidePas** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_menu_model.cpp:231`</sub> | Un clic hors de toute option ne valide rien. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `transition.kind` vaut `hmi::ScreenTransition::Kind::None`. |

**`test_options_model.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **OptionsModelTest.SelectionParDefautEtLibelleVSync** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_options_model.cpp:62`</sub> | À l'ouverture, la première option (V-Sync) est sélectionnée et affiche l'état fourni. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `options.selectedIndex()` vaut `0`.<br/>Vérifie que `options.scrollOffset()` vaut `0`.<br/>Vérifie que `options.optionLabel(0)` vaut `"V-Sync : Active"`.<br/>Vérifie que `options.optionLabel(1)` vaut `"Touches de jeu"`.<br/>Vérifie que `options.optionLabel(2)` vaut `"Touches de l'editeur"`.<br/>Vérifie que `options.optionLabel(3)` vaut `"Touches de la manette"`.<br/>Vérifie que `options.optionLabel(4)` vaut `"Retour"`. |
| **OptionsModelTest.VisibleOptionCountA720p** (Bloquant)<br/><sub>`Source/Test/Unit/HMI/Interface/test_options_model.cpp:86`</sub> | À 720p, seules 4 des 5 options tiennent sans défilement. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::OptionsModel::visibleOptionCount(VIEWPORT_HEIGHT)` vaut `4`.<br/>Vérifie que `hmi::OptionsModel::visibleOptionCount(VIEWPORT_HEIGHT)` est strictement inférieur à `hmi::OptionsModel::OPTION_COUNT`. |
| **OptionsModelTest.ValiderVSyncRenvoieToggle** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_options_model.cpp:101`</sub> | Valider la première option (V-Sync) renvoie l'action ToggleVSync. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.has_value()` est vrai.<br/>Vérifie que `*action` vaut `hmi::OptionsAction::ToggleVSync`. |
| **OptionsModelTest.FlecheBasPuisValiderRenvoieBack** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_options_model.cpp:122`</sub> | Quatre flèches bas puis valider (Retour) renvoie l'action Back, en ayant fait défiler la fenêtre. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `none.has_value()` est faux.<br/>Vérifie que `options.selectedIndex()` vaut `4`.<br/>Vérifie que `options.scrollOffset()` est strictement supérieur à `0`.<br/>Vérifie que `action.has_value()` est vrai.<br/>Vérifie que `*action` vaut `hmi::OptionsAction::Back`. |
| **OptionsModelTest.FlecheHautBoucleSurRetour** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_options_model.cpp:155`</sub> | La flèche haut depuis la première option boucle sur la dernière (Retour), fenêtre défilée en conséquence. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `options.selectedIndex()` vaut `4`.<br/>Vérifie que `options.scrollOffset()` est strictement supérieur à `0`. |
| **OptionsModelTest.ValiderTouchesDeJeuRenvoieOpenGameKeybindings** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_options_model.cpp:175`</sub> | Valider l'option « Touches de jeu » renvoie l'action OpenGameKeybindings. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.has_value()` est vrai.<br/>Vérifie que `*action` vaut `hmi::OptionsAction::OpenGameKeybindings`. |
| **OptionsModelTest.ValiderTouchesDeLEditeurRenvoieOpenEditorKeybindings** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_options_model.cpp:196`</sub> | Valider l'option « Touches de l'éditeur » renvoie l'action OpenEditorKeybindings. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.has_value()` est vrai.<br/>Vérifie que `*action` vaut `hmi::OptionsAction::OpenEditorKeybindings`. |
| **OptionsModelTest.ValiderTouchesDeLaManetteRenvoieOpenGamepadBindings** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_options_model.cpp:219`</sub> | Valider l'option « Touches de la manette » renvoie l'action OpenGamepadBindings. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.has_value()` est vrai.<br/>Vérifie que `*action` vaut `hmi::OptionsAction::OpenGamepadBindings`. |
| **OptionsModelTest.ClicSourisValideTouchesDeJeu** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_options_model.cpp:243`</sub> | Le survol d'une option visible à la souris la sélectionne ; un clic la valide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `options.selectedIndex()` vaut `1`.<br/>Vérifie que `action.has_value()` est vrai.<br/>Vérifie que `*action` vaut `hmi::OptionsAction::OpenGameKeybindings`. |
| **OptionsModelTest.ClicApresDefilementValideRetour** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_options_model.cpp:267`</sub> | Après défilement jusqu'à Retour, un clic à sa position affichée la valide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `options.selectedIndex()` vaut `4`.<br/>Vérifie que `scrollOffset` est strictement supérieur à `0`.<br/>Vérifie que `action.has_value()` est vrai.<br/>Vérifie que `*action` vaut `hmi::OptionsAction::Back`. |
| **OptionsModelTest.MoletteDefileSansChangerLaSelection** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_options_model.cpp:296`</sub> | La molette défile la fenêtre visible sans changer la sélection courante. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `options.selectedIndex()` vaut `0`.<br/>Vérifie que `options.scrollOffset()` vaut `1`. |
| **OptionsModelTest.SetVSyncEnabledChangeLeLibelle** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_options_model.cpp:319`</sub> | `setVSyncEnabled` resynchronise le libellé affiché après une bascule. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `options.optionLabel(0)` vaut `"V-Sync : Active"`.<br/>Vérifie que `options.optionLabel(0)` vaut `"V-Sync : Desactive"`. |
| **OptionsModelTest.ClicHorsOptionNeValidePas** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_options_model.cpp:338`</sub> | Un clic hors de toute option ne valide rien. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.has_value()` est faux. |

**`test_save_log_button.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **SaveLogButtonTest.AGaucheDuBoutonLangue** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_save_log_button.cpp:30`</sub> | Le bouton est à gauche du bouton de langue, aligné sur le même bord bas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `save.x + save.width` est strictement inférieur à `language.x`.<br/>Vérifie que `save.y + save.height` vaut `static_cast<float>(VIEWPORT_HEIGHT) - hmi::LanguageSelector::MARGIN` (comparaison flottante). |
| **SaveLogButtonTest.ClicDansLeBouton** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_save_log_button.cpp:50`</sub> | Un clic dans le bouton est détecté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `button.clicked(mouseClick(x, y), VIEWPORT_WIDTH, VIEWPORT_HEIGHT)` est vrai. |
| **SaveLogButtonTest.ClicHorsBouton** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_save_log_button.cpp:69`</sub> | Un clic hors du bouton n'est pas détecté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `button.clicked(mouseClick(5, 5), VIEWPORT_WIDTH, VIEWPORT_HEIGHT)` est faux. |

**`test_screen_manager.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **ScreenManagerTest.ConstructionCreeEcranInitial** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_screen_manager.cpp:49`</sub> | La construction fabrique l'écran initial demandé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `harness.requested.size()` vaut `1u`.<br/>Vérifie que `harness.requested.front()` vaut `hmi::ScreenId::Menu`.<br/>Vérifie que `manager.currentScreen()` diffère de `nullptr`. |
| **ScreenManagerTest.TransitionNoneResteSurEcran** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_screen_manager.cpp:68`</sub> | Une transition « rester » conserve l'écran actif et n'en fabrique pas d'autre. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `manager.update(input, 0.0f)` est faux.<br/>Vérifie que `manager.currentScreen()` vaut `before`.<br/>Vérifie que `harness.requested.size()` vaut `1u`. |
| **ScreenManagerTest.TransitionSwitchRemplaceEcran** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_screen_manager.cpp:91`</sub> | Une transition « basculer » remplace l'écran actif par celui demandé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `manager.update(input, 0.0f)` est faux.<br/>Vérifie que `harness.requested.size()` vaut `2u`.<br/>Vérifie que `harness.requested.back()` vaut `hmi::ScreenId::Editor`.<br/>Vérifie que `manager.currentScreen()` diffère de `before`.<br/>Vérifie que `manager.shouldQuit()` est faux. |
| **ScreenManagerTest.TransitionQuitFermeApplication** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_screen_manager.cpp:116`</sub> | Une transition « quitter » ferme l'application et se propage à la boucle. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `manager.update(input, 0.0f)` est vrai.<br/>Vérifie que `manager.shouldQuit()` est vrai.<br/>Vérifie que `manager.currentScreen()` vaut `nullptr`.<br/>Vérifie que `manager.update(input, 0.0f)` est vrai. |

**`test_session_log.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **SessionLogTest.SerialiseUneLigneParMessage** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_session_log.cpp:15`</sub> | Chaque message donne une ligne, dans l'ordre d'arrivée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::serializeSessionLog(entries)` vaut `"premier message\\nsecond message\\n"`. |
| **SessionLogTest.VideDonneChaineVide** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_session_log.cpp:34`</sub> | Une session sans message produit un texte vide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::serializeSessionLog({})` vaut `""`. |

#### Localization (8)

**`test_localization.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LocalizationTest.AnalyseIgnoreCommentairesEtEspaces** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Localization/test_localization.cpp:17`</sub> | L'analyse ignore les lignes vides et les commentaires, et retire les espaces autour de '='. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `strings.size()` vaut `2u`.<br/>Vérifie que `strings.at("menu.quitter")` vaut `"Quitter"`.<br/>Vérifie que `strings.at("menu.titre")` vaut `"ProjectGaming"`. |
| **LocalizationTest.AnalyseConserveEgalDansLaValeur** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Localization/test_localization.cpp:42`</sub> | Seul le premier '=' sépare ; un '=' dans la valeur est conservé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `strings.size()` vaut `1u`.<br/>Vérifie que `strings.at("expression")` vaut `"a = b + c"`. |
| **LocalizationTest.CleExistanteResolue** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Localization/test_localization.cpp:59`</sub> | Une clé existante est résolue dans la langue active. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `localization.text("menu.quitter")` vaut `"Quitter"`.<br/>Vérifie que `localization.activeLanguage()` vaut `"fr"`. |
| **LocalizationTest.CleInconnueRenvoieLaCle** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Localization/test_localization.cpp:77`</sub> | Une clé inconnue partout est renvoyée telle quelle (repli déterministe, pas de plantage). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `localization.text("menu.inconnue")` vaut `"menu.inconnue"`. |
| **LocalizationTest.ChangementDeLangue** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Localization/test_localization.cpp:96`</sub> | Changer de langue résout les valeurs de la nouvelle langue. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `localization.activeLanguage()` vaut `"en"`.<br/>Vérifie que `localization.text("menu.quitter")` vaut `"Quit"`. |
| **LocalizationTest.RepliSurLangueParDefaut** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Localization/test_localization.cpp:115`</sub> | Une clé manquante dans la langue active retombe sur la langue par défaut. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `localization.text("menu.quitter")` vaut `"Quit"`.<br/>Vérifie que `localization.text("menu.titre")` vaut `"Jeu"`. |
| **LocalizationTest.LangueAbsenteEstRecuperable** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Localization/test_localization.cpp:134`</sub> | Charger une langue absente échoue proprement et conserve la langue active (récupérable). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `localization.loadLanguage("xx")` est faux.<br/>Vérifie que `localization.activeLanguage()` vaut `"fr"`.<br/>Vérifie que `localization.text("menu.quitter")` vaut `"Quitter"`. |
| **LocalizationTest.CatalogueFrancaisLivreSeCharge** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Localization/test_localization.cpp:155`</sub> | Le catalogue français livré (Source/Elements/Localization) se charge et résout ses clés. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `localization.loadDefaultLanguage("fr")` est vrai.<br/>Vérifie que `localization.activeLanguage()` vaut `"fr"`.<br/>Vérifie que `localization.text("menu.quitter")` vaut `"Quitter"`.<br/>Vérifie que `localization.text("menu.jouer")` vaut `"Jouer"`. |

## Tests d'intégration (79)

### Animation Personnage — `test_animation_personnage.cpp` (5)

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **AnimationPersonnageIntegration.ImmobileAuSolEstEnRepos** (Majeur)<br/><sub>`Source/Test/Integration/test_animation_personnage.cpp:36`</sub> | Au sol et immobile, le clip est Idle et l'image alterne 0/1 après chaque durée d'image. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `animation.clip` vaut `core::AnimationClip::Idle`.<br/>Vérifie que `animation.frameIndex` vaut `0`.<br/>Vérifie que `animation.clip` vaut `core::AnimationClip::Idle`.<br/>Vérifie que `animation.frameIndex` vaut `1`. |
| **AnimationPersonnageIntegration.EnMouvementAuSolCourt** (Majeur)<br/><sub>`Source/Test/Integration/test_animation_personnage.cpp:67`</sub> | Au sol et en mouvement, le clip est Run et l'image boucle sur les 4 images dans l'ordre. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `animation.clip` vaut `core::AnimationClip::Run`.<br/>Vérifie que `animation.frameIndex` vaut `0`.<br/>Vérifie que `animation.clip` vaut `core::AnimationClip::Run`.<br/>Vérifie que `animation.frameIndex` vaut `expectedFrame`. |
| **AnimationPersonnageIntegration.EnLAirEstEnSaut** (Majeur)<br/><sub>`Source/Test/Integration/test_animation_personnage.cpp:100`</sub> | En l'air, le clip est Jump et l'image reste figée sur 0, quelle que soit la durée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `animation.clip` vaut `core::AnimationClip::Jump`.<br/>Vérifie que `animation.frameIndex` vaut `0`. |
| **AnimationPersonnageIntegration.ChangementDeClipReinitialiseLImage** (Majeur)<br/><sub>`Source/Test/Integration/test_animation_personnage.cpp:125`</sub> | Un changement de clip réinitialise immédiatement l'image et le chronomètre à zéro. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `animation.clip` vaut `core::AnimationClip::Run`.<br/>Vérifie que `animation.frameIndex` vaut `1`.<br/>Vérifie que `animation.clip` vaut `core::AnimationClip::Jump`.<br/>Vérifie que `animation.frameIndex` vaut `0`.<br/>Vérifie que `animation.elapsed` vaut `0.0f` (comparaison flottante). |
| **AnimationPersonnageIntegration.EntiteSansAnimationIgnoree** (Mineur)<br/><sub>`Source/Test/Integration/test_animation_personnage.cpp:166`</sub> | Une entité sans composant Animation n'est pas affectée par le système. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.hasComponent<core::Animation>(entity)` est faux. |

### Bloc Réduit — `test_bloc_reduit.cpp` (2)

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **BlocReduitIntegration.ArreteAuBordDeSaBoiteReelle** (Majeur)<br/><sub>`Source/Test/Integration/test_bloc_reduit.cpp:112`</sub> | Un bloc réduit coincé contre un mur arrête le personnage au bord de sa boîte réelle, pas de la case entière. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `blocks.positions()[0]` vaut `(core::GridPosition{2, 2})`.<br/>Vérifie que `world.getComponent<core::Transform>(player).position.x` vaut `1.25f`, à `1e-3f` près. |
| **BlocReduitIntegration.EspaceAutourResteFranchissable** (Majeur)<br/><sub>`Source/Test/Integration/test_bloc_reduit.cpp:154`</sub> | L'espace autour d'un bloc réduit, hors de sa boîte réelle, reste franchissable. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `endX - startX` est strictement supérieur à `8.0f`. |

### Boucle Simulation — `test_boucle_simulation.cpp` (2)

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **BoucleSimulationIntegration.CadenceurPiloteLaSimulation** (Majeur)<br/><sub>`Source/Test/Integration/test_boucle_simulation.cpp:50`</sub> | Le cadenceur découpe un temps réel variable en pas fixes, et la position finale correspond exactement au nombre de pas réellement exécutés (aucune dérive). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `totalSteps` vaut `4`.<br/>Vérifie que `position.x` vaut `6.0f * totalSteps * STEP`, à `TOLERANCE` près. |
| **BoucleSimulationIntegration.MemeEntreeMemeResultat** (Majeur)<br/><sub>`Source/Test/Integration/test_boucle_simulation.cpp:76`</sub> | Déterminisme : même séquence de frames et même état initial -> même résultat. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `firstPosition.x` vaut `secondPosition.x`, à `TOLERANCE` près.<br/>Vérifie que `firstPosition.y` vaut `secondPosition.y`, à `TOLERANCE` près. |

### Dangers avancés — `test_danger_avance.cpp` (4)

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **DangerAvanceIntegrationTest.DangerDirectionnelMortelSeulementSurSaBande** (Majeur)<br/><sub>`Source/Test/Integration/test_danger_avance.cpp:77`</sub> | Un danger directionnel n'est mortel que sur la bande de son bord désigné. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `outcomeAt(5.0f)` vaut `core::LevelOutcome::Playing`.<br/>Vérifie que `outcomeAt(5.85f)` vaut `core::LevelOutcome::Lost`. |
| **DangerAvanceIntegrationTest.DangerMobileRattrapeLePersonnageImmobile** (Majeur)<br/><sub>`Source/Test/Integration/test_danger_avance.cpp:115`</sub> | Un danger mobile finit par rattraper un personnage sur sa trajectoire. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `outcome` vaut `core::LevelOutcome::Playing`.<br/>Vérifie que `outcome` vaut `core::LevelOutcome::Lost`. |
| **DangerAvanceIntegrationTest.DangerCommuteMortelSeulementApresDeclenchement** (Majeur)<br/><sub>`Source/Test/Integration/test_danger_avance.cpp:160`</sub> | Un danger commuté ne devient mortel qu'une fois son déclencheur actionné. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `walkAndCheckIfEverLost(false)` est faux.<br/>Vérifie que `walkAndCheckIfEverLost(true)` est vrai. |
| **DangerAvanceIntegrationTest.DangerTemporiseMortelSeulementPendantSaFenetreActive** (Majeur)<br/><sub>`Source/Test/Integration/test_danger_avance.cpp:218`</sub> | Un danger temporisé n'est mortel que pendant sa fenêtre active. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `outcomeAtSpawn(0)` vaut `core::LevelOutcome::Lost`.<br/>Vérifie que `outcomeAtSpawn(15)` vaut `core::LevelOutcome::Playing`. |

### Ecs Mouvement — `test_ecs_mouvement.cpp` (4)

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **EcsMouvementIntegration.EntiteMobileAvanceDeVitesseFoisPas** (Majeur)<br/><sub>`Source/Test/Integration/test_ecs_mouvement.cpp:25`</sub> | Une entité Transform + Velocity avance de `velocity * fixedDelta` par pas ; après N pas, la position attendue est déterministe. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `position.x` vaut `2.0f * steps * STEP`, à `TOLERANCE` près.<br/>Vérifie que `position.y` vaut `-3.0f * steps * STEP`, à `TOLERANCE` près. |
| **EcsMouvementIntegration.EntiteSansVelociteNeBougePas** (Majeur)<br/><sub>`Source/Test/Integration/test_ecs_mouvement.cpp:55`</sub> | Une entité sans Velocity ne bouge pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `position.x` vaut `5.0f`, à `TOLERANCE` près.<br/>Vérifie que `position.y` vaut `5.0f`, à `TOLERANCE` près. |
| **EcsMouvementIntegration.DeuxEntitesEvoluentIndependamment** (Majeur)<br/><sub>`Source/Test/Integration/test_ecs_mouvement.cpp:81`</sub> | Deux entités de vitesses différentes évoluent indépendamment. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `slowPosition.x` vaut `1.0f`, à `TOLERANCE` près.<br/>Vérifie que `slowPosition.y` vaut `0.0f`, à `TOLERANCE` près.<br/>Vérifie que `fastPosition.x` vaut `0.0f`, à `TOLERANCE` près.<br/>Vérifie que `fastPosition.y` vaut `4.0f`, à `TOLERANCE` près. |
| **EcsMouvementIntegration.IntegrationViaWorldUpdate** (Majeur)<br/><sub>`Source/Test/Integration/test_ecs_mouvement.cpp:114`</sub> | Enregistré dans le World, le système s'exécute via World::update (chaîne complète). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `position.x` vaut `10.0f * steps * STEP`, à `TOLERANCE` près. |

### Niveau Ecs — `test_niveau_ecs.cpp` (2)

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **NiveauEcsIntegration.DuJsonAuxEntites** (Majeur)<br/><sub>`Source/Test/Integration/test_niveau_ecs.cpp:49`</sub> | Le niveau chargé peuple le monde d'une entité par tuile non vide, bien placée et typée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `tilesByPosition.size()` vaut `3u`.<br/>Vérifie que `tilesByPosition.count({0, 1})` vaut `0u`.<br/>Vérifie que `tilesByPosition.at({0, 0})` vaut `static_cast<int>(core::TileType::Entry)`.<br/>Vérifie que `tilesByPosition.at({1, 0})` vaut `static_cast<int>(core::TileType::Solid)`.<br/>Vérifie que `tilesByPosition.at({1, 1})` vaut `static_cast<int>(core::TileType::Exit)`. |
| **NiveauEcsIntegration.FichierDemoVersMonde** (Majeur)<br/><sub>`Source/Test/Integration/test_niveau_ecs.cpp:85`</sub> | Le niveau de démonstration livré, chargé depuis le fichier, peuple bien le monde. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `nonEmptyTiles` est strictement supérieur à `0`.<br/>Vérifie que `entities` vaut `nonEmptyTiles`. |

### Physique Personnage — `test_physique_personnage.cpp` (60)

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **PhysiquePersonnageIntegration.TombeSousGraviteVitesseCroissante** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:389`</sub> | Sans sol, le personnage tombe et sa vitesse verticale croît (gravité continue). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `vy1` est strictement supérieur à `0.0f`.<br/>Vérifie que `vy2` est strictement supérieur à `vy1`.<br/>Vérifie que `y2` est strictement supérieur à `y1`.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est faux. |
| **PhysiquePersonnageIntegration.ChuteConvergeVersUneVitesseTerminale** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:421`</sub> | En chute prolongée, la vitesse verticale converge vers une vitesse terminale sans la dépasser. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `lastVy` est inférieur ou égal à `expectedTerminal + TOLERANCE`.<br/>Vérifie que `lastVy` vaut `expectedTerminal`, à `0.05f` près. |
| **PhysiquePersonnageIntegration.AccelerationDeChuteDecroitVersLeRegimePermanent** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:456`</sub> | L'accélération verticale décroît à mesure que la vitesse approche le régime permanent. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `delta` est inférieur ou égal à `previousDelta + TOLERANCE`. |
| **PhysiquePersonnageIntegration.MasseSuperieureTombePlusVite** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:496`</sub> | Une masse plus grande produit une vitesse terminale plus élevée, à traînée égale. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `heavyVy` est strictement supérieur à `lightVy`. |
| **PhysiquePersonnageIntegration.AtterritSurLeSolEtEstAuSol** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:525`</sub> | Le personnage se pose sur le sol : vitesse verticale annulée, état « au sol » vrai. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `transform.position.y` vaut `5.0f`, à `TOLERANCE` près.<br/>Vérifie que `velocity.value.y` vaut `0.0f`, à `TOLERANCE` près.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai. |
| **PhysiquePersonnageIntegration.BloqueParUnMurADroite** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:555`</sub> | Poussé contre un mur, le personnage s'arrête au ras du mur (blocage horizontal). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `transform.position.x` vaut `3.0f`, à `TOLERANCE` près. |
| **PhysiquePersonnageIntegration.AvanceAVitesseConstante** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:584`</sub> | Sur terrain libre, l'avancée horizontale vaut vitesse × temps (vitesse constante). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `endX - startX` vaut `config.moveSpeed * steps * STEP`, à `1e-3f` près. |
| **PhysiquePersonnageIntegration.NeTraversePasLeSolEnChuteRapide** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:615`</sub> | Même en chute très rapide (grand pas), le personnage ne traverse pas le sol (balayage continu). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `transform.position.y` vaut `49.0f`, à `TOLERANCE` près.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai. |
| **PhysiquePersonnageIntegration.Deterministe** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:647`</sub> | Mêmes entrées → même résultat : la simulation est déterministe (pas fixe). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `first.x` vaut `second.x` (comparaison flottante).<br/>Vérifie que `first.y` vaut `second.y` (comparaison flottante). |
| **PhysiquePersonnageIntegration.SauteDepuisLeSol** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:677`</sub> | Au sol, une pression de saut fait décoller le personnage (vitesse ascendante, il s'élève). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai.<br/>Vérifie que `world.getComponent<core::Velocity>(player).value.y` est strictement inférieur à `0.0f`.<br/>Vérifie que `world.getComponent<core::Transform>(player).position.y` est strictement inférieur à `groundY`. |
| **PhysiquePersonnageIntegration.PasDeSautEnLAir** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:717`</sub> | En l'air, une pression de saut n'a aucun effet : pas de double saut (`EX-GP-013`). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Player>(player).grounded` est faux.<br/>Vérifie que `world.getComponent<core::Velocity>(player).value.y` est strictement supérieur à `0.0f`. |
| **PhysiquePersonnageIntegration.HauteurDeSautVariable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:746`</sub> | Hauteur de saut variable : maintenir le bouton saute plus haut que le relâcher tôt. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `fullJump` est strictement supérieur à `shortHop + 0.3f`.<br/>Vérifie que `fullJump` est strictement supérieur à `2.0f`.<br/>Vérifie que `fullJump` est strictement inférieur à `3.0f`. |
| **PhysiquePersonnageIntegration.DoubleSautNombreParametrable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:766`</sub> | Double saut : 1 saut au sol + N sauts aériens (paramétrable), rechargés au contact du sol. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `successfulJumps(1)` vaut `2`.<br/>Vérifie que `successfulJumps(2)` vaut `3`.<br/>Vérifie que `successfulJumps(0)` vaut `1`. |
| **PhysiquePersonnageIntegration.ChutePlusRapideQueLaMontee** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:784`</sub> | Gravité asymétrique : la chute accélère plus vite que la montée ne décélère (EX-GP-018). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `riseDelta` est strictement supérieur à `0.0f`.<br/>Vérifie que `fallDelta` est strictement supérieur à `0.0f`.<br/>Vérifie que `fallDelta` est strictement supérieur à `riseDelta * 1.3f`. |
| **PhysiquePersonnageIntegration.ApexHangReduitLaGravite** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:833`</sub> | Apex hang : près du sommet du saut, la gravité est réduite (contrôle flottant). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `earlyRise` est strictement supérieur à `0.0f`.<br/>Vérifie que `atApex` est strictement supérieur à `0.0f`.<br/>Vérifie que `atApex` est strictement inférieur à `earlyRise * 0.8f`. |
| **PhysiquePersonnageIntegration.FastFallAccelereLaChute** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:880`</sub> | Fast-fall : maintenir « bas » en l'air fait tomber plus loin qu'une chute libre normale. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `fallDistance(true)` est strictement supérieur à `fallDistance(false)`. |
| **PhysiquePersonnageIntegration.DashHorizontalRapide** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:908`</sub> | Dash horizontal : une ruée rapide (≫ vitesse normale) fait parcourir une grande distance. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Velocity>(player).value.x` est strictement supérieur à `config.moveSpeed * 2.0f`.<br/>Vérifie que `world.getComponent<core::Transform>(player).position.x - startX` est strictement supérieur à `config.dashSpeed * config.dashDuration * 0.7f`. |
| **PhysiquePersonnageIntegration.DashDiagonalHautDroite** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:945`</sub> | Dash diagonal (8 directions) : viser haut-droite envoie en +X et −Y. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `velocity.value.x` est strictement supérieur à `0.0f`.<br/>Vérifie que `velocity.value.y` est strictement inférieur à `0.0f`. |
| **PhysiquePersonnageIntegration.GraviteSuspenduePendantDash** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:976`</sub> | Pendant un dash horizontal, la gravité est suspendue (la composante verticale reste nulle). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Velocity>(player).value.y` vaut `0.0f`, à `0.1f` près. |
| **PhysiquePersonnageIntegration.DashUneSeuleFoisEnLAir** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1007`</sub> | Une seule ruée par phase aérienne : le second dash en l'air est refusé jusqu'au retour au sol. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Player>(player).grounded` est faux.<br/>Vérifie que `world.getComponent<core::Player>(player).dashAvailable` est vrai.<br/>Vérifie que `world.getComponent<core::Velocity>(player).value.x` est strictement supérieur à `config.dashSpeed * 0.9f`.<br/>Vérifie que `world.getComponent<core::Player>(player).dashAvailable` est faux.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est faux.<br/>Vérifie que `world.getComponent<core::Velocity>(player).value.x` est strictement inférieur à `config.dashSpeed * 0.9f`. |
| **PhysiquePersonnageIntegration.DashNeTraversePasLeMur** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1062`</sub> | Un dash vers un mur ne le traverse pas (résolu par le balayage). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Transform>(player).position.x` est inférieur ou égal à `4.0f + 0.01f`. |
| **PhysiquePersonnageIntegration.BudgetDeSautsRefuseAuDela** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1090`</sub> | Budget de sauts (EX-GP-024) : avec 1 saut, le premier fonctionne, le suivant est refusé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Velocity>(player).value.y` est strictement inférieur à `0.0f`.<br/>Vérifie que `world.getComponent<core::Player>(player).jumpsRemaining` vaut `0`.<br/>Vérifie que `world.getComponent<core::Velocity>(player).value.y` est strictement supérieur à `-1.0f`. |
| **PhysiquePersonnageIntegration.BudgetDeDashsRefuseAuDela** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1127`</sub> | Budget de dashs (EX-GP-024) : avec 1 dash, le premier fonctionne, le suivant est refusé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Velocity>(player).value.x` est strictement supérieur à `10.0f`.<br/>Vérifie que `world.getComponent<core::Player>(player).dashesRemaining` vaut `0`.<br/>Vérifie que `world.getComponent<core::Velocity>(player).value.x` est strictement inférieur à `10.0f`. |
| **PhysiquePersonnageIntegration.WallSlideRalentitLaChute** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1164`</sub> | Wall slide : collé à un mur en l'air, la vitesse de chute est plafonnée (descente ralentie). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Player>(player).wallDirection` diffère de `0.0f`.<br/>Vérifie que `velocity.value.y` est strictement supérieur à `0.0f`.<br/>Vérifie que `velocity.value.y` est inférieur ou égal à `config.wallSlideSpeed + 0.5f`. |
| **PhysiquePersonnageIntegration.WallJumpEjecteAlOpposeDuMur** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1196`</sub> | Wall jump : contre un mur à droite, un saut éjecte vers la gauche et le haut. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Player>(player).wallDirection` diffère de `0.0f`.<br/>Vérifie que `velocity.value.x` est strictement inférieur à `0.0f`.<br/>Vérifie que `velocity.value.y` est strictement inférieur à `0.0f`. |
| **PhysiquePersonnageIntegration.PasDeWallJumpSansMur** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1232`</sub> | Sans mur, la logique de wall jump ne s'active pas (et sans saut aérien, aucun saut en l'air). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Player>(player).wallDirection` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `velocity.value.x` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `velocity.value.y` est strictement supérieur à `before`. |
| **PhysiquePersonnageIntegration.Niveau2FranchissableAvecSaut** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1263`</sub> | Le niveau saut est franchissable **avec le saut** : en avançant et sautant, on atteint la sortie. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playLevelFile("demo-saut.json", rightAndJump)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.Niveau2RequiertLeSaut** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1286`</sub> | Le niveau saut **exige** le saut : en avançant seulement (sans sauter), la marche bloque. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playLevelFile("demo-saut.json", rightOnly)` diffère de `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.Niveau3FranchissableAvecDash** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1308`</sub> | Le niveau dash (couloir bas + fosse) est franchissable **au dash** : avancer + dasher franchit. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playLevelFile("demo-dash.json", rightAndDash)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.Niveau3RequiertLeDash** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1330`</sub> | Le niveau dash **exige** le dash : en avançant seulement, on tombe dans la fosse de danger. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playLevelFile("demo-dash.json", rightOnly)` diffère de `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.Niveau4FranchissableAvecInterrupteur** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1352`</sub> | Le niveau interrupteur : toucher l'interrupteur ouvre la porte → la sortie devient atteignable. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playPuzzleFile("demo-interrupteur.json", right)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.PorteFermeeBloque** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1373`</sub> | Une porte **fermée** (interrupteur non touché) **bloque** : la sortie reste inatteignable. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `simulatePuzzle(level, right, 600)` diffère de `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.CoyoteTimeAutoriseUnSautJusteApresLeBord** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1406`</sub> | Coyote time : sauter juste après avoir quitté un bord fonctionne ; trop tard, non. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `jumpAfterLeavingLedge(0)` est vrai.<br/>Vérifie que `jumpAfterLeavingLedge(15)` est faux. |
| **PhysiquePersonnageIntegration.JumpBufferingHonoreUnSautPreAppuye** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1423`</sub> | Jump buffering : un saut pré-appuyé peu avant l'atterrissage s'exécute à la pose ; trop tôt, non. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bufferedJump(2)` est vrai.<br/>Vérifie que `bufferedJump(30)` est faux. |
| **PhysiquePersonnageIntegration.NiveauDemoEstFranchissableEnAllantADroite** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1441`</sub> | Le niveau de démonstration livré est **franchissable** sans saut : en maintenant « droite », le personnage descend l'escalier de paliers et atteint la sortie sans jamais mourir. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `outcome` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.SuitUnePenteAscendanteEnMarchant** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1480`</sub> | Marcher sur une pente ascendante fait monter le personnage progressivement, sans traverser la surface. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bottom` est inférieur ou égal à `6.0f + TOLERANCE`.<br/>Vérifie que `bottom` est supérieur ou égal à `5.0f - TOLERANCE`.<br/>Vérifie que `sawSlopeSample` est vrai.<br/>Vérifie que `yExitSlope` est strictement inférieur à `yEnterSlope - 0.5f`.<br/>Vérifie que `guard` est strictement inférieur à `600`.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai.<br/>Vérifie que `world.getComponent<core::Transform>(player).position.y` vaut `4.0f`, à `0.05f` près. |
| **PhysiquePersonnageIntegration.SuitUnePenteDescendanteEnMarchant** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1543`</sub> | Marcher sur une pente descendante fait descendre le personnage progressivement, symétrique de la montée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bottom` est inférieur ou égal à `6.0f + TOLERANCE`.<br/>Vérifie que `bottom` est supérieur ou égal à `5.0f - TOLERANCE`.<br/>Vérifie que `sawSlopeSample` est vrai.<br/>Vérifie que `yExitSlope` est strictement supérieur à `yEnterSlope + 0.5f`.<br/>Vérifie que `guard` est strictement inférieur à `600`.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai.<br/>Vérifie que `world.getComponent<core::Transform>(player).position.y` vaut `5.0f`, à `0.05f` près. |
| **PhysiquePersonnageIntegration.ChuteRapideSurUnePenteSansLaTraverser** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1607`</sub> | Même en chute très rapide, le personnage tombant sur une pente se pose sur sa surface au premier contact, sans la traverser. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `transform.position.y` vaut `49.2f`, à `TOLERANCE` près.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai. |
| **PhysiquePersonnageIntegration.SauteDepuisUnePenteImpulsionNormale** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1640`</sub> | Sauter depuis une pente produit une impulsion identique à un saut depuis un sol plat : le suivi de pente ne l'absorbe pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai.<br/>Vérifie que `world.getComponent<core::Velocity>(player).value.y` est strictement inférieur à `-10.0f`.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est faux.<br/>Vérifie que `world.getComponent<core::Transform>(player).position.y` est strictement inférieur à `landedY`. |
| **PhysiquePersonnageIntegration.TransitionPenteSolPlatSansAACoup** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1685`</sub> | La transition entre une pente et le sol plat se fait sans à-coup ni blocage : la position verticale varie continûment. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `maxStep` est strictement inférieur à `0.5f`. |
| **PhysiquePersonnageIntegration.SuitUnArrondiAscendantEnMarchant** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1737`</sub> | Marcher sur un arrondi ascendant fait monter le personnage en suivant une courbe distincte d'une pente linéaire, sans le traverser. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bottom` est inférieur ou égal à `6.0f + TOLERANCE`.<br/>Vérifie que `bottom` est supérieur ou égal à `5.0f - TOLERANCE`.<br/>Vérifie que `sawMidSample` est vrai.<br/>Vérifie que `midBottom` est strictement inférieur à `5.3f`.<br/>Vérifie que `guard` est strictement inférieur à `600`.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai.<br/>Vérifie que `world.getComponent<core::Transform>(player).position.y` vaut `4.0f`, à `0.05f` près. |
| **PhysiquePersonnageIntegration.NiveauPenteFranchissable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1797`</sub> | Le niveau pente est franchissable en marchant, sans saut. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playLevelFile("demo-pente.json", rightOnly)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauPlaquePressionFranchissable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1813`</sub> | Le niveau plaque de pression est franchissable : la plaque ouvre la porte, un saut par-dessus le mur permet de rejoindre la sortie. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playReactiveFile("demo-plaque-pression.json", script)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauPlaquePressionExigeUnSaut** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1845`</sub> | Le niveau plaque de pression exige un saut : marcher sans sauter reste bloqué contre le mur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playLevelFile("demo-plaque-pression.json", rightOnly)` diffère de `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauArrondiFranchissable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1863`</sub> | Le niveau arrondi est franchissable en marchant, sans saut. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playLevelFile("demo-arrondi.json", rightOnly)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.PlafondInclineBloqueSelonSaSilhouette** (Bloquant)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1880`</sub> | Une pente de plafond bloque un saut selon sa silhouette réelle, pas comme un carré plein uniforme. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `thinSideMinY` est strictement inférieur à `thickSideMinY - 0.3f`. |
| **PhysiquePersonnageIntegration.PlafondInclineSupportePersonnageParLeDessus** (Bloquant)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1938`</sub> | La face du haut d'une pente de plafond supporte le personnage qui tombe dessus par le dessus. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `player_.grounded` est vrai.<br/>Vérifie que `transform.position.y` vaut `static_cast<float>(CEILING_ROW) - core::kPlayerHeight`, à `0.05f` près. |
| **PhysiquePersonnageIntegration.SuitUnArrondiConcaveAscendantEnMarchant** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1979`</sub> | Marcher sur un arrondi concave ascendant suit une courbe inversée par rapport à l'arrondi convexe, sans le traverser. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bottom` est inférieur ou égal à `6.0f + TOLERANCE`.<br/>Vérifie que `bottom` est supérieur ou égal à `5.0f - TOLERANCE`.<br/>Vérifie que `sawMidSample` est vrai.<br/>Vérifie que `midBottom` est strictement supérieur à `5.7f`.<br/>Vérifie que `guard` est strictement inférieur à `600`.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai.<br/>Vérifie que `world.getComponent<core::Transform>(player).position.y` vaut `5.0f - size.y`, à `0.05f` près. |
| **PhysiquePersonnageIntegration.ConcaveDePlafondBloqueSelonSaSilhouette** (Bloquant)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2055`</sub> | Un arrondi concave de plafond bloque un saut selon sa silhouette réelle, pas comme un carré plein uniforme. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `thinSideMinY` est strictement inférieur à `thickSideMinY - 0.3f`. |
| **PhysiquePersonnageIntegration.ConcaveDePlafondSupportePersonnageParLeDessus** (Bloquant)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2119`</sub> | La face du haut d'un arrondi concave de plafond supporte le personnage qui tombe dessus par le dessus. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `player_.grounded` est vrai.<br/>Vérifie que `transform.position.y` vaut `static_cast<float>(CEILING_ROW) - core::kPlayerHeight`, à `0.05f` près. |
| **PhysiquePersonnageIntegration.ArrondisConcavesDeSolAdjacentsSansChuteALaJointure** (Bloquant)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2163`</sub> | Deux arrondis concaves de sol adjacents restent praticables à leur jointure, sans chute jusqu'au fond de la case voisine. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `guard` est strictement inférieur à `600`.<br/>Vérifie que `maxBottomNearPeak` est strictement inférieur à `5.4f`.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai.<br/>Vérifie que `world.getComponent<core::Transform>(player).position.y` vaut `6.0f - size.y`, à `0.05f` près. |
| **PhysiquePersonnageIntegration.ConcaveDePlafondBordFinResteBloqueSansTeleportation** (Bloquant)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2231`</sub> | Un arrondi concave de plafond bloque un saut visant son bord plein, même tout près du bord de sa propre case. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `y` est strictement supérieur à `static_cast<float>(CEILING_ROW) - 0.5f`. |
| **PhysiquePersonnageIntegration.NiveauDoubleSautFranchissable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2449`</sub> | Le niveau double saut est franchissable en enchaînant saut au sol et saut aérien. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playReactivePhysicsOnly("demo-double-saut.json", script)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauDoubleSautRequiertLeSautAerien** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2481`</sub> | Le niveau double saut exige le saut aérien : un seul saut ne suffit pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playReactivePhysicsOnly("demo-double-saut.json", script)` diffère de `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauWallJumpFranchissable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2503`</sub> | Le niveau wall jump est franchissable en enchaînant wall slide et wall jump. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playReactivePhysicsOnly("demo-wall-jump.json", script)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauBlocFranchissable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2529`</sub> | Le niveau bloc poussable est franchissable en poussant le bloc contre le mur pour s'en servir de marche. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playReactiveFile("demo-bloc.json", script)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauBlocReduitFranchissable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2561`</sub> | Le niveau bloc à taille réduite est franchissable, le bloc comblant la fosse à la hauteur du chemin. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playReactiveFile("demo-bloc-reduit.json", script)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauBudgetFranchissable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2585`</sub> | Le niveau budget est franchissable avec exactement le budget de sauts prévu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `outcome` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauBudgetRequiertLesDeuxSauts** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2623`</sub> | Le niveau budget exige ses deux sauts : un seul saut autorisé ne suffit pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `outcome` diffère de `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauFinalFranchissable** (Bloquant)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2661`</sub> | Le niveau final combine plusieurs mécaniques en un seul parcours cohérent et reste franchissable. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playReactiveFile("demo-final.json", script)` vaut `core::LevelOutcome::Won`. |

## Tests système (3)

### Parcours Complet — `test_parcours_complet.cpp` (1)

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **ParcoursCompletSysteme.FranchitTouteLaSequence** (Bloquant)<br/><sub>`Source/Test/Systeme/test_parcours_complet.cpp:199`</sub> | Parcours complet : chaque niveau de la séquence est franchi (`Won`) dans l'ordre, puis « retour au titre ». Reproduit la boucle titre → niveau 1 → niveau 2 → titre du jeu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `sequence.empty()` est faux.<br/>Vérifie que `playLevel(level)` vaut `core::LevelOutcome::Won`. |

### Éditeur de niveaux — `test_parcours_edition.cpp` (2)

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **ParcoursEditionSysteme.CreerEditerEnregistrerRechargerEtJouer** (Critique)<br/><sub>`Source/Test/Systeme/test_parcours_edition.cpp:26`</sub> | Parcours complet d'édition : peindre, lier un mécanisme, annuler une erreur, redimensionner, enregistrer, recharger, et confirmer que le niveau produit est directement jouable. | 1. Peindre un niveau (entrée, sortie, interrupteur/porte liés) dans un `LevelDraft`.<br/>2. Peindre une tuile par erreur puis l'annuler (`undo`).<br/>3. Redimensionner la grille.<br/>4. Valider, enregistrer sur disque, recharger.<br/>5. Vérifier le contenu rechargé et l'issue (`evaluateOutcome`) à l'entrée et à la sortie. | Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.tileMap().tile(1, 1)` vaut `core::TileType::Empty`.<br/>Vérifie que `draft.tileMap().width()` vaut `6`.<br/>Vérifie que `draft.tileMap().tile(4, 1)` vaut `core::TileType::Exit`.<br/>Vérifie que `validated.ok()` est vrai.<br/>Vérifie que `core::LevelWriter::saveToFile(*validated.level, path)` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `level.entry()` vaut `(core::GridPosition{0, 1})`.<br/>Vérifie que `level.exit()` vaut `(core::GridPosition{4, 1})`.<br/>Vérifie que `level.jumpBudget()` vaut `2`.<br/>Vérifie que `level.tileMap().tile(1, 1)` vaut `core::TileType::Empty`.<br/>Vérifie que `level.mechanisms().size()` vaut `1u`.<br/>Vérifie que `level.mechanisms().front().switchPosition` vaut `(core::GridPosition{2, 0})`.<br/>Vérifie que `level.mechanisms().front().doorPosition` vaut `(core::GridPosition{2, 2})`.<br/>Vérifie que `core::evaluateOutcome(atEntry, level)` vaut `core::LevelOutcome::Playing`.<br/>Vérifie que `core::evaluateOutcome(atExit, level)` vaut `core::LevelOutcome::Won`. |
| **ParcoursEditionSysteme.EditeConfigureEnregistreEtRechargeLesDangersAvances** (Critique)<br/><sub>`Source/Test/Systeme/test_parcours_edition.cpp:95`</sub> | Parcours d'édition des dangers avancés : peindre, lier, configurer, enregistrer, recharger. | 1. Peindre les sept nouveaux types de danger dans un `LevelDraft`.<br/>2. Lier un interrupteur à un danger commuté (même geste qu'une porte).<br/>3. Configurer l'axe/la portée d'un danger mobile et la période/le déphasage d'un danger temporisé.<br/>4. Enregistrer sur disque, recharger. | Vérifie que `validated.ok()` est vrai.<br/>Vérifie que `core::LevelWriter::saveToFile(*validated.level, path)` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(1, 0)` vaut `core::TileType::DangerUp`.<br/>Vérifie que `level.tileMap().tile(2, 0)` vaut `core::TileType::DangerRight`.<br/>Vérifie que `level.tileMap().tile(4, 0)` vaut `core::TileType::DangerSwitched`.<br/>Vérifie que `level.tileMap().tile(5, 0)` vaut `core::TileType::DangerMover`.<br/>Vérifie que `level.tileMap().tile(6, 0)` vaut `core::TileType::DangerBlink`.<br/>Vérifie que `level.dangerLinks().size()` vaut `1u`.<br/>Vérifie que `level.dangerLinks().front().triggerPosition` vaut `(core::GridPosition{3, 0})`.<br/>Vérifie que `level.dangerLinks().front().dangerPosition` vaut `(core::GridPosition{4, 0})`.<br/>Vérifie que `level.moverConfigs().size()` vaut `1u`.<br/>Vérifie que `level.moverConfigs().front().axis` vaut `core::DangerMoverAxis::Vertical`.<br/>Vérifie que `level.moverConfigs().front().range` vaut `2`.<br/>Vérifie que `level.blinkConfigs().size()` vaut `1u`.<br/>Vérifie que `level.blinkConfigs().front().period` vaut `90`.<br/>Vérifie que `level.blinkConfigs().front().phase` vaut `15`.<br/>Vérifie que `level.blinkConfigs().front().activeDuration` vaut `30`. |
