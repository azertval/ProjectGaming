# Cahier de test {#cahiertest}

**972 cas de test**, générés depuis les blocs `\castest{...}` du code par `scripts/generate_cahier_test.py` (ne pas éditer directement — modifier le commentaire du test concerné puis relancer le script). Organisés ici selon l'arborescence de `Source/Test/` pour rester lisibles page par page.

## Tests unitaires (880)

### Core

**`test_core.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **EngineTest.VersionNonVide** (Majeur)<br/><sub>`Source/Test/Unit/Core/test_core.cpp:12`</sub> | Vérifie que la version du moteur n'est pas vide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `engine.version().empty()` est faux. |

#### Diagnostics (14)

**`test_assert.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **AssertTest.ConditionVraieNInvoquePasLeHandler** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_assert.cpp:14`</sub> | Une condition vraie n'invoque pas le gestionnaire d'assertion. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `invoked` est faux. |
| **AssertTest.ConditionFausseInvoqueLeHandler** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_assert.cpp:37`</sub> | Une condition fausse invoque le gestionnaire une fois, avec le message (Debug uniquement). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `count` vaut `1`.<br/>Vérifie que `capturedMessage` vaut `"echec attendu"`. |

**`test_log_format.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LogFormatTest.LigneContientTousLesChamps** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_log_format.cpp:14`</sub> | La ligne formatée contient horodatage, niveau, catégorie, position source et message. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `line.find("12:34:56")` diffère de `std::string::npos`.<br/>Vérifie que `line.find("WARNING")` diffère de `std::string::npos`.<br/>Vérifie que `line.find("HMI")` diffère de `std::string::npos`.<br/>Vérifie que `line.find("Foo.cpp:42")` diffère de `std::string::npos`.<br/>Vérifie que `line.find("message test")` diffère de `std::string::npos`. |
| **LogFormatTest.CheminReduitAuNomDeFichier** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_log_format.cpp:37`</sub> | Le chemin source est réduit à son nom de fichier dans la ligne. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `line.find("Foo.cpp:7")` diffère de `std::string::npos`.<br/>Vérifie que `line.find("ProjectGaming")` vaut `std::string::npos`.<br/>Vérifie que `line.find("Source")` vaut `std::string::npos`. |
| **LogFormatTest.FileNameIsoleLeNom** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_log_format.cpp:57`</sub> | fileName isole le nom de fichier des chemins Windows et POSIX, ou renvoie l'entrée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::fileName("a/b/c/File.cpp")` vaut `"File.cpp"`.<br/>Vérifie que `core::fileName("a\\\\b\\\\File.cpp")` vaut `"File.cpp"`.<br/>Vérifie que `core::fileName("File.cpp")` vaut `"File.cpp"`. |
| **LogFormatTest.HorodatageFormatHeure** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_log_format.cpp:74`</sub> | L'horodatage courant respecte le format HH:MM:SS (longueur 8). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `timestamp.size()` vaut `8u`.<br/>Vérifie que `timestamp[2]` vaut `':'`.<br/>Vérifie que `timestamp[5]` vaut `':'`. |

**`test_log_level_parse.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LogLevelParseTest.NiveauxReconnus** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_log_level_parse.cpp:12`</sub> | Les noms de niveaux reconnus sont convertis (y compris l'alias « warn »). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::parseLogLevel("trace")` vaut `core::LogLevel::Trace`.<br/>Vérifie que `core::parseLogLevel("info")` vaut `core::LogLevel::Info`.<br/>Vérifie que `core::parseLogLevel("warning")` vaut `core::LogLevel::Warning`.<br/>Vérifie que `core::parseLogLevel("warn")` vaut `core::LogLevel::Warning`.<br/>Vérifie que `core::parseLogLevel("error")` vaut `core::LogLevel::Error`. |
| **LogLevelParseTest.InsensibleALaCasse** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_log_level_parse.cpp:30`</sub> | L'analyse est insensible à la casse. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::parseLogLevel("INFO")` vaut `core::LogLevel::Info`.<br/>Vérifie que `core::parseLogLevel("Warning")` vaut `core::LogLevel::Warning`.<br/>Vérifie que `core::parseLogLevel("Error")` vaut `core::LogLevel::Error`. |
| **LogLevelParseTest.ValeurInconnue** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_log_level_parse.cpp:46`</sub> | Une valeur inconnue ou vide n'est pas convertie. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::parseLogLevel("verbose").has_value()` est faux.<br/>Vérifie que `core::parseLogLevel("").has_value()` est faux. |

**`test_logger.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LoggerTest.FiltreParNiveauMinimal** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_logger.cpp:15`</sub> | Un message au-dessus du niveau minimal est diffusé ; en dessous, il est filtré. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `observed->entries().size()` vaut `1u`.<br/>Vérifie que `observed->entries()[0].level` vaut `core::LogLevel::Error`.<br/>Vérifie que `observed->entries()[0].message` vaut `"garde"`. |
| **LoggerTest.DiffuseAPlusieursSinks** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_logger.cpp:41`</sub> | Le même message atteint tous les sinks enregistrés. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `firstObserved->entries().size()` vaut `1u`.<br/>Vérifie que `secondObserved->entries().size()` vaut `1u`. |
| **LoggerTest.ClearSinksArreteLaDiffusion** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_logger.cpp:66`</sub> | clearSinks retire les destinations : plus rien n'est diffusé ensuite. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `observedAfterClear->entries().size()` vaut `1u`.<br/>Vérifie que `observedAfterClear->entries()[0].message` vaut `"personne"`. |

**`test_sinks.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **MemoryLogSinkTest.ConserveNiveauEtTexteDansLOrdre** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_sinks.cpp:12`</sub> | Le sink mémoire conserve fidèlement niveau et texte, dans l'ordre. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `sink.entries().size()` vaut `2u`.<br/>Vérifie que `sink.entries()[0].level` vaut `core::LogLevel::Info`.<br/>Vérifie que `sink.entries()[0].message` vaut `"premier"`.<br/>Vérifie que `sink.entries()[1].level` vaut `core::LogLevel::Error`.<br/>Vérifie que `sink.entries()[1].message` vaut `"second"`. |
| **MemoryLogSinkTest.ClearVideLesMessages** (Majeur)<br/><sub>`Source/Test/Unit/Core/Diagnostics/test_sinks.cpp:34`</sub> | clear vide les messages mémorisés. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `sink.entries().empty()` est vrai. |

#### Ecs (41)

**`test_animation_clip.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **AnimationClipTest.ClipExistantResoluParNom** (Critique)<br/><sub>`Source/Test/Unit/Core/Ecs/test_animation_clip.cpp:29`</sub> | Un clip existant est résolu par son nom. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `clips.clipCount()` vaut `2`.<br/>Vérifie que `clips.indexOf("idle")` est supérieur ou égal à `0`.<br/>Vérifie que `clips.indexOf("run")` est supérieur ou égal à `0`.<br/>Vérifie que `clips.clipAt(clips.indexOf("idle")).name` vaut `"idle"`.<br/>Vérifie que `clips.clipAt(clips.indexOf("run")).name` vaut `"run"`. |
| **AnimationClipTest.ClipInexistantRepliDeterministe** (Critique)<br/><sub>`Source/Test/Unit/Core/Ecs/test_animation_clip.cpp:52`</sub> | Un nom inconnu ou un index hors bornes retombe sur le premier clip, sans plantage. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `clips.indexOf("inconnu")` vaut `-1`.<br/>Vérifie que `clips.clipAt(-1).name` vaut `"idle"`.<br/>Vérifie que `clips.clipAt(42).name` vaut `"idle"`. |
| **AnimationClipTest.JeuVideRepliSurUnClipParDefaut** (Critique)<br/><sub>`Source/Test/Unit/Core/Ecs/test_animation_clip.cpp:75`</sub> | Un jeu de clips vide se replie sur un clip par défaut portant au moins une image. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `clips.clipCount()` vaut `0`.<br/>Vérifie que `fallback.name.empty()` est vrai.<br/>Vérifie que `fallback.frames.empty()` est faux.<br/>Vérifie que `fallback.frameDuration` vaut `0.0f`. |
| **AnimationClipTest.ClipAUneSeuleImage** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_animation_clip.cpp:95`</sub> | Un clip d'une seule image est un clip valide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `clip.frames.size()` vaut `1u`. |
| **AnimationClipTest.DureesInegalesEntreClips** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_animation_clip.cpp:112`</sub> | Chaque clip garde sa propre durée d'image. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `clips.clipAt(clips.indexOf("idle")).frameDuration` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `clips.clipAt(clips.indexOf("run")).frameDuration` vaut `0.1f` (comparaison flottante). |
| **AnimationClipTest.ClipJoueUneFoisAvecClipSuivant** (Critique)<br/><sub>`Source/Test/Unit/Core/Ecs/test_animation_clip.cpp:132`</sub> | Un clip joué une fois désigne un clip suivant, résolu dans le même jeu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `opening.endMode` vaut `core::ClipEndMode::OneShot`.<br/>Vérifie que `opening.nextClip` vaut `"open"`.<br/>Vérifie que `clips.indexOf(opening.nextClip)` est supérieur ou égal à `0`. |
| **AnimationClipTest.AjouterUnClipDeMemeNomLeRemplace** (Critique)<br/><sub>`Source/Test/Unit/Core/Ecs/test_animation_clip.cpp:154`</sub> | Ajouter un clip de même nom remplace l'existant, à index constant. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `clips.clipCount()` vaut `1`.<br/>Vérifie que `clips.indexOf("idle")` vaut `firstIndex`.<br/>Vérifie que `clips.clipAt(firstIndex).frames.size()` vaut `3u`. |

**`test_component_pool.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **ComponentPoolTest.AjoutPuisAcces** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_component_pool.cpp:25`</sub> | `add` puis `get` renvoie la valeur stockée ; `has` est cohérent. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `pool.has(entity)` est faux.<br/>Vérifie que `pool.has(entity)` est vrai.<br/>Vérifie que `pool.get(entity).x` vaut `3`.<br/>Vérifie que `pool.get(entity).y` vaut `4`.<br/>Vérifie que `pool.size()` vaut `1u`. |
| **ComponentPoolTest.GetRenvoieReferenceModifiable** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_component_pool.cpp:49`</sub> | `get` renvoie une référence modifiable sur le composant stocké. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `pool.get(entity).x` vaut `42`. |
| **ComponentPoolTest.RemoveAuMilieuSwapAndPop** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_component_pool.cpp:71`</sub> | `remove` d'un élément au milieu (swap-and-pop) laisse les autres composants accessibles et corrects, et le stockage dense reste contigu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `pool.has(middle)` est faux.<br/>Vérifie que `pool.has(first)` est vrai.<br/>Vérifie que `pool.has(last)` est vrai.<br/>Vérifie que `pool.get(first)` vaut `10`.<br/>Vérifie que `pool.get(last)` vaut `30`.<br/>Vérifie que `pool.size()` vaut `2u`.<br/>Vérifie que `pool.components().size()` vaut `2u`.<br/>Vérifie que `pool.entities().size()` vaut `2u`. |
| **ComponentPoolTest.RemoveDernierElement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_component_pool.cpp:107`</sub> | Retirer le dernier élément ne perturbe pas les précédents. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `pool.has(a)` est vrai.<br/>Vérifie que `pool.has(b)` est faux.<br/>Vérifie que `pool.get(a)` vaut `1`.<br/>Vérifie que `pool.size()` vaut `1u`. |
| **ComponentPoolTest.HandlePerimeNePossedePasLeComposant** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_component_pool.cpp:134`</sub> | Un handle périmé (index recyclé, génération différente) ne possède pas le composant de l'ancienne entité. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `pool.has(original)` est vrai.<br/>Vérifie que `pool.has(recycled)` est faux. |
| **ComponentPoolTest.RemoveIfPresent** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_component_pool.cpp:161`</sub> | `removeIfPresent` retire si le composant existe, sinon ne fait rien. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `pool.removeIfPresent(entity)` est faux.<br/>Vérifie que `pool.removeIfPresent(entity)` est vrai.<br/>Vérifie que `pool.has(entity)` est faux. |
| **ComponentPoolTest.GetSurEntiteAbsenteViolePrecondition** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_component_pool.cpp:182`</sub> | `get` sur une entité absente viole une précondition (assertion en Debug). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que l'opération lève bien une exception `std::runtime_error`. |

**`test_entity_manager.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **EntityManagerTest.CreeEntitesVivantesEtDistinctes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_entity_manager.cpp:13`</sub> | `create` renvoie des entités vivantes et distinctes. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `manager.isAlive(a)` est vrai.<br/>Vérifie que `manager.isAlive(b)` est vrai.<br/>Vérifie que `a` diffère de `b`.<br/>Vérifie que `manager.aliveCount()` vaut `2u`. |
| **EntityManagerTest.DestructionInvalideLeHandle** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_entity_manager.cpp:34`</sub> | Après destruction, l'ancien handle n'est plus vivant. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `manager.isAlive(entity)` est faux.<br/>Vérifie que `manager.aliveCount()` vaut `0u`. |
| **EntityManagerTest.RecyclageChangeLaGeneration** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_entity_manager.cpp:55`</sub> | Un index recyclé produit une génération différente : l'ancien handle reste invalide, le nouveau est valide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `recycled.index` vaut `first.index`.<br/>Vérifie que `recycled.generation` diffère de `first.generation`.<br/>Vérifie que `manager.isAlive(first)` est faux.<br/>Vérifie que `manager.isAlive(recycled)` est vrai. |
| **EntityManagerTest.DestructionHandlePerimeSansEffet** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_entity_manager.cpp:83`</sub> | Détruire un handle périmé est sans effet (idempotent) et ne touche pas l'entité vivante qui occupe désormais le même index. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `manager.isAlive(live)` est vrai.<br/>Vérifie que `manager.aliveCount()` vaut `1u`. |
| **EntityManagerTest.EntiteInvalideJamaisVivante** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_entity_manager.cpp:107`</sub> | L'entité invalide conventionnelle n'est jamais vivante. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `manager.isAlive(core::INVALID_ENTITY)` est faux.<br/>Vérifie que `manager.isAlive(created)` est vrai.<br/>Vérifie que `manager.isAlive(core::INVALID_ENTITY)` est faux. |
| **EntityManagerTest.EgaliteHandleInvalide** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_entity_manager.cpp:125`</sub> | Le handle invalide se compare comme tel. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::INVALID_ENTITY` vaut `core::INVALID_ENTITY`.<br/>Vérifie que `core::INVALID_ENTITY.index` vaut `core::Entity::INVALID_INDEX`. |

**`test_player_components.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **PlayerComponentsTest.ColliderParDefautEstNul** (Mineur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_player_components.cpp:17`</sub> | Collider par défaut nul | 1. Construire un `core::Collider` par défaut.<br/>2. Lire `size.x` et `size.y`. | Vérifie que `collider.size.x` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `collider.size.y` vaut `0.0f` (comparaison flottante). |
| **PlayerComponentsTest.PlayerParDefautPasAuSol** (Mineur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_player_components.cpp:31`</sub> | Player par défaut (en l'air, budgets illimités) | 1. Construire un `core::Player` par défaut.<br/>2. Lire ses champs. | Vérifie que `player.grounded` est faux.<br/>Vérifie que `player.coyoteTimer` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `player.jumpBufferTimer` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `player.airJumpsRemaining` vaut `0`.<br/>Vérifie que `player.facing` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `player.wallDirection` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `player.wallJumpLockTimer` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `player.dashAvailable` est faux.<br/>Vérifie que `player.dashTimer` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `player.jumpsRemaining` vaut `-1`.<br/>Vérifie que `player.dashesRemaining` vaut `-1`.<br/>Vérifie que `player.mass` vaut `1.0f` (comparaison flottante). |
| **PlayerComponentsTest.PlayerInputParDefautImmobile** (Mineur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_player_components.cpp:56`</sub> | PlayerInput par défaut neutre | 1. Construire un `core::PlayerInput` par défaut.<br/>2. Lire ses champs. | Vérifie que `input.moveX` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `input.moveY` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `input.jumpPressed` est faux.<br/>Vérifie que `input.jumpHeld` est faux.<br/>Vérifie que `input.dashPressed` est faux. |
| **PlayerComponentsTest.PhysicsConfigParDefautPlausible** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_player_components.cpp:73`</sub> | PhysicsConfig par défaut plausible | 1. Construire un `core::PhysicsConfig` par défaut.<br/>2. Vérifier chaque réglage. | Vérifie que `config.moveSpeed` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.gravity` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.fallDragCoefficient` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.jumpSpeed` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.coyoteTime` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.jumpBufferTime` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.jumpCutFactor` est supérieur ou égal à `0.0f`.<br/>Vérifie que `config.jumpCutFactor` est inférieur ou égal à `1.0f`.<br/>Vérifie que `config.airJumps` est supérieur ou égal à `1`.<br/>Vérifie que `config.wallSlideSpeed` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.wallJumpSpeedX` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.wallJumpSpeedY` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.dashSpeed` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.dashDuration` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.fallGravityMultiplier` est strictement supérieur à `1.0f`.<br/>Vérifie que `config.apexThreshold` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.apexGravityMultiplier` est strictement supérieur à `0.0f`.<br/>Vérifie que `config.apexGravityMultiplier` est strictement inférieur à `1.0f`.<br/>Vérifie que `config.fastFallMultiplier` est strictement supérieur à `1.0f`. |
| **PlayerComponentsTest.TailleEtSpawnHumanoide** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_player_components.cpp:108`</sub> | Taille humanoïde et spawn centré | 1. Lire `playerSize()`.<br/>2. Calculer `playerSpawnPosition(3, 5)`. | Vérifie que `core::playerSize().x` vaut `0.4f` (comparaison flottante).<br/>Vérifie que `core::playerSize().y` vaut `0.8f` (comparaison flottante).<br/>Vérifie que `spawn.x` vaut `3.3f` (comparaison flottante).<br/>Vérifie que `spawn.y` vaut `5.1f` (comparaison flottante). |
| **PlayerComponentsTest.AggregationRenseigneLesChamps** (Mineur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_player_components.cpp:126`</sub> | Agrégation des composants | 1. Initialiser `Collider` et `PlayerInput` par accolades.<br/>2. Lire les champs. | Vérifie que `collider.size.x` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `collider.size.y` vaut `3.0f` (comparaison flottante).<br/>Vérifie que `input.moveX` vaut `-1.0f` (comparaison flottante). |

**`test_sprite.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **SpriteTest.ValeursParDefaut** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_sprite.cpp:12`</sub> | Un sprite par défaut a une teinte blanche opaque, la couche 0 et une région nulle. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `sprite.layer` vaut `0`.<br/>Vérifie que `sprite.region.x` vaut `0`.<br/>Vérifie que `sprite.region.y` vaut `0`.<br/>Vérifie que `sprite.region.width` vaut `0`.<br/>Vérifie que `sprite.region.height` vaut `0`.<br/>Vérifie que `sprite.tint.r` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `sprite.tint.g` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `sprite.tint.b` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `sprite.tint.a` vaut `1.0f` (comparaison flottante). |
| **SpriteTest.ChampsAssignables** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_sprite.cpp:37`</sub> | Les champs sont librement assignables (donnée pure). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `sprite.region.x` vaut `16`.<br/>Vérifie que `sprite.region.y` vaut `32`.<br/>Vérifie que `sprite.region.width` vaut `16`.<br/>Vérifie que `sprite.region.height` vaut `16`.<br/>Vérifie que `sprite.layer` vaut `2`.<br/>Vérifie que `sprite.tint.g` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `sprite.tint.a` vaut `0.8f` (comparaison flottante). |
| **SpriteTest.CopieValeur** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_sprite.cpp:62`</sub> | Le composant est utilisable comme un composant d'ECS (stockage/copie de données pures). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `copy.layer` vaut `5`.<br/>Vérifie que `copy.region.width` vaut `16`.<br/>Vérifie que `copy.tint.r` vaut `0.2f` (comparaison flottante). |

**`test_view.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **ViewTest.SelectionneUniquementLIntersection** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_view.cpp:27`</sub> | Une vue <A, B> itère exactement les entités possédant A et B. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `visited.size()` vaut `1u`.<br/>Vérifie que `visited.front()` vaut `both`. |
| **ViewTest.ComposantsCorrespondentALEntite** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_view.cpp:58`</sub> | Les composants fournis par la vue correspondent bien à l'entité itérée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `velocity.value` vaut `position.value * 10`.<br/>Vérifie que `position.value` vaut `1`.<br/>Vérifie que `position.value` vaut `2`. |
| **ViewTest.ModificationViaVueEstVisible** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_view.cpp:92`</sub> | La modification d'un composant via la vue est visible ensuite (référence). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `positions.get(entity).value` vaut `8`. |
| **ViewTest.VueVideNIterePas** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_view.cpp:118`</sub> | Une vue sans entité correspondante s'itère sans erreur (aucune visite). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `visits` vaut `0`. |
| **ViewTest.IterationForEtEachCoherentes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_view.cpp:145`</sub> | L'itération par `for` visite les mêmes entités que `each`. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `fromEach.size()` vaut `2u`.<br/>Vérifie que `fromFor` vaut `fromEach`. |

**`test_world.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **WorldTest.CycleComposant** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_world.cpp:53`</sub> | Le cycle add/has/get/remove d'un composant est cohérent via le `World`. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.hasComponent<Health>(entity)` est faux.<br/>Vérifie que `world.hasComponent<Health>(entity)` est vrai.<br/>Vérifie que `world.getComponent<Health>(entity).value` vaut `100`.<br/>Vérifie que `world.getComponent<Health>(entity).value` vaut `60`.<br/>Vérifie que `world.hasComponent<Health>(entity)` est faux. |
| **WorldTest.HasComponentSansPool** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_world.cpp:80`</sub> | `hasComponent` est faux quand aucune pool du type n'existe encore. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.hasComponent<Health>(entity)` est faux. |
| **WorldTest.DestroyEntityRetireTousLesComposants** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_world.cpp:96`</sub> | `destroyEntity` retire l'entité de toutes les pools et la rend non vivante. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.isAlive(entity)` est faux.<br/>Vérifie que `world.hasComponent<Health>(entity)` est faux.<br/>Vérifie que `world.hasComponent<Position>(entity)` est faux. |
| **WorldTest.SystemesExecutesDansLOrdre** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_world.cpp:119`</sub> | Les systèmes enregistrés s'exécutent dans l'ordre d'enregistrement. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.systemCount()` vaut `3u`.<br/>Vérifie que `log` vaut `(std::vector<int>{1, 2, 3})`. |
| **WorldTest.UpdateNFoisExecuteNFois** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_world.cpp:142`</sub> | `update` appelé N fois exécute chaque système N fois (cadencement déterministe). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `log.size()` vaut `static_cast<std::size_t>(calls)`. |
| **WorldTest.FixedDeltaTransmisAuxSystemes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_world.cpp:166`</sub> | Le `fixedDelta` passé à `update` est transmis tel quel aux systèmes. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `observed` vaut `0.25f` (comparaison flottante). |
| **WorldTest.ViewViaWorld** (Majeur)<br/><sub>`Source/Test/Unit/Core/Ecs/test_world.cpp:186`</sub> | La vue exposée par le `World` itère l'intersection des composants. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `visited.size()` vaut `1u`.<br/>Vérifie que `visited.front()` vaut `both`. |

#### Gameplay (29)

**`test_block_controller.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **BlockControllerTest.PositionInitiale** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:42`</sub> | À la construction, le bloc est à sa position du niveau, et cette case est solide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions().size()` vaut `1u`.<br/>Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 1})`.<br/>Vérifie que `controller.collisionMap(level.tileMap()).isSolid(2, 1)` est vrai. |
| **BlockControllerTest.PousseVersLaDroiteSiLibre** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:63`</sub> | Toucher un bloc à droite en se déplaçant vers la droite le pousse d'une case, si la case suivante est libre. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions().size()` vaut `1u`.<br/>Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{3, 1})`. |
| **BlockControllerTest.PousseeRefuseeContreUnMur** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:86`</sub> | Un bloc ne peut pas être poussé à travers un mur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 1})`. |
| **BlockControllerTest.PousseeRefuseeContreUnAutreBloc** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:112`</sub> | Un bloc ne peut pas être poussé sur un autre bloc. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 1})`.<br/>Vérifie que `controller.positions()[1]` vaut `(core::GridPosition{3, 1})`. |
| **BlockControllerTest.ContactSansIntentionNePousseRien** (Mineur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:139`</sub> | Sans intention de déplacement horizontal, un bloc touché ne bouge pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 1})`. |
| **BlockControllerTest.BlocSoutenuNeTombePas** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:158`</sub> | Un bloc posé sur le sol, non poussé, ne tombe jamais. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 1})`. |
| **BlockControllerTest.BlocNonSoutenuTombe** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:179`</sub> | Un bloc sans support tombe d'une case toutes les FALL_INTERVAL_STEPS mises à jour. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 0})`.<br/>Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 1})`.<br/>Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 1})`. |
| **BlockControllerTest.CollisionMapEffaceLAncienneCase** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:218`</sub> | collisionMap efface la case d'origine d'un bloc déplacé (pas de mur fantôme). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `collision.isSolid(2, 1)` est faux.<br/>Vérifie que `collision.isSolid(3, 1)` est vrai. |
| **BlockControllerTest.ReconnaitLesTroisFacteursDeTaille** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:241`</sub> | Les trois types de bloc sont reconnus avec le facteur de taille attendu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions().size()` vaut `3u`.<br/>Vérifie que `controller.scales().size()` vaut `3u`.<br/>Vérifie que `controller.scales()[0]` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `controller.scales()[1]` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `controller.scales()[2]` vaut `0.25f` (comparaison flottante). |
| **BlockControllerTest.BoxAtCentreEtReduitSelonLaTaille** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:272`</sub> | boxAt renvoie une boîte centrée et réduite pour un bloc réduit, pleine case pour un bloc plein. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `fullBox.min.x` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `fullBox.min.y` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `fullBox.max.x` vaut `3.0f` (comparaison flottante).<br/>Vérifie que `fullBox.max.y` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `halfBox.min.x` vaut `3.25f` (comparaison flottante).<br/>Vérifie que `halfBox.min.y` vaut `1.25f` (comparaison flottante).<br/>Vérifie que `halfBox.max.x` vaut `3.75f` (comparaison flottante).<br/>Vérifie que `halfBox.max.y` vaut `1.75f` (comparaison flottante). |
| **BlockControllerTest.CollisionMapNeSolidifiePasLesBlocsReduits** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:306`</sub> | collisionMap ne rend jamais solide la case d'un bloc réduit, à la différence d'un bloc plein. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `collision.isSolid(1, 1)` est vrai.<br/>Vérifie que `collision.isSolid(2, 1)` est faux.<br/>Vérifie que `collision.isSolid(3, 1)` est faux. |
| **BlockControllerTest.BlocReduitPousseSelonSaBoiteReelle** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:335`</sub> | Un bloc réduit se pousse comme un bloc plein, mais le contact se teste contre sa boîte réduite, pas la case entière. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 1})`.<br/>Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{3, 1})`. |
| **BlockControllerTest.BlocNeTombePasATraversUnePente** (Bloquant)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:373`</sub> | Un bloc suspendu au-dessus d'une pente ne tombe pas au travers. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 0})`. |
| **BlockControllerTest.PousseeRefuseeContreUnePente** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_block_controller.cpp:406`</sub> | Un bloc ne peut pas être poussé sur une pente. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.positions()[0]` vaut `(core::GridPosition{2, 1})`. |

**`test_danger_controller.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **DangerControllerTest.MobileDemarreASaPositionDeDepart** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_danger_controller.cpp:44`</sub> | Au chargement, un danger mobile est à sa position de départ. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.moverCount()` vaut `1u`.<br/>Vérifie que `box.min.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `box.min.y` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `box.max.x` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `box.max.y` vaut `2.0f` (comparaison flottante). |
| **DangerControllerTest.MobileHorizontalAllerRetourDeterministe** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_danger_controller.cpp:66`</sub> | Un danger mobile horizontal fait un aller-retour déterministe. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.moverBox(0).min.x` vaut `2.0f`, à `1e-4f` près.<br/>Vérifie que `controller.moverBox(0).min.x` vaut `3.0f`, à `1e-4f` près.<br/>Vérifie que `controller.moverBox(0).min.x` vaut `2.0f`, à `1e-4f` près.<br/>Vérifie que `controller.moverBox(0).min.x` vaut `1.0f`, à `1e-4f` près.<br/>Vérifie que `controller.moverBox(0).min.y` vaut `1.0f` (comparaison flottante). |
| **DangerControllerTest.MobileVerticalDeplaceLaLigne** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_danger_controller.cpp:101`</sub> | Un danger mobile vertical déplace sa ligne, jamais sa colonne. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `box.min.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `box.min.y` vaut `3.0f`, à `1e-4f` près. |
| **DangerControllerTest.PorteeNulleImmobiliseLeMobile** (Mineur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_danger_controller.cpp:121`</sub> | Une portée nulle immobilise le danger mobile. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `box.min.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `box.min.y` vaut `1.0f` (comparaison flottante). |
| **DangerControllerTest.TemporiseAlterneSelonSaPeriode** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_danger_controller.cpp:142`</sub> | Un danger temporisé alterne actif/inactif selon sa période. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.isBlinkActive(position)` est vrai.<br/>Vérifie que `controller.isBlinkActive(position)` est vrai.<br/>Vérifie que `controller.isBlinkActive(position)` est faux.<br/>Vérifie que `controller.isBlinkActive(position)` est faux.<br/>Vérifie que `controller.isBlinkActive(position)` est vrai. |
| **DangerControllerTest.DephasageDecaleLeMotif** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_danger_controller.cpp:168`</sub> | Un déphasage décale le motif actif/inactif. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.isBlinkActive(position)` est faux.<br/>Vérifie que `controller.isBlinkActive(position)` est vrai. |
| **DangerControllerTest.TemporiseFauxSansConfiguration** (Mineur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_danger_controller.cpp:188`</sub> | isBlinkActive renvoie faux pour une position sans configuration. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.isBlinkActive(core::GridPosition{0, 0})` est faux. |

**`test_mechanism_controller.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **MechanismControllerTest.ContactOuvreLaPorte** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_mechanism_controller.cpp:75`</sub> | Au départ, la porte est **fermée** (solide) ; toucher l'interrupteur l'**ouvre**. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.collisionMap().isSolid(4, 1)` est vrai.<br/>Vérifie que `controller.isDoorOpen(0)` est faux.<br/>Vérifie que `controller.collisionMap().isSolid(4, 1)` est faux.<br/>Vérifie que `controller.isDoorOpen(0)` est vrai. |
| **MechanismControllerTest.BasculeSurFront** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_mechanism_controller.cpp:97`</sub> | La bascule est **sur front** : rester sur l'interrupteur ne re-bascule pas ; revenir bascule. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.isDoorOpen(0)` est vrai.<br/>Vérifie que `controller.isDoorOpen(0)` est vrai.<br/>Vérifie que `controller.isDoorOpen(0)` est vrai.<br/>Vérifie que `controller.isDoorOpen(0)` est faux.<br/>Vérifie que `controller.collisionMap().isSolid(4, 1)` est vrai. |
| **MechanismControllerTest.SansContactRienNeChange** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_mechanism_controller.cpp:124`</sub> | Loin de l'interrupteur, rien ne change (la porte reste fermée). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.collisionMap().isSolid(4, 1)` est vrai.<br/>Vérifie que `controller.isDoorOpen(0)` est faux. |
| **MechanismControllerTest.PlaqueDePressionActivationContinue** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_mechanism_controller.cpp:142`</sub> | Une plaque de pression ouvre la porte tant que le poids y repose, et la referme dès qu'il en part. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.collisionMap().isSolid(4, 1)` est vrai.<br/>Vérifie que `controller.isDoorOpen(0)` est vrai.<br/>Vérifie que `controller.collisionMap().isSolid(4, 1)` est faux.<br/>Vérifie que `controller.isDoorOpen(0)` est vrai.<br/>Vérifie que `controller.isDoorOpen(0)` est faux.<br/>Vérifie que `controller.collisionMap().isSolid(4, 1)` est vrai.<br/>Vérifie que `controller.isDoorOpen(0)` est vrai. |
| **MechanismControllerTest.PlaqueDePressionPoidsInsuffisant** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_mechanism_controller.cpp:173`</sub> | Un poids insuffisant sur la plaque de pression n'ouvre pas la porte. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.isDoorOpen(0)` est faux.<br/>Vérifie que `controller.collisionMap().isSolid(4, 1)` est vrai. |
| **MechanismControllerTest.DangerCommuteActiveParInterrupteur** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_mechanism_controller.cpp:191`</sub> | Un danger commuté devient actif au contact de son interrupteur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.isDangerActive(core::GridPosition{4, 1})` est faux.<br/>Vérifie que `controller.isDangerActive(core::GridPosition{4, 1})` est vrai.<br/>Vérifie que `controller.isDangerActive(core::GridPosition{4, 1})` est vrai.<br/>Vérifie que `controller.isDangerActive(core::GridPosition{4, 1})` est vrai.<br/>Vérifie que `controller.isDangerActive(core::GridPosition{4, 1})` est faux. |
| **MechanismControllerTest.DangerCommuteActivationContinuePlaqueDePression** (Majeur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_mechanism_controller.cpp:219`</sub> | Un danger commuté lié à une plaque de pression est actif tant que le poids y repose. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.isDangerActive(core::GridPosition{4, 1})` est vrai.<br/>Vérifie que `controller.isDangerActive(core::GridPosition{4, 1})` est faux. |
| **MechanismControllerTest.DangerActiveFauxSansLiaison** (Mineur)<br/><sub>`Source/Test/Unit/Core/Gameplay/test_mechanism_controller.cpp:241`</sub> | isDangerActive renvoie faux pour une position sans liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `controller.isDangerActive(core::GridPosition{4, 1})` est faux. |

#### Levels (188)

**`test_danger_geometry.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **DangerGeometryTest.DangerClassiqueOccupeLaCaseEntiere** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_danger_geometry.cpp:13`</sub> | Danger classique occupe toute la case. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `box.min.x` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `box.min.y` vaut `3.0f` (comparaison flottante).<br/>Vérifie que `box.max.x` vaut `3.0f` (comparaison flottante).<br/>Vérifie que `box.max.y` vaut `4.0f` (comparaison flottante). |
| **DangerGeometryTest.DangerUpBordeLeHaut** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_danger_geometry.cpp:32`</sub> | DangerUp réduit sa zone mortelle au bord haut. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `box.min.x` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `box.max.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `box.min.y` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `box.max.y` vaut `core::kDangerEdgeThickness` (comparaison flottante). |
| **DangerGeometryTest.DangerDownBordeLeBas** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_danger_geometry.cpp:51`</sub> | DangerDown réduit sa zone mortelle au bord bas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `box.min.x` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `box.max.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `box.min.y` vaut `1.0f - core::kDangerEdgeThickness` (comparaison flottante).<br/>Vérifie que `box.max.y` vaut `1.0f` (comparaison flottante). |
| **DangerGeometryTest.DangerLeftBordeLaGauche** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_danger_geometry.cpp:70`</sub> | DangerLeft réduit sa zone mortelle au bord gauche. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `box.min.y` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `box.max.y` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `box.min.x` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `box.max.x` vaut `core::kDangerEdgeThickness` (comparaison flottante). |
| **DangerGeometryTest.DangerRightBordeLaDroite** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_danger_geometry.cpp:89`</sub> | DangerRight réduit sa zone mortelle au bord droit. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `box.min.y` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `box.max.y` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `box.min.x` vaut `1.0f - core::kDangerEdgeThickness` (comparaison flottante).<br/>Vérifie que `box.max.x` vaut `1.0f` (comparaison flottante). |
| **DangerGeometryTest.DangersTemporelsOccupentLaCaseEntiere** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_danger_geometry.cpp:108`</sub> | Les dangers temporels occupent toute la case. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `box.min.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `box.min.y` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `box.max.x` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `box.max.y` vaut `3.0f` (comparaison flottante). |

**`test_decor.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **DecorTest.AddDecorAjouteEnFinDeVecteur** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor.cpp:29`</sub> | addDecor ajoute un décor en fin de vecteur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.decors().size()` vaut `2u`.<br/>Vérifie que `draft.decors()[0].assetName` vaut `"bush.png"`.<br/>Vérifie que `draft.decors()[1].assetName` vaut `"torch.png"`. |
| **DecorTest.RemoveDecorRetireAuRangDonne** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor.cpp:50`</sub> | removeDecor retire le décor au rang donné. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.decors().size()` vaut `1u`.<br/>Vérifie que `draft.decors()[0].assetName` vaut `"torch.png"`.<br/>Vérifie que `draft.decors().size()` vaut `1u`. |
| **DecorTest.SansDecorLeChampDecorsEstAbsentDuJson** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor.cpp:74`</sub> | Un niveau sans décor n'écrit pas le champ decors. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `loaded.level->decors().empty()` est vrai.<br/>Vérifie que `json.find("\\"decors\\"")` vaut `std::string::npos`. |
| **DecorTest.NiveauSansChampDecorsSeChargeSansDecor** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor.cpp:96`</sub> | Un niveau sans champ decors se charge sans erreur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `loaded.level->decors().empty()` est vrai. |
| **DecorTest.UnDecorRoundTripExactement** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor.cpp:119`</sub> | Un décor unique round-trip exactement via le JSON. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `built.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->decors().size()` vaut `1u`.<br/>Vérifie que `roundTripped.assetName` vaut `"tree.png"`.<br/>Vérifie que `roundTripped.position` vaut `(Vector2{3.5f, 7.25f})`.<br/>Vérifie que `roundTripped.scale` vaut `(Vector2{2.0f, 0.5f})`.<br/>Vérifie que `roundTripped.rotation` vaut `1.5f` (comparaison flottante).<br/>Vérifie que `roundTripped.layer` vaut `DecorLayer::Foreground`.<br/>Vérifie que `roundTripped.manipulable` est vrai. |
| **DecorTest.OrdrePreserveALEcritureEtALaRelecture** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor.cpp:158`</sub> | L'ordre des décors est préservé à l'écriture et à la relecture. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `built.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->decors().size()` vaut `3u`.<br/>Vérifie que `reloaded.level->decors()[0].assetName` vaut `"mountain.png"`.<br/>Vérifie que `reloaded.level->decors()[1].assetName` vaut `"bush.png"`.<br/>Vérifie que `reloaded.level->decors()[2].assetName` vaut `"branch.png"`. |
| **DecorTest.RedimensionnementConserveLesDecorsHorsBornes** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor.cpp:196`</sub> | Redimensionner conserve les décors hors bornes. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.decors().size()` vaut `1u`.<br/>Vérifie que `draft.decors()[0].position` vaut `(Vector2{9.5f, 9.5f})`. |
| **DecorTest.UndoRedoCouvrentUnAjout** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor.cpp:217`</sub> | undo/redo couvrent un ajout de décor. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.decors().size()` vaut `1u`.<br/>Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.decors().empty()` est vrai.<br/>Vérifie que `draft.redo()` est vrai.<br/>Vérifie que `draft.decors().size()` vaut `1u`.<br/>Vérifie que `draft.decors()[0].assetName` vaut `"bush.png"`. |
| **DecorTest.UndoRestitueUnDecorSupprime** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor.cpp:241`</sub> | undo restitue un décor supprimé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.decors().size()` vaut `1u`.<br/>Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.decors().size()` vaut `2u`.<br/>Vérifie que `draft.decors()[0].assetName` vaut `"bush.png"`.<br/>Vérifie que `draft.decors()[1].assetName` vaut `"torch.png"`. |
| **DecorTest.FromLevelRestitueLesDecors** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor.cpp:265`</sub> | fromLevel restitue les décors d'un niveau chargé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `draft.decors().size()` vaut `2u`.<br/>Vérifie que `draft.decors()[0].assetName` vaut `"bush.png"`.<br/>Vérifie que `draft.decors()[0].layer` vaut `DecorLayer::Background`.<br/>Vérifie que `draft.decors()[1].assetName` vaut `"branch.png"`.<br/>Vérifie que `draft.decors()[1].layer` vaut `DecorLayer::Foreground`. |
| **DecorTest.SansChampLayerLaCoucheParDefautEstDecor** (Mineur)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor.cpp:298`</sub> | Sans champ layer, la couche par défaut est Decor. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `loaded.level->decors().size()` vaut `1u`.<br/>Vérifie que `loaded.level->decors().front().layer` vaut `DecorLayer::Decor`. |

**`test_decor_mutations.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **DecorMutationsTest.MoveDecorDeplaceLaPosition** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:34`</sub> | Déplacer un décor change sa position. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.moveDecor(0, Vector2{4.5f, 2.25f})` est vrai.<br/>Vérifie que `draft.decors()[0].position` vaut `(Vector2{4.5f, 2.25f})`. |
| **DecorMutationsTest.MoveDecorHorsBornesRenvoieFauxSansEffet** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:53`</sub> | Déplacer un décor hors bornes rend faux, sans effet. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.moveDecor(0, Vector2{4.5f, 2.25f})` est faux. |
| **DecorMutationsTest.MoveDecorUndoRedo** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:69`</sub> | Le déplacement d'un décor s'annule et se refait. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.decors()[0].position` vaut `(Vector2{1.0f, 1.0f})`.<br/>Vérifie que `draft.redo()` est vrai.<br/>Vérifie que `draft.decors()[0].position` vaut `(Vector2{4.5f, 2.25f})`. |
| **DecorMutationsTest.ResizeDecorChangeLEchelleEtLaPositionAtomiquement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:95`</sub> | Redimensionner un décor change l'échelle et la position atomiquement. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.resizeDecor(0, Vector2{0.5f, 0.5f}, Vector2{2.0f, 0.5f})` est vrai.<br/>Vérifie que `draft.decors()[0].position` vaut `(Vector2{0.5f, 0.5f})`.<br/>Vérifie que `draft.decors()[0].scale` vaut `(Vector2{2.0f, 0.5f})`. |
| **DecorMutationsTest.ResizeDecorHorsBornesRenvoieFauxSansEffet** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:114`</sub> | Redimensionner un décor hors bornes rend faux, sans effet. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.resizeDecor(0, Vector2{0.0f, 0.0f}, Vector2{2.0f, 2.0f})` est faux. |
| **DecorMutationsTest.ResizeDecorEchelleNulleOuNegativeRejeteeSansToucherLaPosition** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:131`</sub> | Une échelle nulle ou négative est rejetée sans toucher à la position. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.resizeDecor(0, Vector2{9.0f, 9.0f}, Vector2{0.0f, 1.0f})` est faux.<br/>Vérifie que `draft.resizeDecor(0, Vector2{9.0f, 9.0f}, Vector2{1.0f, 0.0f})` est faux.<br/>Vérifie que `draft.resizeDecor(0, Vector2{9.0f, 9.0f}, Vector2{-1.0f, 1.0f})` est faux.<br/>Vérifie que `draft.resizeDecor(0, Vector2{9.0f, 9.0f}, Vector2{1.0f, -1.0f})` est faux.<br/>Vérifie que `draft.decors()[0].scale` vaut `(Vector2{1.0f, 1.0f})`.<br/>Vérifie que `draft.decors()[0].position` vaut `(Vector2{1.0f, 1.0f})`. |
| **DecorMutationsTest.ResizeDecorUndoRedo** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:154`</sub> | Le redimensionnement d'un décor s'annule et se refait, position et échelle ensemble. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.decors()[0].scale` vaut `(Vector2{1.0f, 1.0f})`.<br/>Vérifie que `draft.decors()[0].position` vaut `(Vector2{1.0f, 1.0f})`.<br/>Vérifie que `draft.redo()` est vrai.<br/>Vérifie que `draft.decors()[0].scale` vaut `(Vector2{3.0f, 3.0f})`.<br/>Vérifie que `draft.decors()[0].position` vaut `(Vector2{0.5f, 0.5f})`. |
| **DecorMutationsTest.RotateDecorNominal** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:181`</sub> | Faire pivoter un décor écrit son angle. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.rotateDecor(0, 1.0f)` est vrai.<br/>Vérifie que `draft.decors()[0].rotation` vaut `1.0f` (comparaison flottante). |
| **DecorMutationsTest.RotateDecorHorsBornesRenvoieFauxSansEffet** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:199`</sub> | Faire pivoter un décor hors bornes rend faux, sans effet. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.rotateDecor(0, 1.0f)` est faux. |
| **DecorMutationsTest.RotateDecorNormaliseHorsDeZeroDeuxPi** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:216`</sub> | L'angle d'un décor est normalisé dans [0, 2π), même depuis une valeur négative. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.rotateDecor(0, TWO_PI + 0.5f)` est vrai.<br/>Vérifie que `draft.decors()[0].rotation` vaut `0.5f`, à `1e-4f` près.<br/>Vérifie que `draft.decors()[0].rotation` est supérieur ou égal à `0.0f`.<br/>Vérifie que `draft.decors()[0].rotation` est strictement inférieur à `TWO_PI`.<br/>Vérifie que `draft.rotateDecor(0, -0.5f)` est vrai.<br/>Vérifie que `draft.decors()[0].rotation` vaut `TWO_PI - 0.5f`, à `1e-4f` près.<br/>Vérifie que `draft.decors()[0].rotation` est supérieur ou égal à `0.0f`.<br/>Vérifie que `draft.decors()[0].rotation` est strictement inférieur à `TWO_PI`. |
| **DecorMutationsTest.RotateDecorUndoRedo** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:241`</sub> | La rotation d'un décor s'annule et se refait. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.decors()[0].rotation` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `draft.redo()` est vrai.<br/>Vérifie que `draft.decors()[0].rotation` vaut `1.5f` (comparaison flottante). |
| **DecorMutationsTest.SetDecorLayerEnvoieEnFinDeVecteurDeSaNouvelleCouche** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:267`</sub> | Changer un décor de couche l'envoie en fin de vecteur et rend son nouvel index. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `newIndex.has_value()` est vrai.<br/>Vérifie que `*newIndex` vaut `1u`.<br/>Vérifie que `draft.decors().size()` vaut `2u`.<br/>Vérifie que `draft.decors()[0].assetName` vaut `"b.png"`.<br/>Vérifie que `draft.decors()[1].assetName` vaut `"a.png"`.<br/>Vérifie que `draft.decors()[1].layer` vaut `DecorLayer::Foreground`. |
| **DecorMutationsTest.SetDecorLayerVersLaMemeCoucheEstUnNoOpReussi** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:297`</sub> | Changer un décor vers sa couche actuelle réussit sans rien déplacer. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `newIndex.has_value()` est vrai.<br/>Vérifie que `*newIndex` vaut `0u`.<br/>Vérifie que `draft.decors()[0].assetName` vaut `"a.png"`. |
| **DecorMutationsTest.SetDecorLayerHorsBornesRenvoieNullopt** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:318`</sub> | Changer de couche hors bornes ne rend aucun index. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.setDecorLayer(0, DecorLayer::Foreground).has_value()` est faux. |
| **DecorMutationsTest.SetDecorLayerUndoRedo** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:333`</sub> | Le changement de couche d'un décor s'annule et se refait. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.decors()[0].layer` vaut `DecorLayer::Decor`.<br/>Vérifie que `draft.redo()` est vrai.<br/>Vérifie que `draft.decors()[0].layer` vaut `DecorLayer::Foreground`. |
| **DecorMutationsTest.BringDecorForwardEchangeAvecLeProchainDeMemeCouche** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:385`</sub> | Avancer un décor l'échange avec le suivant de sa couche, sautant les autres. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `newIndex.has_value()` est vrai.<br/>Vérifie que `*newIndex` vaut `2u`.<br/>Vérifie que `draft.decors().size()` vaut `4u`.<br/>Vérifie que `draft.decors()[0].assetName` vaut `"C.png"`.<br/>Vérifie que `draft.decors()[1].assetName` vaut `"B.png"`.<br/>Vérifie que `draft.decors()[2].assetName` vaut `"A.png"`.<br/>Vérifie que `draft.decors()[3].assetName` vaut `"D.png"`. |
| **DecorMutationsTest.BringDecorForwardDejaEnTeteEstUnNoOpReussi** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:411`</sub> | Avancer un décor déjà en tête de sa couche réussit sans rien changer. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `newIndex.has_value()` est vrai.<br/>Vérifie que `*newIndex` vaut `3u`.<br/>Vérifie que `draft.decors()[3].assetName` vaut `"D.png"`. |
| **DecorMutationsTest.SendDecorBackwardEchangeAvecLePrecedentDeMemeCouche** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:431`</sub> | Reculer un décor l'échange avec le précédent de sa couche. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `newIndex.has_value()` est vrai.<br/>Vérifie que `*newIndex` vaut `0u`.<br/>Vérifie que `draft.decors()[0].assetName` vaut `"C.png"`.<br/>Vérifie que `draft.decors()[1].assetName` vaut `"B.png"`.<br/>Vérifie que `draft.decors()[2].assetName` vaut `"A.png"`.<br/>Vérifie que `draft.decors()[3].assetName` vaut `"D.png"`. |
| **DecorMutationsTest.SendDecorBackwardDejaEnQueueEstUnNoOpReussi** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:454`</sub> | Reculer un décor déjà en queue de sa couche réussit sans rien changer. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `newIndex.has_value()` est vrai.<br/>Vérifie que `*newIndex` vaut `0u`.<br/>Vérifie que `draft.decors()[0].assetName` vaut `"A.png"`. |
| **DecorMutationsTest.BringDecorToFrontAmeneAuDernierRangDeSaCouche** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:474`</sub> | Mettre au premier plan place le décor au dernier rang de sa couche, couche inchangée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `newIndex.has_value()` est vrai.<br/>Vérifie que `*newIndex` vaut `3u`.<br/>Vérifie que `draft.decors().size()` vaut `4u`.<br/>Vérifie que `draft.decors()[0].assetName` vaut `"B.png"`.<br/>Vérifie que `draft.decors()[1].assetName` vaut `"C.png"`.<br/>Vérifie que `draft.decors()[2].assetName` vaut `"D.png"`.<br/>Vérifie que `draft.decors()[3].assetName` vaut `"A.png"`.<br/>Vérifie que `draft.decors()[3].layer` vaut `DecorLayer::Decor`. |
| **DecorMutationsTest.SendDecorToBackAmeneAuPremierRangDeSaCouche** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:501`</sub> | Mettre à l'arrière-plan place le décor au premier rang de sa couche, couche inchangée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `newIndex.has_value()` est vrai.<br/>Vérifie que `*newIndex` vaut `0u`.<br/>Vérifie que `draft.decors().size()` vaut `4u`.<br/>Vérifie que `draft.decors()[0].assetName` vaut `"D.png"`.<br/>Vérifie que `draft.decors()[1].assetName` vaut `"A.png"`.<br/>Vérifie que `draft.decors()[2].assetName` vaut `"B.png"`.<br/>Vérifie que `draft.decors()[3].assetName` vaut `"C.png"`.<br/>Vérifie que `draft.decors()[0].layer` vaut `DecorLayer::Decor`. |
| **DecorMutationsTest.BringDecorToFrontEtSendDecorToBackDejaEnPlaceSontDesNoOp** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:528`</sub> | Premier plan et arrière-plan sur un décor déjà en place ne déplacent rien. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.bringDecorToFront(3)` vaut `3u`.<br/>Vérifie que `draft.sendDecorToBack(0)` vaut `0u`. |
| **DecorMutationsTest.ReordonnancementHorsBornesRenvoieNullopt** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:546`</sub> | Les quatre opérations de réordonnancement hors bornes ne rendent aucun index. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.bringDecorForward(0).has_value()` est faux.<br/>Vérifie que `draft.sendDecorBackward(0).has_value()` est faux.<br/>Vérifie que `draft.bringDecorToFront(0).has_value()` est faux.<br/>Vérifie que `draft.sendDecorToBack(0).has_value()` est faux. |
| **DecorMutationsTest.BringDecorToFrontUndoRedo** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_decor_mutations.cpp:566`</sub> | Le réordonnancement d'un décor s'annule et se refait. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.decors()[0].assetName` vaut `"A.png"`.<br/>Vérifie que `draft.redo()` est vrai.<br/>Vérifie que `draft.decors()[3].assetName` vaut `"A.png"`. |

**`test_level.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **TileMapTest.GrilleNeuveVideAuxBonnesDimensions** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level.cpp:14`</sub> | Une grille neuve a les bonnes dimensions et n'est composée que de cases Empty. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `map.width()` vaut `4`.<br/>Vérifie que `map.height()` vaut `3`.<br/>Vérifie que `map.tile(column, row)` vaut `core::TileType::Empty`. |
| **TileMapTest.EcritureLectureDUneTuile** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level.cpp:37`</sub> | Écrire puis lire une tuile restitue le type posé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `map.tile(2, 1)` vaut `core::TileType::Solid`.<br/>Vérifie que `map.tile(0, 0)` vaut `core::TileType::Empty`. |
| **TileMapTest.Bornes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level.cpp:55`</sub> | Les bornes de la grille sont correctement détectées. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `map.inBounds(0, 0)` est vrai.<br/>Vérifie que `map.inBounds(3, 2)` est vrai.<br/>Vérifie que `map.inBounds(4, 2)` est faux.<br/>Vérifie que `map.inBounds(3, 3)` est faux.<br/>Vérifie que `map.inBounds(-1, 0)` est faux. |
| **TileMapTest.Solidite** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level.cpp:75`</sub> | Seules les tuiles solides bloquent statiquement. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `map.isSolid(0, 0)` est vrai.<br/>Vérifie que `map.isSolid(1, 0)` est faux.<br/>Vérifie que `map.isSolid(2, 0)` est faux. |
| **TileMapTest.IsSolidParType** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level.cpp:96`</sub> | `isSolid(TileType)` : vrai seulement pour Solid. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::isSolid(core::TileType::Solid)` est vrai.<br/>Vérifie que `core::isSolid(core::TileType::Empty)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::Door)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::Danger)` est faux. |
| **TileMapTest.IsSolidFauxPourLesDangersAvances** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level.cpp:114`</sub> | Les sept nouvelles variantes de danger ne sont jamais solides. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::isSolid(core::TileType::DangerUp)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::DangerDown)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::DangerLeft)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::DangerRight)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::DangerMover)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::DangerSwitched)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::DangerBlink)` est faux. |
| **LevelTest.RestitueSesComposantes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level.cpp:134`</sub> | Un Level restitue ses composantes (nom, grille, entrée/sortie, mécanismes). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `level.name()` vaut `"Tutoriel"`.<br/>Vérifie que `level.tileMap().width()` vaut `5`.<br/>Vérifie que `level.entry()` vaut `(core::GridPosition{1, 1})`.<br/>Vérifie que `level.exit()` vaut `(core::GridPosition{3, 2})`.<br/>Vérifie que `level.mechanisms().size()` vaut `1u`.<br/>Vérifie que `level.mechanisms().front().switchPosition` vaut `(core::GridPosition{2, 1})`.<br/>Vérifie que `level.mechanisms().front().doorPosition` vaut `(core::GridPosition{4, 2})`. |
| **LevelTest.RestitueSesDangersAvances** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level.cpp:165`</sub> | Un Level restitue ses liaisons et configurations de dangers avancés. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `level.dangerLinks().size()` vaut `1u`.<br/>Vérifie que `level.dangerLinks().front().triggerPosition` vaut `(core::GridPosition{0, 0})`.<br/>Vérifie que `level.dangerLinks().front().dangerPosition` vaut `(core::GridPosition{4, 0})`.<br/>Vérifie que `level.moverConfigs().size()` vaut `1u`.<br/>Vérifie que `level.moverConfigs().front().axis` vaut `core::DangerMoverAxis::Vertical`.<br/>Vérifie que `level.moverConfigs().front().range` vaut `3`.<br/>Vérifie que `level.blinkConfigs().size()` vaut `1u`.<br/>Vérifie que `level.blinkConfigs().front().period` vaut `90`.<br/>Vérifie que `level.blinkConfigs().front().phase` vaut `10`.<br/>Vérifie que `level.blinkConfigs().front().activeDuration` vaut `30`. |
| **LevelTest.RestitueSonFondEtSonJeuDeSkinsOuLeurAbsence** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level.cpp:209`</sub> | Un Level restitue son fond et son jeu de skins, ou leur absence. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `sansFond.background().has_value()` est faux.<br/>Vérifie que `sansFond.skinSet().has_value()` est faux.<br/>Vérifie que `avecFond.background().has_value()` est vrai.<br/>Vérifie que `*avecFond.background()` vaut `"forest.png"`.<br/>Vérifie que `avecFond.skinSet().has_value()` est vrai.<br/>Vérifie que `*avecFond.skinSet()` vaut `"foret"`. |

**`test_level_draft.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LevelDraftTest.BrouillonVierge** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:23`</sub> | Un brouillon vierge a une grille entièrement vide, sans entrée ni sortie. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.name()` vaut `"Nouveau"`.<br/>Vérifie que `draft.tileMap().width()` vaut `5`.<br/>Vérifie que `draft.tileMap().height()` vaut `4`.<br/>Vérifie que `draft.entry().has_value()` est faux.<br/>Vérifie que `draft.exit().has_value()` est faux.<br/>Vérifie que `draft.mechanisms().empty()` est vrai.<br/>Vérifie que `draft.jumpBudget()` vaut `-1`.<br/>Vérifie que `draft.dashBudget()` vaut `-1`.<br/>Vérifie que `draft.tileMap().tile(0, 0)` vaut `TileType::Empty`. |
| **LevelDraftTest.PaintTilePoseLeType** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:46`</sub> | paintTile pose le type demandé sur la case visée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Solid`. |
| **LevelDraftTest.SetEntryDeplaceLEntreeExistante** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:62`</sub> | Poser une seconde entrée déplace la première (unicité, EX-EDIT-004). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.entry().has_value()` est vrai.<br/>Vérifie que `*draft.entry()` vaut `(GridPosition{0, 0})`.<br/>Vérifie que `*draft.entry()` vaut `(GridPosition{2, 2})`.<br/>Vérifie que `draft.tileMap().tile(0, 0)` vaut `TileType::Empty`.<br/>Vérifie que `draft.tileMap().tile(2, 2)` vaut `TileType::Entry`. |
| **LevelDraftTest.SetExitDeplaceLaSortieExistante** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:84`</sub> | Poser une seconde sortie déplace la première (unicité, EX-EDIT-004). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `*draft.exit()` vaut `(GridPosition{3, 3})`.<br/>Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Empty`.<br/>Vérifie que `draft.tileMap().tile(3, 3)` vaut `TileType::Exit`. |
| **LevelDraftTest.PeindrePardessusLEntreeLInvalide** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:103`</sub> | Peindre par-dessus l'entrée invalide la position d'entrée mémorisée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.entry().has_value()` est faux.<br/>Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Solid`. |
| **LevelDraftTest.LierPuisDelierUnMecanisme** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:121`</sub> | Lier un interrupteur à une porte crée un mécanisme ; le délier le retire. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.mechanisms().size()` vaut `1u`.<br/>Vérifie que `draft.mechanisms().front().switchPosition` vaut `(GridPosition{0, 0})`.<br/>Vérifie que `draft.mechanisms().front().doorPosition` vaut `(GridPosition{3, 3})`.<br/>Vérifie que `draft.mechanisms().empty()` est vrai. |
| **LevelDraftTest.LierUnePlaqueDePression** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:146`</sub> | Lier une plaque de pression à une porte crée un mécanisme. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.mechanisms().size()` vaut `1u`.<br/>Vérifie que `draft.mechanisms().front().switchPosition` vaut `(GridPosition{0, 0})`.<br/>Vérifie que `draft.mechanisms().front().doorPosition` vaut `(GridPosition{3, 3})`. |
| **LevelDraftTest.RelierUnePorteRemplaceLaLiaisonPrecedente** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:167`</sub> | Relier une porte déjà liée remplace la liaison précédente (une porte, un interrupteur). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.mechanisms().size()` vaut `1u`.<br/>Vérifie que `draft.mechanisms().front().switchPosition` vaut `(GridPosition{1, 0})`. |
| **LevelDraftTest.UnInterrupteurPeutOuvrirPlusieursPortes** (Mineur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:192`</sub> | Un interrupteur peut ouvrir plusieurs portes. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.mechanisms().size()` vaut `2u`. |
| **LevelDraftTest.PeindrePardessusUnInterrupteurRetireSesLiaisons** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:214`</sub> | Peindre par-dessus un interrupteur retire les liaisons qui le référencent. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.mechanisms().empty()` est vrai. |
| **LevelDraftTest.AgrandirConserveLeContenu** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:234`</sub> | Agrandir la grille conserve le contenu existant et complète en cases vides. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.tileMap().width()` vaut `5`.<br/>Vérifie que `draft.tileMap().height()` vaut `5`.<br/>Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Entry`.<br/>Vérifie que `*draft.entry()` vaut `(GridPosition{1, 1})`.<br/>Vérifie que `draft.tileMap().tile(4, 4)` vaut `TileType::Empty`. |
| **LevelDraftTest.ReduireTronqueEtInvalideLEntreePerdue** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:257`</sub> | Réduire la grille tronque le contenu hors bornes et invalide l'entrée perdue. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.tileMap().width()` vaut `3`.<br/>Vérifie que `draft.tileMap().height()` vaut `3`.<br/>Vérifie que `draft.entry().has_value()` est faux.<br/>Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Solid`. |
| **LevelDraftTest.ReduireRetireLesMecanismesHorsBornes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:281`</sub> | Réduire la grille retire les mécanismes dont une extrémité sort des bornes. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.mechanisms().empty()` est vrai. |
| **LevelDraftTest.ToLevelSurBrouillonValideReussit** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:302`</sub> | toLevel() sur un brouillon complet et valide produit un niveau conforme. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.level->name()` vaut `"Niveau test"`.<br/>Vérifie que `result.level->entry()` vaut `(GridPosition{0, 0})`.<br/>Vérifie que `result.level->exit()` vaut `(GridPosition{3, 3})`.<br/>Vérifie que `result.level->jumpBudget()` vaut `2`. |
| **LevelDraftTest.ToLevelSansSortieEchoueProprement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:326`</sub> | toLevel() sur un brouillon sans sortie échoue avec un message récupérable (EX-EDIT-007). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.error.empty()` est faux. |
| **LevelDraftTest.FromLevelRestitueLeContenu** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:347`</sub> | Un brouillon reconstruit depuis un niveau existant restitue son contenu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `draft.name()` vaut `"Depart"`.<br/>Vérifie que `*draft.entry()` vaut `(GridPosition{0, 0})`.<br/>Vérifie que `*draft.exit()` vaut `(GridPosition{2, 2})`. |
| **LevelDraftTest.BrouillonNeufSansHistorique** (Mineur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:369`</sub> | Un brouillon neuf ne peut ni annuler ni refaire. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.canUndo()` est faux.<br/>Vérifie que `draft.canRedo()` est faux. |
| **LevelDraftTest.UndoApresPeintureRestitueLEtatPrecedent** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:385`</sub> | undo() après une peinture restitue l'état exact précédent. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.canUndo()` est vrai.<br/>Vérifie que `undone` est vrai.<br/>Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Empty`.<br/>Vérifie que `draft.canUndo()` est faux.<br/>Vérifie que `draft.canRedo()` est vrai. |
| **LevelDraftTest.RedoApresUndoRestitueLEtatMute** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:408`</sub> | redo() après un undo() restitue l'état muté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `redone` est vrai.<br/>Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Solid`.<br/>Vérifie que `draft.canUndo()` est vrai.<br/>Vérifie que `draft.canRedo()` est faux. |
| **LevelDraftTest.SequenceDeMutationsPuisUndoRestitueLEtatInitial** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:431`</sub> | Une séquence de N mutations suivie de N undo() restitue l'état initial exact. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.canUndo()` est faux.<br/>Vérifie que `draft.tileMap().tile(0, 0)` vaut `TileType::Empty`.<br/>Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Empty`.<br/>Vérifie que `draft.tileMap().tile(2, 2)` vaut `TileType::Empty`.<br/>Vérifie que `draft.tileMap().tile(4, 4)` vaut `TileType::Empty`.<br/>Vérifie que `draft.entry().has_value()` est faux.<br/>Vérifie que `draft.exit().has_value()` est faux. |
| **LevelDraftTest.MutationApresUndoInvalideLeRefaire** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:463`</sub> | Une nouvelle mutation après un undo() invalide la branche de refaire. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.canRedo()` est vrai.<br/>Vérifie que `draft.canRedo()` est faux.<br/>Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Danger`. |
| **LevelDraftTest.UndoRedoSurPileVideSansEffet** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:485`</sub> | undo()/redo() sur une pile vide est sans effet (pas de plantage). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.undo()` est faux.<br/>Vérifie que `draft.redo()` est faux.<br/>Vérifie que `draft.tileMap().tile(0, 0)` vaut `TileType::Empty`. |
| **LevelDraftTest.UndoApresLiaisonMecanismeRestitueLAbsenceDeLiaison** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:502`</sub> | L'annulation d'une liaison de mécanisme restitue la liaison précédente. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.mechanisms().size()` vaut `1u`.<br/>Vérifie que `draft.mechanisms().empty()` est vrai. |
| **LevelDraftTest.SetBackgroundEtSetSkinSetAssignentEtRetirent** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:525`</sub> | setBackground/setSkinSet assignent et retirent les champs correspondants. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.background().has_value()` est faux.<br/>Vérifie que `draft.skinSet().has_value()` est faux.<br/>Vérifie que `draft.background().has_value()` est vrai.<br/>Vérifie que `*draft.background()` vaut `"forest.png"`.<br/>Vérifie que `draft.skinSet().has_value()` est faux.<br/>Vérifie que `draft.skinSet().has_value()` est vrai.<br/>Vérifie que `*draft.skinSet()` vaut `"foret"`.<br/>Vérifie que `draft.background().has_value()` est faux.<br/>Vérifie que `draft.skinSet().has_value()` est vrai.<br/>Vérifie que `*draft.skinSet()` vaut `"foret"`. |
| **LevelDraftTest.UndoRedoApresChangementDeFondEtDeJeuDeSkins** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:556`</sub> | L'annulation d'un changement de fond ou de jeu de skins restitue la valeur precedente. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.canUndo()` est vrai.<br/>Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.skinSet().has_value()` est faux.<br/>Vérifie que `draft.background().has_value()` est vrai.<br/>Vérifie que `*draft.background()` vaut `"forest.png"`.<br/>Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.background().has_value()` est faux.<br/>Vérifie que `draft.redo()` est vrai.<br/>Vérifie que `draft.background().has_value()` est vrai.<br/>Vérifie que `*draft.background()` vaut `"forest.png"`. |
| **LevelDraftTest.PaintRegionAppliqueLeBlocEntier** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:586`</sub> | paintRegion applique un bloc homogène comme une succession de paintTile équivalente. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::Solid`.<br/>Vérifie que `draft.tileMap().tile(2, 1)` vaut `TileType::Solid`.<br/>Vérifie que `draft.tileMap().tile(1, 2)` vaut `TileType::Solid`.<br/>Vérifie que `draft.tileMap().tile(2, 2)` vaut `TileType::Solid`.<br/>Vérifie que `draft.tileMap().tile(0, 0)` vaut `TileType::Empty`. |
| **LevelDraftTest.PaintRegionUnSeulSnapshotUndo** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:612`</sub> | paintRegion ne pousse qu'un seul snapshot undo pour tout le bloc. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.canUndo()` est faux.<br/>Vérifie que `draft.tileMap().tile(0, 0)` vaut `TileType::Empty`.<br/>Vérifie que `draft.tileMap().tile(1, 0)` vaut `TileType::Empty`.<br/>Vérifie que `draft.tileMap().tile(2, 0)` vaut `TileType::Empty`. |
| **LevelDraftTest.PaintRegionDecoupeAuxBords** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:636`</sub> | paintRegion découpe silencieusement le bloc aux bords de la grille. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.tileMap().tile(1, 2)` vaut `TileType::Solid`.<br/>Vérifie que `draft.tileMap().tile(2, 2)` vaut `TileType::Solid`. |
| **LevelDraftTest.PaintRegionDeplaceLEntree** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:659`</sub> | paintRegion qui inclut une position d'entrée déplace l'entrée existante. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.tileMap().tile(0, 0)` vaut `TileType::Empty`.<br/>Vérifie que `*draft.entry()` vaut `(GridPosition{2, 2})`. |
| **LevelDraftTest.PaintRegionRetireLesLiaisonsRecouvertes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:679`</sub> | paintRegion qui recouvre un interrupteur retire les liaisons qui le référencent. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.mechanisms().empty()` est vrai. |
| **LevelDraftTest.PaintRegionBlocVideSansEffet** (Mineur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:702`</sub> | paintRegion avec un bloc vide est sans effet. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.canUndo()` est faux. |
| **LevelDraftTest.WouldResizeDropContentDetecteLaPerte** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:718`</sub> | wouldResizeDropContent détecte la perte de l'entrée, de la sortie ou d'une liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.wouldResizeDropContent(3, 3)` est vrai.<br/>Vérifie que `draft.wouldResizeDropContent(2, 5)` est vrai.<br/>Vérifie que `draft.wouldResizeDropContent(10, 10)` est faux.<br/>Vérifie que `draft.wouldResizeDropContent(5, 5)` est faux. |
| **LevelDraftTest.WouldResizeDropContentFauxSurBrouillonVierge** (Mineur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:747`</sub> | wouldResizeDropContent est faux sur un brouillon vierge, quelle que soit la taille visee. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.wouldResizeDropContent(1, 1)` est faux. |
| **LevelDraftTest.UndoApresRedimensionnementRestitueLesDimensions** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:764`</sub> | L'annulation d'un redimensionnement restitue les dimensions et le contenu précédents. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.entry().has_value()` est faux.<br/>Vérifie que `draft.tileMap().width()` vaut `5`.<br/>Vérifie que `draft.tileMap().height()` vaut `5`.<br/>Vérifie que `draft.entry().has_value()` est vrai.<br/>Vérifie que `*draft.entry()` vaut `(GridPosition{4, 4})`. |
| **LevelDraftTest.PaintTilePoseUnDangerDirectionnel** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:791`</sub> | Peindre un danger directionnel pose le type demandé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.tileMap().tile(1, 1)` vaut `TileType::DangerUp`. |
| **LevelDraftTest.LierUnDangerCommute** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:808`</sub> | Lier un interrupteur à un danger commuté crée une liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.mechanisms().empty()` est vrai.<br/>Vérifie que `draft.dangerLinks().size()` vaut `1u`.<br/>Vérifie que `draft.dangerLinks().front().triggerPosition` vaut `(GridPosition{0, 0})`.<br/>Vérifie que `draft.dangerLinks().front().dangerPosition` vaut `(GridPosition{3, 3})`. |
| **LevelDraftTest.DelierUnDangerCommute** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:831`</sub> | Délier un danger commuté retire sa liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.dangerLinks().empty()` est vrai. |
| **LevelDraftTest.PeindrePardessusUnDangerCommuteRetireSaLiaison** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:853`</sub> | Peindre par-dessus un danger commuté lié retire sa liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.dangerLinks().empty()` est vrai. |
| **LevelDraftTest.SetMoverConfigDefinitLaConfiguration** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:874`</sub> | setMoverConfig définit l'axe et la portée d'un danger mobile. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.moverConfigs().size()` vaut `1u`.<br/>Vérifie que `draft.moverConfigs().front().axis` vaut `core::DangerMoverAxis::Vertical`.<br/>Vérifie que `draft.moverConfigs().front().range` vaut `3`. |
| **LevelDraftTest.PeindrePardessusUnDangerMobileRetireSaConfiguration** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:895`</sub> | Peindre par-dessus un danger mobile configuré retire sa configuration. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.moverConfigs().empty()` est vrai. |
| **LevelDraftTest.SetBlinkConfigDefinitLaConfiguration** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:916`</sub> | setBlinkConfig définit la configuration d'un danger temporisé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.blinkConfigs().size()` vaut `1u`.<br/>Vérifie que `draft.blinkConfigs().front().period` vaut `90`.<br/>Vérifie que `draft.blinkConfigs().front().phase` vaut `15`.<br/>Vérifie que `draft.blinkConfigs().front().activeDuration` vaut `30`. |
| **LevelDraftTest.FromLevelRestitueLesDangersAvances** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:940`</sub> | fromLevel restitue les dangers avancés d'un niveau chargé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `draft.dangerLinks().size()` vaut `1u`.<br/>Vérifie que `draft.dangerLinks().front().dangerPosition` vaut `(GridPosition{2, 1})`.<br/>Vérifie que `draft.moverConfigs().size()` vaut `1u`.<br/>Vérifie que `draft.moverConfigs().front().range` vaut `1`.<br/>Vérifie que `draft.blinkConfigs().size()` vaut `1u`.<br/>Vérifie que `draft.blinkConfigs().front().period` vaut `90`. |
| **LevelDraftTest.ReduireRetireLesDangersAvancesHorsBornes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_draft.cpp:976`</sub> | Réduire la grille retire les dangers avancés hors bornes. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.dangerLinks().empty()` est vrai.<br/>Vérifie que `draft.moverConfigs().empty()` est vrai.<br/>Vérifie que `draft.blinkConfigs().empty()` est vrai. |

**`test_level_loader.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LevelLoaderTest.ChargeUnNiveauValide** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:33`</sub> | Un niveau valide est chargé avec ses dimensions, tuiles, entrée/sortie et mécanismes. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::None`.<br/>Vérifie que `level.name()` vaut `"Tutoriel"`.<br/>Vérifie que `level.tileMap().width()` vaut `4`.<br/>Vérifie que `level.tileMap().height()` vaut `3`.<br/>Vérifie que `level.tileMap().tile(0, 0)` vaut `core::TileType::Solid`.<br/>Vérifie que `level.tileMap().tile(1, 1)` vaut `core::TileType::Entry`.<br/>Vérifie que `level.entry()` vaut `(core::GridPosition{1, 1})`.<br/>Vérifie que `level.exit()` vaut `(core::GridPosition{3, 2})`.<br/>Vérifie que `level.mechanisms().size()` vaut `1u`.<br/>Vérifie que `level.mechanisms().front().switchPosition` vaut `(core::GridPosition{2, 0})`.<br/>Vérifie que `level.mechanisms().front().doorPosition` vaut `(core::GridPosition{3, 0})`. |
| **LevelLoaderTest.NiveauSansVersionSeChargeSansErreur** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:65`</sub> | Un niveau sans champ version se charge sans erreur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::None`. |
| **LevelLoaderTest.VersionSuperieureALaVersionGereeEchoueProprement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:82`</sub> | Un niveau dont la version depasse celle geree echoue proprement. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::UnsupportedFormatVersion`. |
| **LevelLoaderTest.ChargeLeFondEtLeJeuDeSkins** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:108`</sub> | Un niveau designant un fond et un jeu de skins restitue les deux champs. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.level->background().has_value()` est vrai.<br/>Vérifie que `*result.level->background()` vaut `"forest.png"`.<br/>Vérifie que `result.level->skinSet().has_value()` est vrai.<br/>Vérifie que `*result.level->skinSet()` vaut `"foret"`. |
| **LevelLoaderTest.SansFondNiJeuDeSkinsLesDeuxChampsSontAbsents** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:138`</sub> | Un niveau sans fond ni jeu de skins n'en restitue aucun. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.level->background().has_value()` est faux.<br/>Vérifie que `result.level->skinSet().has_value()` est faux. |
| **LevelLoaderTest.ChargeUnePlaqueDePression** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:156`</sub> | Une plaque de pression se charge comme un interrupteur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(2, 0)` vaut `core::TileType::PressurePlate`.<br/>Vérifie que `level.mechanisms().size()` vaut `1u`.<br/>Vérifie que `level.mechanisms().front().switchPosition` vaut `(core::GridPosition{2, 0})`.<br/>Vérifie que `level.mechanisms().front().doorPosition` vaut `(core::GridPosition{3, 0})`. |
| **LevelLoaderTest.ChargeUnBlocPoussable** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:190`</sub> | Un bloc poussable se charge comme une simple tuile, sans identifiant ni liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(2, 0)` vaut `core::TileType::Block`.<br/>Vérifie que `level.mechanisms().empty()` est vrai. |
| **LevelLoaderTest.ChargeLesBlocsATailleReduite** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:221`</sub> | Les deux blocs à taille réduite se chargent comme de simples tuiles, sans identifiant ni liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(0, 0)` vaut `core::TileType::BlockHalf`.<br/>Vérifie que `level.tileMap().tile(1, 0)` vaut `core::TileType::BlockQuarter`.<br/>Vérifie que `level.mechanisms().empty()` est vrai. |
| **LevelLoaderTest.ChargeLesDeuxPentes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:255`</sub> | Les deux orientations de pente se chargent comme de simples tuiles, sans identifiant ni liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(0, 0)` vaut `core::TileType::SlopeUpRight`.<br/>Vérifie que `level.tileMap().tile(1, 0)` vaut `core::TileType::SlopeUpLeft`.<br/>Vérifie que `level.mechanisms().empty()` est vrai. |
| **LevelLoaderTest.ChargeLesDeuxArrondis** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:289`</sub> | Les deux orientations d'arrondi se chargent comme de simples tuiles, sans identifiant ni liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(0, 0)` vaut `core::TileType::RoundedUpRight`.<br/>Vérifie que `level.tileMap().tile(1, 0)` vaut `core::TileType::RoundedUpLeft`.<br/>Vérifie que `level.mechanisms().empty()` est vrai. |
| **LevelLoaderTest.ChargeLesQuatrePentesEtArrondisDePlafond** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:323`</sub> | Les quatre pentes/arrondis de plafond se chargent comme de simples tuiles, sans identifiant ni liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(0, 0)` vaut `core::TileType::SlopeDownRight`.<br/>Vérifie que `level.tileMap().tile(1, 0)` vaut `core::TileType::SlopeDownLeft`.<br/>Vérifie que `level.tileMap().tile(2, 0)` vaut `core::TileType::RoundedDownRight`.<br/>Vérifie que `level.tileMap().tile(3, 0)` vaut `core::TileType::RoundedDownLeft`.<br/>Vérifie que `level.mechanisms().empty()` est vrai. |
| **LevelLoaderTest.ChargeLesQuatreArrondisConcaves** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:361`</sub> | Les quatre arrondis concaves se chargent comme de simples tuiles, sans identifiant ni liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(0, 0)` vaut `core::TileType::ConcaveUpRight`.<br/>Vérifie que `level.tileMap().tile(1, 0)` vaut `core::TileType::ConcaveUpLeft`.<br/>Vérifie que `level.tileMap().tile(2, 0)` vaut `core::TileType::ConcaveDownRight`.<br/>Vérifie que `level.tileMap().tile(3, 0)` vaut `core::TileType::ConcaveDownLeft`.<br/>Vérifie que `level.mechanisms().empty()` est vrai. |
| **LevelLoaderTest.BudgetsOptionnels** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:399`</sub> | Les budgets de mouvements (EX-GP-024) sont chargés s'ils sont présents, illimités (-1) sinon. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `withBudget.ok()` est vrai.<br/>Vérifie que `withBudget.level->jumpBudget()` vaut `2`.<br/>Vérifie que `withBudget.level->dashBudget()` vaut `1`.<br/>Vérifie que `noBudget.ok()` est vrai.<br/>Vérifie que `noBudget.level->jumpBudget()` vaut `-1`.<br/>Vérifie que `noBudget.level->dashBudget()` vaut `-1`. |
| **LevelLoaderTest.JsonMalformeRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:427`</sub> | Un JSON syntaxiquement invalide est rejeté sans plantage. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.error.empty()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::ParseError`. |
| **LevelLoaderTest.ChampManquantRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:444`</sub> | Un champ obligatoire manquant est rejeté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::ParseError`. |
| **LevelLoaderTest.TypeDeTuileInconnuRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:461`</sub> | Un type de tuile inconnu est rejeté avec un message. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.error.empty()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::UnknownTileType`. |
| **LevelLoaderTest.TuileHorsBornesRejetee** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:479`</sub> | Une tuile hors des bornes est rejetée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::OutOfBounds`. |
| **LevelLoaderTest.LiaisonMecanismeNonResolueRejetee** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:496`</sub> | Une porte liée à un interrupteur inexistant est rejetée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.error.empty()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::UnresolvedMechanism`. |
| **LevelLoaderTest.PlusieursEntreesRejetees** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:520`</sub> | Plusieurs entrées sont rejetées (une seule attendue). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::InvalidEntryCount`. |
| **LevelLoaderTest.PlusieursSortiesRejetees** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:543`</sub> | Plusieurs sorties sont rejetées (une seule attendue). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::InvalidExitCount`. |
| **LevelLoaderTest.PositionEnDoubleRejetee** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:566`</sub> | Deux tuiles à la même position sont rejetées. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::DuplicatePosition`. |
| **LevelLoaderTest.TilesNonListeRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:590`</sub> | Un champ 'tiles' qui n'est pas une liste est rejeté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::ParseError`. |
| **LevelLoaderTest.DimensionsNonPositivesRejetees** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:607`</sub> | Des dimensions non positives sont rejetées. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::ParseError`. |
| **LevelLoaderTest.InterrupteurSansIdRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:624`</sub> | Un interrupteur sans 'id' est rejeté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::MissingSwitchId`. |
| **LevelLoaderTest.IdentifiantInterrupteurEnDoubleRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:647`</sub> | Deux interrupteurs avec le même identifiant sont rejetés. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::DuplicateSwitchId`. |
| **LevelLoaderTest.NiveauSansEntreeRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:671`</sub> | Un niveau sans entrée est rejeté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::InvalidEntryCount`. |
| **LevelLoaderTest.NiveauSansSortieRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:688`</sub> | Un niveau sans sortie est rejeté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::InvalidExitCount`. |
| **LevelLoaderTest.FichierIntrouvableRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:705`</sub> | Charger un fichier inexistant échoue proprement (récupérable). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.error.empty()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::FileNotFound`. |
| **LevelLoaderTest.PorteSansLiaisonEstValide** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:723`</sub> | Une porte sans 'opensWith' est une simple tuile : chargement valide, aucun mécanisme. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.level->mechanisms().empty()` est vrai. |
| **LevelLoaderTest.NiveauDeDemoLivreValide** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:747`</sub> | Le niveau de démonstration livré (Source/Elements/Levels) se charge et se valide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai. |
| **LevelLoaderTest.ChargeLesQuatreDangersDirectionnels** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:766`</sub> | Les quatre dangers directionnels se chargent comme de simples tuiles, sans identifiant ni liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(0, 0)` vaut `core::TileType::DangerUp`.<br/>Vérifie que `level.tileMap().tile(1, 0)` vaut `core::TileType::DangerDown`.<br/>Vérifie que `level.tileMap().tile(2, 0)` vaut `core::TileType::DangerLeft`.<br/>Vérifie que `level.tileMap().tile(3, 0)` vaut `core::TileType::DangerRight`.<br/>Vérifie que `level.mechanisms().empty()` est vrai. |
| **LevelLoaderTest.ChargeUnDangerMobileValeursParDefaut** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:804`</sub> | Un danger mobile sans champs explicites reçoit les valeurs par défaut. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(1, 1)` vaut `core::TileType::DangerMover`.<br/>Vérifie que `level.moverConfigs().size()` vaut `1u`.<br/>Vérifie que `level.moverConfigs().front().startPosition` vaut `(core::GridPosition{1, 1})`.<br/>Vérifie que `level.moverConfigs().front().axis` vaut `core::DangerMoverAxis::Horizontal`.<br/>Vérifie que `level.moverConfigs().front().range` vaut `2`. |
| **LevelLoaderTest.ChargeUnDangerMobileValeursExplicites** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:834`</sub> | Un danger mobile avec axe et portée explicites les préserve. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.level->moverConfigs().size()` vaut `1u`.<br/>Vérifie que `result.level->moverConfigs().front().axis` vaut `core::DangerMoverAxis::Vertical`.<br/>Vérifie que `result.level->moverConfigs().front().range` vaut `3`. |
| **LevelLoaderTest.PorteeDangerMobileHorsBornesRejetee** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:861`</sub> | Une portée de danger mobile qui sortirait de la grille est rejetée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::OutOfBounds`. |
| **LevelLoaderTest.ChargeUnDangerCommuteLie** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:886`</sub> | Un danger commuté lié à un interrupteur est résolu au chargement. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(3, 0)` vaut `core::TileType::DangerSwitched`.<br/>Vérifie que `level.mechanisms().empty()` est vrai.<br/>Vérifie que `level.dangerLinks().size()` vaut `1u`.<br/>Vérifie que `level.dangerLinks().front().triggerPosition` vaut `(core::GridPosition{2, 0})`.<br/>Vérifie que `level.dangerLinks().front().dangerPosition` vaut `(core::GridPosition{3, 0})`. |
| **LevelLoaderTest.DangerCommuteSansLiaisonEstValide** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:918`</sub> | Un danger commuté sans 'opensWith' est une simple tuile inerte. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.level->dangerLinks().empty()` est vrai. |
| **LevelLoaderTest.DangerCommuteLieAInterrupteurInexistantRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:943`</sub> | Un danger commuté lié à un interrupteur inexistant est rejeté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::UnresolvedMechanism`. |
| **LevelLoaderTest.ChargeUnDangerTemporiseValeursParDefaut** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:968`</sub> | Un danger temporisé sans champs explicites reçoit les valeurs par défaut. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.level->blinkConfigs().size()` vaut `1u`.<br/>Vérifie que `config.position` vaut `(core::GridPosition{2, 0})`.<br/>Vérifie que `config.period` vaut `120`.<br/>Vérifie que `config.phase` vaut `0`.<br/>Vérifie que `config.activeDuration` vaut `60`. |
| **LevelLoaderTest.ChargeUnDangerTemporiseValeursExplicites** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:1000`</sub> | Un danger temporisé avec valeurs explicites les préserve. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.level->blinkConfigs().size()` vaut `1u`.<br/>Vérifie que `config.period` vaut `90`.<br/>Vérifie que `config.phase` vaut `15`.<br/>Vérifie que `config.activeDuration` vaut `30`. |
| **LevelLoaderTest.LesQuinzeNiveauxDeDemoSeChargentSansErreur** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_loader.cpp:1032`</sub> | Les quinze niveaux de demonstration livres se chargent sans erreur. | 1. Balayer le dossier des niveaux livres.<br/>2. Charger chaque fichier. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.errorCode` vaut `core::LevelValidationError::None`.<br/>Vérifie que `checked` vaut `15`. |

**`test_level_outcome.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LevelOutcomeTest.ZoneLibreEstEnCours** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_outcome.cpp:36`</sub> | En zone libre (ni sortie, ni danger, dans les limites) : partie en cours. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::evaluateOutcome(unitBox(1.0f, 1.0f), level)` vaut `core::LevelOutcome::Playing`. |
| **LevelOutcomeTest.SurLaSortieEstGagne** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_outcome.cpp:51`</sub> | La boîte recouvrant la case de sortie : niveau gagné. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::evaluateOutcome(unitBox(8.0f, 8.0f), level)` vaut `core::LevelOutcome::Won`. |
| **LevelOutcomeTest.SurUnDangerEstPerdu** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_outcome.cpp:66`</sub> | La boîte recouvrant une tuile Danger : niveau perdu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::evaluateOutcome(unitBox(5.0f, 5.0f), level)` vaut `core::LevelOutcome::Lost`. |
| **LevelOutcomeTest.SousLaLimiteBasseEstPerdu** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_outcome.cpp:81`</sub> | La boîte sous la limite basse (chute dans le vide) : niveau perdu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::evaluateOutcome(unitBox(2.0f, 10.0f), level)` vaut `core::LevelOutcome::Lost`. |
| **LevelOutcomeTest.EchecPrioritaireSurSucces** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_outcome.cpp:96`</sub> | Recouvrement simultané sortie + danger : l'échec est prioritaire (déterminisme). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::evaluateOutcome(unitBox(5.0f, 5.0f), level)` vaut `core::LevelOutcome::Lost`. |
| **LevelOutcomeTest.DangerDirectionnelPerduSeulementSurSaBande** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_outcome.cpp:114`</sub> | Un danger directionnel ne provoque l'échec que sur la bande de son bord désigné. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::evaluateOutcome(safeBox, level)` vaut `core::LevelOutcome::Playing`.<br/>Vérifie que `core::evaluateOutcome(dangerBox, level)` vaut `core::LevelOutcome::Lost`. |
| **LevelOutcomeTest.BoiteSupplementaireProvoqueLEchec** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_outcome.cpp:143`</sub> | Une boîte supplémentaire fournie par l'appelant provoque l'échec. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::evaluateOutcome(unitBox(1.0f, 1.0f), level, extraDangerBoxes)` vaut `core::LevelOutcome::Playing`.<br/>Vérifie que `core::evaluateOutcome(unitBox(3.0f, 3.0f), level, extraDangerBoxes)` vaut `core::LevelOutcome::Lost`. |
| **LevelOutcomeTest.SansBoiteSupplementaireComportementInchange** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_outcome.cpp:164`</sub> | Sans boîte supplémentaire, le comportement est inchangé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `core::evaluateOutcome(unitBox(5.0f, 5.0f), level)` vaut `core::LevelOutcome::Lost`.<br/>Vérifie que `core::evaluateOutcome(unitBox(5.0f, 5.0f), level, {})` vaut `core::LevelOutcome::Lost`. |

**`test_level_sequence.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LevelSequenceLoaderTest.SequenceValideSeChargeDansLOrdreExact** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_sequence.cpp:51`</sub> | Une séquence valide se charge dans l'ordre exact du fichier. | 1. Charger une séquence JSON valide depuis une chaîne.<br/>2. Vérifier l'ordre des niveaux et la clé de titre. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.errorCode` vaut `core::LevelSequenceError::None`.<br/>Vérifie que `result.sequence->titleKey` vaut `"sequence.demo.title"`.<br/>Vérifie que `result.sequence->levels.size()` vaut `2u`.<br/>Vérifie que `result.sequence->levels[0]` vaut `"a.json"`.<br/>Vérifie que `result.sequence->levels[1]` vaut `"b.json"`. |
| **LevelSequenceLoaderTest.SequenceSansVersionSeChargeSansErreur** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_sequence.cpp:73`</sub> | Une séquence sans champ version se charge sans erreur. | 1. Charger une séquence JSON sans champ `version`.<br/>2. Vérifier le succès. | Vérifie que `result.ok()` est vrai. |
| **LevelSequenceLoaderTest.VersionSuperieureALaVersionGereeEchoueProprement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_sequence.cpp:88`</sub> | Une séquence dont la version dépasse celle gérée échoue proprement. | 1. Charger une séquence dont `version` dépasse la version gérée.<br/>2. Vérifier l'échec et son code. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelSequenceError::UnsupportedFormatVersion`. |
| **LevelSequenceLoaderTest.JsonMalformeEchoueProprement** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_sequence.cpp:105`</sub> | Un JSON malformé échoue proprement. | 1. Charger une chaîne qui n'est pas du JSON valide.<br/>2. Vérifier l'échec et son code. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.error.empty()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelSequenceError::ParseError`. |
| **LevelSequenceLoaderTest.ListeDeNiveauxVideEchoueProprement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_sequence.cpp:123`</sub> | Une liste de niveaux vide échoue proprement. | 1. Charger une séquence dont `levels` est une liste vide.<br/>2. Vérifier l'échec et son code. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelSequenceError::EmptySequence`. |
| **LevelSequenceDir.FichierIntrouvableRejete** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_sequence.cpp:140`</sub> | Charger un fichier de séquence inexistant échoue proprement. | 1. Charger un chemin de fichier inexistant.<br/>2. Vérifier l'échec et son code. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.error.empty()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelSequenceError::FileNotFound`. |
| **LevelSequenceDir.NiveauReferenceMaisAbsentEchoueProprementEtNommeLeFautif** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_sequence.cpp:158`</sub> | Un niveau référencé mais absent échoue proprement, nommant le fautif. | 1. Écrire un fichier de séquence référençant un niveau absent du même dossier.<br/>2. Charger ce fichier.<br/>3. Vérifier l'échec, son code, et que le nom du niveau fautif apparaît dans le message. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `core::LevelSequenceError::MissingLevelFile`.<br/>Vérifie que `result.error.find("absent.json")` diffère de `std::string::npos`. |
| **LevelSequenceDir.SequenceDontTousLesNiveauxExistentSeCharge** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_sequence.cpp:179`</sub> | Une séquence dont tous les niveaux référencés existent se charge. | 1. Écrire un fichier de séquence et les niveaux qu'il référence, dans le même dossier.<br/>2. Charger le fichier de séquence. | Vérifie que `result.ok()` est vrai. |
| **LevelSequenceLoaderTest.SequenceDeDemoLivreeValide** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_sequence.cpp:199`</sub> | La séquence de démonstration livrée se charge et référence des niveaux qui existent tous. | 1. Charger `sequence-demo.json` depuis le dossier de niveaux livré.<br/>2. Vérifier le succès et qu'au moins un niveau est référencé. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.sequence->levels.empty()` est faux. |

**`test_level_writer.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LevelWriterTest.RoundTripPreserveLeContenu** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:36`</sub> | Sérialiser puis recharger un niveau produit un niveau équivalent (round-trip). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `roundTripped.name()` vaut `original.name()`.<br/>Vérifie que `roundTripped.tileMap().width()` vaut `original.tileMap().width()`.<br/>Vérifie que `roundTripped.tileMap().height()` vaut `original.tileMap().height()`.<br/>Vérifie que `roundTripped.tileMap().tile(column, row)` vaut `original.tileMap().tile(column, row)`.<br/>Vérifie que `roundTripped.entry()` vaut `original.entry()`.<br/>Vérifie que `roundTripped.exit()` vaut `original.exit()`.<br/>Vérifie que `roundTripped.jumpBudget()` vaut `original.jumpBudget()`.<br/>Vérifie que `roundTripped.dashBudget()` vaut `original.dashBudget()`.<br/>Vérifie que `roundTripped.mechanisms().size()` vaut `original.mechanisms().size()`.<br/>Vérifie que `roundTripped.mechanisms().front().switchPosition` vaut `original.mechanisms().front().switchPosition`.<br/>Vérifie que `roundTripped.mechanisms().front().doorPosition` vaut `original.mechanisms().front().doorPosition`. |
| **LevelWriterTest.PlaqueDePressionSurvitAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:81`</sub> | Une plaque de pression survit au round-trip. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(2, 0)` vaut `core::TileType::PressurePlate`.<br/>Vérifie que `reloaded.level->mechanisms().size()` vaut `1u`.<br/>Vérifie que `reloaded.level->mechanisms().front().switchPosition` vaut `(core::GridPosition{2, 0})`.<br/>Vérifie que `reloaded.level->mechanisms().front().doorPosition` vaut `(core::GridPosition{3, 0})`. |
| **LevelWriterTest.BlocPoussableSurvitAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:117`</sub> | Un bloc poussable survit au round-trip. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(2, 0)` vaut `core::TileType::Block`.<br/>Vérifie que `reloaded.level->mechanisms().empty()` est vrai. |
| **LevelWriterTest.BlocsATailleReduiteSurviventAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:150`</sub> | Les deux blocs à taille réduite survivent à un aller-retour JSON. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(0, 0)` vaut `core::TileType::BlockHalf`.<br/>Vérifie que `reloaded.level->tileMap().tile(1, 0)` vaut `core::TileType::BlockQuarter`. |
| **LevelWriterTest.PentesSurviventAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:183`</sub> | Les deux orientations de pente survivent au round-trip. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(0, 0)` vaut `core::TileType::SlopeUpRight`.<br/>Vérifie que `reloaded.level->tileMap().tile(1, 0)` vaut `core::TileType::SlopeUpLeft`. |
| **LevelWriterTest.ArrondisSurviventAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:217`</sub> | Les deux orientations d'arrondi survivent à un aller-retour JSON. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(0, 0)` vaut `core::TileType::RoundedUpRight`.<br/>Vérifie que `reloaded.level->tileMap().tile(1, 0)` vaut `core::TileType::RoundedUpLeft`. |
| **LevelWriterTest.PentesEtArrondisDePlafondSurviventAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:250`</sub> | Les quatre pentes/arrondis de plafond survivent à un aller-retour JSON. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(0, 0)` vaut `core::TileType::SlopeDownRight`.<br/>Vérifie que `reloaded.level->tileMap().tile(1, 0)` vaut `core::TileType::SlopeDownLeft`.<br/>Vérifie que `reloaded.level->tileMap().tile(2, 0)` vaut `core::TileType::RoundedDownRight`.<br/>Vérifie que `reloaded.level->tileMap().tile(3, 0)` vaut `core::TileType::RoundedDownLeft`. |
| **LevelWriterTest.ArrondisConcavesSurviventAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:288`</sub> | Les quatre arrondis concaves survivent à un aller-retour JSON. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(0, 0)` vaut `core::TileType::ConcaveUpRight`.<br/>Vérifie que `reloaded.level->tileMap().tile(1, 0)` vaut `core::TileType::ConcaveUpLeft`.<br/>Vérifie que `reloaded.level->tileMap().tile(2, 0)` vaut `core::TileType::ConcaveDownRight`.<br/>Vérifie que `reloaded.level->tileMap().tile(3, 0)` vaut `core::TileType::ConcaveDownLeft`. |
| **LevelWriterTest.BudgetsIllimitesOmisDuJson** (Mineur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:325`</sub> | Les budgets illimités (-1) ne sont pas écrits dans le JSON produit. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `json.find("jumpBudget")` vaut `std::string::npos`.<br/>Vérifie que `json.find("dashBudget")` vaut `std::string::npos`.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->jumpBudget()` vaut `-1`.<br/>Vérifie que `reloaded.level->dashBudget()` vaut `-1`. |
| **LevelWriterTest.FondEtJeuDeSkinsSurviventAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:352`</sub> | Le fond et le jeu de skins survivent au round-trip. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->background().has_value()` est vrai.<br/>Vérifie que `*reloaded.level->background()` vaut `"forest.png"`.<br/>Vérifie que `reloaded.level->skinSet().has_value()` est vrai.<br/>Vérifie que `*reloaded.level->skinSet()` vaut `"foret"`. |
| **LevelWriterTest.SansFondNiJeuDeSkinsLesClesSontOmisesDuJson** (Mineur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:382`</sub> | Sans fond ni jeu de skins, les cles sont absentes du JSON produit. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `json.find("\\"background\\"")` vaut `std::string::npos`.<br/>Vérifie que `json.find("\\"skinSet\\"")` vaut `std::string::npos`.<br/>Vérifie que `json.find("\\"version\\"")` diffère de `std::string::npos`. |
| **LevelWriterTest.InterrupteurNonRelieRegenereUnIdentifiant** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:404`</sub> | Un interrupteur non relié à une porte est tout de même sérialisé, avec un identifiant. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(1, 1)` vaut `core::TileType::Switch`.<br/>Vérifie que `reloaded.level->mechanisms().empty()` est vrai. |
| **LevelWriterTest.NiveauPuzzleLivreSurvieAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:430`</sub> | Le niveau puzzle livré (demo-interrupteur.json) survit à un round-trip. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->mechanisms().size()` vaut `loaded.level->mechanisms().size()`.<br/>Vérifie que `reloaded.level->entry()` vaut `loaded.level->entry()`.<br/>Vérifie que `reloaded.level->exit()` vaut `loaded.level->exit()`. |
| **LevelWriterTest.SaveToFileEcritUnFichierRechargeable** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:454`</sub> | saveToFile écrit un fichier qui se recharge à l'identique (round-trip disque). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `core::LevelWriter::saveToFile(*loaded.level, path)` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->name()` vaut `loaded.level->name()`.<br/>Vérifie que `reloaded.level->entry()` vaut `loaded.level->entry()`. |
| **LevelWriterTest.SaveToFileVersDossierInexistantEchoueProprement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:481`</sub> | saveToFile vers un dossier inexistant échoue proprement (récupérable, EX-NFR-040). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `core::LevelWriter::saveToFile(*loaded.level, path)` est faux. |
| **LevelWriterTest.DangersDirectionnelsSurviventAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:500`</sub> | Les quatre dangers directionnels survivent à un aller-retour JSON. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(0, 0)` vaut `core::TileType::DangerUp`.<br/>Vérifie que `reloaded.level->tileMap().tile(1, 0)` vaut `core::TileType::DangerDown`.<br/>Vérifie que `reloaded.level->tileMap().tile(2, 0)` vaut `core::TileType::DangerLeft`.<br/>Vérifie que `reloaded.level->tileMap().tile(3, 0)` vaut `core::TileType::DangerRight`. |
| **LevelWriterTest.DangerMobileSurvitAuRoundTripAvecSaConfiguration** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:535`</sub> | Un danger mobile survit au round-trip avec sa configuration. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(1, 1)` vaut `core::TileType::DangerMover`.<br/>Vérifie que `reloaded.level->moverConfigs().size()` vaut `1u`.<br/>Vérifie que `reloaded.level->moverConfigs().front().axis` vaut `core::DangerMoverAxis::Vertical`.<br/>Vérifie que `reloaded.level->moverConfigs().front().range` vaut `3`. |
| **LevelWriterTest.DangerCommuteSurvitAuRoundTrip** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:567`</sub> | Un danger commuté survit au round-trip avec sa liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->tileMap().tile(3, 0)` vaut `core::TileType::DangerSwitched`.<br/>Vérifie que `reloaded.level->dangerLinks().size()` vaut `1u`.<br/>Vérifie que `reloaded.level->dangerLinks().front().triggerPosition` vaut `(core::GridPosition{2, 0})`.<br/>Vérifie que `reloaded.level->dangerLinks().front().dangerPosition` vaut `(core::GridPosition{3, 0})`. |
| **LevelWriterTest.DangerTemporiseSurvitAuRoundTripAvecSaConfiguration** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_level_writer.cpp:601`</sub> | Un danger temporisé survit au round-trip avec sa configuration. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->blinkConfigs().size()` vaut `1u`.<br/>Vérifie que `config.period` vaut `90`.<br/>Vérifie que `config.phase` vaut `15`.<br/>Vérifie que `config.activeDuration` vaut `30`. |

**`test_tile_texture_override.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **TileTextureOverrideTest.SetTextureOverrideAssigneLaTexture** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_texture_override.cpp:29`</sub> | setTextureOverride assigne une texture à une case précise. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.textureOverrides().size()` vaut `1u`.<br/>Vérifie que `draft.textureOverrides().front().position` vaut `(GridPosition{1, 1})`.<br/>Vérifie que `draft.textureOverrides().front().assetName` vaut `"door_red.png"`. |
| **TileTextureOverrideTest.ReassignerRemplaceLOverrideExistant** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_texture_override.cpp:51`</sub> | Réassigner une texture remplace l'override existant. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.textureOverrides().size()` vaut `1u`.<br/>Vérifie que `draft.textureOverrides().front().assetName` vaut `"door_blue.png"`. |
| **TileTextureOverrideTest.RemoveTextureOverrideRetireLOverride** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_texture_override.cpp:72`</sub> | removeTextureOverride retire l'override d'une case. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.textureOverrides().empty()` est vrai.<br/>Vérifie que `draft.textureOverrides().empty()` est vrai. |
| **TileTextureOverrideTest.PeindreUnAutreTypeRetireLOverride** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_texture_override.cpp:95`</sub> | Repeindre un autre type retire l'override. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.textureOverrides().empty()` est vrai. |
| **TileTextureOverrideTest.PeindreLeMemeTypeConserveLOverride** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_texture_override.cpp:116`</sub> | Repeindre le même type conserve l'override. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.textureOverrides().size()` vaut `1u`.<br/>Vérifie que `draft.textureOverrides().front().assetName` vaut `"door_red.png"`. |
| **TileTextureOverrideTest.PaintRegionRetireLOverrideDUneCaseDontLeTypeChange** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_texture_override.cpp:138`</sub> | paintRegion retire l'override d'une case dont le type change. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.textureOverrides().empty()` est vrai. |
| **TileTextureOverrideTest.CollerUneRegionNeCopiePasLesOverridesDeLaSource** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_texture_override.cpp:160`</sub> | Coller une région ne copie pas les overrides de la source. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.textureOverrides().size()` vaut `1u`.<br/>Vérifie que `draft.textureOverrides().front().position` vaut `(GridPosition{0, 0})`. |
| **TileTextureOverrideTest.ReduireLaGrilleTronqueLesOverridesHorsBornes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_texture_override.cpp:185`</sub> | Réduire la grille tronque les overrides hors bornes. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.textureOverrides().empty()` est vrai. |
| **TileTextureOverrideTest.WouldResizeDropContentSignaleLaPerteDUnOverride** (Mineur)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_texture_override.cpp:205`</sub> | wouldResizeDropContent signale la perte d'un override. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.wouldResizeDropContent(2, 2)` est vrai.<br/>Vérifie que `draft.wouldResizeDropContent(5, 5)` est faux. |
| **TileTextureOverrideTest.UndoRedoCouvrentUneAssignation** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_texture_override.cpp:224`</sub> | undo/redo couvrent une assignation de texture. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.textureOverrides().size()` vaut `1u`.<br/>Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.textureOverrides().empty()` est vrai.<br/>Vérifie que `draft.redo()` est vrai.<br/>Vérifie que `draft.textureOverrides().size()` vaut `1u`.<br/>Vérifie que `draft.textureOverrides().front().assetName` vaut `"door_red.png"`. |
| **TileTextureOverrideTest.UndoRestitueUnOverrideRetire** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_texture_override.cpp:249`</sub> | undo restitue un override retiré. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `draft.textureOverrides().empty()` est vrai.<br/>Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.textureOverrides().size()` vaut `1u`.<br/>Vérifie que `draft.textureOverrides().front().assetName` vaut `"door_red.png"`. |
| **TileTextureOverrideTest.FromLevelRestitueLesOverrides** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_texture_override.cpp:272`</sub> | fromLevel restitue les overrides de texture. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `draft.textureOverrides().size()` vaut `1u`.<br/>Vérifie que `draft.textureOverrides().front().position` vaut `(GridPosition{2, 2})`.<br/>Vérifie que `draft.textureOverrides().front().assetName` vaut `"door_red.png"`. |
| **TileTextureOverrideTest.LeChampTextureSurvitAuRoundTripJson** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_texture_override.cpp:300`</sub> | Le champ texture survit au round-trip JSON. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `reloaded.level->textureOverrides().size()` vaut `2u`.<br/>Vérifie que `hasCrate` est vrai.<br/>Vérifie que `hasDoor` est vrai. |
| **TileTextureOverrideTest.SansOverrideLeChampTextureEstAbsentDuJson** (Mineur)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_texture_override.cpp:338`</sub> | Sans override, le champ texture est absent du JSON produit. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `json.find("\\"texture\\"")` vaut `std::string::npos`. |
| **TileTextureOverrideTest.NiveauSansChampTextureSeChargeSansOverride** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_texture_override.cpp:359`</sub> | Un niveau sans champ texture se charge sans override. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `loaded.level->textureOverrides().empty()` est vrai. |

**`test_tile_type_name.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **TileTypeNameTest.AllerRetourSurTousLesTypes** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_type_name.cpp:25`</sub> | Chaque type de tuile fait l'aller-retour par son nom textuel sans perte. | 1. Pour les trente types, convertir le type en nom, puis le nom en type. | Vérifie que `parsed.has_value()` est vrai.<br/>Vérifie que `*parsed` vaut `type`. |
| **TileTypeNameTest.LesNomsSontUniques** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_type_name.cpp:45`</sub> | Deux types de tuiles distincts ne portent jamais le meme nom. | 1. Collecter les noms des trente types dans un ensemble. | Vérifie que `names.size()` vaut `static_cast<std::size_t>(LAST_TILE_TYPE + 1)`. |
| **TileTypeNameTest.NomInconnuRefuse** (Majeur)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_type_name.cpp:63`</sub> | Un nom de type inconnu est refuse au lieu d'etre devine. | 1. Convertir une chaine vide, un nom inexistant et un nom de casse differente. | Vérifie que `core::parseTileType("").has_value()` est faux.<br/>Vérifie que `core::parseTileType("mur").has_value()` est faux.<br/>Vérifie que `core::parseTileType("Solid").has_value()` est faux. |
| **TileTypeNameTest.NomsDuFormatDeNiveauInchanges** (Critique)<br/><sub>`Source/Test/Unit/Core/Levels/test_tile_type_name.cpp:80`</sub> | Les noms du format de niveau restent inchanges. | 1. Verifier le nom de quelques types representatifs de chaque famille. | Vérifie que `core::tileTypeName(core::TileType::Solid)` vaut `"solid"`.<br/>Vérifie que `core::tileTypeName(core::TileType::PressurePlate)` vaut `"pressurePlate"`.<br/>Vérifie que `core::tileTypeName(core::TileType::SlopeUpRight)` vaut `"slopeUpRight"`.<br/>Vérifie que `core::tileTypeName(core::TileType::ConcaveDownLeft)` vaut `"concaveDownLeft"`.<br/>Vérifie que `core::tileTypeName(core::TileType::DangerBlink)` vaut `"dangerBlink"`. |

#### Math (20)

**`test_math_utils.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **MathUtilsTest.EgaliteExacte** (Mineur)<br/><sub>`Source/Test/Unit/Core/Math/test_math_utils.cpp:12`</sub> | approximatelyEqual : égalité exacte | 1. Comparer des valeurs identiques (1, 0, -2,5). | Vérifie que `core::approximatelyEqual(1.0f, 1.0f)` est vrai.<br/>Vérifie que `core::approximatelyEqual(0.0f, 0.0f)` est vrai.<br/>Vérifie que `core::approximatelyEqual(-2.5f, -2.5f)` est vrai. |
| **MathUtilsTest.ToleranceParDefaut** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_math_utils.cpp:26`</sub> | approximatelyEqual : tolérance par défaut | 1. Comparer 1 et 1+1e-6.<br/>2. Comparer 1 et 1,1. | Vérifie que `core::approximatelyEqual(1.0f, 1.0f + 1e-6f)` est vrai.<br/>Vérifie que `core::approximatelyEqual(1.0f, 1.1f)` est faux. |
| **MathUtilsTest.MiseALEchelleGrandesMagnitudes** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_math_utils.cpp:39`</sub> | approximatelyEqual : mise à l'échelle des grandes magnitudes | 1. Comparer 1e6 et 1e6+1.<br/>2. Comparer 1e6 et 1e6+1000. | Vérifie que `core::approximatelyEqual(1.0e6f, 1.0e6f + 1.0f)` est vrai.<br/>Vérifie que `core::approximatelyEqual(1.0e6f, 1.0e6f + 1000.0f)` est faux. |
| **MathUtilsTest.SignesOpposes** (Mineur)<br/><sub>`Source/Test/Unit/Core/Math/test_math_utils.cpp:52`</sub> | approximatelyEqual : signes opposés | 1. Comparer 2 et -2. | Vérifie que `core::approximatelyEqual(2.0f, -2.0f)` est faux. |
| **MathUtilsTest.ToleranceExplicite** (Mineur)<br/><sub>`Source/Test/Unit/Core/Math/test_math_utils.cpp:64`</sub> | approximatelyEqual : tolérance explicite | 1. Comparer 1 et 1,5 sans tolérance.<br/>2. Recomparer avec une tolérance de 1. | Vérifie que `core::approximatelyEqual(1.0f, 1.5f)` est faux.<br/>Vérifie que `core::approximatelyEqual(1.0f, 1.5f, 1.0f)` est vrai. |

**`test_rect.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **RectTest.Bords** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_rect.cpp:17`</sub> | Les bords exposés découlent de la position et de la taille. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `rect.left()` vaut `10.0f`, à `TOLERANCE` près.<br/>Vérifie que `rect.top()` vaut `20.0f`, à `TOLERANCE` près.<br/>Vérifie que `rect.right()` vaut `40.0f`, à `TOLERANCE` près.<br/>Vérifie que `rect.bottom()` vaut `60.0f`, à `TOLERANCE` près. |
| **RectTest.ContientPointInterieur** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_rect.cpp:35`</sub> | Un point strictement intérieur est contenu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `rect.contains(core::Vector2{5.0f, 5.0f})` est vrai. |
| **RectTest.ContientBords** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_rect.cpp:50`</sub> | Contenance inclusive haut/gauche, exclusive bas/droit. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `rect.contains(core::Vector2{0.0f, 0.0f})` est vrai.<br/>Vérifie que `rect.contains(core::Vector2{10.0f, 5.0f})` est faux.<br/>Vérifie que `rect.contains(core::Vector2{5.0f, 10.0f})` est faux. |
| **RectTest.NeContientPasExterieur** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_rect.cpp:67`</sub> | Un point extérieur n'est pas contenu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `rect.contains(core::Vector2{-1.0f, 5.0f})` est faux.<br/>Vérifie que `rect.contains(core::Vector2{5.0f, 100.0f})` est faux. |
| **RectTest.IntersectionRecouvrement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_rect.cpp:83`</sub> | Deux rectangles qui se recouvrent s'intersectent (relation symétrique). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `a.intersects(b)` est vrai.<br/>Vérifie que `b.intersects(a)` est vrai. |
| **RectTest.ContactBordSansIntersection** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_rect.cpp:100`</sub> | Un simple contact par un bord ne compte pas comme intersection. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `a.intersects(b)` est faux. |
| **RectTest.Disjonction** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_rect.cpp:116`</sub> | Deux rectangles disjoints ne s'intersectent pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `a.intersects(b)` est faux. |

**`test_vector2.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **Vector2Test.AdditionSoustraction** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_vector2.cpp:16`</sub> | Vector2 : addition et soustraction | 1. Poser deux vecteurs.<br/>2. Calculer a+b et a-b. | Vérifie que `a + b` vaut `core::Vector2(4.0f, -2.0f)`.<br/>Vérifie que `a - b` vaut `core::Vector2(-2.0f, 6.0f)`. |
| **Vector2Test.EchelleScalaire** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_vector2.cpp:31`</sub> | Vector2 : échelle par un scalaire | 1. Poser un vecteur.<br/>2. Multiplier/diviser par un scalaire, prendre l'opposé. | Vérifie que `v * 2.0f` vaut `core::Vector2(4.0f, -6.0f)`.<br/>Vérifie que `2.0f * v` vaut `core::Vector2(4.0f, -6.0f)`.<br/>Vérifie que `v / 2.0f` vaut `core::Vector2(1.0f, -1.5f)`.<br/>Vérifie que `-v` vaut `core::Vector2(-2.0f, 3.0f)`. |
| **Vector2Test.OperateursComposes** (Mineur)<br/><sub>`Source/Test/Unit/Core/Math/test_vector2.cpp:47`</sub> | Vector2 : opérateurs composés | 1. Appliquer +=, -=, *=, /= à un vecteur. | Vérifie que `v` vaut `core::Vector2(3.0f, 4.0f)`.<br/>Vérifie que `v` vaut `core::Vector2(2.0f, 3.0f)`.<br/>Vérifie que `v` vaut `core::Vector2(4.0f, 6.0f)`.<br/>Vérifie que `v` vaut `core::Vector2(2.0f, 3.0f)`. |
| **Vector2Test.ProduitScalaire** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_vector2.cpp:67`</sub> | Vector2 : produit scalaire | 1. Calculer a·b.<br/>2. Calculer le produit de deux vecteurs orthogonaux. | Vérifie que `a.dot(b)` vaut `11.0f`, à `TOLERANCE` près.<br/>Vérifie que `core::Vector2(1.0f, 0.0f).dot(core::Vector2(0.0f, 1.0f))` vaut `0.0f`, à `TOLERANCE` près. |
| **Vector2Test.Longueur** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_vector2.cpp:83`</sub> | Vector2 : longueur | 1. Calculer `lengthSquared` et `length` de (3, 4). | Vérifie que `v.lengthSquared()` vaut `25.0f`, à `TOLERANCE` près.<br/>Vérifie que `v.length()` vaut `5.0f`, à `TOLERANCE` près. |
| **Vector2Test.NormalisationNonNulle** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_vector2.cpp:97`</sub> | Vector2 : normalisation (non nul) | 1. Normaliser (3, 4). | Vérifie que `normalized.length()` vaut `1.0f`, à `TOLERANCE` près.<br/>Vérifie que `normalized` vaut `core::Vector2(0.6f, 0.8f)`. |
| **Vector2Test.NormalisationVecteurNul** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_vector2.cpp:111`</sub> | Vector2 : normalisation (vecteur nul) | 1. Normaliser le vecteur nul. | Vérifie que `core::Vector2{}.normalized()` vaut `core::Vector2(0.0f, 0.0f)`. |
| **Vector2Test.EgaliteApprochee** (Majeur)<br/><sub>`Source/Test/Unit/Core/Math/test_vector2.cpp:123`</sub> | Vector2 : égalité approchée | 1. Comparer (0,1+0,2 ; 1) à (0,3 ; 1).<br/>2. Comparer deux vecteurs distincts. | Vérifie que `a` vaut `core::Vector2(0.3f, 1.0f)`.<br/>Vérifie que `core::Vector2(0.0f, 0.0f)` diffère de `core::Vector2(1.0f, 0.0f)`. |

#### Physics (39)

**`test_aabb_vs_aabb.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **AabbVsAabbTest.TrajetLibreLoinDeLObstacle** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_aabb_vs_aabb.cpp:29`</sub> | Balayage boîte-boîte : trajet libre, loin de l'obstacle | 1. Placer un obstacle réduit en (2, 1).<br/>2. Balayer une boîte 1×1 partie de loin, dont le trajet ne le croise pas. | Vérifie que `r.hit` est faux.<br/>Vérifie que `r.position.x` vaut `11.0f` (comparaison flottante).<br/>Vérifie que `r.position.y` vaut `11.0f` (comparaison flottante). |
| **AabbVsAabbTest.ButeeHorizontaleContreUnObstacleReduit** (Bloquant)<br/><sub>`Source/Test/Unit/Core/Physics/test_aabb_vs_aabb.cpp:48`</sub> | Balayage boîte-boîte : butée horizontale contre un obstacle réduit | 1. Placer un obstacle réduit (0,5×0,5) centré en (2, 1).<br/>2. Balayer une boîte 1×1 vers la droite, alignée verticalement avec l'obstacle. | Vérifie que `r.hit` est vrai.<br/>Vérifie que `r.position.x` vaut `1.25f` (comparaison flottante).<br/>Vérifie que `r.position.y` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `r.normal.x` vaut `-1.0f` (comparaison flottante). |
| **AabbVsAabbTest.ButeeVerticaleSurUnObstacleReduit** (Bloquant)<br/><sub>`Source/Test/Unit/Core/Physics/test_aabb_vs_aabb.cpp:71`</sub> | Balayage boîte-boîte : butée verticale (dessus d'un obstacle réduit) | 1. Placer un obstacle réduit centré en (2, 1).<br/>2. Balayer une boîte 1×1 en chute verticale au-dessus. | Vérifie que `r.hit` est vrai.<br/>Vérifie que `r.position.y` vaut `0.25f` (comparaison flottante).<br/>Vérifie que `r.normal.y` vaut `-1.0f` (comparaison flottante). |
| **AabbVsAabbTest.EspaceAutourDeLObstacleReduitEstFranchissable** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_aabb_vs_aabb.cpp:93`</sub> | Balayage boîte-boîte : le vide autour d'un obstacle réduit reste franchissable | 1. Placer un obstacle réduit centré en (2, 1) (marge de 0,25 de chaque côté).<br/>2. Balayer une boîte étroite qui passe dans cette marge, sans jamais chevaucher l'obstacle. | Vérifie que `r.hit` est faux.<br/>Vérifie que `r.position.x` vaut `3.5f` (comparaison flottante). |
| **AabbVsAabbTest.GlissementDiagonalLeLongDUnObstacleReduit** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_aabb_vs_aabb.cpp:116`</sub> | Balayage boîte-boîte : glissement diagonal contre un obstacle réduit | 1. Placer un obstacle réduit centré en (2, 1).<br/>2. Balayer une boîte en diagonale (droite + bas), bloquée en X par l'obstacle mais pas en Y. | Vérifie que `r.normal.x` vaut `-1.0f` (comparaison flottante).<br/>Vérifie que `r.position.x` vaut `1.25f` (comparaison flottante).<br/>Vérifie que `r.normal.y` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `r.position.y` vaut `4.3f` (comparaison flottante). |
| **AabbVsAabbTest.DeplacementNulNeBougeRien** (Mineur)<br/><sub>`Source/Test/Unit/Core/Physics/test_aabb_vs_aabb.cpp:141`</sub> | Balayage boîte-boîte : déplacement nul sur un axe le laisse inchangé | 1. Placer un obstacle réduit.<br/>2. Balayer avec un déplacement nul sur les deux axes. | Vérifie que `r.hit` est faux.<br/>Vérifie que `r.position.x` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `r.position.y` vaut `0.0f` (comparaison flottante). |

**`test_slope_geometry.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **SlopeGeometryTest.SlopeUpRightMonteDeGaucheADroite** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:16`</sub> | SlopeUpRight monte de gauche à droite : haut au bord droit, bas au bord gauche. | 1. Évaluer `slopeSurfaceHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs attendues. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `*center` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `*right` vaut `0.0f`, à `1e-3f` près. |
| **SlopeGeometryTest.SlopeUpLeftMonteDeDroiteAGauche** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:37`</sub> | SlopeUpLeft monte de droite à gauche : haut au bord gauche, bas au bord droit. | 1. Évaluer `slopeSurfaceHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs attendues. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `0.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `*right` vaut `1.0f`, à `1e-3f` près. |
| **SlopeGeometryTest.TypeNonSuivableRenvoieNullopt** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:58`</sub> | Un type de tuile sans surface à suivre renvoie nullopt. | 1. Évaluer `slopeSurfaceHeight` pour Solid, Empty, Block.<br/>2. Vérifier l'absence de valeur. | Vérifie que `core::slopeSurfaceHeight(core::TileType::Solid, 0.5f).has_value()` est faux.<br/>Vérifie que `core::slopeSurfaceHeight(core::TileType::Empty, 0.5f).has_value()` est faux.<br/>Vérifie que `core::slopeSurfaceHeight(core::TileType::Block, 0.5f).has_value()` est faux. |
| **SlopeGeometryTest.PenteNestPasSolideStatiquement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:74`</sub> | isSolid renvoie false pour les deux orientations de pente. | 1. Appeler `core::isSolid` sur `SlopeUpRight` et `SlopeUpLeft`.<br/>2. Vérifier l'assertion. | Vérifie que `core::isSolid(core::TileType::SlopeUpRight)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::SlopeUpLeft)` est faux. |
| **SlopeGeometryTest.IsFollowableSurfaceDistingueLesTypes** (Mineur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:90`</sub> | isFollowableSurface distingue les tuiles suivables des autres. | 1. Appeler `isFollowableSurface` sur les deux pentes et sur `Solid`.<br/>2. Vérifier l'assertion. | Vérifie que `core::isFollowableSurface(core::TileType::SlopeUpRight)` est vrai.<br/>Vérifie que `core::isFollowableSurface(core::TileType::SlopeUpLeft)` est vrai.<br/>Vérifie que `core::isFollowableSurface(core::TileType::Solid)` est faux. |
| **SlopeGeometryTest.RoundedUpRightSuitUnQuartDeCercle** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:107`</sub> | RoundedUpRight suit un profil de quart de cercle, haut à droite. | 1. Évaluer `slopeSurfaceHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs calculées à la main. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `1.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `1.0f - std::sqrt(0.75f)`, à `1e-4f` près.<br/>Vérifie que `*right` vaut `0.0f`, à `1e-3f` près. |
| **SlopeGeometryTest.RoundedUpLeftSuitUnQuartDeCercle** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:131`</sub> | RoundedUpLeft suit un profil de quart de cercle, haut à gauche. | 1. Évaluer `slopeSurfaceHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs calculées à la main. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `0.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `1.0f - std::sqrt(0.75f)`, à `1e-4f` près.<br/>Vérifie que `*right` vaut `1.0f`, à `1e-6f` près. |
| **SlopeGeometryTest.ArrondiNestPasSolideStatiquement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:159`</sub> | isSolid renvoie false pour les deux orientations d'arrondi. | 1. Appeler `core::isSolid` sur `RoundedUpRight` et `RoundedUpLeft`.<br/>2. Vérifier l'assertion. | Vérifie que `core::isSolid(core::TileType::RoundedUpRight)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::RoundedUpLeft)` est faux. |
| **SlopeGeometryTest.IsFollowableSurfaceReconnaitLesArrondis** (Mineur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:174`</sub> | isFollowableSurface reconnaît les deux orientations d'arrondi. | 1. Appeler `isFollowableSurface` sur `RoundedUpRight` et `RoundedUpLeft`.<br/>2. Vérifier l'assertion. | Vérifie que `core::isFollowableSurface(core::TileType::RoundedUpRight)` est vrai.<br/>Vérifie que `core::isFollowableSurface(core::TileType::RoundedUpLeft)` est vrai. |
| **SlopeGeometryTest.PenteEtArrondiDePlafondNeSontPasSolidesStatiquement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:191`</sub> | Les pentes/arrondis de plafond ne sont pas solides pour la grille classique, comme leurs équivalents de sol. | 1. Appeler `core::isSolid` sur les quatre types de plafond.<br/>2. Vérifier l'assertion. | Vérifie que `core::isSolid(core::TileType::SlopeDownRight)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::SlopeDownLeft)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::RoundedDownRight)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::RoundedDownLeft)` est faux. |
| **SlopeGeometryTest.PenteEtArrondiDePlafondOntUneFaceDuHautPlate** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:214`</sub> | Les pentes/arrondis de plafond ont une face du haut plate (0,0), sans déplacement latéral calé. | 1. Appeler `isFollowableSurface` et `slopeSurfaceHeight` sur les quatre types de plafond.<br/>2. Vérifier les valeurs. | Vérifie que `core::isFollowableSurface(type)` est faux.<br/>Vérifie que `height.has_value()` est vrai.<br/>Vérifie que `*height` vaut `0.0f` (comparaison flottante). |
| **SlopeGeometryTest.IsCeilingSlopeReconnaitExactementLesQuatreTypes** (Mineur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:238`</sub> | isCeilingSlope reconnaît exactement les quatre types de plafond, aucun autre. | 1. Appeler `isCeilingSlope` sur les quatre types de plafond et sur `SlopeUpRight`/ `Solid`.<br/>2. Vérifier l'assertion. | Vérifie que `core::isCeilingSlope(core::TileType::SlopeDownRight)` est vrai.<br/>Vérifie que `core::isCeilingSlope(core::TileType::SlopeDownLeft)` est vrai.<br/>Vérifie que `core::isCeilingSlope(core::TileType::RoundedDownRight)` est vrai.<br/>Vérifie que `core::isCeilingSlope(core::TileType::RoundedDownLeft)` est vrai.<br/>Vérifie que `core::isCeilingSlope(core::TileType::SlopeUpRight)` est faux.<br/>Vérifie que `core::isCeilingSlope(core::TileType::Solid)` est faux. |
| **SlopeGeometryTest.CeilingSlopeDownRightEstLeMiroirDeSlopeUpRight** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:258`</sub> | ceilingSlopeHeight(SlopeDownRight) est le miroir vertical exact de slopeSurfaceHeight(SlopeUpRight). | 1. Évaluer `ceilingSlopeHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs attendues. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `0.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `*right` vaut `1.0f`, à `1e-3f` près. |
| **SlopeGeometryTest.CeilingSlopeDownLeftEstLeMiroirDeSlopeUpLeft** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:283`</sub> | ceilingSlopeHeight(SlopeDownLeft) est le miroir vertical exact de slopeSurfaceHeight(SlopeUpLeft). | 1. Évaluer `ceilingSlopeHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs attendues. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `1.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `*right` vaut `0.0f`, à `1e-3f` près. |
| **SlopeGeometryTest.CeilingRoundedDownRightEstLeMiroirDeRoundedUpRight** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:308`</sub> | ceilingSlopeHeight(RoundedDownRight) est le miroir vertical exact de slopeSurfaceHeight(RoundedUpRight). | 1. Évaluer `ceilingSlopeHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs calculées à la main. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `0.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `std::sqrt(0.75f)`, à `1e-4f` près.<br/>Vérifie que `*right` vaut `1.0f`, à `1e-6f` près. |
| **SlopeGeometryTest.CeilingRoundedDownLeftEstLeMiroirDeRoundedUpLeft** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:333`</sub> | ceilingSlopeHeight(RoundedDownLeft) est le miroir vertical exact de slopeSurfaceHeight(RoundedUpLeft). | 1. Évaluer `ceilingSlopeHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs calculées à la main. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `1.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `std::sqrt(0.75f)`, à `1e-4f` près.<br/>Vérifie que `*right` vaut `0.0f`, à `1e-6f` près. |
| **SlopeGeometryTest.CeilingSlopeHeightNulloptSiPasDePlafond** (Mineur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:360`</sub> | ceilingSlopeHeight renvoie nullopt pour un type sans silhouette de plafond. | 1. Évaluer `ceilingSlopeHeight` pour `Solid` et `SlopeUpRight` (variante de sol).<br/> 2. Vérifier l'absence de valeur. | Vérifie que `core::ceilingSlopeHeight(core::TileType::Solid, 0.5f).has_value()` est faux.<br/>Vérifie que `core::ceilingSlopeHeight(core::TileType::SlopeUpRight, 0.5f).has_value()` est faux. |
| **SlopeGeometryTest.ConcaveUpRightSuitUnQuartDeCercleConcave** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:378`</sub> | ConcaveUpRight suit un profil de quart de cercle concave, mêmes bords que RoundedUpRight, courbure inversée. | 1. Évaluer `slopeSurfaceHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs calculées à la main. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `1.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `std::sqrt(0.75f)`, à `1e-4f` près.<br/>Vérifie que `*right` vaut `0.0f`, à `1e-6f` près. |
| **SlopeGeometryTest.ConcaveUpLeftSuitUnQuartDeCercleConcave** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:404`</sub> | ConcaveUpLeft suit un profil de quart de cercle concave, haut à gauche. | 1. Évaluer `slopeSurfaceHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs calculées à la main. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `0.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `std::sqrt(0.75f)`, à `1e-4f` près.<br/>Vérifie que `*right` vaut `1.0f`, à `1e-6f` près. |
| **SlopeGeometryTest.ConcaveNestPasSolideStatiquement** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:428`</sub> | isSolid renvoie false pour les deux orientations d'arrondi concave. | 1. Appeler `core::isSolid` sur `ConcaveUpRight` et `ConcaveUpLeft`.<br/>2. Vérifier l'assertion. | Vérifie que `core::isSolid(core::TileType::ConcaveUpRight)` est faux.<br/>Vérifie que `core::isSolid(core::TileType::ConcaveUpLeft)` est faux. |
| **SlopeGeometryTest.IsFollowableSurfaceReconnaitLesConcaves** (Mineur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:443`</sub> | isFollowableSurface reconnaît les deux orientations d'arrondi concave. | 1. Appeler `isFollowableSurface` sur `ConcaveUpRight` et `ConcaveUpLeft`.<br/>2. Vérifier l'assertion. | Vérifie que `core::isFollowableSurface(core::TileType::ConcaveUpRight)` est vrai.<br/>Vérifie que `core::isFollowableSurface(core::TileType::ConcaveUpLeft)` est vrai. |
| **SlopeGeometryTest.ConcaveDePlafondNeSontPasSolidesEtOntUneFaceDuHautPlate** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:460`</sub> | Les arrondis concaves de plafond ne sont pas solides, n'offrent pas de déplacement latéral calé, et ont une face du haut plate. | 1. Appeler `isSolid`/`isFollowableSurface`/`slopeSurfaceHeight` sur les deux types.<br/> 2. Vérifier les valeurs. | Vérifie que `core::isSolid(type)` est faux.<br/>Vérifie que `core::isFollowableSurface(type)` est faux.<br/>Vérifie que `height.has_value()` est vrai.<br/>Vérifie que `*height` vaut `0.0f` (comparaison flottante). |
| **SlopeGeometryTest.IsCeilingSlopeReconnaitAussiLesConcaves** (Mineur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:483`</sub> | isCeilingSlope reconnaît les deux types de plafond concave. | 1. Appeler `isCeilingSlope` sur `ConcaveDownRight`/`ConcaveDownLeft` et sur `ConcaveUpRight`.<br/>2. Vérifier l'assertion. | Vérifie que `core::isCeilingSlope(core::TileType::ConcaveDownRight)` est vrai.<br/>Vérifie que `core::isCeilingSlope(core::TileType::ConcaveDownLeft)` est vrai.<br/>Vérifie que `core::isCeilingSlope(core::TileType::ConcaveUpRight)` est faux. |
| **SlopeGeometryTest.CeilingConcaveDownRightEstLeMiroirDeConcaveUpRight** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:500`</sub> | ceilingSlopeHeight(ConcaveDownRight) est le miroir vertical exact de slopeSurfaceHeight(ConcaveUpRight). | 1. Évaluer `ceilingSlopeHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs calculées à la main. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `0.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `1.0f - std::sqrt(0.75f)`, à `1e-4f` près.<br/>Vérifie que `*right` vaut `1.0f`, à `1e-6f` près. |
| **SlopeGeometryTest.CeilingConcaveDownLeftEstLeMiroirDeConcaveUpLeft** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_slope_geometry.cpp:525`</sub> | ceilingSlopeHeight(ConcaveDownLeft) est le miroir vertical exact de slopeSurfaceHeight(ConcaveUpLeft). | 1. Évaluer `ceilingSlopeHeight` aux deux bords et au centre de la case.<br/>2. Comparer aux valeurs calculées à la main. | Vérifie que `left.has_value()` est vrai.<br/>Vérifie que `center.has_value()` est vrai.<br/>Vérifie que `right.has_value()` est vrai.<br/>Vérifie que `*left` vaut `1.0f`, à `1e-6f` près.<br/>Vérifie que `*center` vaut `1.0f - std::sqrt(0.75f)`, à `1e-4f` près.<br/>Vérifie que `*right` vaut `0.0f`, à `1e-6f` près. |

**`test_swept_collision.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **SweptCollisionTest.TrajetLibre** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_swept_collision.cpp:30`</sub> | Balayage : trajet libre | 1. Grille sans tuile solide.<br/>2. Balayer une boîte 1×1 d'un déplacement (3, 2). | Vérifie que `r.hit` est faux.<br/>Vérifie que `r.position.x` vaut `3.0f` (comparaison flottante).<br/>Vérifie que `r.position.y` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `r.normal.x` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `r.normal.y` vaut `0.0f` (comparaison flottante). |
| **SweptCollisionTest.ButeeHorizontale** (Bloquant)<br/><sub>`Source/Test/Unit/Core/Physics/test_swept_collision.cpp:50`</sub> | Balayage : butée horizontale (mur à droite) | 1. Placer un mur solide à droite.<br/>2. Balayer la boîte vers la droite. | Vérifie que `r.hit` est vrai.<br/>Vérifie que `r.position.x` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `r.position.y` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `r.normal.x` vaut `-1.0f` (comparaison flottante). |
| **SweptCollisionTest.ButeeVerticale** (Bloquant)<br/><sub>`Source/Test/Unit/Core/Physics/test_swept_collision.cpp:70`</sub> | Balayage : butée verticale (sol) | 1. Placer un sol solide en dessous.<br/>2. Balayer la boîte vers le bas. | Vérifie que `r.hit` est vrai.<br/>Vérifie que `r.position.y` vaut `4.0f` (comparaison flottante).<br/>Vérifie que `r.position.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `r.normal.y` vaut `-1.0f` (comparaison flottante). |
| **SweptCollisionTest.NonTunneling** (Bloquant)<br/><sub>`Source/Test/Unit/Core/Physics/test_swept_collision.cpp:90`</sub> | Balayage : non-tunneling à vitesse élevée | 1. Mur à 5 tuiles.<br/>2. Balayer d'un déplacement de 10 (≫ une tuile) vers le mur. | Vérifie que `r.hit` est vrai.<br/>Vérifie que `r.position.x` vaut `4.0f` (comparaison flottante).<br/>Vérifie que `r.normal.x` vaut `-1.0f` (comparaison flottante). |
| **SweptCollisionTest.GlissementLeLongDuMur** (Bloquant)<br/><sub>`Source/Test/Unit/Core/Physics/test_swept_collision.cpp:110`</sub> | Balayage : glissement le long d'un mur | 1. Mur vertical à droite.<br/>2. Balayer d'un déplacement diagonal (droite + bas). | Vérifie que `r.hit` est vrai.<br/>Vérifie que `r.position.x` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `r.position.y` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `r.normal.x` vaut `-1.0f` (comparaison flottante). |
| **SweptCollisionTest.ButeeGauche** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_swept_collision.cpp:132`</sub> | Balayage : butée horizontale (mur à gauche) | 1. Mur solide à gauche.<br/>2. Balayer la boîte vers la gauche. | Vérifie que `r.hit` est vrai.<br/>Vérifie que `r.position.x` vaut `3.0f` (comparaison flottante).<br/>Vérifie que `r.normal.x` vaut `1.0f` (comparaison flottante). |
| **SweptCollisionTest.ButeePlafond** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_swept_collision.cpp:151`</sub> | Balayage : butée verticale (plafond) | 1. Plafond solide au-dessus.<br/>2. Balayer la boîte vers le haut. | Vérifie que `r.hit` est vrai.<br/>Vérifie que `r.position.y` vaut `3.0f` (comparaison flottante).<br/>Vérifie que `r.normal.y` vaut `1.0f` (comparaison flottante). |
| **SweptCollisionTest.MarcheSurLeSolSansBlocageHorizontal** (Majeur)<br/><sub>`Source/Test/Unit/Core/Physics/test_swept_collision.cpp:171`</sub> | Balayage : marcher sur un sol sans blocage horizontal | 1. Sol continu ; boîte posée dessus.<br/>2. Balayer horizontalement le long du sol. | Vérifie que `r.hit` est faux.<br/>Vérifie que `r.position.x` vaut `3.0f` (comparaison flottante). |

#### Time (7)

**`test_fixed_timestep.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **FixedTimestepTest.UnPasExact** (Majeur)<br/><sub>`Source/Test/Unit/Core/Time/test_fixed_timestep.cpp:16`</sub> | Un temps écoulé égal au pas fixe produit exactement un pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `timestep.advance(STEP)` vaut `1`. |
| **FixedTimestepTest.TempsInsuffisant** (Majeur)<br/><sub>`Source/Test/Unit/Core/Time/test_fixed_timestep.cpp:31`</sub> | Un temps écoulé inférieur au pas ne produit aucun pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `timestep.advance(STEP * 0.5f)` vaut `0`. |
| **FixedTimestepTest.TempsNulOuNegatif** (Majeur)<br/><sub>`Source/Test/Unit/Core/Time/test_fixed_timestep.cpp:46`</sub> | Un temps écoulé nul ou négatif ne produit aucun pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `timestep.advance(0.0f)` vaut `0`.<br/>Vérifie que `timestep.advance(-1.0f)` vaut `0`. |
| **FixedTimestepTest.ResteConserve** (Majeur)<br/><sub>`Source/Test/Unit/Core/Time/test_fixed_timestep.cpp:62`</sub> | 2,5 pas donnent 2 pas, et le reste (0,5 pas) est conservé puis complété. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `timestep.advance(STEP * 2.5f)` vaut `2`.<br/>Vérifie que `timestep.interpolationAlpha()` vaut `0.5f`, à `1e-4f` près.<br/>Vérifie que `timestep.advance(STEP * 0.6f)` vaut `1`. |
| **FixedTimestepTest.PlafondAntiSpirale** (Majeur)<br/><sub>`Source/Test/Unit/Core/Time/test_fixed_timestep.cpp:82`</sub> | Un temps écoulé énorme est plafonné (anti-spirale de la mort). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `timestep.advance(STEP * 1000.0f)` vaut `maximum`.<br/>Vérifie que `timestep.advance(0.0f)` vaut `0`. |
| **FixedTimestepTest.PasFixeExpose** (Majeur)<br/><sub>`Source/Test/Unit/Core/Time/test_fixed_timestep.cpp:100`</sub> | Le pas fixe exposé correspond à la configuration. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `timestep.fixedDeltaSeconds()` vaut `STEP`, à `1e-6f` près. |
| **FixedTimestepTest.PauseSansAppelNAccumuleAucunPas** (Critique)<br/><sub>`Source/Test/Unit/Core/Time/test_fixed_timestep.cpp:119`</sub> | Une pause simulée (aucun appel à advance()) n'accumule aucun pas. | 1. Avancer normalement de quelques pas.<br/>2. Simuler une pause de longue durée en n'appelant PAS advance() (aucun appel, pas un appel à zéro).<br/>3. Reprendre avec un petit delta, comme après réarmement de l'horloge de référence. | Vérifie que `timestep.advance(STEP * 3.0f)` vaut `3`.<br/>Vérifie que `timestep.advance(STEP)` vaut `1`. |

### HMI

#### Diagnostics (2)

**`test_session_log.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **SessionLogTest.SerialiseUneLigneParMessage** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Diagnostics/test_session_log.cpp:15`</sub> | Chaque message donne une ligne, dans l'ordre d'arrivée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::serializeSessionLog(entries)` vaut `"premier message\\nsecond message\\n"`. |
| **SessionLogTest.VideDonneChaineVide** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Diagnostics/test_session_log.cpp:34`</sub> | Une session sans message produit un texte vide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::serializeSessionLog({})` vaut `""`. |

#### Editor (176)

**`test_asset_file_operations.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **AssetFileOps.ImporteUnAssetConforme** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_asset_file_operations.cpp:54`</sub> | Importer un asset conforme le copie sans déplacer l'original. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok` est vrai.<br/>Vérifie que `std::filesystem::exists(result.path)` est vrai.<br/>Vérifie que `std::filesystem::exists(source)` est vrai. |
| **AssetFileOps.RefuseUnAssetNonConforme** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_asset_file_operations.cpp:78`</sub> | Un asset non conforme est refusé en nommant le fichier, sans rien copier. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok` est faux.<br/>Vérifie que `result.error.find("mur.png")` diffère de `std::string::npos`.<br/>Vérifie que `ops.list().empty()` est vrai. |
| **AssetFileOps.RefuseUneCollisionAlImport** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_asset_file_operations.cpp:103`</sub> | Importer un second fichier de même nom est refusé, sans écraser le premier. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `ops.import(first, hmi::AssetFamily::Background, 4, 4).ok` est vrai.<br/>Vérifie que `result.ok` est faux. |
| **AssetFileOps.RenommeEtConserveLExtension** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_asset_file_operations.cpp:130`</sub> | Renommer un asset conserve son extension et déplace le fichier. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `imported.ok` est vrai.<br/>Vérifie que `renamed.ok` est vrai.<br/>Vérifie que `renamed.path.filename().string()` vaut `"pierre.png"`.<br/>Vérifie que `std::filesystem::exists(imported.path)` est faux. |
| **AssetFileOps.RefuseUnNomInvalideAuRenommage** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_asset_file_operations.cpp:152`</sub> | Un nom invalide au renommage est refusé sans rien changer. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `imported.ok` est vrai.<br/>Vérifie que `ops.rename(imported.path, "a/b").ok` est faux.<br/>Vérifie que `std::filesystem::exists(imported.path)` est vrai. |
| **AssetFileOps.DupliqueSousUnNomUnique** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_asset_file_operations.cpp:174`</sub> | Dupliquer deux fois le même asset produit deux copies distinctes. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `imported.ok` est vrai.<br/>Vérifie que `first.ok` est vrai.<br/>Vérifie que `second.ok` est vrai.<br/>Vérifie que `first.path` diffère de `second.path`.<br/>Vérifie que `ops.list().size()` vaut `3U`. |
| **AssetFileOps.SupprimeLeFichier** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_asset_file_operations.cpp:198`</sub> | Supprimer retire effectivement l'asset du disque. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `imported.ok` est vrai.<br/>Vérifie que `ops.remove(imported.path).ok` est vrai.<br/>Vérifie que `std::filesystem::exists(imported.path)` est faux. |
| **AssetFileOps.OperationSurFichierAbsentEchoueProprement** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_asset_file_operations.cpp:219`</sub> | Renommer, dupliquer ou supprimer un fichier absent échoue proprement. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `ops.rename(absent, "autre").ok` est faux.<br/>Vérifie que `ops.duplicate(absent).ok` est faux.<br/>Vérifie que `ops.remove(absent).ok` est faux. |

**`test_asset_library.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **AssetLibraryTest.NeRetientQueLesImagesTrieesParOrdreAlphabetique** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_asset_library.cpp:44`</sub> | Le balayage ne retient que les images, triées par ordre alphabétique. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `assets` vaut `(std::vector<std::string>{"crate.png", "stone.png"})`. |
| **AssetLibraryTest.ExtensionInsensibleALaCasse** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_asset_library.cpp:66`</sub> | L'extension d'image est reconnue quelle que soit sa casse. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `assets` vaut `(std::vector<std::string>{"crate.Png", "stone.PNG"})`. |
| **AssetLibraryTest.FiltreParSousChaineInsensibleALaCasse** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_asset_library.cpp:86`</sub> | La recherche filtre par sous-chaîne insensible à la casse, liste vide si rien ne correspond. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::listAssetFiles(dir, "STONE")` vaut `(std::vector<std::string>{"stone.png", "stone_cracked.png"})`.<br/>Vérifie que `hmi::listAssetFiles(dir, "crate")` vaut `(std::vector<std::string>{"crate.png"})`.<br/>Vérifie que `hmi::listAssetFiles(dir, "inexistant").empty()` est vrai. |
| **AssetLibraryTest.DossierAbsentListeVide** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_asset_library.cpp:109`</sub> | Un dossier d'assets absent donne une liste vide, sans exception. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::listAssetFiles(std::filesystem::temp_directory_path() / "pg_asset_library_absent") .empty()` est vrai. |

**`test_asset_references.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **AssetReferencesTest.AssetCiteParUnJeuEstTrouve** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_asset_references.cpp:18`</sub> | Un asset cité par un jeu de skins est retrouvé, avec le nom du jeu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `references.size()` vaut `1U`.<br/>Vérifie que `references.front().setName` vaut `"foret"`. |
| **AssetReferencesTest.AssetCiteParPlusieursJeuxEtTypesEstTrouveAChaqueFois** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_asset_references.cpp:41`</sub> | Un asset cité par plusieurs jeux et types donne une référence par citation. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `references.size()` vaut `3U`. |
| **AssetReferencesTest.AssetNonReferenceNeDonneAucuneReference** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_asset_references.cpp:66`</sub> | Un asset non référencé ne donne aucune référence. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::findSkinCatalogReferences(catalog, "grass.png").empty()` est vrai. |
| **AssetReferencesTest.NomProcheMaisDifferentNeDeclenchePasDeFauxPositif** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_asset_references.cpp:85`</sub> | Un nom proche mais différent ne déclenche pas de fausse référence. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::findSkinCatalogReferences(catalog, "stone.png").empty()` est vrai. |
| **AssetReferencesTest.DescribeReferencesEstVideSansReference** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_asset_references.cpp:103`</sub> | Sans référence, le message descriptif est vide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::describeReferences({}).empty()` est vrai. |
| **AssetReferencesTest.DescribeReferencesNommeLesJeuxEtLesTypes** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_asset_references.cpp:117`</sub> | Le message descriptif nomme les jeux de skins et les types concernés. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `message.find("foret")` diffère de `std::string::npos`.<br/>Vérifie que `message.find("grotte")` diffère de `std::string::npos`.<br/>Vérifie que `message.find("solid")` diffère de `std::string::npos`.<br/>Vérifie que `message.find("block")` diffère de `std::string::npos`. |

**`test_decor_geometry.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **DecorGeometryTest.DecorWorldBoundsCalculeLeRectangleEnglobantEnUnitesMonde** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_geometry.cpp:25`</sub> | decorWorldBounds calcule le rectangle englobant en unites monde. | 1. Calculer le rectangle d'un decor a echelle non uniforme. | Vérifie que `bounds.position` vaut `(Vector2{3.0f, 4.0f})`.<br/>Vérifie que `bounds.size` vaut `(Vector2{4.0f, 0.5f})`. |
| **DecorGeometryTest.PoigneesTailleEcranConstante** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_geometry.cpp:46`</sub> | Les poignees gardent une taille ecran constante quel que soit le zoom. | 1. Calculer les poignees d'un decor a deux echelles ecran differentes. | Vérifie que `wide.topLeft.size.x` vaut `narrow.topLeft.size.x * 2.0f` (comparaison flottante).<br/>Vérifie que `wide.topLeft.size.y` vaut `narrow.topLeft.size.y * 2.0f` (comparaison flottante). |
| **DecorGeometryTest.PoigneesPositionneesAuxCoinsEtAuDessusDuBordSuperieur** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_geometry.cpp:67`</sub> | Les poignees sont positionnees sur les coins et au-dessus du bord superieur. | 1. Calculer les poignees d'un decor connu. | Vérifie que `layout.topLeft.contains(Vector2{10.0f, 10.0f})` est vrai.<br/>Vérifie que `layout.topRight.contains(Vector2{14.0f, 10.0f})` est vrai.<br/>Vérifie que `layout.bottomLeft.contains(Vector2{10.0f, 12.0f})` est vrai.<br/>Vérifie que `layout.bottomRight.contains(Vector2{14.0f, 12.0f})` est vrai.<br/>Vérifie que `layout.rotation.position.x + layout.rotation.size.x * 0.5f` vaut `12.0f`, à `1e-3f` près.<br/>Vérifie que `layout.rotation.position.y + layout.rotation.size.y` est strictement inférieur à `10.0f`. |
| **DecorGeometryTest.HitTestDecorHandlesIdentifieLaPoigneeTouchee** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_geometry.cpp:90`</sub> | hitTestDecorHandles identifie la poignee touchee. | 1. Tester un point sur chaque poignee, puis un point hors de toutes. | Vérifie que `hmi::hitTestDecorHandles(Vector2{0.0f, 0.0f}, layout)` vaut `hmi::DecorHandle::TopLeft`.<br/>Vérifie que `hmi::hitTestDecorHandles(Vector2{4.0f, 0.0f}, layout)` vaut `hmi::DecorHandle::TopRight`.<br/>Vérifie que `hmi::hitTestDecorHandles(Vector2{0.0f, 2.0f}, layout)` vaut `hmi::DecorHandle::BottomLeft`.<br/>Vérifie que `hmi::hitTestDecorHandles(Vector2{4.0f, 2.0f}, layout)` vaut `hmi::DecorHandle::BottomRight`.<br/>Vérifie que `hmi::hitTestDecorHandles(Vector2{2.0f, -2.4f}, layout)` vaut `hmi::DecorHandle::Rotation`.<br/>Vérifie que `hmi::hitTestDecorHandles(Vector2{2.0f, 1.0f}, layout)` vaut `hmi::DecorHandle::None`. |
| **DecorGeometryTest.DecorRotatedPointARotationNulleEstLIdentite** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_geometry.cpp:113`</sub> | decorRotatedPoint a rotation nulle est l'identite. | 1. Calculer decorRotatedPoint a rotation 0. | Vérifie que `point.x` vaut `14.0f`, à `1e-4f` près.<br/>Vérifie que `point.y` vaut `10.0f`, à `1e-4f` près. |
| **DecorGeometryTest.DecorRotatedPointA90DegresTourneDansLeSensHoraire** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_geometry.cpp:132`</sub> | decorRotatedPoint a 90 degres tourne dans le sens horaire. | 1. Calculer decorRotatedPoint pour le coin superieur droit, tourne de 90 degres. | Vérifie que `point.x` vaut `4.0f`, à `1e-3f` près.<br/>Vérifie que `point.y` vaut `4.0f`, à `1e-3f` près. |
| **DecorGeometryTest.PoigneesDeCoinTournentAvecLeDecor** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_geometry.cpp:154`</sub> | Les poignees de decorHandleLayout tournent avec le decor. | 1. Calculer les poignees d'un decor carre a rotation nulle, puis a 90 degres. | Vérifie que `rotatedTopLeftCenter.x` vaut `uprightTopRightCenter.x`, à `1e-3f` près.<br/>Vérifie que `rotatedTopLeftCenter.y` vaut `uprightTopRightCenter.y`, à `1e-3f` près. |

**`test_decor_gesture.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **DecorGestureTest.DesignateDecorAtChoisitLePlusAuDessusEnCasDeSuperposition** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_gesture.cpp:39`</sub> | Un clic sur des décors superposés désigne le plus au-dessus. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hit.has_value()` est vrai.<br/>Vérifie que `hit->index` vaut `1u`.<br/>Vérifie que `hit->handle` vaut `DecorHandle::Body`. |
| **DecorGestureTest.DesignateDecorAtDansLeVideRenvoieNullopt** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_gesture.cpp:63`</sub> | Un clic dans le vide ne désigne aucun décor. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hit.has_value()` est faux. |
| **DecorGestureTest.DesignateDecorAtPrivilegieLesPoigneesDuDecorSelectionne** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_gesture.cpp:83`</sub> | Les poignées du décor sélectionné priment sur le corps d'un autre décor. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hit.has_value()` est vrai.<br/>Vérifie que `hit->index` vaut `0u`.<br/>Vérifie que `hit->handle` vaut `DecorHandle::TopLeft`. |
| **DecorGestureTest.BeginSelectionneImmediatementMemeSansGlisser** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_gesture.cpp:114`</sub> | L'appui sélectionne immédiatement, même sans glisser. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `state.selectedIndex.has_value()` est vrai.<br/>Vérifie que `*state.selectedIndex` vaut `2u`. |
| **DecorGestureTest.UnDeplacementSousLeSeuilNeProduitAucuneAction** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_gesture.cpp:137`</sub> | Un déplacement sous le seuil ne produit aucune action. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `preview.kind` vaut `DecorGestureActionKind::None`.<br/>Vérifie que `committed.kind` vaut `DecorGestureActionKind::None`. |
| **DecorGestureTest.UnDeplacementAuDelaDuSeuilProduitUneActionDeplacer** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_gesture.cpp:164`</sub> | Un déplacement au-delà du seuil produit un aperçu de déplacement suivant le curseur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `preview.kind` vaut `DecorGestureActionKind::Move`.<br/>Vérifie que `preview.position.x` vaut `1.0f + hmi::DECOR_DRAG_THRESHOLD * 5.0f` (comparaison flottante).<br/>Vérifie que `preview.position.y` vaut `1.0f` (comparaison flottante). |
| **DecorGestureTest.DeplacerProduitLaPositionFinaleAuRelachement** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_gesture.cpp:195`</sub> | Le relâchement produit la position finale, décalée depuis la position d'origine. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.kind` vaut `DecorGestureActionKind::Move`.<br/>Vérifie que `action.index` vaut `0u`.<br/>Vérifie que `action.position.x` vaut `4.0f` (comparaison flottante).<br/>Vérifie que `action.position.y` vaut `2.5f` (comparaison flottante). |
| **DecorGestureTest.DeplacerAvecAimantationArrondiALaGrilleEntiere** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_gesture.cpp:223`</sub> | Avec aimantation, la position finale est arrondie à la case entière. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.kind` vaut `DecorGestureActionKind::Move`.<br/>Vérifie que `action.position.x` vaut `4.0f` (comparaison flottante).<br/>Vérifie que `action.position.y` vaut `1.0f` (comparaison flottante). |
| **DecorGestureTest.DeplacerSansAimantationConserveLaPositionExacte** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_gesture.cpp:247`</sub> | Sans aimantation, la position exacte est conservée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.position.x` vaut `4.4f` (comparaison flottante).<br/>Vérifie que `action.position.y` vaut `1.4f` (comparaison flottante). |
| **DecorGestureTest.RedimensionnerDepuisLeCoinBasDroitEtireDepuisLeCoinHautGaucheFixe** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_gesture.cpp:274`</sub> | Tirer le coin bas-droit étire depuis le coin haut-gauche, resté fixe. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.kind` vaut `DecorGestureActionKind::Resize`.<br/>Vérifie que `action.position.x` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `action.position.y` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `action.scale.x` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `action.scale.y` vaut `3.0f` (comparaison flottante). |
| **DecorGestureTest.RedimensionnerDepuisLeCoinHautGaucheDeplaceLAncreOpposee** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_gesture.cpp:301`</sub> | Tirer le coin haut-gauche déplace la position et l'échelle, ancre au coin bas-droit. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.kind` vaut `DecorGestureActionKind::Resize`.<br/>Vérifie que `action.position.x` vaut `-4.0f` (comparaison flottante).<br/>Vérifie que `action.position.y` vaut `-2.0f` (comparaison flottante).<br/>Vérifie que `action.scale.x` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `action.scale.y` vaut `2.0f` (comparaison flottante). |
| **DecorGestureTest.RedimensionnerNeDescendJamaisSousUneTailleMinimale** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_gesture.cpp:330`</sub> | Le redimensionnement ne descend jamais sous une taille minimale strictement positive. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.kind` vaut `DecorGestureActionKind::Resize`.<br/>Vérifie que `action.scale.x` est strictement supérieur à `0.0f`.<br/>Vérifie que `action.scale.y` est strictement supérieur à `0.0f`. |
| **DecorGestureTest.PivoterVersLaDroiteProduitUnQuartDeTour** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_gesture.cpp:361`</sub> | Amener le curseur à droite du centre produit un quart de tour. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.kind` vaut `DecorGestureActionKind::Rotate`.<br/>Vérifie que `action.rotation` vaut `TWO_PI * 0.25f`, à `1e-4f` près. |
| **DecorGestureTest.PivoterNormaliseDansZeroDeuxPi** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_gesture.cpp:385`</sub> | L'angle produit par la rotation reste dans [0, 2π). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `action.rotation` est supérieur ou égal à `0.0f`.<br/>Vérifie que `action.rotation` est strictement inférieur à `TWO_PI`. |
| **DecorGestureTest.AbandonNeProduitAucuneAction** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_gesture.cpp:412`</sub> | Abandonner un geste ne produit aucune action et conserve la sélection. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `state.phase` vaut `hmi::DecorGesturePhase::Idle`.<br/>Vérifie que `state.selectedIndex.has_value()` est vrai.<br/>Vérifie que `*state.selectedIndex` vaut `0u`. |

**`test_decor_list_model.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **DecorListModelTest.GroupeLesLignesParCouche** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_list_model.cpp:31`</sub> | buildDecorListRows groupe les lignes par couche. | 1. Construire des decors sur les trois couches, dans un ordre entremele. | Vérifie que `rows.size()` vaut `4u`.<br/>Vérifie que `rows[0].assetName` vaut `"b.png"`.<br/>Vérifie que `rows[0].layer` vaut `DecorLayer::Background`.<br/>Vérifie que `rows[1].assetName` vaut `"d.png"`.<br/>Vérifie que `rows[1].layer` vaut `DecorLayer::Background`.<br/>Vérifie que `rows[2].assetName` vaut `"c.png"`.<br/>Vérifie que `rows[2].layer` vaut `DecorLayer::Decor`.<br/>Vérifie que `rows[3].assetName` vaut `"a.png"`.<br/>Vérifie que `rows[3].layer` vaut `DecorLayer::Foreground`. |
| **DecorListModelTest.PreserveLOrdreDeSuperpositionALInterieurDUneCouche** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_list_model.cpp:61`</sub> | L'ordre de superposition est preserve a l'interieur d'une couche. | 1. Construire trois decors de la meme couche. | Vérifie que `rows.size()` vaut `3u`.<br/>Vérifie que `rows[0].assetName` vaut `"first.png"`.<br/>Vérifie que `rows[1].assetName` vaut `"second.png"`.<br/>Vérifie que `rows[2].assetName` vaut `"third.png"`. |
| **DecorListModelTest.ChaqueLigneConserveSonRangDOrigine** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_list_model.cpp:85`</sub> | Chaque ligne porte le rang d'origine du decor. | 1. Construire des decors dont le tri d'affichage change l'ordre. | Vérifie que `rows.size()` vaut `2u`.<br/>Vérifie que `rows[0].assetName` vaut `"background.png"`.<br/>Vérifie que `rows[0].index` vaut `1u`.<br/>Vérifie que `rows[1].assetName` vaut `"foreground.png"`.<br/>Vérifie que `rows[1].index` vaut `0u`. |
| **DecorListModelTest.AssetIntrouvableEstMarqueManquant** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_list_model.cpp:109`</sub> | Un asset introuvable est marque manquant. | 1. Construire un decor dont l'asset n'est pas dans la liste des assets connus. | Vérifie que `rows.size()` vaut `1u`.<br/>Vérifie que `rows[0].assetMissing` est vrai. |
| **DecorListModelTest.AssetPresentNEstPasMarqueManquant** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_list_model.cpp:128`</sub> | Un asset present n'est pas marque manquant. | 1. Construire un decor dont l'asset est dans la liste des assets connus. | Vérifie que `rows.size()` vaut `1u`.<br/>Vérifie que `rows[0].assetMissing` est faux. |
| **DecorListModelTest.SansDecorLaListeEstVide** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_decor_list_model.cpp:147`</sub> | Sans decor, la liste est vide. | 1. Appeler buildDecorListRows sur un vecteur vide. | Vérifie que `rows.empty()` est vrai. |

**`test_editor_status.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **EditorStatusTest.AucunNiveauOuvertNAfficheRien** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_editor_status.cpp:76`</sub> | Aucun niveau ouvert n'affiche aucune zone ni aide. | 1. Construire un contexte sans niveau.<br/>2. Calculer les lignes. | Vérifie que `lines.permanent.size()` vaut `6u`.<br/>Vérifie que `zone` vaut `""`.<br/>Vérifie que `lines.help` vaut `""`. |
| **EditorStatusTest.AucuneCaseSurvoleeLaisseLaZoneVide** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_editor_status.cpp:97`</sub> | Aucune case survolee laisse la zone de coordonnees vide. | 1. Construire un niveau sans case survolee.<br/>2. Calculer les lignes. | Vérifie que `lines.permanent[0]` vaut `"Niveau : Salle des epreuves"`.<br/>Vérifie que `lines.permanent[3]` vaut `""`. |
| **EditorStatusTest.IndicateurDeModificationSuitLEtatDirty** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_editor_status.cpp:116`</sub> | L'indicateur de modification suit l'etat "dirty" du niveau. | 1. Calculer les lignes avec dirty=true, puis dirty=false.<br/>2. Comparer la zone d'indicateur. | Vérifie que `hmi::editorStatusLines(dirtyContext, localization).permanent[1]` vaut `"Modifie"`.<br/>Vérifie que `hmi::editorStatusLines(cleanContext, localization).permanent[1]` vaut `""`. |
| **EditorStatusTest.AideChangeAvecLOutilActif** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_editor_status.cpp:140`</sub> | L'aide contextuelle change avec l'outil actif. | 1. Calculer les lignes avec l'outil Pinceau, puis Lien.<br/>2. Comparer l'aide. | Vérifie que `hmi::editorStatusLines(paintContext, localization).help` vaut `"Aide pinceau"`.<br/>Vérifie que `hmi::editorStatusLines(linkContext, localization).help` vaut `"Aide lien"`. |
| **EditorStatusTest.MemeContexteProduitLaMemeAide** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_editor_status.cpp:165`</sub> | La decision est deterministe : meme contexte, meme resultat. | 1. Calculer deux fois les lignes pour le meme contexte.<br/>2. Comparer. | Vérifie que `first.help` vaut `second.help`.<br/>Vérifie que `first.permanent` vaut `second.permanent`. |
| **EditorStatusTest.ContextePixelEditProduitLesZonesEtLAideAttendues** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_editor_status.cpp:188`</sub> | Le contexte d'atelier pixel art produit les zones et l'aide attendues. | 1. Construire un contexte d'edition d'asset avec un pixel survole.<br/>2. Calculer les lignes. | Vérifie que `lines.permanent[0]` vaut `"Asset : mur.png"`.<br/>Vérifie que `lines.permanent[1]` vaut `""`.<br/>Vérifie que `lines.permanent[2]` vaut `"Pinceau"`.<br/>Vérifie que `lines.permanent[3]` vaut `"(3, 5)"`.<br/>Vérifie que `lines.permanent[4]` vaut `"Zoom : 800%"`.<br/>Vérifie que `lines.permanent[5]` vaut `"Couleur : #ff0000ff"`.<br/>Vérifie que `lines.help` vaut `"Aide pinceau pixel"`. |
| **EditorStatusTest.ModeContraintFigureDansLaZoneCouleur** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_editor_status.cpp:215`</sub> | Le mode contraint figure dans la zone couleur. | 1. Calculer les lignes avec le mode contraint actif, puis inactif.<br/>2. Comparer la zone couleur (index 5). | Vérifie que `hmi::editorStatusLines(constrainedContext, localization).permanent[5]` vaut `"Couleur : #ff0000ff (contrainte)"`.<br/>Vérifie que `hmi::editorStatusLines(freeContext, localization).permanent[5]` vaut `"Couleur : #ff0000ff"`. |
| **EditorStatusTest.AucunAssetOuvertLaisseLaZoneVide** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_editor_status.cpp:242`</sub> | Aucun asset ouvert laisse la zone d'asset vide. | 1. Construire un contexte d'atelier sans nom d'asset.<br/>2. Calculer les lignes. | Vérifie que `lines.permanent[0]` vaut `""`.<br/>Vérifie que `lines.permanent[2]` vaut `"Pinceau"`. |
| **EditorStatusTest.AideDeLAtelierChangeAvecLOutilActif** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_editor_status.cpp:263`</sub> | L'aide de l'atelier change avec l'outil de canevas actif. | 1. Calculer les lignes avec l'outil Gomme, puis Pipette.<br/>2. Comparer l'aide. | Vérifie que `hmi::editorStatusLines(eraserContext, localization).help` vaut `"Aide gomme"`.<br/>Vérifie que `hmi::editorStatusLines(eyedropperContext, localization).help` vaut `"Aide pipette"`. |
| **EditorStatusTest.ClesDeTraductionExistentDansLesDeuxCatalogues** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_editor_status.cpp:289`</sub> | Les cles de traduction de la barre d'etat existent dans les deux catalogues. | 1. Charger fr.lang puis en.lang depuis les catalogues livres.<br/>2. Resoudre chaque cle utilisee. | Vérifie que `localization.loadDefaultLanguage(language)` est vrai.<br/>Vérifie que `localization.text(key)` diffère de `key`. |

**`test_level_file_operations.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LevelFileOps.CreeUnNiveauValide** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_file_operations.cpp:36`</sub> | Créer un niveau écrit un fichier valide, immédiatement listé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok` est vrai.<br/>Vérifie que `std::filesystem::exists(result.path)` est vrai.<br/>Vérifie que `ops.list().size()` vaut `1U`. |
| **LevelFileOps.FichierDeSequenceExcluDeLaListe** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_file_operations.cpp:55`</sub> | Un fichier de séquence n'apparaît jamais dans la liste des niveaux. | 1. Créer un niveau valide, puis écrire à côté un fichier `sequence-demo.json` quelconque.<br/>2. Lister le dossier. | Vérifie que `ops.create("MonNiveau", 10, 6).ok` est vrai.<br/>Vérifie que `listed.size()` vaut `1U`.<br/>Vérifie que `listed.front().filename().string()` vaut `"MonNiveau.json"`. |
| **LevelFileOps.RefuseNomInvalideEtCollision** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_file_operations.cpp:79`</sub> | Un nom invalide ou déjà pris est refusé, sans écraser le niveau existant. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `ops.create("a/b", 10, 6).ok` est faux.<br/>Vérifie que `ops.create("Niveau", 10, 6).ok` est vrai.<br/>Vérifie que `ops.create("Niveau", 10, 6).ok` est faux. |
| **LevelFileOps.RenommeEtDeplaceLeFichier** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_file_operations.cpp:96`</sub> | Renommer déplace le fichier : l'ancien chemin disparaît, le nouveau existe. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `created.ok` est vrai.<br/>Vérifie que `renamed.ok` est vrai.<br/>Vérifie que `std::filesystem::exists(created.path)` est faux.<br/>Vérifie que `std::filesystem::exists(renamed.path)` est vrai. |
| **LevelFileOps.DupliqueSousUnNomUnique** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_file_operations.cpp:116`</sub> | Dupliquer deux fois le même niveau produit deux copies distinctes. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `base.ok` est vrai.<br/>Vérifie que `first.ok` est vrai.<br/>Vérifie que `second.ok` est vrai.<br/>Vérifie que `first.path` diffère de `second.path`.<br/>Vérifie que `ops.list().size()` vaut `3U`. |
| **LevelFileOps.SupprimeLeFichier** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_file_operations.cpp:138`</sub> | Supprimer retire effectivement le fichier du disque. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `created.ok` est vrai.<br/>Vérifie que `ops.remove(created.path).ok` est vrai.<br/>Vérifie que `std::filesystem::exists(created.path)` est faux. |

**`test_level_name_validation.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LevelNameValidationTest.NomSimpleValide** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_name_validation.cpp:12`</sub> | Un nom simple, sans caractère interdit, est valide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::isValidLevelName("Niveau 1")` est vrai.<br/>Vérifie que `hmi::isValidLevelName("Foret enchantee")` est vrai. |
| **LevelNameValidationTest.NomVideOuEspacesInvalide** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_name_validation.cpp:27`</sub> | Un nom vide ou composé uniquement d'espaces est invalide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::isValidLevelName("")` est faux.<br/>Vérifie que `hmi::isValidLevelName(" ")` est faux. |
| **LevelNameValidationTest.CaractereInterditInvalide** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_name_validation.cpp:42`</sub> | Un nom contenant un caractère interdit par le système de fichiers Windows est invalide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::isValidLevelName("Niveau:1")` est faux.<br/>Vérifie que `hmi::isValidLevelName("Niveau/1")` est faux.<br/>Vérifie que `hmi::isValidLevelName("Niveau\\\\1")` est faux.<br/>Vérifie que `hmi::isValidLevelName("Niveau*1")` est faux.<br/>Vérifie que `hmi::isValidLevelName("Niveau?1")` est faux.<br/>Vérifie que `hmi::isValidLevelName("Niveau\\"1")` est faux.<br/>Vérifie que `hmi::isValidLevelName("Niveau<1")` est faux.<br/>Vérifie que `hmi::isValidLevelName("Niveau>1")` est faux.<br/>Vérifie que `hmi::isValidLevelName("Niveau\|1")` est faux. |
| **LevelNameValidationTest.NomAccentueValide** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_name_validation.cpp:67`</sub> | Un nom accentué (Unicode) reste valide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::isValidLevelName("Fort\\xC3\\xA9resse")` est vrai. |
| **LevelNameValidationTest.TrimRetireLesEspacesDeBord** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_level_name_validation.cpp:81`</sub> | trimLevelName retire les espaces de bord sans toucher au contenu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::trimLevelName(" Niveau 1 ")` vaut `"Niveau 1"`.<br/>Vérifie que `hmi::trimLevelName("Niveau 1")` vaut `"Niveau 1"`.<br/>Vérifie que `hmi::trimLevelName(" ")` vaut `""`. |

**`test_link_geometry.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LinkGeometry.IsTriggerTileReconnaitDeclencheurs** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_geometry.cpp:19`</sub> | Seuls l'interrupteur et la plaque de pression sont reconnus comme déclencheurs. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::isTriggerTile(TileType::Switch)` est vrai.<br/>Vérifie que `hmi::isTriggerTile(TileType::PressurePlate)` est vrai.<br/>Vérifie que `hmi::isTriggerTile(TileType::Door)` est faux.<br/>Vérifie que `hmi::isTriggerTile(TileType::DangerSwitched)` est faux.<br/>Vérifie que `hmi::isTriggerTile(TileType::Solid)` est faux. |
| **LinkGeometry.IsLinkTargetTileReconnaitCibles** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_geometry.cpp:39`</sub> | Seuls la porte et le danger commutable sont reconnus comme cibles de liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::isLinkTargetTile(TileType::Door)` est vrai.<br/>Vérifie que `hmi::isLinkTargetTile(TileType::DangerSwitched)` est vrai.<br/>Vérifie que `hmi::isLinkTargetTile(TileType::Switch)` est faux.<br/>Vérifie que `hmi::isLinkTargetTile(TileType::PressurePlate)` est faux. |
| **LinkGeometry.LinkSegmentRelieLesCentresDeCase** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_geometry.cpp:58`</sub> | Le trait de liaison relie les centres des deux cases. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `segment.a.x` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `segment.a.y` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `segment.b.x` vaut `3.5f` (comparaison flottante).<br/>Vérifie que `segment.b.y` vaut `0.5f` (comparaison flottante). |
| **LinkGeometry.LinkSegmentVertical** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_geometry.cpp:77`</sub> | Un lien vertical ne fait varier que l'ordonnée, du déclencheur vers la cible. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `segment.a.x` vaut `segment.b.x` (comparaison flottante).<br/>Vérifie que `segment.a.y` est strictement inférieur à `segment.b.y`. |
| **LinkGeometry.LinkSegmentDiagonalSansDecalage** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_geometry.cpp:94`</sub> | Un lien diagonal seul relie les centres sans aucun décalage. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `segment.a.x` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `segment.a.y` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `segment.b.x` vaut `2.5f` (comparaison flottante).<br/>Vérifie que `segment.b.y` vaut `2.5f` (comparaison flottante). |
| **LinkGeometry.LinkSegmentDecalageAntiSuperpositionBaseCommune** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_geometry.cpp:114`</sub> | Des liens frères gardent une base commune et écartent symétriquement leur extrémité cible. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `first.a.x` vaut `second.a.x` (comparaison flottante).<br/>Vérifie que `first.a.y` vaut `second.a.y` (comparaison flottante).<br/>Vérifie que `second.a.x` vaut `third.a.x` (comparaison flottante).<br/>Vérifie que `second.a.y` vaut `third.a.y` (comparaison flottante).<br/>Vérifie que `second.b.y` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `first.b.y` diffère de `second.b.y`.<br/>Vérifie que `third.b.y` diffère de `second.b.y`.<br/>Vérifie que `second.b.y - first.b.y` vaut `third.b.y - second.b.y` (comparaison flottante).<br/>Vérifie que `hmi::linkSegment(trigger, target, 0, 3).b.y` vaut `first.b.y`. |
| **LinkGeometry.ArrowHeadOrienteeVersLaCible** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_geometry.cpp:151`</sub> | La pointe de flèche a ses ailes en retrait et symétriques par rapport à l'axe. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `head.left.x` est strictement inférieur à `4.0f`.<br/>Vérifie que `head.right.x` est strictement inférieur à `4.0f`.<br/>Vérifie que `head.left.y` vaut `-head.right.y` (comparaison flottante).<br/>Vérifie que `head.left.y` diffère de `0.0f`. |
| **LinkGeometry.ArrowHeadSegmentDegenere** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_geometry.cpp:174`</sub> | Un segment dégénéré replie la pointe sur le point, sans division par zéro. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `head.left.x` vaut `point.x` (comparaison flottante).<br/>Vérifie que `head.left.y` vaut `point.y` (comparaison flottante).<br/>Vérifie que `head.right.x` vaut `point.x` (comparaison flottante).<br/>Vérifie que `head.right.y` vaut `point.y` (comparaison flottante). |
| **LinkGeometry.BuildLinkRowsOrdreEtContenu** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_geometry.cpp:194`</sub> | La liste des liens est ordonnée (mécanismes puis dangers) et fidèle au brouillon. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `rows.size()` vaut `2u`.<br/>Vérifie que `rows[0].kind` vaut `hmi::LinkKind::Mechanism`.<br/>Vérifie que `rows[0].trigger` vaut `(GridPosition{0, 0})`.<br/>Vérifie que `rows[0].target` vaut `(GridPosition{1, 0})`.<br/>Vérifie que `rows[1].kind` vaut `hmi::LinkKind::DangerLink`.<br/>Vérifie que `rows[1].trigger` vaut `(GridPosition{2, 0})`.<br/>Vérifie que `rows[1].target` vaut `(GridPosition{3, 0})`. |
| **LinkGeometry.BuildLinkRowsVideSansLiaison** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_geometry.cpp:224`</sub> | Un brouillon sans liaison donne une liste vide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::buildLinkRows(draft).empty()` est vrai. |

**`test_link_gesture.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LinkGesture.ClicHorsCategorieIgnore** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_gesture.cpp:20`</sub> | Un clic hors catégorie est ignoré quand aucune attente n'est en cours. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `decision.action` vaut `LinkGestureAction::Ignore`. |
| **LinkGesture.PremierClicSurDeclencheurPoseLAttente** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_gesture.cpp:36`</sub> | Sans attente, un clic sur un déclencheur pose l'attente. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `decision.action` vaut `LinkGestureAction::SetPending`.<br/>Vérifie que `decision.cell` vaut `(GridPosition{2, 3})`. |
| **LinkGesture.PremierClicSurCiblePoseLAttente** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_gesture.cpp:53`</sub> | Sans attente, un clic sur une cible pose aussi l'attente. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `decision.action` vaut `LinkGestureAction::SetPending`.<br/>Vérifie que `decision.cell` vaut `(GridPosition{4, 0})`. |
| **LinkGesture.DeuxDeclencheursRemplacentLAttente** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_gesture.cpp:70`</sub> | Deux déclencheurs successifs remplacent l'attente au lieu de se lier. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `decision.action` vaut `LinkGestureAction::ReplacePending`.<br/>Vérifie que `decision.cell` vaut `(GridPosition{1, 0})`. |
| **LinkGesture.DeuxCiblesRemplacentLAttente** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_gesture.cpp:88`</sub> | Deux cibles successives remplacent l'attente au lieu de se lier. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `decision.action` vaut `LinkGestureAction::ReplacePending`.<br/>Vérifie que `decision.cell` vaut `(GridPosition{1, 0})`. |
| **LinkGesture.DeclencheurPuisCibleNonLieeCreeLaLiaison** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_gesture.cpp:106`</sub> | Déclencheur en attente puis cible non liée crée la liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `decision.action` vaut `LinkGestureAction::Link`.<br/>Vérifie que `decision.switchPosition` vaut `(GridPosition{0, 0})`.<br/>Vérifie que `decision.targetPosition` vaut `(GridPosition{3, 3})`. |
| **LinkGesture.CiblePuisDeclencheurDejaLieSupprimeLaLiaison** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_gesture.cpp:125`</sub> | Le même geste sur une paire déjà liée supprime la liaison. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `decision.action` vaut `LinkGestureAction::Unlink`.<br/>Vérifie que `decision.switchPosition` vaut `(GridPosition{0, 0})`.<br/>Vérifie que `decision.targetPosition` vaut `(GridPosition{3, 3})`. |
| **LinkGesture.AttentePerimeeEstIgnoreeEtRemplacee** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_gesture.cpp:145`</sub> | Une attente périmée est ignorée et remplacée par le clic courant. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `decision.action` vaut `LinkGestureAction::SetPending`.<br/>Vérifie que `decision.cell` vaut `(GridPosition{3, 3})`. |
| **LinkGesture.ClicHorsCategorieAvecAttenteIgnore** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_link_gesture.cpp:162`</sub> | Un clic hors catégorie est ignoré sans perdre l'attente en cours. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `decision.action` vaut `LinkGestureAction::Ignore`. |

**`test_mechanism_animation_assignments.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **MechanismAnimationAssignmentsTest.UneLigneParFamille** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_mechanism_animation_assignments.cpp:60`</sub> | La section Animations propose exactement les six familles de mecanismes a etat. | 1. Construire les lignes pour un catalogue vide. | Vérifie que `rows.size()` vaut `std::size(hmi::MECHANISM_ANIMATION_TYPES)`.<br/>Vérifie que `rows[index].type` vaut `hmi::MECHANISM_ANIMATION_TYPES[index]`.<br/>Vérifie que `rows[index].asset.empty()` est vrai.<br/>Vérifie que `rows[index].missingClips.empty()` est vrai. |
| **MechanismAnimationAssignmentsTest.AssetSansAnimationListeToutManquant** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_mechanism_animation_assignments.cpp:85`</sub> | Un asset assigne sans .anim.json liste tous les clips attendus comme manquants. | 1. Assigner un asset a Door, sans creer de fichier d'animation. | Vérifie que `door` diffère de `nullptr`.<br/>Vérifie que `door->asset` vaut `"door.png"`.<br/>Vérifie que `door->missingClips` vaut `hmi::mechanismExpectedClips(core::TileType::Door)`. |
| **MechanismAnimationAssignmentsTest.AssetCompletNeManqueRien** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_mechanism_animation_assignments.cpp:109`</sub> | Un asset fournissant tous les clips attendus ne signale rien de manquant. | 1. Assigner un asset a Switch et deposer un .anim.json avec ses deux clips attendus. | Vérifie que `row` diffère de `nullptr`.<br/>Vérifie que `row->missingClips.empty()` est vrai. |
| **MechanismAnimationAssignmentsTest.AssetPartielSignaleLeReste** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_mechanism_animation_assignments.cpp:135`</sub> | Un asset partiel signale uniquement les clips absents. | 1. Assigner un asset a Door dont le .anim.json ne fournit que « closed » et « open ». | Vérifie que `door` diffère de `nullptr`.<br/>Vérifie que `door->missingClips` vaut `(std::vector<std::string>{hmi::MECHANISM_CLIP_DOOR_OPENING, hmi::MECHANISM_CLIP_DOOR_CLOSING})`. |

**`test_palette_appearance.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **PaletteAppearanceTest.ModePhysiqueMontreLaCouleurPlate** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_palette_appearance.cpp:39`</sub> | En mode Physique, la palette montre la couleur plate du type. | 1. Demander la vignette de Solid en mode Physique, alors qu'un skin lui est assigne. | Vérifie que `thumbnail.source` vaut `hmi::PaletteThumbnailSource::Atlas`.<br/>Vérifie que `thumbnail.region.x` vaut `physical.x`.<br/>Vérifie que `thumbnail.region.y` vaut `physical.y`.<br/>Vérifie que `thumbnail.masked` est faux. |
| **PaletteAppearanceTest.ModeTextureMontreLeSkin** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_palette_appearance.cpp:61`</sub> | En mode Texture, la palette montre le fichier reellement assigne. | 1. Demander la vignette de Block, assigne a un skin en mode image unique. | Vérifie que `thumbnail.source` vaut `hmi::PaletteThumbnailSource::Skin`.<br/>Vérifie que `thumbnail.asset` vaut `"crate.png"`.<br/>Vérifie que `thumbnail.region.width` vaut `TILE`.<br/>Vérifie que `thumbnail.masked` est faux. |
| **PaletteAppearanceTest.SkinARaccordsMontreLaCaseRepresentative** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_palette_appearance.cpp:84`</sub> | Un skin a raccords montre l'interieur plein de sa planche. | 1. Demander la vignette de Solid, assigne a une planche a raccords. | Vérifie que `thumbnail.region.x` vaut `cell.column * TILE`.<br/>Vérifie que `thumbnail.region.y` vaut `cell.row * TILE`.<br/>Vérifie que `thumbnail.region.width` vaut `TILE`. |
| **PaletteAppearanceTest.PenteSkinneeEstDetouree** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_palette_appearance.cpp:108`</sub> | Une pente skinnee est signalee comme devant etre detouree. | 1. Demander la vignette d'une pente montante assignee a un skin. | Vérifie que `slope.masked` est vrai.<br/>Vérifie que `block.masked` est faux. |
| **PaletteAppearanceTest.TypeNonSkinneMontreLeDamier** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_palette_appearance.cpp:132`</sub> | Un type non skinne montre le damier dans la palette, comme dans le niveau. | 1. Demander la vignette d'un type absent du catalogue, en mode Texture.<br/> 2. Demander une vignette sans catalogue du tout. | Vérifie que `unassigned.source` vaut `hmi::PaletteThumbnailSource::MissingTexture`.<br/>Vérifie que `unassigned.region.width` vaut `hmi::MISSING_TEXTURE_SIZE`.<br/>Vérifie que `noCatalog.source` vaut `hmi::PaletteThumbnailSource::MissingTexture`. |
| **PaletteAppearanceTest.PaletteEtCanevasDecidentPareil** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_palette_appearance.cpp:158`</sub> | La palette et le canevas resolvent la meme source pour chaque type. | 1. Pour plusieurs types et les deux modes, comparer la source choisie par la palette a celle choisie par la resolution du canevas. | Vérifie que `sameAtlas \|\| sameSkin \|\| sameMissing` est vrai. |

**`test_panel_focus.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **PanelFocusTest.OutilLienMetEnAvantLePanneauLiens** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_panel_focus.cpp:13`</sub> | L'outil Lien met en avant le panneau Liens. | 1. Interroger la table pour l'outil Lien.<br/>2. Verifier le panneau retourne. | Vérifie que `hmi::panelForTool(hmi::EditorTool::Link)` vaut `hmi::PanelId::Links`. |
| **PanelFocusTest.OutilTextureMetEnAvantLePanneauTextures** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_panel_focus.cpp:26`</sub> | L'outil Texture met en avant le panneau Textures. | 1. Interroger la table pour l'outil Texture par instance.<br/>2. Verifier le panneau retourne. | Vérifie que `hmi::panelForTool(hmi::EditorTool::TextureAssign)` vaut `hmi::PanelId::Textures`. |
| **PanelFocusTest.OutilsSansPanneauDedieNeMettentRienEnAvant** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_panel_focus.cpp:41`</sub> | Les outils sans panneau dedie ne mettent rien en avant. | 1. Interroger la table pour chaque outil sans panneau dedie.<br/>2. Verifier l'absence de resultat. | Vérifie que `hmi::panelForTool(tool)` vaut `std::nullopt`. |
| **PanelFocusTest.AucunDoublonDOutilDansLaTable** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_panel_focus.cpp:58`</sub> | La table ne contient aucun doublon d'outil. | 1. Parcourir toutes les paires d'entrees de la table.<br/>2. Comparer leurs outils. | Vérifie que `catalog[i].tool` diffère de `catalog[j].tool`. |
| **PanelFocusTest.OutilsDeCanevasMettentEnAvantLePanneauDuCanevas** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_panel_focus.cpp:77`</sub> | Chaque outil de canevas pixel art met en avant le panneau du canevas. | 1. Interroger la table pour chacun des quatre outils de canevas.<br/>2. Verifier le panneau retourne. | Vérifie que `hmi::panelForPixelTool(tool)` vaut `hmi::PanelId::PixelCanvas`. |
| **PanelFocusTest.AucunDoublonDOutilDansLaTableDeCanevas** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_panel_focus.cpp:96`</sub> | La table des outils de canevas ne contient aucun doublon. | 1. Parcourir toutes les paires d'entrees de la table des outils de canevas.<br/> 2. Comparer leurs outils. | Vérifie que `catalog[i].tool` diffère de `catalog[j].tool`. |

**`test_pixel_asset_io.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **PixelAssetIOTest.ChaqueTailleProposeeEstConforme** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_asset_io.cpp:15`</sub> | Chaque taille proposee est conforme au contrat de sa famille. | 1. Pour chaque famille, produire les tailles proposees.<br/>2. Valider chacune contre le contrat de la meme famille. | Vérifie que `validation.valid` est vrai. |
| **PixelAssetIOTest.FamilleADimensionsLibresNeProposeAucuneTaille** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_asset_io.cpp:42`</sub> | Une famille a dimensions libres ne propose aucune taille. | 1. Produire les tailles proposees pour Fond, Decor et Police. | Vérifie que `hmi::validAssetSizes(hmi::AssetFamily::Background).empty()` est vrai.<br/>Vérifie que `hmi::validAssetSizes(hmi::AssetFamily::Decor).empty()` est vrai.<br/>Vérifie que `hmi::validAssetSizes(hmi::AssetFamily::Font).empty()` est vrai. |
| **PixelAssetIOTest.TileSkinProposeDesLargeursMultiplesDeCase** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_asset_io.cpp:58`</sub> | Un skin de tuile propose des largeurs multiples de case, hauteur fixe. | 1. Produire les tailles proposees pour TileSkin.<br/>2. Verifier la hauteur et la largeur de chaque taille. | Vérifie que `sizes.empty()` est faux.<br/>Vérifie que `height` vaut `hmi::TextureAtlas::TILE_SIZE`.<br/>Vérifie que `width % hmi::TextureAtlas::TILE_SIZE` vaut `0`. |
| **PixelAssetIOTest.AutotileSheetProposePlusieursTailles** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_asset_io.cpp:79`</sub> | Les familles a taille non contrainte proposent plusieurs options. | 1. Produire les tailles proposees pour AutotileSheet. | Vérifie que `sizes.size()` est strictement supérieur à `1U`. |

**`test_pixel_autotile_preview.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **PixelAutotilePreviewTest.SeuleUneImage4x4EstUnePlancheARaccords** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_autotile_preview.cpp:31`</sub> | Seule une image de dimensions exactement 4x4 cases est une planche a raccords. | 1. Tester une image 4x4 cases.<br/>2. Tester des images plus petites, plus grandes, et non carrees. | Vérifie que `hmi::isBitmask16Candidate(sheetImage(4, 4, 0), TILE)` est vrai.<br/>Vérifie que `hmi::isBitmask16Candidate(sheetImage(3, 4, 0), TILE)` est faux.<br/>Vérifie que `hmi::isBitmask16Candidate(sheetImage(4, 3, 0), TILE)` est faux.<br/>Vérifie que `hmi::isBitmask16Candidate(sheetImage(8, 8, 0), TILE)` est faux.<br/>Vérifie que `hmi::isBitmask16Candidate(sheetImage(1, 1, 0), TILE)` est faux. |
| **PixelAutotilePreviewTest.BitmaskCellAtPixelResoutLaCaseAttendue** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_autotile_preview.cpp:51`</sub> | bitmaskCellAtPixel resout la case attendue ou nullopt. | 1. Interroger une position au milieu de la case (2,1) d'une planche 4x4.<br/> 2. Interroger une position hors bornes.<br/>3. Interroger la meme position sur une image qui n'est pas une planche. | Vérifie que `cell.has_value()` est vrai.<br/>Vérifie que `cell->column` vaut `2`.<br/>Vérifie que `cell->row` vaut `1`.<br/>Vérifie que `hmi::bitmaskCellAtPixel(sheet, TILE, sheet.width, 0).has_value()` est faux.<br/>Vérifie que `hmi::bitmaskCellAtPixel(sheet, TILE, -1, 0).has_value()` est faux.<br/>Vérifie que `hmi::bitmaskCellAtPixel(notASheet, TILE, 1, 1).has_value()` est faux. |
| **PixelAutotilePreviewTest.ApercuDAssemblageUtiliseLaMemeTableQueLeRendu** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_autotile_preview.cpp:80`</sub> | L'apercu d'assemblage utilise la meme table que le rendu. | 1. Construire une planche 4x4 dont chaque case a une couleur distincte.<br/>2. Construire l'apercu d'assemblage.<br/>3. Comparer la case centrale de l'apercu a la case attendue de la source (via autotileCell du masque plein). | Vérifie que `preview.width` vaut `TILE * 3`.<br/>Vérifie que `preview.height` vaut `TILE * 3`.<br/>Vérifie que `hmi::pickColor(preview, TILE + 1, TILE + 1)` vaut `expectedColor`. |
| **PixelAutotilePreviewTest.PlancheTropPetiteNeDebordePas** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_autotile_preview.cpp:121`</sub> | Une planche trop petite laisse la case correspondante transparente. | 1. Construire l'apercu d'assemblage a partir d'une planche degeneree (1x1 case). | `EXPECT_NO_THROW(preview = hmi::buildAutotileAssemblyPreview(tinySheet, TILE))`<br/>Vérifie que `preview.width` vaut `TILE * 3`.<br/>Vérifie que `preview.height` vaut `TILE * 3`. |

**`test_pixel_canvas_geometry.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **PixelCanvasGeometryTest.ScreenToImagePixelPourDiversesVues** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_canvas_geometry.cpp:16`</sub> | screenToImagePixel donne le bon pixel pour diverses combinaisons de zoom et de decalage. | 1. Construire plusieurs vues (zoom, decalage) differentes.<br/>2. Convertir une position ecran connue vers un pixel image. | Vérifie que `hmi::screenToImagePixel(view, 16, 16, 0.0, 0.0)` vaut `std::make_pair(0, 0)`.<br/>Vérifie que `hmi::screenToImagePixel(view, 16, 16, 20.0, 12.0)` vaut `std::make_pair(2, 1)`.<br/>Vérifie que `hmi::screenToImagePixel(zoomed, 16, 16, 20.0, 12.0)` vaut `std::make_pair(1, 0)`.<br/>Vérifie que `hmi::screenToImagePixel(panned, 16, 16, 0.0, 0.0)` vaut `std::make_pair(3, 1)`. |
| **PixelCanvasGeometryTest.ScreenToImagePixelHorsBornesRenvoieAbsence** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_canvas_geometry.cpp:40`</sub> | screenToImagePixel renvoie nullopt hors de l'image, y compris en negatif. | 1. Convertir une position ecran negative.<br/>2. Convertir une position au-dela du bord droit/bas de l'image. | Vérifie que `hmi::screenToImagePixel(view, 4, 4, -1.0, 0.0).has_value()` est faux.<br/>Vérifie que `hmi::screenToImagePixel(view, 4, 4, 0.0, -8.0).has_value()` est faux.<br/>Vérifie que `hmi::screenToImagePixel(view, 4, 4, 32.0, 0.0).has_value()` est faux. |
| **PixelCanvasGeometryTest.AllerRetourRectangleVersPixel** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_canvas_geometry.cpp:59`</sub> | Le centre du rectangle d'un pixel reconverti donne ce meme pixel, a tout zoom. | 1. Pour plusieurs zooms et decalages, calculer le rectangle ecran d'un pixel.<br/> 2. Reconvertir le centre de ce rectangle. | Vérifie que `roundTrip.has_value()` est vrai.<br/>Vérifie que `*roundTrip` vaut `std::make_pair(x, y)`. |
| **PixelCanvasGeometryTest.PixelCanvasRealSizeReutiliseThumbnailPixelSize** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_canvas_geometry.cpp:95`</sub> | pixelCanvasRealSize reutilise thumbnailPixelSize (LOT-56) sans la redefinir. | 1. Calculer la taille reelle du canevas a plusieurs facteurs d'echelle.<br/>2. Calculer independamment thumbnailPixelSize sur les memes tailles logiques. | Vérifie que `real.width` vaut `hmi::thumbnailPixelSize(IMAGE_WIDTH * ZOOM, factor)`.<br/>Vérifie que `real.height` vaut `hmi::thumbnailPixelSize(IMAGE_HEIGHT * ZOOM, factor)`. |
| **PixelCanvasGeometryTest.CommandesDeZoomRestentBorneesEtEntieres** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_canvas_geometry.cpp:120`</sub> | Les commandes de zoom restent bornees et entieres. | 1. Zoomer avant a repetition au-dela du maximum.<br/>2. Zoomer arriere a repetition en-deca du minimum. | Vérifie que `zoom` vaut `hmi::PIXEL_CANVAS_MAX_ZOOM`.<br/>Vérifie que `zoom` vaut `hmi::PIXEL_CANVAS_MIN_ZOOM`. |
| **PixelCanvasGeometryTest.GrilleVisibleAuSeuilAttendu** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_canvas_geometry.cpp:144`</sub> | pixelCanvasGridVisible bascule au seuil attendu. | 1. Evaluer pixelCanvasGridVisible juste en-deca et juste au niveau du seuil. | Vérifie que `hmi::pixelCanvasGridVisible(hmi::PIXEL_CANVAS_GRID_VISIBLE_MIN_ZOOM - 1)` est faux.<br/>Vérifie que `hmi::pixelCanvasGridVisible(hmi::PIXEL_CANVAS_GRID_VISIBLE_MIN_ZOOM)` est vrai. |

**`test_pixel_history.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **PixelHistoryTest.SuiteDOperationsAnnulableEtRetablissable** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_history.cpp:47`</sub> | Une suite d'operations est annulable puis retablissable a l'identique. | 1. Appliquer trois operations successives.<br/>2. Annuler deux fois.<br/> 3. Retablir deux fois. | Vérifie que `hmi::pickColor(image, 0, 0)` vaut `RED`.<br/>Vérifie que `hmi::pickColor(image, 1, 0)` vaut `GREEN`.<br/>Vérifie que `hmi::pickColor(image, 2, 0)` vaut `BLUE`.<br/>Vérifie que `history.undo(image)` est vrai.<br/>Vérifie que `hmi::pickColor(image, 2, 0)` vaut `0u`.<br/>Vérifie que `history.undo(image)` est vrai.<br/>Vérifie que `hmi::pickColor(image, 1, 0)` vaut `0u`.<br/>Vérifie que `hmi::pickColor(image, 0, 0)` vaut `RED`.<br/>Vérifie que `history.redo(image)` est vrai.<br/>Vérifie que `hmi::pickColor(image, 1, 0)` vaut `GREEN`.<br/>Vérifie que `history.redo(image)` est vrai.<br/>Vérifie que `hmi::pickColor(image, 2, 0)` vaut `BLUE`. |
| **PixelHistoryTest.NouvelleOperationApresAnnulationVideLeRetablissement** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_history.cpp:82`</sub> | Une nouvelle operation apres une annulation vide la pile de retablissement. | 1. Appliquer deux operations.<br/>2. Annuler la derniere.<br/>3. Appliquer une operation differente. | Vérifie que `history.undo(image)` est vrai.<br/>Vérifie que `history.canRedo()` est vrai.<br/>Vérifie que `history.canRedo()` est faux.<br/>Vérifie que `history.redo(image)` est faux. |
| **PixelHistoryTest.AnnulerAuDelaDuFondDePileSansEffet** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_history.cpp:108`</sub> | Annuler au-dela du fond de pile est sans effet. | 1. Sur un historique vide, appeler undo().<br/>2. Appliquer une operation, l'annuler, puis annuler une seconde fois. | Vérifie que `history.undo(image)` est faux.<br/>Vérifie que `history.undo(image)` est vrai.<br/>Vérifie que `history.undo(image)` est faux.<br/>Vérifie que `hmi::pickColor(image, 0, 0)` vaut `RED`. |
| **PixelHistoryTest.JumpToEquivautADesAnnulationsSuccessives** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_history.cpp:131`</sub> | jumpTo produit le meme tampon que des annulations successives. | 1. Appliquer quatre operations sur deux tampons identiques independants.<br/> 2. Sur le premier, annuler successivement jusqu'a l'entree d'index 1.<br/>3. Sur le second, appeler jumpTo(1) directement. | Vérifie que `undoHistory.undo(byUndo)` est vrai.<br/>Vérifie que `undoHistory.undo(byUndo)` est vrai.<br/>Vérifie que `jumpHistory.jumpTo(byJump, 1)` est vrai.<br/>Vérifie que `byUndo.pixels` vaut `byJump.pixels`. |
| **PixelHistoryTest.JumpToHorsBornesEchoueProprement** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_history.cpp:164`</sub> | jumpTo hors bornes echoue proprement. | 1. Appliquer une operation.<br/>2. Appeler jumpTo avec un index hors bornes. | Vérifie que `history.jumpTo(image, 5)` est faux.<br/>Vérifie que `hmi::pickColor(image, 0, 0)` vaut `GREEN`. |
| **PixelHistoryTest.ProfondeurPlafonneeOublieLesEntreesLesPlusAnciennes** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_history.cpp:183`</sub> | Au-dela de la profondeur maximale, les entrees les plus anciennes sont oubliees. | 1. Construire un historique de profondeur 2.<br/>2. Appliquer trois operations. | Vérifie que `history.appliedEntries().size()` vaut `2U`.<br/>Vérifie que `hmi::pickColor(image, 0, 0)` vaut `RED`.<br/>Vérifie que `hmi::pickColor(image, 1, 0)` vaut `GREEN`.<br/>Vérifie que `hmi::pickColor(image, 2, 0)` vaut `BLUE`. |
| **PixelHistoryTest.AppliedEntriesExposeLaListeOrdonnee** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_history.cpp:210`</sub> | appliedEntries expose la liste ordonnee des operations. | 1. Appliquer pinceau puis gomme.<br/>2. Lire appliedEntries(). | Vérifie que `history.appliedEntries().size()` vaut `2U`.<br/>Vérifie que `history.appliedEntries()[0].kind` vaut `hmi::PixelOperationKind::Brush`.<br/>Vérifie que `history.appliedEntries()[1].kind` vaut `hmi::PixelOperationKind::Eraser`. |
| **PixelHistoryTest.ClesDeNomDOperationExistentDansLesDeuxCatalogues** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_history.cpp:232`</sub> | Chaque cle de nom d'operation existe dans les deux catalogues. | 1. Charger fr.lang puis en.lang.<br/>2. Resoudre la cle de chaque type d'operation. | Vérifie que `localization.loadDefaultLanguage(language)` est vrai.<br/>Vérifie que `localization.text(key)` diffère de `key`. |

**`test_pixel_operations.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **PixelOperationsTest.SetPixelChangeLaCouleurEtRenvoieLaRegion** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:36`</sub> | setPixel change la couleur du pixel vise et renvoie la region attendue. | 1. Poser un pixel dans les bornes d'une image.<br/>2. Poser un pixel hors bornes. | Vérifie que `region.empty()` est faux.<br/>Vérifie que `region.minX` vaut `1`.<br/>Vérifie que `region.maxX` vaut `1`.<br/>Vérifie que `region.minY` vaut `2`.<br/>Vérifie que `region.maxY` vaut `2`.<br/>Vérifie que `hmi::pickColor(image, 1, 2)` vaut `GREEN`.<br/>Vérifie que `outOfBounds.empty()` est vrai. |
| **PixelOperationsTest.ErasePixelPreserveLaTeinteRvb** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:61`</sub> | erasePixel met l'alpha a zero sans changer la teinte RVB. | 1. Effacer un pixel colore.<br/>2. Lire sa couleur. | Vérifie que `erased.has_value()` est vrai.<br/>Vérifie que `*erased & 0xFF000000u` vaut `0u`.<br/>Vérifie que `*erased & 0x00FFFFFFu` vaut `RED & 0x00FFFFFFu`. |
| **PixelOperationsTest.PickColorRenvoieLaCouleurOuAbsentHorsBornes** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:80`</sub> | pickColor renvoie la couleur du pixel ou nullopt hors bornes. | 1. Poser une couleur connue sur un pixel.<br/>2. La prelever avec pickColor.<br/> 3. Prelever hors bornes. | Vérifie que `hmi::pickColor(image, 1, 1)` vaut `BLUE`.<br/>Vérifie que `hmi::pickColor(image, -1, 0).has_value()` est faux.<br/>Vérifie que `hmi::pickColor(image, 3, 0).has_value()` est faux. |
| **PixelOperationsTest.DrawLineDiagonaleSansTrou** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:99`</sub> | drawLine peint un segment diagonal sans trou. | 1. Tracer une ligne diagonale entre deux positions eloignees.<br/>2. Lire chaque pixel du chemin attendu. | Vérifie que `region.minX` vaut `0`.<br/>Vérifie que `region.minY` vaut `0`.<br/>Vérifie que `region.maxX` vaut `5`.<br/>Vérifie que `region.maxY` vaut `5`.<br/>Vérifie que `hmi::pickColor(image, i, i)` vaut `GREEN`. |
| **PixelOperationsTest.DrawLineVerticaleSansTrou** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:122`</sub> | drawLine peint un segment purement vertical sans trou. | 1. Tracer une ligne verticale.<br/>2. Lire chaque pixel de la colonne. | Vérifie que `hmi::pickColor(image, 1, y)` vaut `RED`.<br/>Vérifie que `hmi::pickColor(image, 0, 3)` vaut `TRANSPARENT_BLACK`. |
| **PixelOperationsTest.EraseLineEffaceSansTrou** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:142`</sub> | eraseLine efface un segment complet sans trou. | 1. Effacer une ligne diagonale sur une image coloree.<br/>2. Lire chaque pixel du chemin. | Vérifie que `region` vaut `(hmi::PixelRegion{0, 0, 4, 4})`.<br/>Vérifie que `pixel.has_value()` est vrai.<br/>Vérifie que `*pixel & 0xFF000000u` vaut `0u`.<br/>Vérifie que `*pixel & 0x00FFFFFFu` vaut `RED & 0x00FFFFFFu`. |
| **PixelOperationsTest.FloodFillZoneFermeeNeDeborde** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:166`</sub> | floodFill remplit une zone fermee sans deborder. | 1. Construire une image avec un carre interieur cerne d'une bordure d'une autre couleur.<br/>2. Remplir depuis le centre du carre. | Vérifie que `hmi::pickColor(image, x, y)` vaut `GREEN`.<br/>Vérifie que `hmi::pickColor(image, i, 0)` vaut `BLUE`.<br/>Vérifie que `hmi::pickColor(image, i, 4)` vaut `BLUE`.<br/>Vérifie que `hmi::pickColor(image, 0, i)` vaut `BLUE`.<br/>Vérifie que `hmi::pickColor(image, 4, i)` vaut `BLUE`. |
| **PixelOperationsTest.FloodFillZoneOuverteRempliJusquAuBord** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:202`</sub> | floodFill remplit une zone ouverte jusqu'au bord sans deborder du tampon. | 1. Construire une image entierement d'une couleur (zone "ouverte" par construction : aucune bordure).<br/>2. Remplir depuis un coin. | Vérifie que `region.minX` vaut `0`.<br/>Vérifie que `region.minY` vaut `0`.<br/>Vérifie que `region.maxX` vaut `3`.<br/>Vérifie que `region.maxY` vaut `2`.<br/>Vérifie que `pixel` vaut `GREEN`. |
| **PixelOperationsTest.FloodFillCouleurDejaPresenteNeChangeRien** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:226`</sub> | floodFill sur la couleur deja presente est un no-op sans boucle infinie. | 1. Construire une grande image uniforme.<br/>2. La remplir avec sa propre couleur. | Vérifie que `region.empty()` est vrai.<br/>Vérifie que `pixel` vaut `BLUE`. |
| **PixelOperationsTest.FloodFillImageUnPixel** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:248`</sub> | floodFill fonctionne sur une image d'un seul pixel. | 1. Construire une image 1x1.<br/>2. La remplir d'une autre couleur. | Vérifie que `region` vaut `(hmi::PixelRegion{0, 0, 0, 0})`.<br/>Vérifie que `hmi::pickColor(image, 0, 0)` vaut `GREEN`. |
| **PixelOperationsTest.FloodFillImageEntierementUniformeRecoloreTout** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:266`</sub> | floodFill sur une image entierement uniforme la recolore integralement. | 1. Construire une image uniforme.<br/>2. La remplir d'une autre couleur depuis un pixel quelconque. | Vérifie que `pixel` vaut `GREEN`. |
| **PixelOperationsTest.UnionPixelRegionCouvreLesDeuxRegions** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:287`</sub> | unionPixelRegion couvre les deux regions, une region vide etant neutre. | 1. Unir deux regions disjointes.<br/>2. Unir une region avec une region vide. | Vérifie que `hmi::unionPixelRegion(a, b)` vaut `(hmi::PixelRegion{0, 0, 7, 7})`.<br/>Vérifie que `hmi::unionPixelRegion(a, hmi::PixelRegion{})` vaut `a`.<br/>Vérifie que `hmi::unionPixelRegion(hmi::PixelRegion{}, b)` vaut `b`. |
| **PixelOperationsTest.DeuxSymetriesHorizontalesRestituentLOrigine** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:321`</sub> | Deux symetries horizontales restituent l'image d'origine. | 1. Appliquer flipHorizontal deux fois sur une sous-region.<br/>2. Repeter sur l'image entiere. | Vérifie que `partial.pixels` vaut `original.pixels`.<br/>Vérifie que `whole.pixels` vaut `original.pixels`. |
| **PixelOperationsTest.DeuxSymetriesVerticalesRestituentLOrigine** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:347`</sub> | Deux symetries verticales restituent l'image d'origine. | 1. Appliquer flipVertical deux fois sur une sous-region. | Vérifie que `image.pixels` vaut `original.pixels`. |
| **PixelOperationsTest.QuatreRotationsRestituentLOrigineSurRegionCarree** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:368`</sub> | Quatre rotations restituent l'image d'origine sur une region carree. | 1. Appliquer rotateClockwise quatre fois sur une region carree. | Vérifie que `image.pixels` vaut `original.pixels`. |
| **PixelOperationsTest.RotationNonCarreeTronqueLeDebordement** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:390`</sub> | Rotation non carree : le debordement est tronque au cadre. | 1. Pivoter une region rectangulaire proche du bord de l'image.<br/>2. Verifier que l'image garde sa taille et qu'aucune ecriture n'a debord du tampon. | `EXPECT_NO_THROW(hmi::rotateClockwise(image, region))`<br/>Vérifie que `image.pixels.size()` vaut `originalSize`.<br/>Vérifie que `image.width` vaut `5`.<br/>Vérifie que `image.height` vaut `5`. |
| **PixelOperationsTest.CollerPresDUnBordTronqueSansDeborder** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:416`</sub> | Coller pres d'un bord tronque sans ecriture hors limites. | 1. Copier une region.<br/>2. La coller a une position debordant du cadre. | `EXPECT_NO_THROW(touched = hmi::pasteClipboard(image, clip, 3, 3))`<br/>Vérifie que `image.pixels.size()` vaut `originalSize`.<br/>Vérifie que `touched` vaut `(hmi::PixelRegion{3, 3, 3, 3})`. |
| **PixelOperationsTest.TransformationSurRegionVideEstSansEffet** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:440`</sub> | Une transformation sur une region vide est sans effet. | 1. Appliquer flipHorizontal, rotateClockwise et moveRegion sur une region vide. | Vérifie que `hmi::flipHorizontal(image, empty).empty()` est vrai.<br/>Vérifie que `hmi::rotateClockwise(image, empty).empty()` est vrai.<br/>Vérifie que `hmi::moveRegion(image, empty, 1, 1).empty()` est vrai.<br/>Vérifie que `image.pixels` vaut `original.pixels`. |
| **PixelOperationsTest.DeplacerUneRegionLaisseLaZoneQuitteeTransparente** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:461`</sub> | Deplacer une region laisse la zone quittee transparente. | 1. Deplacer une region a l'interieur du cadre.<br/>2. Verifier la zone quittee et la zone reposee.<br/>3. Deplacer une region entierement hors cadre. | Vérifie que `hmi::pickColor(image, 1, 1).value() & 0xFF000000u` vaut `0u`.<br/>Vérifie que `hmi::pickColor(image, 4, 4)` vaut `GREEN`.<br/>`EXPECT_NO_THROW(hmi::moveRegion(farImage, hmi::PixelRegion{0, 0, 1, 1}, 100, 100))`<br/>Vérifie que `farImage.pixels.size()` vaut `16U`. |
| **PixelOperationsTest.ReadRegionPuisWriteRegionRestitueLeContenu** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_operations.cpp:487`</sub> | readRegion puis writeRegion restituent le contenu d'origine. | 1. Lire une region d'une image.<br/>2. Modifier l'image dans cette region.<br/> 3. Reecrire les pixels lus a l'etape 1. | Vérifie que `before.size()` vaut `9U`.<br/>Vérifie que `hmi::pickColor(image, 2, 2)` vaut `GREEN`.<br/>Vérifie que `hmi::pickColor(image, x, y)` vaut `RED`. |

**`test_pixel_palette.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **PixelPaletteFile.AllerRetourJsonRestitueLeContenu** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_palette.cpp:50`</sub> | Ecrire puis relire une palette restitue le meme contenu, alpha compris. | 1. Construire une palette de trois entrees, dont une avec alpha partiel.<br/> 2. L'enregistrer puis la relire. | Vérifie que `original.saveToFile(path)` est vrai.<br/>Vérifie que `reloaded.entries().size()` vaut `3U`.<br/>Vérifie que `reloaded.entries()[0].name` vaut `"Rouge"`.<br/>Vérifie que `reloaded.entries()[0].color` vaut `RED`.<br/>Vérifie que `reloaded.entries()[1].name` vaut `"Vert"`.<br/>Vérifie que `reloaded.entries()[1].color` vaut `GREEN`.<br/>Vérifie que `reloaded.entries()[2].name` vaut `"Bleu translucide"`.<br/>Vérifie que `reloaded.entries()[2].color` vaut `BLUE_SEMI`. |
| **PixelPaletteFile.FichierAbsentProduitUnePaletteVide** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_palette.cpp:79`</sub> | Un fichier absent produit une palette vide sans exception. | 1. Charger un chemin qui n'existe pas. | `EXPECT_NO_THROW(palette = hmi::PixelPalette::loadFromFile(path))`<br/>Vérifie que `palette.entries().empty()` est vrai. |
| **PixelPaletteFile.FichierMalformeProduitUnePaletteVide** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_palette.cpp:94`</sub> | Un fichier malforme produit une palette vide sans exception. | 1. Ecrire un contenu JSON invalide au chemin de test.<br/>2. Le charger. | `EXPECT_NO_THROW(palette = hmi::PixelPalette::loadFromFile(path))`<br/>Vérifie que `palette.entries().empty()` est vrai. |
| **PixelPaletteTest.DeuxExtractionsProduisentLaMemeListe** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_palette.cpp:114`</sub> | Deux extractions du meme asset produisent la meme liste. | 1. Construire une image avec des couleurs repetees.<br/>2. L'extraire deux fois. | Vérifie que `first.size()` vaut `2U`.<br/>Vérifie que `first[0].color` vaut `RED`.<br/>Vérifie que `first[0].count` vaut `2`.<br/>Vérifie que `first[1].color` vaut `GREEN`.<br/>Vérifie que `first[1].count` vaut `2`.<br/>Vérifie que `second.size()` vaut `first.size()`.<br/>Vérifie que `first[i].color` vaut `second[i].color`.<br/>Vérifie que `first[i].count` vaut `second[i].count`. |
| **PixelPaletteTest.ImageEntierementTransparenteProduitUneExtractionVide** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_palette.cpp:145`</sub> | Une image entierement transparente produit une extraction vide. | 1. Construire une image entierement transparente.<br/>2. L'extraire. | Vérifie que `hmi::extractPalette(image).empty()` est vrai. |
| **PixelPaletteTest.CouleurDejaPresenteRenvoyeeTelleQuelle** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_palette.cpp:159`</sub> | Une couleur deja presente est renvoyee telle quelle. | 1. Contraindre une couleur presente dans la palette. | Vérifie que `hmi::nearestPaletteColor(GREEN, palette)` vaut `GREEN`. |
| **PixelPaletteTest.DepartageADistanceEgaleEstStable** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_palette.cpp:174`</sub> | Le departage a distance egale est stable. | 1. Construire une palette avec deux couleurs equidistantes d'une couleur cible.<br/> 2. Contraindre la couleur cible plusieurs fois. | Vérifie que `first` vaut `CANDIDATE_A`.<br/>Vérifie que `second` vaut `first`. |
| **PixelPaletteTest.PaletteVideLaisseLaCouleurInchangee** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_palette.cpp:199`</sub> | Une palette vide laisse la couleur inchangee. | 1. Contraindre une couleur avec une palette vide. | Vérifie que `hmi::nearestPaletteColor(RED, {})` vaut `RED`. |
| **PixelPaletteTest.ContraintePreserveToujoursLAlpha** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_pixel_palette.cpp:213`</sub> | La contrainte preserve toujours l'alpha d'origine. | 1. Contraindre un pixel totalement transparent.<br/>2. Contraindre un pixel semi-transparent. | Vérifie que `hmi::nearestPaletteColor(transparent, palette)` vaut `transparent`.<br/>Vérifie que `constrained & 0xFF000000u` vaut `semiTransparentGreen & 0xFF000000u`.<br/>Vérifie que `constrained & 0x00FFFFFFu` vaut `GREEN & 0x00FFFFFFu`. |

**`test_skin_assignments.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **SkinAssignmentsTest.ChaqueTypeApparaitUneFois** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_skin_assignments.cpp:59`</sub> | Chaque type de tuile peignable apparait exactement une fois dans le panneau. | 1. Construire les lignes du panneau pour un catalogue quelconque.<br/> 2. Collecter les types de toutes les lignes. | Vérifie que `seen.insert(row.type).second` est vrai.<br/>Vérifie que `row.type` diffère de `core::TileType::Empty`.<br/>Vérifie que `seen` vaut `expected`. |
| **SkinAssignmentsTest.LignesRefletentLAssignation** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_skin_assignments.cpp:96`</sub> | Les lignes du panneau refletent l'assignation courante du jeu consulte. | 1. Construire les lignes pour un catalogue ou seul Solid est assigne. | Vérifie que `row.asset` vaut `"stone.png"`.<br/>Vérifie que `row.mode` vaut `hmi::SkinMode::Bitmask16`.<br/>Vérifie que `row.asset.empty()` est vrai. |
| **SkinAssignmentsTest.SectionsSuiventLaPalette** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_skin_assignments.cpp:121`</sub> | Les sections du panneau suivent les categories de la palette. | 1. Comparer les libelles de sections aux categories de la taxonomie. | Vérifie que `sectionLabels` vaut `categoryLabels`. |
| **SkinAssignmentsTest.AssignationPuisRetraitDepuisLePanneau** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_skin_assignments.cpp:148`</sub> | Assigner puis retirer un skin depuis le panneau modifie le catalogue. | 1. Appliquer une assignation a un type non skinne, puis relire les lignes.<br/> 2. Appliquer une assignation vide au meme type, puis relire. | Vérifie que `assigned.has_value()` est vrai.<br/>Vérifie que `assigned->asset` vaut `"spikes.png"`.<br/>Vérifie que `catalog.resolve("foret", core::TileType::Danger).has_value()` est faux. |
| **SkinAssignmentsTest.JeuNonPreciseViseLeDefaut** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_skin_assignments.cpp:173`</sub> | Une assignation sans jeu precise vise le jeu par defaut. | 1. Appliquer une assignation en passant un nom de jeu vide. | Vérifie que `catalog.defaultSetName()` vaut `"foret"`.<br/>Vérifie que `assignments.find(core::TileType::Door)` diffère de `assignments.end()`. |
| **SkinAssignmentsTest.BalayageNeRetientQueLesImages** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_skin_assignments.cpp:192`</sub> | Le balayage du dossier de skins ne retient que les images, triees. | 1. Creer un dossier contenant deux PNG, un fichier texte et un README. | Vérifie que `assets` vaut `expected`. |
| **SkinAssignmentsTest.DossierAbsentListeVide** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_skin_assignments.cpp:216`</sub> | Un dossier de skins absent donne une liste vide, sans erreur. | 1. Balayer un chemin qui n'existe pas. | Vérifie que `hmi::listSkinAssets(std::filesystem::temp_directory_path() / "projectgaming_dossier_inexistant") .empty()` est vrai. |
| **SkinAssignmentsTest.LibellesTaxonomieTraduitsDansLesDeuxLangues** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_skin_assignments.cpp:232`</sub> | Chaque libelle de la taxonomie a une traduction dans les deux catalogues. | 1. Pour chaque libelle de la taxonomie, obtenir sa cle de traduction.<br/> 2. Verifier que la cle est resolue en francais puis en anglais. | Vérifie que `loc.loadLanguage(language)` est vrai.<br/>Vérifie que `key.empty()` est faux.<br/>Vérifie que `loc.text(key)` diffère de `key`. |
| **SkinAssignmentsTest.LibellesDuPanneauTraduits** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_skin_assignments.cpp:272`</sub> | Les libelles propres au panneau Textures existent dans les deux catalogues. | 1. Pour chaque cle utilisee par le panneau, la resoudre en francais puis en anglais. | Vérifie que `loc.loadLanguage(language)` est vrai.<br/>Vérifie que `loc.text(key)` diffère de `key`. |

**`test_texture_assign_gesture.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **TextureAssignGesture.CaseVideIgnoree** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_texture_assign_gesture.cpp:23`</sub> | Une case vide est toujours ignorée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `decision.action` vaut `TextureAssignAction::Ignore`. |
| **TextureAssignGesture.AucunAssetSelectionneIgnore** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_texture_assign_gesture.cpp:39`</sub> | Sans asset sélectionné, le clic est ignoré. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `decision.action` vaut `TextureAssignAction::Ignore`. |
| **TextureAssignGesture.CaseSansOverrideAssigneLAssetSelectionne** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_texture_assign_gesture.cpp:55`</sub> | Sur une case sans surcharge, le clic assigne l'asset sélectionné. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `decision.action` vaut `TextureAssignAction::Assign`.<br/>Vérifie que `decision.cell` vaut `(GridPosition{2, 3})`.<br/>Vérifie que `decision.assetName` vaut `"door_red.png"`. |
| **TextureAssignGesture.CaseAvecOverrideDifferentRemplace** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_texture_assign_gesture.cpp:73`</sub> | Sur une case surchargée avec un autre asset, le clic remplace. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `decision.action` vaut `TextureAssignAction::Assign`.<br/>Vérifie que `decision.assetName` vaut `"door_red.png"`. |
| **TextureAssignGesture.ReclicSurLeMemeAssetRetire** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_texture_assign_gesture.cpp:91`</sub> | Recliquer avec le même asset retire la surcharge. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `decision.action` vaut `TextureAssignAction::Remove`.<br/>Vérifie que `decision.cell` vaut `(GridPosition{2, 3})`. |
| **TextureAssignGesture.ClicDroitAvecOverrideRetire** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_texture_assign_gesture.cpp:109`</sub> | Le clic droit retire la surcharge, indépendamment de la sélection. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `decision.action` vaut `TextureAssignAction::Remove`.<br/>Vérifie que `decision.cell` vaut `(GridPosition{2, 3})`. |
| **TextureAssignGesture.ClicDroitSansOverrideIgnore** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_texture_assign_gesture.cpp:126`</sub> | Le clic droit sur une case sans surcharge est sans effet. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `decision.action` vaut `TextureAssignAction::Ignore`. |
| **TextureAssignGesture.ClicDroitSurCaseVideIgnore** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_texture_assign_gesture.cpp:142`</sub> | Le clic droit sur une case vide est ignoré. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `decision.action` vaut `TextureAssignAction::Ignore`. |
| **TextureAssignGesture.ToutTypeNonVideEstEligible** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_texture_assign_gesture.cpp:159`</sub> | Tout type non vide est éligible à une surcharge de texture. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `decision.action` vaut `TextureAssignAction::Assign`. |

**`test_thumbnail_geometry.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **ThumbnailGeometryTest.DimensionnementAuxFacteursUsuels** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_thumbnail_geometry.cpp:14`</sub> | Le dimensionnement produit la taille en pixels reels attendue. | 1. Calculer la taille en pixels pour une taille logique de 48 aux facteurs 1, 1.25, 1.5 et 2. | Vérifie que `hmi::thumbnailPixelSize(48, 1.0)` vaut `48`.<br/>Vérifie que `hmi::thumbnailPixelSize(48, 1.25)` vaut `60`.<br/>Vérifie que `hmi::thumbnailPixelSize(48, 1.5)` vaut `72`.<br/>Vérifie que `hmi::thumbnailPixelSize(48, 2.0)` vaut `96`. |
| **ThumbnailGeometryTest.FacteurNonEntierNeProduitJamaisZero** (Critique)<br/><sub>`Source/Test/Unit/HMI/Editor/test_thumbnail_geometry.cpp:32`</sub> | Un facteur non entier ne produit jamais de dimension nulle. | 1. Calculer la taille en pixels pour la plus petite icone (16) a 1.25 et a un facteur tres faible (0.1). | Vérifie que `hmi::thumbnailPixelSize(16, 1.25)` est strictement supérieur à `0`.<br/>Vérifie que `hmi::thumbnailPixelSize(1, 0.1)` est strictement supérieur à `0`.<br/>Vérifie que `hmi::thumbnailPixelSize(0, 2.0)` est strictement supérieur à `0`. |
| **ThumbnailGeometryTest.FonctionPure** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_thumbnail_geometry.cpp:48`</sub> | Le dimensionnement est une fonction pure. | 1. Appeler deux fois avec les memes entrees. | Vérifie que `hmi::thumbnailPixelSize(32, 1.5)` vaut `hmi::thumbnailPixelSize(32, 1.5)`. |

**`test_tile_taxonomy.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **TileTaxonomy.ChaqueTypeFigureExactementUneFois** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_taxonomy.cpp:37`</sub> | Chaque type de tuile figure exactement une fois dans la taxonomie. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `types.size()` vaut `TILE_TYPE_COUNT`.<br/>Vérifie que `unique.size()` vaut `types.size()`.<br/>Vérifie que `unique.size()` vaut `TILE_TYPE_COUNT`. |
| **TileTaxonomy.ChaqueEntreeAUnLibelle** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Editor/test_tile_taxonomy.cpp:59`</sub> | Chaque catégorie, sous-groupe et tuile de la taxonomie porte un libellé non vide. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `category.label.empty()` est faux.<br/>Vérifie que `entry.label.empty()` est faux.<br/>Vérifie que `subgroup.label.empty()` est faux.<br/>Vérifie que `entry.label.empty()` est faux. |

#### Game (18)

**`test_game_hud.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **GameHudTest.BudgetsFinisAffichentLesDeuxCompteurs** (Critique)<br/><sub>`Source/Test/Unit/HMI/Game/test_game_hud.cpp:28`</sub> | Un niveau avec des budgets finis affiche les deux compteurs et le nom du tableau. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `lines.size()` vaut `3u`.<br/>Vérifie que `lines[0]` vaut `"Sauts : 3"`.<br/>Vérifie que `lines[1]` vaut `"Dashs : 1"`.<br/>Vérifie que `lines[2]` vaut `"Salle des epreuves"`. |
| **GameHudTest.NiveauSansBudgetNAfficheAucunCompteur** (Critique)<br/><sub>`Source/Test/Unit/HMI/Game/test_game_hud.cpp:55`</sub> | Un niveau sans budget n'affiche aucun compteur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `lines.size()` vaut `1u`.<br/>Vérifie que `lines[0]` vaut `"Promenade"`. |
| **GameHudTest.BudgetPartielNAfficheQuUnCompteur** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Game/test_game_hud.cpp:76`</sub> | Un budget partiellement défini n'affiche qu'un seul compteur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `lines.size()` vaut `2u`.<br/>Vérifie que `lines[0]` vaut `"Sauts : 2"`.<br/>Vérifie que `lines[1]` vaut `"Chute libre"`. |
| **GameHudTest.CompteurSuitLaDecroissanceDuBudget** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Game/test_game_hud.cpp:99`</sub> | Le compteur affiché suit immédiatement la décroissance du budget. | 1. Composer les lignes avec 3 sauts restants, puis avec 2 (après un saut simulé).<br/> 2. Comparer les deux résultats. | Vérifie que `hmi::gameHudLines(player, "Niveau", localization)[0]` vaut `"Sauts : 3"`.<br/>Vérifie que `hmi::gameHudLines(player, "Niveau", localization)[0]` vaut `"Sauts : 2"`. |
| **GameHudTest.ClesDeTraductionExistentDansLesDeuxCatalogues** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Game/test_game_hud.cpp:122`</sub> | Les clés de traduction du HUD existent dans les deux catalogues livrés. | 1. Charger fr.lang puis en.lang depuis les catalogues livrés.<br/>2. Résoudre les clés du HUD. | Vérifie que `localization.loadDefaultLanguage(language)` est vrai.<br/>Vérifie que `localization.text("hud.jumps_remaining")` diffère de `"hud.jumps_remaining"`.<br/>Vérifie que `localization.text("hud.dashes_remaining")` diffère de `"hud.dashes_remaining"`. |

**`test_progression.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **ProgressionTest.ProgressionNeuveEstVide** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Game/test_progression.cpp:44`</sub> | Une progression neuve est vide. | 1. Construire une `Progression` par défaut.<br/>2. Vérifier ses champs. | Vérifie que `progression.sequenceId().empty()` est vrai.<br/>Vérifie que `progression.currentLevel().empty()` est vrai.<br/>Vérifie que `progression.completedLevels().empty()` est vrai. |
| **ProgressionTest.MarquerDeuxFoisLeMemeTableauEstSansEffet** (Critique)<br/><sub>`Source/Test/Unit/HMI/Game/test_progression.cpp:61`</sub> | Marquer un tableau terminé deux fois est sans effet. | 1. Marquer un tableau terminé.<br/>2. Le marquer une seconde fois.<br/>3. Vérifier la taille de l'ensemble. | Vérifie que `progression.completedLevels().size()` vaut `1U`.<br/>Vérifie que `progression.isCompleted("demo-saut.json")` est vrai.<br/>Vérifie que `progression.isCompleted("demo-dash.json")` est faux. |
| **ProgressionTest.ResetEffaceToutLaProgression** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Game/test_progression.cpp:81`</sub> | reset() efface toute la progression. | 1. Construire une progression avec du contenu.<br/>2. Appeler `reset()`.<br/>3. Vérifier qu'elle est redevenue vide. | Vérifie que `progression.sequenceId().empty()` est vrai.<br/>Vérifie que `progression.currentLevel().empty()` est vrai.<br/>Vérifie que `progression.completedLevels().empty()` est vrai. |
| **ProgressionDir.AllerRetourProduitExactementLaMemeProgression** (Critique)<br/><sub>`Source/Test/Unit/HMI/Game/test_progression.cpp:104`</sub> | Sauvegarder puis relire une progression produit exactement la même. | 1. Construire une progression avec plusieurs tableaux terminés.<br/>2. La sauvegarder.<br/>3. La relire depuis le même fichier. | Vérifie que `original.save(path)` est vrai.<br/>Vérifie que `reloaded.sequenceId()` vaut `original.sequenceId()`.<br/>Vérifie que `reloaded.currentLevel()` vaut `original.currentLevel()`.<br/>Vérifie que `reloaded.completedLevels()` vaut `original.completedLevels()`. |
| **ProgressionDir.FichierAbsentDonneUnePartieNeuve** (Critique)<br/><sub>`Source/Test/Unit/HMI/Game/test_progression.cpp:131`</sub> | Un fichier de progression absent donne une partie neuve. | 1. Charger depuis un chemin qui n'existe pas.<br/>2. Vérifier le résultat. | Vérifie que `progression.sequenceId().empty()` est vrai.<br/>Vérifie que `progression.currentLevel().empty()` est vrai.<br/>Vérifie que `progression.completedLevels().empty()` est vrai. |
| **ProgressionDir.FichierVideDonneUnePartieNeuve** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Game/test_progression.cpp:147`</sub> | Un fichier de progression vide donne une partie neuve. | 1. Écrire un fichier vide.<br/>2. Le charger. | Vérifie que `progression.sequenceId().empty()` est vrai.<br/>Vérifie que `progression.completedLevels().empty()` est vrai. |
| **ProgressionDir.FichierMalformeDonneUnePartieNeuve** (Critique)<br/><sub>`Source/Test/Unit/HMI/Game/test_progression.cpp:164`</sub> | Un fichier de progression malformé donne une partie neuve. | 1. Écrire du JSON invalide.<br/>2. Le charger. | Vérifie que `progression.sequenceId().empty()` est vrai.<br/>Vérifie que `progression.completedLevels().empty()` est vrai. |
| **ProgressionDir.EntreeInconnueDansCompletedLevelsEstIgnoree** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Game/test_progression.cpp:184`</sub> | Une entrée inconnue de completedLevels est ignorée sans invalider le reste. | 1. Écrire un fichier dont `completedLevels` contient une entrée numérique.<br/>2. Le charger. | Vérifie que `progression.sequenceId()` vaut `"sequence-demo.json"`.<br/>Vérifie que `progression.completedLevels().size()` vaut `2U`.<br/>Vérifie que `progression.isCompleted("demo-deplacement.json")` est vrai.<br/>Vérifie que `progression.isCompleted("demo-saut.json")` est vrai. |
| **ProgressionTest.TableauRetireDeLaSequenceNInvalidePasLeReste** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Game/test_progression.cpp:211`</sub> | Un tableau retiré de la séquence n'invalide pas le reste de la progression. | 1. Marquer trois tableaux terminés.<br/>2. Simuler le retrait d'un tableau de la séquence (aucune action sur la progression : elle ne connaît pas la séquence).<br/>3. Vérifier que les deux autres restent marqués terminés. | Vérifie que `progression.isCompleted("demo-deplacement.json")` est vrai.<br/>Vérifie que `progression.isCompleted("demo-dash.json")` est vrai.<br/>Vérifie que `progression.completedLevels().size()` vaut `3U`. |
| **ProgressionUnlockTest.ProgressionVideSeulLePremierTableauEstJouable** (Critique)<br/><sub>`Source/Test/Unit/HMI/Game/test_progression.cpp:243`</sub> | Progression vide : seul le premier tableau est jouable. | 1. Construire une progression vide.<br/>2. Interroger `isLevelUnlocked` pour chaque tableau de la séquence. | Vérifie que `hmi::isLevelUnlocked(progression, FIVE_LEVEL_SEQUENCE, "demo-deplacement.json")` est vrai.<br/>Vérifie que `hmi::isLevelUnlocked(progression, FIVE_LEVEL_SEQUENCE, "demo-saut.json")` est faux.<br/>Vérifie que `hmi::isLevelUnlocked(progression, FIVE_LEVEL_SEQUENCE, "demo-double-saut.json")` est faux.<br/>Vérifie que `hmi::isLevelUnlocked(progression, FIVE_LEVEL_SEQUENCE, "demo-wall-jump.json")` est faux.<br/>Vérifie que `hmi::isLevelUnlocked(progression, FIVE_LEVEL_SEQUENCE, "demo-dash.json")` est faux. |
| **ProgressionUnlockTest.TroisTableauxTerminesDeverrouillentLeQuatrieme** (Critique)<br/><sub>`Source/Test/Unit/HMI/Game/test_progression.cpp:263`</sub> | Trois tableaux terminés : les trois plus le quatrième sont jouables. | 1. Marquer les trois premiers tableaux terminés.<br/>2. Interroger `isLevelUnlocked` pour chaque tableau de la séquence. | Vérifie que `hmi::isLevelUnlocked(progression, FIVE_LEVEL_SEQUENCE, "demo-deplacement.json")` est vrai.<br/>Vérifie que `hmi::isLevelUnlocked(progression, FIVE_LEVEL_SEQUENCE, "demo-saut.json")` est vrai.<br/>Vérifie que `hmi::isLevelUnlocked(progression, FIVE_LEVEL_SEQUENCE, "demo-double-saut.json")` est vrai.<br/>Vérifie que `hmi::isLevelUnlocked(progression, FIVE_LEVEL_SEQUENCE, "demo-wall-jump.json")` est vrai.<br/>Vérifie que `hmi::isLevelUnlocked(progression, FIVE_LEVEL_SEQUENCE, "demo-dash.json")` est faux. |
| **ProgressionUnlockTest.SequenceEntierementTermineeTousJouables** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Game/test_progression.cpp:287`</sub> | Séquence entièrement terminée : tous les tableaux sont jouables. | 1. Marquer tous les tableaux de la séquence terminés.<br/>2. Interroger `isLevelUnlocked` pour chacun. | Vérifie que `hmi::isLevelUnlocked(progression, FIVE_LEVEL_SEQUENCE, level)` est vrai. |
| **ProgressionUnlockTest.TableauVerrouilleNEstJamaisJouable** (Critique)<br/><sub>`Source/Test/Unit/HMI/Game/test_progression.cpp:309`</sub> | Un tableau verrouillé n'est jamais jouable. | 1. Ne terminer aucun tableau.<br/>2. Interroger `isLevelUnlocked` pour un tableau loin dans la séquence. | Vérifie que `hmi::isLevelUnlocked(progression, FIVE_LEVEL_SEQUENCE, "demo-dash.json")` est faux. |

#### Graphics (244)

**`test_animated_tiles.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **AnimatedTilesTest.SkinAnimeProduitDesRegionsDifferentesAuFilDesPas** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animated_tiles.cpp:63`</sub> | Un skin animé produit des régions différentes au fil des pas, sur un même rang. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `first.x` diffère de `second.x`.<br/>Vérifie que `first.y` vaut `second.y`. |
| **AnimatedTilesTest.SkinNonAnimeProduitUneRegionConstante** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animated_tiles.cpp:91`</sub> | Un skin non animé produit une région constante d'un appel à l'autre. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `first.region.x` vaut `0`.<br/>Vérifie que `first.region.x` vaut `second.region.x`.<br/>Vérifie que `first.region.width` vaut `TILE`. |
| **AnimatedTilesTest.SkinAnimeUtiliseLaRegionCouranteDeAnimatedFrame** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animated_tiles.cpp:131`</sub> | Un skin animé utilise la région de l'image courante, pas la planche entière. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `appearance.region.x` vaut `currentFrame.x`.<br/>Vérifie que `appearance.region.width` vaut `TILE`. |
| **AnimatedTilesTest.MiseEnPhaseDeuxTuilesDuMemeTypeAffichentLaMemeImageAuMemePas** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animated_tiles.cpp:166`</sub> | Deux tuiles du même asset affichent la même image à chaque pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `regionOne.x` vaut `regionTwo.x`.<br/>Vérifie que `tileOne.frameIndex` vaut `tileTwo.frameIndex`. |
| **AnimatedTilesTest.Bitmask16ExclutLAnimation** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animated_tiles.cpp:198`</sub> | Le mode à raccords automatiques exclut l'animation. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::animationExcludedForTile(hmi::SkinMode::Bitmask16, core::TileType::Solid)` est vrai. |
| **AnimatedTilesTest.SilhouetteDetoureeExclutLAnimation** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animated_tiles.cpp:212`</sub> | Une tuile à silhouette détourée exclut l'animation. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::animationExcludedForTile(hmi::SkinMode::Single, core::TileType::SlopeUpRight)` est vrai. |
| **AnimatedTilesTest.SingleSansSilhouetteNExclutPasLAnimation** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animated_tiles.cpp:227`</sub> | Une tuile pleine en mode image simple n'exclut pas l'animation. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::animationExcludedForTile(hmi::SkinMode::Single, core::TileType::Solid)` est faux.<br/>Vérifie que `hmi::animationExcludedForTile(hmi::SkinMode::Single, core::TileType::Block)` est faux. |

**`test_animation_catalog.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **AnimationCatalogTest.RoundTripClipsMultiplesDureeParDefautEtOneShot** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animation_catalog.cpp:28`</sub> | Un descripteur valide se relit intégralement, durée par défaut et clip suivant compris. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `description.frameWidth` vaut `16`.<br/>Vérifie que `description.frameHeight` vaut `16`.<br/>Vérifie que `description.clips.clipCount()` vaut `3`.<br/>Vérifie que `closed.frames` vaut `(std::vector<int>{0})`.<br/>Vérifie que `closed.endMode` vaut `core::ClipEndMode::Loop`.<br/>Vérifie que `closed.frameDuration` vaut `hmi::AnimationCatalog::DEFAULT_FRAME_DURATION_SECONDS` (comparaison flottante).<br/>Vérifie que `opening.frames` vaut `(std::vector<int>{1, 2, 3, 4})`.<br/>Vérifie que `opening.frameDuration` vaut `0.06f` (comparaison flottante).<br/>Vérifie que `opening.endMode` vaut `core::ClipEndMode::OneShot`.<br/>Vérifie que `opening.nextClip` vaut `"open"`.<br/>Vérifie que `description.clips.indexOf(opening.nextClip)` est supérieur ou égal à `0`. |
| **AnimationCatalogTest.JsonInvalideEstUneErreurExploitable** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animation_catalog.cpp:66`</sub> | Un JSON invalide donne une erreur d'analyse avec un message exploitable. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `hmi::AnimationCatalogError::ParseError`.<br/>Vérifie que `result.error.empty()` est faux. |
| **AnimationCatalogTest.VersionInconnueEstRefusee** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animation_catalog.cpp:84`</sub> | Une version de format inconnue est refusée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `hmi::AnimationCatalogError::UnsupportedVersion`. |
| **AnimationCatalogTest.ClipSuivantInexistantEstRefuse** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animation_catalog.cpp:105`</sub> | Un clip suivant inexistant est refusé au chargement. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `hmi::AnimationCatalogError::MalformedStructure`. |
| **AnimationCatalogTest.IndiceDImageNegatifEstRefuse** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animation_catalog.cpp:127`</sub> | Un indice d'image négatif est refusé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `hmi::AnimationCatalogError::MalformedStructure`. |
| **AnimationCatalogTest.ClipSansFramesEstRefuse** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animation_catalog.cpp:148`</sub> | Un clip sans images est refusé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `hmi::AnimationCatalogError::MalformedStructure`. |
| **AnimationCatalogTest.FichierAbsentEstFileNotFoundSansException** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animation_catalog.cpp:169`</sub> | Un fichier absent donne un code dédié, sans exception. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `hmi::AnimationCatalogError::FileNotFound`. |
| **AnimationCatalogTest.DescriptorFileNameRemplaceLExtension** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animation_catalog.cpp:187`</sub> | Le nom du descripteur remplace l'extension en conservant le chemin. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::AnimationCatalog::descriptorFileName("water.png")` vaut `"water.anim.json"`.<br/>Vérifie que `hmi::AnimationCatalog::descriptorFileName("Skins/lava.png")` vaut `"Skins/lava.anim.json"`. |
| **AnimationCatalogTest.CoherenceAvecLePngValideeSurSpritesheetAUnRang** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animation_catalog.cpp:202`</sub> | Un descripteur cohérent avec les dimensions du PNG est validé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `valid.valid` est vrai. |
| **AnimationCatalogTest.TailleDImageIncoherenteAvecLePngEstRefusee** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animation_catalog.cpp:224`</sub> | Des dimensions de PNG incohérentes avec l'image sont refusées. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `wrongHeight.valid` est faux.<br/>Vérifie que `wrongHeight.message.empty()` est faux.<br/>Vérifie que `wrongWidth.valid` est faux. |
| **AnimationCatalogTest.IndiceDImageHorsBornesDeLaSpritesheetReelleEstRefuse** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animation_catalog.cpp:252`</sub> | Un indice d'image hors des bornes réelles de la spritesheet est refusé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `tooShort.valid` est faux.<br/>Vérifie que `tooShort.message.empty()` est faux. |
| **AnimationCatalogTest.FrameRegionPremiereEtDerniereImageSpritesheetAUnRang** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_animation_catalog.cpp:276`</sub> | La région d'une image se déduit de son indice par décalage horizontal, ordonnée constante. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `first.x` vaut `0`.<br/>Vérifie que `first.y` vaut `0`.<br/>Vérifie que `first.width` vaut `16`.<br/>Vérifie que `first.height` vaut `16`.<br/>Vérifie que `last.x` vaut `5 * 16`.<br/>Vérifie que `last.y` vaut `0`.<br/>Vérifie que `last.width` vaut `16`.<br/>Vérifie que `last.height` vaut `16`. |

**`test_asset_contract.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **AssetContractTest.SkinDeTuileConformeAccepte** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_asset_contract.cpp:22`</sub> | Un skin de tuile de 16x16 px est accepte. | 1. Valider un asset de la famille « skin de tuile » aux dimensions d'une case. | Vérifie que `validation.valid` est vrai.<br/>Vérifie que `validation.message.empty()` est vrai. |
| **AssetContractTest.SkinDeTuileNonConformeRefuseAvecMessage** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_asset_contract.cpp:41`</sub> | Un skin de tuile non conforme est refuse avec un message exploitable. | 1. Valider un skin de tuile de 24x16 px.<br/>2. Inspecter le message d'erreur. | Vérifie que `validation.valid` est faux.<br/>Vérifie que `contains(validation.message, "mur.png")` est vrai.<br/>Vérifie que `contains(validation.message, "24x16")` est vrai.<br/>Vérifie que `contains(validation.message, "16x16")` est vrai. |
| **AssetContractTest.SkinDeTuileAnimeSpritesheetHorizontaleAcceptee** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_asset_contract.cpp:62`</sub> | Une spritesheet animee (plusieurs cases de large, une case de haut) est acceptee. | 1. Valider un skin de tuile de 64x16 px (4 images). | Vérifie que `validation.valid` est vrai. |
| **AssetContractTest.SkinDeTuileSurPlusieursRangsRefuse** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_asset_contract.cpp:81`</sub> | Une image sur plusieurs rangs (hauteur non conforme) est refusee. | 1. Valider un skin de tuile de 16x32 px. | Vérifie que `validation.valid` est faux. |
| **AssetContractTest.PlancheARaccordsExigeQuatreCasesMinimum** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_asset_contract.cpp:99`</sub> | Une planche a raccords exige un multiple de case et au moins 4x4 cases. | 1. Valider une planche 4x4 cases, puis 3x4 cases, puis un multiple rompu. | Vérifie que `hmi::validateAsset(hmi::AssetFamily::AutotileSheet, "sol.png", 4 * TILE, 4 * TILE).valid` est vrai.<br/>Vérifie que `hmi::validateAsset(hmi::AssetFamily::AutotileSheet, "sol.png", 3 * TILE, 4 * TILE).valid` est faux.<br/>Vérifie que `hmi::validateAsset(hmi::AssetFamily::AutotileSheet, "sol.png", 4 * TILE + 1, 4 * TILE) .valid` est faux. |
| **AssetContractTest.FondDeNiveauSansContrainteDeGrille** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_asset_contract.cpp:121`</sub> | Un fond de niveau accepte toute dimension strictement positive. | 1. Valider un fond aux dimensions quelconques, puis un fond de largeur nulle. | Vérifie que `hmi::validateAsset(hmi::AssetFamily::Background, "foret.png", 640, 361).valid` est vrai.<br/>Vérifie que `hmi::validateAsset(hmi::AssetFamily::Background, "foret.png", 0, 361).valid` est faux. |
| **AssetContractTest.SpritesheetPersonnageMultipleDeCase** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_asset_contract.cpp:135`</sub> | Une spritesheet de personnage doit se decouper en cases entieres. | 1. Valider une spritesheet multiple de la case, puis une spritesheet decalee. | Vérifie que `hmi::validateAsset(hmi::AssetFamily::CharacterSheet, "heros.png", 5 * TILE, 3 * TILE) .valid` est vrai.<br/>Vérifie que `hmi::validateAsset(hmi::AssetFamily::CharacterSheet, "heros.png", 5 * TILE, 3 * TILE + 7) .valid` est faux. |
| **AssetContractTest.AtlasLivreConforme** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_asset_contract.cpp:156`</sub> | L'atlas historique reste conforme au contrat de sa famille. | 1. Valider les dimensions de la grille d'atlas livree. | Vérifie que `hmi::validateAsset(hmi::AssetFamily::Atlas, "atlas.png", gridSide, gridSide + 2 * hmi::TextureAtlas::PLAYER_FRAME_SIZE) .valid` est vrai. |
| **AssetContractTest.ToutesLesFamillesSontNommees** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_asset_contract.cpp:173`</sub> | Chaque famille d'asset porte un nom lisible non vide. | 1. Nommer chaque famille declaree. | Vérifie que `name.empty()` est faux.<br/>Vérifie que `name` diffère de `"Inconnu"`. |

**`test_asset_paths.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **AssetPathsTest.FichierExistantResolu** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_asset_paths.cpp:42`</sub> | Un fichier existant dans le dossier d'assets est résolu vers son chemin complet. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `resolved.has_value()` est vrai.<br/>Vérifie que `*resolved` vaut `filePath`. |
| **AssetPathsTest.AssetAbsentSignaleSansException** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_asset_paths.cpp:65`</sub> | Un asset absent est signalé par std::nullopt, sans exception. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `assetPaths.resolve("introuvable.png")` vaut `std::nullopt`. |
| **AssetPathsTest.DossierInexistantSansException** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_asset_paths.cpp:83`</sub> | Un dossier d'assets inexistant est traité comme rien à résoudre. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `assetPaths.resolve("atlas.png")` vaut `std::nullopt`. |
| **AssetPathsTest.DossierNestPasUnAssetValide** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_asset_paths.cpp:100`</sub> | Un chemin désignant un dossier n'est pas résolu comme un asset. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `assetPaths.resolve("sous-dossier")` vaut `std::nullopt`. |

**`test_background_fit.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **BackgroundFitTest.MemeRatioAucunRecadrage** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_background_fit.cpp:22`</sub> | Meme ratio : aucun recadrage. | 1. Calculer le recadrage d'une image carree sur une cible carree. | Vérifie que `fit.u0` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `fit.v0` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `fit.u1` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `fit.v1` vaut `1.0f` (comparaison flottante). |
| **BackgroundFitTest.ImagePlusLargeRecadreeHorizontalementEtCentree** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_background_fit.cpp:41`</sub> | Image plus large que la cible : recadrage horizontal centre. | 1. Calculer le recadrage d'une image 16:9 sur une cible carree. | Vérifie que `fit.v0` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `fit.v1` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `fit.u0` est strictement supérieur à `0.0f`.<br/>Vérifie que `fit.u1` est strictement inférieur à `1.0f`.<br/>Vérifie que `fit.u0` vaut `1.0f - fit.u1` (comparaison flottante).<br/>Vérifie que `fit.u1 - fit.u0` vaut `9.0f / 16.0f`, à `1e-4f` près. |
| **BackgroundFitTest.ImagePlusHauteRecadreeVerticalementEtCentree** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_background_fit.cpp:64`</sub> | Image plus haute que la cible : recadrage vertical centre. | 1. Calculer le recadrage d'une image 9:16 sur une cible carree. | Vérifie que `fit.u0` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `fit.u1` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `fit.v0` est strictement supérieur à `0.0f`.<br/>Vérifie que `fit.v1` est strictement inférieur à `1.0f`.<br/>Vérifie que `fit.v0` vaut `1.0f - fit.v1` (comparaison flottante).<br/>Vérifie que `fit.v1 - fit.v0` vaut `9.0f / 16.0f`, à `1e-4f` près. |
| **BackgroundFitTest.SansFondDesigneAucunQuadCompose** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_background_fit.cpp:85`</sub> | Sans fond designe, aucun quad n'est compose. | 1. Composer une scene sans texture de fond, en mode Texture. | Vérifie que `scene.size()` vaut `0u`. |
| **BackgroundFitTest.FondResoluEmetUnQuadSurLesBornesDuNiveau** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_background_fit.cpp:102`</sub> | Fond designe et resolu : quad emis sur Background, bornes du niveau. | 1. Composer une scene avec une texture de fond resolue, en mode Texture. | Vérifie que `recorder.size()` vaut `1u`.<br/>Vérifie que `recorder.countOnLayer(hmi::RenderLayer::Background)` vaut `1`.<br/>Vérifie que `recorder.containsSpriteAt(0.0f, 0.0f)` est vrai.<br/>Vérifie que `quad.sprite.width` vaut `20.0f` (comparaison flottante).<br/>Vérifie que `quad.sprite.height` vaut `12.0f` (comparaison flottante). |
| **BackgroundFitTest.ModePhysiqueAucunQuadDeFond** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_background_fit.cpp:129`</sub> | Mode Physique : aucun quad de fond, meme fond resolu. | 1. Composer la meme scene qu'en mode Texture, mais en mode Physique. | Vérifie que `scene.size()` vaut `0u`. |
| **BackgroundFitTest.RepliEnDamierEmetUnQuadCommeUnFondValide** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_background_fit.cpp:149`</sub> | Repli en damier : meme composition qu'un fond valide. | 1. Composer une scene avec une texture de repli (damier), en mode Texture. | Vérifie que `scene.size()` vaut `1u`.<br/>Vérifie que `scene.statistics().submitted` vaut `1`. |

**`test_bitmap_font.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **BitmapFontTest.MesureChaineVideLargeurNulle** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_bitmap_font.cpp:30`</sub> | La mesure d'une chaîne vide donne une largeur nulle et une hauteur d'une ligne. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `extent.width` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `extent.height` vaut `static_cast<float>(font.metrics.lineHeight)` (comparaison flottante). |
| **BitmapFontTest.MesureUnCaractere** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_bitmap_font.cpp:48`</sub> | La mesure d'un seul caractère donne exactement son avance. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `glyphA` diffère de `nullptr`.<br/>Vérifie que `extent.width` vaut `static_cast<float>(glyphA->advance)` (comparaison flottante).<br/>Vérifie que `extent.height` vaut `static_cast<float>(font.metrics.lineHeight)` (comparaison flottante). |
| **BitmapFontTest.MesureChaineAccentueeParPointsDeCode** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_bitmap_font.cpp:68`</sub> | Une chaîne accentuée est mesurée par points de code, pas par octets UTF-8. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `glyphE` diffère de `nullptr`.<br/>Vérifie que `extent.width` vaut `static_cast<float>(glyphE->advance) * 6.0f` (comparaison flottante). |
| **BitmapFontTest.CaractereAbsentEstSubstitue** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_bitmap_font.cpp:91`</sub> | Un caractère absent de la police est substitué par le glyphe de remplacement. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `substituted` diffère de `nullptr`.<br/>Vérifie que `replacement` diffère de `nullptr`.<br/>Vérifie que `substituted->x` vaut `replacement->x`.<br/>Vérifie que `substituted->y` vaut `replacement->y`. |
| **BitmapFontTest.MetriquesJsonValideEstLue** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_bitmap_font.cpp:112`</sub> | Un fichier de métriques JSON valide se lit correctement (champs et glyphes). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `result.metrics->lineHeight` vaut `10`.<br/>Vérifie que `result.metrics->replacementCodePoint` vaut `U'?'`.<br/>Vérifie que `glyphB` diffère de `nullptr`.<br/>Vérifie que `glyphB->x` vaut `6`.<br/>Vérifie que `glyphB->y` vaut `0`.<br/>Vérifie que `glyphB->width` vaut `6`.<br/>Vérifie que `glyphB->height` vaut `10`.<br/>Vérifie que `glyphB->advance` vaut `6`. |
| **BitmapFontTest.JsonInvalideEstUneErreurExploitable** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_bitmap_font.cpp:138`</sub> | Un JSON de métriques malformé est une erreur exploitable, jamais une exception. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `hmi::FontMetricsError::ParseError`.<br/>Vérifie que `result.error.empty()` est faux. |
| **BitmapFontTest.VersionInconnueEstRefusee** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_bitmap_font.cpp:156`</sub> | Une version de format supérieure à celle gérée est refusée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `hmi::FontMetricsError::UnsupportedVersion`. |
| **BitmapFontTest.FichierMetriquesAbsentEstFileNotFound** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_bitmap_font.cpp:176`</sub> | Un fichier de métriques absent est un échec FileNotFound, sans exception. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `hmi::FontMetricsError::FileNotFound`. |
| **BitmapFontTest.MetriquesCoherentesAvecLePngValidees** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_bitmap_font.cpp:193`</sub> | Des métriques cohérentes avec les dimensions du PNG sont validées. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `valid.valid` est vrai. |
| **BitmapFontTest.MetriquesIncoherentesAvecLePngRefusees** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_bitmap_font.cpp:212`</sub> | Des métriques dont un glyphe déborde des dimensions du PNG sont refusées. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `invalid.valid` est faux.<br/>Vérifie que `invalid.message.find("font.png")` diffère de `std::string::npos`. |
| **BitmapFontTest.ReplitProceduralCouvreAsciiEtAccentsDeFaconDeterministe** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_bitmap_font.cpp:233`</sub> | Le repli procédural couvre l'ASCII imprimable et les accents français, de façon déterministe. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `first.image.width` vaut `second.image.width`.<br/>Vérifie que `first.image.height` vaut `second.image.height`.<br/>Vérifie que `first.image.pixels` vaut `second.image.pixels`.<br/>Vérifie que `first.metrics.glyphs.size()` vaut `second.metrics.glyphs.size()`.<br/>Vérifie que `first.metrics.glyph(codePoint)` diffère de `nullptr`.<br/>Vérifie que `first.metrics.glyph(codePoint)` diffère de `nullptr`. |
| **BitmapFontTest.ReplitProceduralEstAutoCoherent** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_bitmap_font.cpp:271`</sub> | Chaque glyphe procédural désigne une région entièrement contenue dans l'image générée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `coherence.valid` est vrai. |

**`test_cache_registry.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **CacheRegistryTest.ChargeUneSeuleFoisPourUneCleRepetee** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_cache_registry.cpp:40`</sub> | Une clé demandée deux fois n'est chargée qu'une fois et rend la même entrée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `first` diffère de `nullptr`.<br/>Vérifie que `second` diffère de `nullptr`.<br/>Vérifie que `*first` vaut `42`.<br/>Vérifie que `second` vaut `first`.<br/>Vérifie que `loader.callCount()` vaut `1`. |
| **CacheRegistryTest.InvalidateForceLeRechargementDeLaCleSeule** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_cache_registry.cpp:65`</sub> | invalidate ne force le rechargement que de la clé visée, pas des autres. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaderA.callCount()` vaut `2`.<br/>Vérifie que `loaderB.callCount()` vaut `1`. |
| **CacheRegistryTest.InvalidateAllViseTout** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_cache_registry.cpp:92`</sub> | invalidateAll vide le registre et fait relire toutes les clés. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `registry.size()` vaut `2U`.<br/>Vérifie que `registry.size()` vaut `0U`.<br/>Vérifie que `loaderA.callCount()` vaut `2`.<br/>Vérifie que `loaderB.callCount()` vaut `2`. |
| **CacheRegistryTest.UnEchecEstMemoriseSansRetenterLeChargement** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_cache_registry.cpp:120`</sub> | Un échec de chargement est mémorisé : la clé absente n'est pas relue. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `first` vaut `nullptr`.<br/>Vérifie que `second` vaut `nullptr`.<br/>Vérifie que `loader.callCount()` vaut `1`. |
| **CacheRegistryTest.InvalidateSurUnEchecPermetUnNouvelEssai** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_cache_registry.cpp:143`</sub> | Invalider une clé en échec autorise un nouvel essai, qui peut réussir. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result` diffère de `nullptr`.<br/>Vérifie que `*result` vaut `7`. |

**`test_camera2d.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **Camera2DTest.CentreAuMilieuDeLEcran** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:22`</sub> | Le centre de la caméra se projette au centre de l'écran. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `screen.x` vaut `WIDTH * 0.5f`, à `TOLERANCE` près.<br/>Vérifie que `screen.y` vaut `HEIGHT * 0.5f`, à `TOLERANCE` près. |
| **Camera2DTest.CenterEtZoomRenvoientLesValeursPosees** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:42`</sub> | center() et zoom() renvoient exactement les valeurs posées. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `camera.center().x` vaut `3.0f` (comparaison flottante).<br/>Vérifie que `camera.center().y` vaut `4.0f` (comparaison flottante).<br/>Vérifie que `camera.zoom()` vaut `2.5f` (comparaison flottante). |
| **Camera2DTest.EchelleEtAxeY** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:61`</sub> | Une unité monde vaut 16 pixels ; l'axe Y va vers le bas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `right.x` vaut `WIDTH * 0.5f + 16.0f`, à `TOLERANCE` près.<br/>Vérifie que `right.y` vaut `HEIGHT * 0.5f`, à `TOLERANCE` près.<br/>Vérifie que `down.y` vaut `HEIGHT * 0.5f + 16.0f`, à `TOLERANCE` près. |
| **Camera2DTest.Zoom** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:83`</sub> | Le zoom multiplie l'échelle en pixels. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `right.x` vaut `WIDTH * 0.5f + 32.0f`, à `TOLERANCE` près. |
| **Camera2DTest.ConversionsReciproques** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:101`</sub> | `screenToWorld` est la réciproque de `worldToScreen`. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `roundTrip.x` vaut `world.x`, à `TOLERANCE` près.<br/>Vérifie que `roundTrip.y` vaut `world.y`, à `TOLERANCE` près. |
| **Camera2DTest.ProjectionCentreVersOrigineClip** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:122`</sub> | La matrice de projection envoie le centre de la caméra à l'origine du clip space. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.x` vaut `0.0f`, à `TOLERANCE` près.<br/>Vérifie que `result.y` vaut `0.0f`, à `TOLERANCE` près.<br/>Vérifie que `result.w` vaut `1.0f`, à `TOLERANCE` près. |
| **Camera2DTest.BordEcranVersBordClip** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:149`</sub> | Un coin de l'écran correspond à un bord du clip space (±1). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.x` vaut `1.0f`, à `TOLERANCE` près. |
| **Camera2DTest.FitZoomEntierPourPetitNiveau** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:173`</sub> | fitZoom reste entier tant que le facteur brut est supérieur ou égal à 1 (petit niveau). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `zoom` est supérieur ou égal à `1.0f`.<br/>Vérifie que `zoom` vaut `std::floor(zoom)` (comparaison flottante). |
| **Camera2DTest.FitZoomFractionnairePourGrandNiveau** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:192`</sub> | fitZoom devient fractionnaire pour un niveau plus grand que la surface disponible. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `zoom` est strictement supérieur à `0.0f`.<br/>Vérifie que `zoom` est strictement inférieur à `1.0f`.<br/>Vérifie que `100.0f * hmi::Camera2D::PIXELS_PER_UNIT * zoom` est inférieur ou égal à `1280.0f + TOLERANCE`.<br/>Vérifie que `100.0f * hmi::Camera2D::PIXELS_PER_UNIT * zoom` est inférieur ou égal à `720.0f + TOLERANCE`. |
| **Camera2DTest.FitZoomAppliqueLaMarge** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_camera2d.cpp:213`</sub> | fitZoom applique la marge avant l'arrondi. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `zoomSansMarge` vaut `5.0f` (comparaison flottante).<br/>Vérifie que `zoomAvecMarge` vaut `4.0f` (comparaison flottante). |

**`test_decor_visuals.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **DecorVisualsTest.DecorRenderLayerProjetteLesTroisCouches** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_decor_visuals.cpp:91`</sub> | decorRenderLayer projette les trois couches sur les deux calques reserves. | 1. Appeler decorRenderLayer pour chaque DecorLayer. | Vérifie que `hmi::decorRenderLayer(DecorLayer::Background)` vaut `hmi::RenderLayer::Decor`.<br/>Vérifie que `hmi::decorRenderLayer(DecorLayer::Decor)` vaut `hmi::RenderLayer::Decor`.<br/>Vérifie que `hmi::decorRenderLayer(DecorLayer::Foreground)` vaut `hmi::RenderLayer::Foreground`. |
| **DecorVisualsTest.ResolveDecorAppearanceRenvoieLAssetCharge** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_decor_visuals.cpp:108`</sub> | resolveDecorAppearance renvoie l'asset charge. | 1. Resoudre l'apparence d'un decor dont l'asset est charge. | Vérifie que `appearance.texture` vaut `decorTexture`.<br/>Vérifie que `appearance.pixelWidth` vaut `48`.<br/>Vérifie que `appearance.pixelHeight` vaut `32`. |
| **DecorVisualsTest.ResolveDecorAppearanceRetombeSurLeDamierSiIntrouvable** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_decor_visuals.cpp:127`</sub> | resolveDecorAppearance retombe sur le damier si l'asset est introuvable. | 1. Resoudre l'apparence d'un decor dont l'asset n'est pas charge. | Vérifie que `appearance.texture` vaut `missingTexture`.<br/>Vérifie que `appearance.pixelWidth` vaut `hmi::MISSING_TEXTURE_SIZE`.<br/>Vérifie que `appearance.pixelHeight` vaut `hmi::MISSING_TEXTURE_SIZE`. |
| **DecorVisualsTest.AucunDecorComposeEnModePhysique** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_decor_visuals.cpp:146`</sub> | Aucun decor n'est compose en mode Physique. | 1. Composer un monde contenant un decor, en mode Physique. | Vérifie que `scene.size()` vaut `0u`. |
| **DecorVisualsTest.DecorEnModeTextureProduitUnQuadALaTailleDeSonAsset** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_decor_visuals.cpp:166`</sub> | Un decor en mode Texture produit un quad a la taille de son asset. | 1. Composer un monde contenant un decor a l'echelle 2, en mode Texture. | Vérifie que `scene.size()` vaut `1u`.<br/>Vérifie que `quad.x` vaut `3.0f` (comparaison flottante).<br/>Vérifie que `quad.y` vaut `4.0f` (comparaison flottante).<br/>Vérifie que `quad.width` vaut `48.0f / hmi::Camera2D::PIXELS_PER_UNIT * 2.0f` (comparaison flottante).<br/>Vérifie que `quad.height` vaut `32.0f / hmi::Camera2D::PIXELS_PER_UNIT * 2.0f` (comparaison flottante).<br/>Vérifie que `scene.quads()[0].texture` vaut `decorTexture`. |
| **DecorVisualsTest.LaRotationDUnDecorAtteintLeQuadCompose** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_decor_visuals.cpp:194`</sub> | La rotation d'un decor atteint le quad compose. | 1. Composer un decor dont le Transform porte une rotation non nulle. | Vérifie que `scene.size()` vaut `1u`.<br/>Vérifie que `scene.quads()[0].sprite.rotation` vaut `1.2f` (comparaison flottante). |
| **DecorVisualsTest.PremierPlanApresLePersonnageArrierePlanAvantLesTuiles** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_decor_visuals.cpp:216`</sub> | Premier plan apres le personnage, arriere-plan avant les tuiles. | 1. Composer un monde avec un decor d'arriere-plan, une tuile, le personnage et un decor de premier plan. | Vérifie que `recorder.isLayerOrderRespected()` est vrai.<br/>Vérifie que `sequence.size()` vaut `4u`.<br/>Vérifie que `sequence[0]` vaut `hmi::RenderLayer::Decor`.<br/>Vérifie que `sequence[1]` vaut `hmi::RenderLayer::Tile`.<br/>Vérifie que `sequence[2]` vaut `hmi::RenderLayer::Player`.<br/>Vérifie que `sequence[3]` vaut `hmi::RenderLayer::Foreground`. |
| **DecorVisualsTest.OrdreIntraCoucheRespecteLeRangDuVecteur** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_decor_visuals.cpp:250`</sub> | Deux decors de la meme couche respectent l'ordre du vecteur. | 1. Composer deux decors de meme asset et de meme couche, rangs 0 puis 1. | Vérifie que `scene.size()` vaut `2u`.<br/>Vérifie que `scene.quads()[0].sprite.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `scene.quads()[1].sprite.x` vaut `5.0f` (comparaison flottante). |

**`test_image_encode.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **ImageEncode.AllerRetourExactAlphaCompris** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_image_encode.cpp:73`</sub> | Encoder puis decoder une image restitue exactement les memes pixels, alpha compris. | 1. Encoder une image 2x2 couvrant plusieurs niveaux d'alpha vers un fichier PNG.<br/> 2. Decoder ce fichier. | Vérifie que `hmi::encodeImageFile(path, original)` est vrai.<br/>Vérifie que `decoded.has_value()` est vrai.<br/>Vérifie que `decoded->width` vaut `original.width`.<br/>Vérifie que `decoded->height` vaut `original.height`.<br/>Vérifie que `decoded->pixels` vaut `original.pixels`. |
| **ImageEncode.DossierInexistantEchoueProprement** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_image_encode.cpp:98`</sub> | Encoder vers un dossier inexistant echoue proprement, sans exception. | 1. Tenter d'encoder une image vers un chemin dont le dossier parent n'existe pas. | `EXPECT_NO_THROW(result = hmi::encodeImageFile(path, original))`<br/>Vérifie que `result` est faux. |
| **ImageEncode.DimensionsIncoherentesEchouentProprement** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_image_encode.cpp:117`</sub> | Encoder une image aux dimensions incoherentes echoue proprement. | 1. Construire une image dont le nombre de pixels ne correspond pas a largeur*hauteur. <br/>2. Tenter de l'encoder. | `EXPECT_NO_THROW(result = hmi::encodeImageFile(path, broken))`<br/>Vérifie que `result` est faux.<br/>Vérifie que `fileCount()` vaut `0U`. |
| **ImageEncode.AucunFichierTemporaireApresSucces** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_image_encode.cpp:140`</sub> | Aucun fichier temporaire ne subsiste apres un encodage reussi. | 1. Encoder une image vers un fichier.<br/>2. Lister le dossier de destination. | Vérifie que `hmi::encodeImageFile(dir / "asset.png", sampleImage())` est vrai.<br/>Vérifie que `fileCount()` vaut `1U`.<br/>Vérifie que `std::filesystem::exists(dir / "asset.png")` est vrai. |
| **ImageEncode.EcrasementAtomiqueSansResidu** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_image_encode.cpp:158`</sub> | Enregistrer par-dessus un asset existant remplace son contenu sans residu. | 1. Encoder une premiere image vers un fichier.<br/>2. Encoder une seconde image differente vers le meme fichier.<br/>3. Decoder le fichier et lister le dossier. | Vérifie que `hmi::encodeImageFile(path, sampleImage())` est vrai.<br/>Vérifie que `hmi::encodeImageFile(path, second)` est vrai.<br/>Vérifie que `decoded.has_value()` est vrai.<br/>Vérifie que `decoded->width` vaut `1`.<br/>Vérifie que `decoded->height` vaut `1`.<br/>Vérifie que `fileCount()` vaut `1U`. |

**`test_layer_visibility.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LayerVisibilityTest.TousLesCalquesSontVisiblesParDefaut** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_layer_visibility.cpp:85`</sub> | Tous les calques sont visibles par defaut. | 1. Construire un jeu de visibilités par defaut.<br/>2. Interroger chaque calque. | Vérifie que `visibility.visible(hmi::RenderLayer::Background)` est vrai.<br/>Vérifie que `visibility.visible(hmi::RenderLayer::Decor)` est vrai.<br/>Vérifie que `visibility.visible(hmi::RenderLayer::Shadow)` est vrai.<br/>Vérifie que `visibility.visible(hmi::RenderLayer::Tile)` est vrai.<br/>Vérifie que `visibility.visible(hmi::RenderLayer::Object)` est vrai.<br/>Vérifie que `visibility.visible(hmi::RenderLayer::Player)` est vrai.<br/>Vérifie que `visibility.visible(hmi::RenderLayer::Foreground)` est vrai.<br/>Vérifie que `visibility.visible(hmi::RenderLayer::UI)` est vrai.<br/>Vérifie que `visibility.visible(hmi::RenderLayer::EditorOverlay)` est vrai. |
| **LayerVisibilityTest.MasquerUnCalqueNeChangeQueCeCalque** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_layer_visibility.cpp:108`</sub> | Masquer un calque ne change que ce calque. | 1. Masquer le calque Décor.<br/>2. Interroger Décor et Personnage. | Vérifie que `visibility.visible(hmi::RenderLayer::Decor)` est faux.<br/>Vérifie que `visibility.visible(hmi::RenderLayer::Player)` est vrai. |
| **LayerVisibilityTest.ShowAllRetablitTousLesCalques** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_layer_visibility.cpp:125`</sub> | showAll retablit tous les calques. | 1. Masquer plusieurs calques.<br/>2. Appeler showAll. | Vérifie que `visibility.visible(hmi::RenderLayer::Background)` est vrai.<br/>Vérifie que `visibility.visible(hmi::RenderLayer::Player)` est vrai.<br/>Vérifie que `visibility.visible(hmi::RenderLayer::Foreground)` est vrai. |
| **LayerVisibilityTest.CalqueMasqueNEmetAucunePrimitiveLesAutresInchanges** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_layer_visibility.cpp:148`</sub> | Un calque masque n'emet aucune primitive ; les autres sont inchanges. | 1. Peupler une entite Personnage et une entite Decor de premier plan.<br/>2. Masquer Personnage et composer en mode Texture. | Vérifie que `recorder.countOnLayer(hmi::RenderLayer::Player)` vaut `0`.<br/>Vérifie que `recorder.countOnLayer(hmi::RenderLayer::Foreground)` vaut `1`. |
| **LayerVisibilityTest.CombinaisonsDeuxUnSeulAucun** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_layer_visibility.cpp:177`</sub> | Combinaisons de visibilite : deux, un seul, aucun. | 1. Peupler un Decor d'arriere-plan et un Personnage.<br/>2. Composer en mode Texture avec les deux visibles, puis un seul, puis aucun. | Vérifie que `scene.size()` vaut `2u`.<br/>Vérifie que `scene.size()` vaut `1u`.<br/>Vérifie que `scene.quads().front().layer` vaut `hmi::RenderLayer::Decor`.<br/>Vérifie que `scene.size()` vaut `0u`. |
| **LayerVisibilityTest.DefautNeutreSurLaCompositionDUneScene** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_layer_visibility.cpp:224`</sub> | Un jeu de visibilite par defaut est neutre sur la composition. | 1. Peupler tuile, decor et personnage.<br/>2. Composer avec et sans jeu de visibilites explicite, en mode Texture. | Vérifie que `withoutVisibility.size()` vaut `withDefaultVisibility.size()`.<br/>Vérifie que `withoutVisibility.size()` vaut `3u`. |

**`test_mechanism_transitions.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **MechanismTransitionsTest.PremierCalculRejointEtatCibleSansTransition** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_transitions.cpp:71`</sub> | Le premier calcul rejoint l'état cible sans jouer de transition. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `state.animation.clips->clipAt(state.animation.clipIndex).name` vaut `MECHANISM_CLIP_DOOR_OPEN`. |
| **MechanismTransitionsTest.ChangementDetatDeclencheLaTransitionUneSeuleFois** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_transitions.cpp:95`</sub> | Un changement d'état déclenche la transition une seule fois, puis la laisse aboutir. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `state.animation.clips->clipAt(state.animation.clipIndex).name` vaut `MECHANISM_CLIP_DOOR_CLOSED`.<br/>Vérifie que `state.animation.clips->clipAt(state.animation.clipIndex).name` vaut `MECHANISM_CLIP_DOOR_OPENING`.<br/>Vérifie que `state.animation.clips->clipAt(state.animation.clipIndex).name` vaut `MECHANISM_CLIP_DOOR_OPEN`. |
| **MechanismTransitionsTest.ClipDeTransitionAbsentBasculeDirectementSansAvertissement** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_transitions.cpp:131`</sub> | Un clip de transition absent fait basculer directement, sans avertissement. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `state.animation.clips->clipAt(state.animation.clipIndex).name` vaut `MECHANISM_CLIP_DOOR_OPEN`.<br/>Vérifie que `warned.empty()` est vrai. |
| **MechanismTransitionsTest.ClipDEtatAbsentReplieEtJournaliseUneSeuleFois** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_transitions.cpp:166`</sub> | Un clip d'état absent se replie et n'est journalisé qu'une fois par asset. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `warned.size()` vaut `1u`.<br/>Vérifie que `warned.size()` vaut `1u`. |
| **MechanismTransitionsTest.AucunChangementNeReinitialisePasLimageEnCours** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_transitions.cpp:197`</sub> | À état inchangé, un appel supplémentaire ne réinitialise pas l'image en cours. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `state.animation.clips->clipAt(state.animation.clipIndex).name` vaut `MECHANISM_CLIP_DOOR_OPENING`.<br/>Vérifie que `state.animation.frameIndex` vaut `1`.<br/>Vérifie que `state.animation.frameIndex` vaut `1`. |
| **MechanismTransitionsTest.SeuleLaPorteTransitionneLesAutresBasculentDirectement** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_transitions.cpp:228`</sub> | Un interrupteur bascule directement entre ses deux états, sans avertissement. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `state.animation.clips->clipAt(state.animation.clipIndex).name` vaut `MECHANISM_CLIP_SWITCH_ACTIVE`.<br/>Vérifie que `warned.empty()` est vrai. |

**`test_mechanism_visuals.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **MechanismVisualsTest.DoorTargetClips** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_visuals.cpp:14`</sub> | La porte demande le clip fermé ou ouvert selon son état logique. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mechanismTargetClip(core::TileType::Door, false)` vaut `MECHANISM_CLIP_DOOR_CLOSED`.<br/>Vérifie que `mechanismTargetClip(core::TileType::Door, true)` vaut `MECHANISM_CLIP_DOOR_OPEN`. |
| **MechanismVisualsTest.SwitchTargetClips** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_visuals.cpp:28`</sub> | L'interrupteur demande le clip inactif ou actif selon son état. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mechanismTargetClip(core::TileType::Switch, false)` vaut `MECHANISM_CLIP_SWITCH_INACTIVE`.<br/>Vérifie que `mechanismTargetClip(core::TileType::Switch, true)` vaut `MECHANISM_CLIP_SWITCH_ACTIVE`. |
| **MechanismVisualsTest.PressurePlateTargetClips** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_visuals.cpp:42`</sub> | La plaque de pression demande le clip relâché ou enfoncé selon son état. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mechanismTargetClip(core::TileType::PressurePlate, false)` vaut `MECHANISM_CLIP_PLATE_RELEASED`.<br/>Vérifie que `mechanismTargetClip(core::TileType::PressurePlate, true)` vaut `MECHANISM_CLIP_PLATE_PRESSED`. |
| **MechanismVisualsTest.DangerSwitchedTargetClips** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_visuals.cpp:58`</sub> | Le danger commutable demande le clip inactif ou actif selon son état. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mechanismTargetClip(core::TileType::DangerSwitched, false)` vaut `MECHANISM_CLIP_DANGER_SWITCHED_INACTIVE`.<br/>Vérifie que `mechanismTargetClip(core::TileType::DangerSwitched, true)` vaut `MECHANISM_CLIP_DANGER_SWITCHED_ACTIVE`. |
| **MechanismVisualsTest.DangerBlinkTargetClips** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_visuals.cpp:75`</sub> | Le danger clignotant demande le clip inoffensif ou mortel selon sa phase. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mechanismTargetClip(core::TileType::DangerBlink, false)` vaut `MECHANISM_CLIP_DANGER_BLINK_HARMLESS`.<br/>Vérifie que `mechanismTargetClip(core::TileType::DangerBlink, true)` vaut `MECHANISM_CLIP_DANGER_BLINK_LETHAL`. |
| **MechanismVisualsTest.DangerMoverHasSingleClipIrrespectiveOfActive** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_visuals.cpp:92`</sub> | Le danger mobile a un seul clip, quel que soit l'état passé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mechanismTargetClip(core::TileType::DangerMover, false)` vaut `MECHANISM_CLIP_DANGER_MOVER_IDLE`.<br/>Vérifie que `mechanismTargetClip(core::TileType::DangerMover, true)` vaut `MECHANISM_CLIP_DANGER_MOVER_IDLE`. |
| **MechanismVisualsTest.StatelessTileProducesNoClipRequest** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_visuals.cpp:109`</sub> | Une tuile sans état ne demande aucun clip et n'est pas un mécanisme. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mechanismTargetClip(core::TileType::Solid, false).has_value()` est faux.<br/>Vérifie que `mechanismTargetClip(core::TileType::Block, true).has_value()` est faux.<br/>Vérifie que `mechanismTargetClip(core::TileType::Empty, false).has_value()` est faux.<br/>Vérifie que `isStatefulMechanism(core::TileType::Solid)` est faux.<br/>Vérifie que `isStatefulMechanism(core::TileType::Block)` est faux. |
| **MechanismVisualsTest.StatefulMechanismsAreRecognized** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_visuals.cpp:127`</sub> | Les six familles de mécanismes à état sont reconnues. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `isStatefulMechanism(core::TileType::Door)` est vrai.<br/>Vérifie que `isStatefulMechanism(core::TileType::Switch)` est vrai.<br/>Vérifie que `isStatefulMechanism(core::TileType::PressurePlate)` est vrai.<br/>Vérifie que `isStatefulMechanism(core::TileType::DangerSwitched)` est vrai.<br/>Vérifie que `isStatefulMechanism(core::TileType::DangerBlink)` est vrai.<br/>Vérifie que `isStatefulMechanism(core::TileType::DangerMover)` est vrai. |
| **MechanismVisualsTest.DoorTransitionsOnStateChangeOnly** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_visuals.cpp:147`</sub> | La porte ne demande une transition que sur un changement d'état. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `opening.has_value()` est vrai.<br/>Vérifie que `*opening` vaut `MECHANISM_CLIP_DOOR_OPENING`.<br/>Vérifie que `closing.has_value()` est vrai.<br/>Vérifie que `*closing` vaut `MECHANISM_CLIP_DOOR_CLOSING`.<br/>Vérifie que `mechanismTransitionClip(core::TileType::Door, true, true).has_value()` est faux.<br/>Vérifie que `mechanismTransitionClip(core::TileType::Door, false, false).has_value()` est faux. |
| **MechanismVisualsTest.OnlyDoorTransitionsOtherFamiliesSnapDirectly** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_visuals.cpp:171`</sub> | Seule la porte demande une transition ; les autres familles basculent directement. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mechanismTransitionClip(core::TileType::Switch, false, true).has_value()` est faux.<br/>Vérifie que `mechanismTransitionClip(core::TileType::PressurePlate, false, true).has_value()` est faux.<br/>Vérifie que `mechanismTransitionClip(core::TileType::DangerSwitched, false, true).has_value()` est faux.<br/>Vérifie que `mechanismTransitionClip(core::TileType::DangerBlink, false, true).has_value()` est faux.<br/>Vérifie que `mechanismTransitionClip(core::TileType::DangerMover, false, true).has_value()` est faux. |
| **MechanismVisualsTest.ExpectedClipsMatchTargetAndTransitionNames** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_visuals.cpp:191`</sub> | Les clips attendus par famille correspondent aux états et transitions réellement demandés. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `doorClips` vaut `(std::vector<std::string>{MECHANISM_CLIP_DOOR_CLOSED, MECHANISM_CLIP_DOOR_OPENING, MECHANISM_CLIP_DOOR_OPEN, MECHANISM_CLIP_DOOR_CLOSING})`.<br/>Vérifie que `mechanismExpectedClips(core::TileType::DangerMover)` vaut `(std::vector<std::string>{MECHANISM_CLIP_DANGER_MOVER_IDLE})`.<br/>Vérifie que `mechanismExpectedClips(core::TileType::Solid).empty()` est vrai. |
| **MechanismVisualsTest.TextureModeAlwaysFullyOpaque** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_visuals.cpp:212`</sub> | En mode Texture, l'opacité de diagnostic vaut toujours 1. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mechanismDiagnosticAlpha(RenderMode::Texture, true, 0.25f, 1.0f)` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `mechanismDiagnosticAlpha(RenderMode::Texture, false, 0.25f, 1.0f)` vaut `1.0f` (comparaison flottante). |
| **MechanismVisualsTest.PhysiqueModeUsesTheProvidedAlphaPair** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_mechanism_visuals.cpp:228`</sub> | En mode Physique, la paire d'opacités fournie est utilisée dans les deux sens. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mechanismDiagnosticAlpha(RenderMode::Physique, true, 0.25f, 1.0f)` vaut `0.25f` (comparaison flottante).<br/>Vérifie que `mechanismDiagnosticAlpha(RenderMode::Physique, false, 0.25f, 1.0f)` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `mechanismDiagnosticAlpha(RenderMode::Physique, true, 1.0f, 0.35f)` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `mechanismDiagnosticAlpha(RenderMode::Physique, false, 1.0f, 0.35f)` vaut `0.35f` (comparaison flottante). |

**`test_missing_texture.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **MissingTextureTest.DimensionsAttendues** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_missing_texture.cpp:27`</sub> | Le damier genere a les dimensions demandees. | 1. Generer le damier par defaut, puis un damier de 32 px. | Vérifie que `image.width` vaut `hmi::MISSING_TEXTURE_SIZE`.<br/>Vérifie que `image.height` vaut `hmi::MISSING_TEXTURE_SIZE`.<br/>Vérifie que `image.pixels.size()` vaut `static_cast<std::size_t>(image.width) * static_cast<std::size_t>(image.height)`.<br/>Vérifie que `large.width` vaut `32`.<br/>Vérifie que `large.height` vaut `32`. |
| **MissingTextureTest.GenerationDeterministe** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_missing_texture.cpp:49`</sub> | La generation du damier est deterministe. | 1. Generer deux fois le damier. | Vérifie que `hmi::buildMissingTextureImage().pixels` vaut `hmi::buildMissingTextureImage().pixels`. |
| **MissingTextureTest.CarreauxAlternesMagentaEtNoir** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_missing_texture.cpp:63`</sub> | Les carreaux du damier alternent magenta et noir. | 1. Generer le damier.<br/>2. Lire un pixel de chacun des quatre premiers carreaux. | Vérifie que `pixelAt(image, 0, 0)` vaut `MAGENTA`.<br/>Vérifie que `pixelAt(image, STEP, 0)` vaut `BLACK`.<br/>Vérifie que `pixelAt(image, 0, STEP)` vaut `BLACK`.<br/>Vérifie que `pixelAt(image, STEP, STEP)` vaut `MAGENTA`. |
| **MissingTextureTest.DamierEntierementOpaque** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_missing_texture.cpp:84`</sub> | Tous les pixels du damier sont opaques. | 1. Generer le damier.<br/>2. Verifier le canal alpha de chaque pixel. | Vérifie que `pixel >> 24` vaut `0xFFu`. |
| **MissingTextureTest.AvertissementNommeAsset** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_missing_texture.cpp:102`</sub> | L'avertissement de texture manquante nomme l'asset attendu. | 1. Construire le message pour un asset donne. | Vérifie que `message.find("Backgrounds/foret.png")` diffère de `std::string::npos`. |

**`test_parallax.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **ParallaxTest.LeFacteurDeLaCoucheDecorVautUn** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_parallax.cpp:63`</sub> | Le facteur de la couche Decor vaut 1.0, valeur de reference. | 1. Lire parallaxFactor pour les trois couches. | Vérifie que `hmi::parallaxFactor(DecorLayer::Decor)` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `hmi::parallaxFactor(DecorLayer::Background)` est strictement inférieur à `1.0f`.<br/>Vérifie que `hmi::parallaxFactor(DecorLayer::Foreground)` est strictement supérieur à `1.0f`. |
| **ParallaxTest.FacteurUnLaissePositionInchangee** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_parallax.cpp:79`</sub> | Facteur 1 laisse la position inchangee. | 1. Calculer la position de rendu d'un decor a facteur 1.0. | Vérifie que `rendered.x` vaut `position.x` (comparaison flottante).<br/>Vérifie que `rendered.y` vaut `position.y` (comparaison flottante). |
| **ParallaxTest.FacteursOpposesProduisentDesDecalagesOpposesEtProportionnels** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_parallax.cpp:99`</sub> | Des facteurs opposes autour de 1 produisent des decalages opposes. | 1. Calculer le decalage pour un facteur inferieur et un facteur superieur a 1, equidistants. | Vérifie que `offsetLower` vaut `-offsetHigher` (comparaison flottante).<br/>Vérifie que `offsetLower` diffère de `0.0f`. |
| **ParallaxTest.MemePositionRelativeDonneMemePositionEcranEntreSalles** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_parallax.cpp:124`</sub> | Meme position relative -> meme position ecran dans deux salles. | 1. Placer un decor a la meme position RELATIVE dans deux salles de centres differents.<br/>2. Calculer sa position de rendu dans chaque salle. | Vérifie que `screen1.x` vaut `screen2.x` (comparaison flottante).<br/>Vérifie que `screen1.y` vaut `screen2.y` (comparaison flottante). |
| **ParallaxTest.RoundToScreenPixelArrondiAuPixelEcran** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_parallax.cpp:157`</sub> | roundToScreenPixel arrondit au pixel ecran le plus proche. | 1. Arrondir une position fractionnaire a 16 px/unite. | Vérifie que `rounded.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `rounded.y` vaut `2.5f` (comparaison flottante). |
| **ParallaxTest.ParallaxModelPositionEstLInverseDeParallaxRenderPosition** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_parallax.cpp:174`</sub> | parallaxModelPosition est l'inverse de parallaxRenderPosition. | 1. Calculer la position de rendu d'une position modele (facteur != 1).<br/>2. Appliquer parallaxModelPosition au resultat. | Vérifie que `roundTrip.x` vaut `modelPosition.x` (comparaison flottante).<br/>Vérifie que `roundTrip.y` vaut `modelPosition.y` (comparaison flottante). |
| **ParallaxTest.ParallaxModelPositionFacteurUnLaissePositionInchangee** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_parallax.cpp:197`</sub> | parallaxModelPosition a facteur 1 laisse la position inchangee. | 1. Appliquer parallaxModelPosition a facteur 1.0. | Vérifie que `model.x` vaut `renderPosition.x` (comparaison flottante).<br/>Vérifie que `model.y` vaut `renderPosition.y` (comparaison flottante). |
| **ParallaxTest.DecorParallaxeVisibleNEstPasEcarte** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_parallax.cpp:218`</sub> | Un decor parallaxe visible n'est pas ecarte par le culling. | 1. Composer un decor hors cadre a sa position simulee mais dont le facteur arriere-plan le ramene dans le cadrage. | Vérifie que `scene.size()` vaut `0u`.<br/>Vérifie que `scene.size()` vaut `1u`. |
| **ParallaxTest.DecorParallaxeHorsCadreEstEcarte** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_parallax.cpp:257`</sub> | Un decor parallaxe hors cadre est ecarte par le culling. | 1. Composer un decor dans le cadrage a sa position simulee mais dont le facteur premier plan le pousse hors du cadrage. | Vérifie que `scene.size()` vaut `1u`.<br/>Vérifie que `scene.size()` vaut `0u`. |

**`test_player_sprite.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **PlayerSpriteTest.AncrageImageDeMemeTailleQueLaHitbox** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_player_sprite.cpp:31`</sub> | Image de la taille de la hitbox : quad sans decalage. | 1. Calculer le quad pour une image 16x16 px (1x1 unite) et une hitbox 1x1 unite. | Vérifie que `quad.size.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `quad.size.y` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `quad.offset.x` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `quad.offset.y` vaut `0.0f` (comparaison flottante). |
| **PlayerSpriteTest.ImagePlusGrandeCentreeEtAncreeParLeBas** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_player_sprite.cpp:53`</sub> | Image plus grande que la hitbox : centree horizontalement, alignee par le bas. | 1. Calculer le quad pour une image 32x32 px et la hitbox reelle du personnage (`core::playerSize()`). | Vérifie que `quad.size.x` vaut `imageWidthWorld` (comparaison flottante).<br/>Vérifie que `quad.size.y` vaut `imageHeightWorld` (comparaison flottante).<br/>Vérifie que `quad.offset.x` vaut `(hitboxSize.x - imageWidthWorld) * 0.5f` (comparaison flottante).<br/>Vérifie que `quad.offset.x` est strictement inférieur à `0.0f`.<br/>Vérifie que `quad.offset.y + quad.size.y` vaut `hitboxSize.y` (comparaison flottante). |
| **PlayerSpriteTest.ImagePlusGrandeNeDeplacePasLaHitbox** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_player_sprite.cpp:80`</sub> | Le calcul du quad ne modifie jamais la hitbox (fonction pure). | 1. Calculer le quad pour plusieurs tailles d'image, avec la meme hitbox. | Vérifie que `small.size.x` diffère de `large.size.x`.<br/>Vérifie que `hitboxSize.x` vaut `core::playerSize().x`.<br/>Vérifie que `hitboxSize.y` vaut `core::playerSize().y`. |
| **PlayerSpriteTest.AncrageHorizontalSymetrique** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_player_sprite.cpp:106`</sub> | L'ancrage horizontal est symetrique (prealable au retournement). | 1. Calculer le quad d'une image plus large que la hitbox. | Vérifie que `overflowLeft` vaut `overflowRight` (comparaison flottante). |
| **PlayerSpriteTest.ClipDeclareUtiliseTelQuel** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_player_sprite.cpp:125`</sub> | Un clip declare est utilise tel quel. | 1. Resoudre "run" dans un jeu qui le declare. | Vérifie que `hmi::resolveDeclaredPlayerClip(declared, "fall")` vaut `"fall"`. |
| **PlayerSpriteTest.ClipsLot48RepliSurLAtlasProcedural** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_player_sprite.cpp:139`</sub> | Chute/atterrissage/glissade/dash retombent sur le clip le plus proche. | 1. Resoudre fall/land/wallslide/dash dans le jeu procedural (idle/run/jump seulement). | Vérifie que `hmi::resolveDeclaredPlayerClip(declared, "fall")` vaut `"jump"`.<br/>Vérifie que `hmi::resolveDeclaredPlayerClip(declared, "land")` vaut `"idle"`.<br/>Vérifie que `hmi::resolveDeclaredPlayerClip(declared, "wallslide")` vaut `"jump"`.<br/>Vérifie que `hmi::resolveDeclaredPlayerClip(declared, "dash")` vaut `"run"`. |
| **PlayerSpriteTest.AucunClipDeclareRetombeSurIdle** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_player_sprite.cpp:158`</sub> | Aucun clip declare : repli final sur idle. | 1. Resoudre "dash" dans un jeu totalement vide. | Vérifie que `hmi::resolveDeclaredPlayerClip(declared, "dash")` vaut `"idle"`. |
| **PlayerSpriteTest.NomsProceduralsSontIdleRunJump** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_player_sprite.cpp:173`</sub> | L'atlas procedural ne declare que idle/run/jump. | 1. Lire proceduralPlayerClipNames(). | Vérifie que `hmi::proceduralPlayerClipNames()` vaut `expected`. |
| **PlayerSpriteTest.ModePhysiqueIgnoreLeTag** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_player_sprite.cpp:215`</sub> | RenderMode::Physique ignore PlayerSpriteTag. | 1. Composer un personnage taggue en RenderMode::Physique. | Vérifie que `recorder.size()` vaut `1u`.<br/>Vérifie que `recorder.containsSpriteAt(5.0f, 5.0f)` est vrai. |
| **PlayerSpriteTest.ModeTextureAppliqueLeQuadDuTag** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_player_sprite.cpp:243`</sub> | RenderMode::Texture applique le quad du tag. | 1. Composer un personnage taggue (quad plus grand que la hitbox) en RenderMode::Texture. | Vérifie que `scene.quads().size()` vaut `1u`.<br/>Vérifie que `quad.x` vaut `5.0f - 0.5f` (comparaison flottante).<br/>Vérifie que `quad.y` vaut `5.0f - 1.0f` (comparaison flottante).<br/>Vérifie que `quad.width` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `quad.height` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `scene.quads().front().texture` vaut `characterSheetTexture`. |
| **PlayerSpriteTest.ReplinProceduralLieLAtlasEnModeTexture** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_player_sprite.cpp:276`</sub> | Sans spritesheet, RenderMode::Texture lie l'atlas. | 1. Composer un personnage taggue avec usesCharacterSheet=false. | Vérifie que `scene.quads().size()` vaut `1u`.<br/>Vérifie que `scene.quads().front().texture` vaut `atlasTexture`. |
| **PlayerSpriteTest.RetournementEchangeUvSansDeplacerLeQuad** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_player_sprite.cpp:301`</sub> | Le retournement echange u0/u1 sans deplacer le quad. | 1. Composer le meme personnage, flipHorizontal=false puis true. | Vérifie que `right.u0` vaut `left.u1` (comparaison flottante).<br/>Vérifie que `right.u1` vaut `left.u0` (comparaison flottante).<br/>Vérifie que `right.x` vaut `left.x` (comparaison flottante).<br/>Vérifie que `right.y` vaut `left.y` (comparaison flottante).<br/>Vérifie que `right.width` vaut `left.width` (comparaison flottante).<br/>Vérifie que `right.height` vaut `left.height` (comparaison flottante). |

**`test_procedural_atlas.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **ProceduralAtlasTest.DimensionsAttendues** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_procedural_atlas.cpp:14`</sub> | L'image générée a les dimensions attendues (grille de tuiles + lignes de personnage). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `image.width` vaut `gridSide`.<br/>Vérifie que `image.height` vaut `gridSide + frameRows * hmi::TextureAtlas::PLAYER_FRAME_SIZE`.<br/>Vérifie que `image.pixels.size()` vaut `static_cast<std::size_t>(image.width) * static_cast<std::size_t>(image.height)`. |
| **ProceduralAtlasTest.GenerationDeterministe** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_procedural_atlas.cpp:40`</sub> | La génération est déterministe : deux appels produisent des pixels identiques. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `first.width` vaut `second.width`.<br/>Vérifie que `first.height` vaut `second.height`.<br/>Vérifie que `first.pixels` vaut `second.pixels`. |
| **ProceduralAtlasTest.DamierDeTransparenceDansLaDerniereTuile** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_procedural_atlas.cpp:61`</sub> | La dernière tuile de la grille contient des pixels opaques et transparents (damier). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `sawOpaque` est vrai.<br/>Vérifie que `sawTransparent` est vrai. |

**`test_quad_recorder.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **QuadRecorderTest.CalquePrimeSurTexture** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_quad_recorder.cpp:65`</sub> | Le calque prime sur la texture dans l'ordre de soumission. | 1. Composer un quad Player (texture A) puis un quad Tile (texture B).<br/>2. Trier et capturer la scene. | Vérifie que `recorder.size()` vaut `2u`.<br/>Vérifie que `recorder.quads()[0].layer` vaut `hmi::RenderLayer::Tile`.<br/>Vérifie que `recorder.quads()[1].layer` vaut `hmi::RenderLayer::Player`.<br/>Vérifie que `recorder.isLayerOrderRespected()` est vrai. |
| **QuadRecorderTest.TroisCalquesDeuxTexturesOrdonnes** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_quad_recorder.cpp:91`</sub> | Trois calques et deux textures : ordre des calques et groupes de texture contigus. | 1. Composer six quads en alternant calques et textures.<br/>2. Trier et capturer. | Vérifie que `recorder.isLayerOrderRespected()` est vrai.<br/>Vérifie que `recorder.areTextureGroupsContiguous()` est vrai.<br/>Vérifie que `layers.size()` vaut `3u`.<br/>Vérifie que `layers[0]` vaut `hmi::RenderLayer::Background`.<br/>Vérifie que `layers[1]` vaut `hmi::RenderLayer::Tile`.<br/>Vérifie que `layers[2]` vaut `hmi::RenderLayer::Player`.<br/>Vérifie que `recorder.countOnLayer(hmi::RenderLayer::Tile)` vaut `3`.<br/>Vérifie que `recorder.countWithTexture(textureA)` vaut `3`.<br/>Vérifie que `recorder.countWithTexture(textureB)` vaut `3`. |
| **QuadRecorderTest.TriStableAClefEgale** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_quad_recorder.cpp:128`</sub> | Le tri est stable : a cle egale l'ordre de composition est preserve. | 1. Composer trois quads identiques en cle, a des positions differentes.<br/>2. Trier. | Vérifie que `scene.size()` vaut `3u`.<br/>Vérifie que `scene.quads()[0].sprite.x` vaut `3.0f` (comparaison flottante).<br/>Vérifie que `scene.quads()[1].sprite.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `scene.quads()[2].sprite.x` vaut `2.0f` (comparaison flottante). |
| **QuadRecorderTest.TriFinParSpriteLayer** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_quad_recorder.cpp:152`</sub> | Le tri fin par Sprite::layer subsiste a l'interieur d'un calque. | 1. Composer trois quads du meme calque avec des tris fins decroissants.<br/>2. Trier. | Vérifie que `scene.size()` vaut `3u`.<br/>Vérifie que `scene.quads()[0].sortOrder` vaut `0`.<br/>Vérifie que `scene.quads()[1].sortOrder` vaut `1`.<br/>Vérifie que `scene.quads()[2].sortOrder` vaut `2`. |
| **QuadRecorderTest.DeuxTexturesDeuxPasses** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_quad_recorder.cpp:176`</sub> | Le nombre de passes egale le nombre de groupes de texture. | 1. Composer quatre quads d'un meme calque en alternant deux textures.<br/>2. Trier. | Vérifie que `scene.batchCount()` vaut `2`.<br/>Vérifie que `recorder.textureSequence().size()` vaut `2u`.<br/>Vérifie que `recorder.areTextureGroupsContiguous()` est vrai. |
| **QuadRecorderTest.RectanglesEtSegmentsMelanges** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_quad_recorder.cpp:203`</sub> | Segments et rectangles cohabitent dans la meme scene ordonnee. | 1. Composer un rectangle puis un segment sur le meme calque.<br/>2. Trier et capturer. | Vérifie que `scene.size()` vaut `2u`.<br/>Vérifie que `scene.quads()[0].kind` vaut `hmi::QuadKind::Sprite`.<br/>Vérifie que `scene.quads()[1].kind` vaut `hmi::QuadKind::Line`. |
| **QuadRecorderTest.SceneDeReferenceNonRegression** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_quad_recorder.cpp:232`</sub> | Non-regression : la scene de reference produit les primitives attendues. | 1. Peupler un monde de trois tuiles puis d'un personnage tague Player.<br/>2. Composer la scene et la trier.<br/>3. Capturer et inspecter la liste. | Vérifie que `recorder.size()` vaut `4u`.<br/>Vérifie que `recorder.countOnLayer(hmi::RenderLayer::Tile)` vaut `3`.<br/>Vérifie que `recorder.countOnLayer(hmi::RenderLayer::Player)` vaut `1`.<br/>Vérifie que `recorder.isLayerOrderRespected()` est vrai.<br/>Vérifie que `recorder.quads()[0].sprite.x` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `recorder.quads()[1].sprite.x` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `recorder.quads()[2].sprite.x` vaut `2.0f` (comparaison flottante).<br/>Vérifie que `recorder.quads()[3].layer` vaut `hmi::RenderLayer::Player`.<br/>Vérifie que `recorder.quads()[0].sprite.width` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `recorder.quads()[3].sprite.width` vaut `0.4f` (comparaison flottante).<br/>Vérifie que `recorder.quads()[3].sprite.height` vaut `0.8f` (comparaison flottante).<br/>Vérifie que `recorder.quads()[3].sprite.v0` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `recorder.statistics().batches` vaut `1`.<br/>Vérifie que `recorder.statistics().culled` vaut `0`.<br/>Vérifie que `recorder.statistics().submitted` vaut `4`. |
| **QuadRecorderTest.CalqueParDefautSansTag** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_quad_recorder.cpp:290`</sub> | Une entite sans RenderLayerTag est composee sur le calque des tuiles. | 1. Composer un monde d'une seule entite non taguee. | Vérifie que `scene.size()` vaut `1u`.<br/>Vérifie que `scene.quads()[0].layer` vaut `hmi::DEFAULT_RENDER_LAYER`.<br/>Vérifie que `hmi::DEFAULT_RENDER_LAYER` vaut `hmi::RenderLayer::Tile`. |
| **QuadRecorderTest.OrdonnancementDeclare** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_quad_recorder.cpp:312`</sub> | L'ordonnancement place le premier plan au-dessus du personnage. | 1. Comparer les valeurs declarees de l'enumeration des calques. | Vérifie que `hmi::RenderLayer::Background` est strictement inférieur à `hmi::RenderLayer::Decor`.<br/>Vérifie que `hmi::RenderLayer::Decor` est strictement inférieur à `hmi::RenderLayer::Shadow`.<br/>Vérifie que `hmi::RenderLayer::Shadow` est strictement inférieur à `hmi::RenderLayer::Tile`.<br/>Vérifie que `hmi::RenderLayer::Tile` est strictement inférieur à `hmi::RenderLayer::Object`.<br/>Vérifie que `hmi::RenderLayer::Object` est strictement inférieur à `hmi::RenderLayer::Player`.<br/>Vérifie que `hmi::RenderLayer::Player` est strictement inférieur à `hmi::RenderLayer::Foreground`.<br/>Vérifie que `hmi::RenderLayer::Foreground` est strictement inférieur à `hmi::RenderLayer::UI`.<br/>Vérifie que `hmi::RenderLayer::UI` est strictement inférieur à `hmi::RenderLayer::EditorOverlay`. |

**`test_render_culling.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **RenderCullingTest.SansCadrageAucunRejet** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_culling.cpp:59`</sub> | Sans cadrage fixe, aucune primitive n'est ecartee. | 1. Composer un quad tres eloigne dans une scene sans cadrage. | Vérifie que `scene.isCullingEnabled()` est faux.<br/>Vérifie que `scene.addSprite(hmi::RenderLayer::Tile, texture, 0, tileQuad(9999.0f, 9999.0f))` est vrai.<br/>Vérifie que `scene.statistics().culled` vaut `0`. |
| **RenderCullingTest.HorsCadrageEcarte** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_culling.cpp:77`</sub> | Une primitive hors cadrage est ecartee, la meme dans le cadrage est conservee. | 1. Fixer le cadrage de reference.<br/>2. Composer un quad dedans, puis un quad loin dehors. | Vérifie que `scene.addSprite(hmi::RenderLayer::Tile, texture, 0, tileQuad(15.0f, 15.0f))` est vrai.<br/>Vérifie que `scene.addSprite(hmi::RenderLayer::Tile, texture, 0, tileQuad(80.0f, 15.0f))` est faux.<br/>Vérifie que `recorder.size()` vaut `1u`.<br/>Vérifie que `recorder.containsSpriteAt(15.0f, 15.0f)` est vrai.<br/>Vérifie que `recorder.statistics().considered` vaut `2`.<br/>Vérifie que `recorder.statistics().culled` vaut `1`.<br/>Vérifie que `recorder.statistics().submitted` vaut `1`. |
| **RenderCullingTest.AChevalSurLaFrontiereConserve** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_culling.cpp:106`</sub> | Une primitive a cheval sur la frontiere du cadrage est conservee. | 1. Fixer le cadrage de reference.<br/>2. Composer un quad chevauchant le bord droit, puis un quad entierement dans la marge. | Vérifie que `scene.addSprite(hmi::RenderLayer::Tile, texture, 0, tileQuad(29.5f, 15.0f))` est vrai.<br/>Vérifie que `scene.addSprite(hmi::RenderLayer::Tile, texture, 0, tileQuad(30.2f, 15.0f))` est vrai.<br/>Vérifie que `scene.addSprite(hmi::RenderLayer::Tile, texture, 0, tileQuad(31.5f, 15.0f))` est faux. |
| **RenderCullingTest.SegmentHorizontalConserve** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_culling.cpp:129`</sub> | Un segment horizontal traversant le cadrage est conserve. | 1. Fixer le cadrage de reference.<br/>2. Composer un segment horizontal a l'interieur, puis un segment horizontal tres au-dessus. | Vérifie que `scene.addLine(hmi::RenderLayer::EditorOverlay, texture, 0, inside)` est vrai.<br/>Vérifie que `scene.addLine(hmi::RenderLayer::EditorOverlay, texture, 0, outside)` est faux. |
| **RenderCullingTest.PrimitiveEtireeConservee** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_culling.cpp:159`</sub> | Une primitive etiree dont l'ancrage est hors cadrage reste soumise. | 1. Fixer le cadrage de reference.<br/>2. Composer un quad ancre en (0, 0) et large de 200 unites. | Vérifie que `scene.addSprite(hmi::RenderLayer::Background, texture, 0, background)` est vrai. |
| **RenderCullingTest.SpriteQuadBoundsSansRotationEstLeRectangleBrut** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_culling.cpp:183`</sub> | spriteQuadBounds sans rotation vaut le rectangle brut. | 1. Calculer la boite englobante d'un quad de rotation nulle. | Vérifie que `bounds.position.x` vaut `5.0f` (comparaison flottante).<br/>Vérifie que `bounds.position.y` vaut `3.0f` (comparaison flottante).<br/>Vérifie que `bounds.size.x` vaut `4.0f` (comparaison flottante).<br/>Vérifie que `bounds.size.y` vaut `2.0f` (comparaison flottante). |
| **RenderCullingTest.SpriteQuadBoundsA90DegresPermuteLargeurEtHauteur** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_culling.cpp:209`</sub> | spriteQuadBounds a 90 degres permute largeur et hauteur. | 1. Calculer la boite englobante d'un quad large tourne de 90 degres. | Vérifie que `bounds.size.x` vaut `2.0f`, à `1e-3f` près.<br/>Vérifie que `bounds.size.y` vaut `4.0f`, à `1e-3f` près.<br/>Vérifie que `bounds.position.x + bounds.size.x * 0.5f` vaut `2.0f`, à `1e-3f` près.<br/>Vérifie que `bounds.position.y + bounds.size.y * 0.5f` vaut `1.0f`, à `1e-3f` près. |
| **RenderCullingTest.QuadTourneChevauchantLeCadrageResteSoumis** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_culling.cpp:237`</sub> | Un quad tourne dont la boite reelle chevauche le cadrage reste soumis. | 1. Fixer un cadrage de reference.<br/>2. Composer un quad tourne de 45 degres juste a l'exterieur du rectangle non tourne, mais dont la diagonale rentre dans le cadrage. | Vérifie que `scene.addSprite(hmi::RenderLayer::EditorOverlay, texture, 0, quad)` est vrai. |
| **RenderCullingTest.VolumeProportionnelALaSalleVisible** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_culling.cpp:266`</sub> | Le volume compose depend de la salle visible, pas de la taille du niveau. | 1. Peupler une ligne de tuiles couvrant trois salles.<br/>2. Cadrer la premiere salle.<br/>3. Composer la scene. | Vérifie que `scene.statistics().considered` vaut `LEVEL_WIDTH`.<br/>Vérifie que `scene.statistics().submitted` vaut `ROOM_WIDTH + 1`.<br/>Vérifie que `scene.statistics().culled` vaut `LEVEL_WIDTH - ROOM_WIDTH - 1`. |
| **RenderCullingTest.MargeAppliqueeSurLesQuatreCotes** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_culling.cpp:301`</sub> | Le cadrage de culling est le cadrage visible elargi de la marge. | 1. Fixer le cadrage de reference.<br/>2. Lire le rectangle de culling. | Vérifie que `bounds.left()` vaut `VISIBLE_BOUNDS.left() - MARGIN` (comparaison flottante).<br/>Vérifie que `bounds.top()` vaut `VISIBLE_BOUNDS.top() - MARGIN` (comparaison flottante).<br/>Vérifie que `bounds.right()` vaut `VISIBLE_BOUNDS.right() + MARGIN` (comparaison flottante).<br/>Vérifie que `bounds.bottom()` vaut `VISIBLE_BOUNDS.bottom() + MARGIN` (comparaison flottante). |
| **RenderCullingTest.CadrageDeLaCamera** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_culling.cpp:322`</sub> | La camera fournit le rectangle monde qu'elle cadre. | 1. Centrer une camera 320x160 sur (10, 5) au zoom 1.<br/>2. Lire son cadrage. | Vérifie que `bounds.left()` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `bounds.top()` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `bounds.size.x` vaut `20.0f` (comparaison flottante).<br/>Vérifie que `bounds.size.y` vaut `10.0f` (comparaison flottante).<br/>Vérifie que `camera.visibleBounds().size.x` vaut `10.0f` (comparaison flottante). |

**`test_render_mode.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **RenderModeTest.AllerRetourDuNomPersiste** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_mode.cpp:74`</sub> | Le nom persiste du mode fait l'aller-retour sans perte. | 1. Convertir chaque mode en nom, puis le nom en mode. | Vérifie que `hmi::renderModeFromName(hmi::renderModeName(hmi::RenderMode::Physique))` vaut `hmi::RenderMode::Physique`.<br/>Vérifie que `hmi::renderModeFromName(hmi::renderModeName(hmi::RenderMode::Texture))` vaut `hmi::RenderMode::Texture`. |
| **RenderModeTest.PreferenceInvalideRetombeSurLeDefaut** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_mode.cpp:91`</sub> | Une preference absente ou invalide retombe sur le mode Texture. | 1. Convertir une chaine vide, une chaine inconnue et une chaine tronquee. | Vérifie que `hmi::DEFAULT_RENDER_MODE` vaut `hmi::RenderMode::Texture`.<br/>Vérifie que `hmi::renderModeFromName("")` vaut `hmi::DEFAULT_RENDER_MODE`.<br/>Vérifie que `hmi::renderModeFromName("plein-ecran")` vaut `hmi::DEFAULT_RENDER_MODE`.<br/>Vérifie que `hmi::renderModeFromName("phys")` vaut `hmi::DEFAULT_RENDER_MODE`. |
| **RenderModeTest.LectureInsensibleALaCasse** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_mode.cpp:108`</sub> | La lecture du nom persiste est insensible a la casse. | 1. Convertir « PHYSIQUE » et « Texture ». | Vérifie que `hmi::renderModeFromName("PHYSIQUE")` vaut `hmi::RenderMode::Physique`.<br/>Vérifie que `hmi::renderModeFromName("Texture")` vaut `hmi::RenderMode::Texture`. |
| **RenderModeTest.ModePhysiqueRendLaRegionDAtlas** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_mode.cpp:123`</sub> | En mode Physique, le resolveur rend la region d'atlas de chaque type de tuile. | 1. Pour chaque type de tuile, resoudre l'apparence en mode Physique. | Vérifie que `appearance.source` vaut `hmi::AppearanceSource::Atlas`.<br/>Vérifie que `appearance.region.x` vaut `expected.x`.<br/>Vérifie que `appearance.region.y` vaut `expected.y`.<br/>Vérifie que `appearance.region.width` vaut `expected.width`.<br/>Vérifie que `appearance.region.height` vaut `expected.height`. |
| **RenderModeTest.ModeTextureRetombeSurLeDamier** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_mode.cpp:154`</sub> | En mode Texture, une entite sans marque d'habillage retombe sur le damier. | 1. Resoudre l'apparence en mode Texture sans marque d'habillage. | Vérifie que `appearance.source` vaut `hmi::AppearanceSource::MissingTexture`.<br/>Vérifie que `appearance.region.x` vaut `0`.<br/>Vérifie que `appearance.region.y` vaut `0`.<br/>Vérifie que `appearance.region.width` vaut `hmi::MISSING_TEXTURE_SIZE`.<br/>Vérifie que `appearance.region.height` vaut `hmi::MISSING_TEXTURE_SIZE`. |
| **RenderModeTest.ModePhysiqueNonRegression** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_mode.cpp:182`</sub> | Non-regression : le mode Physique produit les primitives d'avant le lot. | 1. Composer la scene de reference en mode Physique.<br/>2. Inspecter la liste capturee. | Vérifie que `recorder.size()` vaut `4u`.<br/>Vérifie que `recorder.isLayerOrderRespected()` est vrai.<br/>Vérifie que `recorder.countOnLayer(hmi::RenderLayer::Tile)` vaut `3`.<br/>Vérifie que `recorder.countOnLayer(hmi::RenderLayer::Player)` vaut `1`.<br/>Vérifie que `recorder.countWithTexture(testTextures().atlas)` vaut `4`.<br/>Vérifie que `recorder.statistics().batches` vaut `1`.<br/>Vérifie que `recorder.quads()[1].sprite.u0` vaut `16.0f / 80.0f` (comparaison flottante).<br/>Vérifie que `recorder.quads()[1].sprite.u1` vaut `32.0f / 80.0f` (comparaison flottante). |
| **RenderModeTest.ModeTextureConserveLaGeometrie** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_mode.cpp:217`</sub> | Le mode Texture lie le damier sans changer la geometrie composee. | 1. Composer la scene de reference dans les deux modes.<br/>2. Comparer textures, positions et tailles. | Vérifie que `physique.size()` vaut `texture.size()`.<br/>Vérifie que `texture[i].texture` vaut `testTextures().missing`.<br/>Vérifie que `texture[i].layer` vaut `physique[i].layer`.<br/>Vérifie que `texture[i].sprite.x` vaut `physique[i].sprite.x` (comparaison flottante).<br/>Vérifie que `texture[i].sprite.y` vaut `physique[i].sprite.y` (comparaison flottante).<br/>Vérifie que `texture[i].sprite.width` vaut `physique[i].sprite.width` (comparaison flottante).<br/>Vérifie que `texture[i].sprite.height` vaut `physique[i].sprite.height` (comparaison flottante). |
| **RenderModeTest.BasculeSansEffetRemanent** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_mode.cpp:250`</sub> | La bascule Physique -> Texture -> Physique est sans effet remanent. | 1. Composer en Physique, puis en Texture, puis de nouveau en Physique.<br/>2. Comparer la premiere et la troisieme liste. | Vérifie que `before.size()` vaut `after.size()`.<br/>Vérifie que `after[i].texture` vaut `before[i].texture`.<br/>Vérifie que `after[i].layer` vaut `before[i].layer`.<br/>Vérifie que `after[i].sortOrder` vaut `before[i].sortOrder`.<br/>Vérifie que `after[i].sprite.x` vaut `before[i].sprite.x` (comparaison flottante).<br/>Vérifie que `after[i].sprite.u0` vaut `before[i].sprite.u0` (comparaison flottante).<br/>Vérifie que `after[i].sprite.v1` vaut `before[i].sprite.v1` (comparaison flottante). |
| **RenderModeTest.F8HorsDesTablesDeRemappage** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_render_mode.cpp:289`</sub> | F8 n'apparait dans aucune table de remappage. | 1. Parcourir les touches par defaut de toutes les actions de jeu et d'edition. | Vérifie que `static_cast<int>(key)` diffère de `VIRTUAL_KEY_F8`.<br/>Vérifie que `static_cast<int>(key)` diffère de `VIRTUAL_KEY_F8`. |

**`test_room_grid.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **RoomGridTest.NiveauPlusPetitQuUneSalleProduitUneSeuleSalle** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_room_grid.cpp:13`</sub> | Un niveau plus petit qu'une salle produit une seule salle couvrant le niveau entier. | 1. Construire un `RoomGrid` pour un niveau plus petit que la taille de salle sur les deux axes.<br/>2. Vérifier `columns()`/`rows()` et le rectangle de l'unique salle. | Vérifie que `grid.columns()` vaut `1`.<br/>Vérifie que `grid.rows()` vaut `1`.<br/>Vérifie que `bounds.column` vaut `0`.<br/>Vérifie que `bounds.row` vaut `0`.<br/>Vérifie que `bounds.width` vaut `10`.<br/>Vérifie que `bounds.height` vaut `6`. |
| **RoomGridTest.NiveauMultipleExactNeRogneAucuneSalle** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_room_grid.cpp:38`</sub> | Un niveau exactement multiple de la taille de salle ne rogne aucune salle. | 1. Construire un `RoomGrid` pour un niveau dont chaque dimension est un multiple exact de la taille de salle.<br/>2. Vérifier `columns()`/`rows()` et le rectangle de chaque salle. | Vérifie que `grid.columns()` vaut `3`.<br/>Vérifie que `grid.rows()` vaut `2`.<br/>Vérifie que `last.column` vaut `hmi::RoomGrid::ROOM_WIDTH_TILES * 2`.<br/>Vérifie que `last.row` vaut `hmi::RoomGrid::ROOM_HEIGHT_TILES * 1`.<br/>Vérifie que `last.width` vaut `hmi::RoomGrid::ROOM_WIDTH_TILES`.<br/>Vérifie que `last.height` vaut `hmi::RoomGrid::ROOM_HEIGHT_TILES`. |
| **RoomGridTest.NiveauNonMultipleRogneLaDerniereSalle** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_room_grid.cpp:65`</sub> | Un niveau non multiple de la taille de salle rogne la dernière colonne/ligne. | 1. Construire un `RoomGrid` pour un niveau dépassant de quelques cases un multiple de la taille de salle.<br/>2. Vérifier le rectangle de la dernière salle sur chaque axe. | Vérifie que `grid.columns()` vaut `2`.<br/>Vérifie que `grid.rows()` vaut `2`.<br/>Vérifie que `last.column` vaut `hmi::RoomGrid::ROOM_WIDTH_TILES`.<br/>Vérifie que `last.row` vaut `hmi::RoomGrid::ROOM_HEIGHT_TILES`.<br/>Vérifie que `last.width` vaut `extraWidth`.<br/>Vérifie que `last.height` vaut `extraHeight`. |
| **RoomGridTest.RoomIndexAtQuatreCoins** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_room_grid.cpp:94`</sub> | `roomIndexAt` renvoie l'indice de salle correct aux quatre coins et au centre. | 1. Construire un `RoomGrid` de 2x2 salles.<br/>2. Interroger `roomIndexAt` à des positions choisies dans chacune des quatre salles. | Vérifie que `grid.roomIndexAt(core::GridPosition{0, 0})` vaut `(core::GridPosition{0, 0})`.<br/>Vérifie que `grid.roomIndexAt(core::GridPosition{width - 1, 0})` vaut `(core::GridPosition{1, 0})`.<br/>Vérifie que `grid.roomIndexAt(core::GridPosition{0, height - 1})` vaut `(core::GridPosition{0, 1})`.<br/>Vérifie que `grid.roomIndexAt(core::GridPosition{width - 1, height - 1})` vaut `(core::GridPosition{1, 1})`. |
| **RoomGridTest.PositionHorsBornesEstBornee** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_room_grid.cpp:117`</sub> | Une position hors des bornes du niveau est bornée à la salle la plus proche. | 1. Construire un `RoomGrid` pour un petit niveau.<br/>2. Interroger `roomIndexAt` avec des coordonnées négatives puis très supérieures aux bornes. | Vérifie que `grid.roomIndexAt(core::GridPosition{-100, -100})` vaut `(core::GridPosition{0, 0})`.<br/>Vérifie que `grid.roomIndexAt(core::GridPosition{1000, 1000})` vaut `(core::GridPosition{0, 0})`. |

**`test_shadow_render.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **ShadowRenderTest.TuilePleineProjetteUnQuadDecaleSurShadow** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_shadow_render.cpp:52`</sub> | Tuile pleine : quad d'ombre decale sur Shadow. | 1. Peupler une tuile Solid.<br/>2. Composer les ombres en mode Texture. | Vérifie que `recorder.size()` vaut `1u`.<br/>Vérifie que `recorder.countOnLayer(hmi::RenderLayer::Shadow)` vaut `1`.<br/>Vérifie que `quad.sprite.x` vaut `3.0f + hmi::SHADOW_OFFSET_X` (comparaison flottante).<br/>Vérifie que `quad.sprite.y` vaut `2.0f + hmi::SHADOW_OFFSET_Y` (comparaison flottante).<br/>Vérifie que `quad.sprite.width` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `quad.sprite.height` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `quad.sprite.a` est strictement inférieur à `1.0f`. |
| **ShadowRenderTest.TuilesNonPhysiquesAucuneOmbre** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_shadow_render.cpp:81`</sub> | Tuiles non physiques : aucune ombre. | 1. Peupler danger, entree, sortie, interrupteur, plaque, porte.<br/>2. Composer les ombres en mode Texture. | Vérifie que `scene.size()` vaut `0u`. |
| **ShadowRenderTest.ModePhysiqueAucuneOmbre** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_shadow_render.cpp:107`</sub> | Mode Physique : aucune ombre. | 1. Peupler une tuile Solid et une silhouette.<br/>2. Composer en mode Physique. | Vérifie que `scene.size()` vaut `0u`. |
| **ShadowRenderTest.DouzeSilhouettesMemeRegionQueRegionForTile** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_shadow_render.cpp:129`</sub> | Silhouettes : ombre a la meme region d'atlas que la tuile. | 1. Peupler chacun des douze types a silhouette.<br/>2. Composer les ombres. | Vérifie que `recorder.size()` vaut `1u`.<br/>Vérifie que `quad.sprite.u0` vaut `expectedU0` (comparaison flottante).<br/>Vérifie que `quad.sprite.v0` vaut `expectedV0` (comparaison flottante).<br/>Vérifie que `quad.sprite.u1` vaut `expectedU1` (comparaison flottante).<br/>Vérifie que `quad.sprite.v1` vaut `expectedV1` (comparaison flottante). |
| **ShadowRenderTest.BlocsReduitsOmbreATailleReelle** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_shadow_render.cpp:166`</sub> | Blocs reduits : ombre a la taille reelle. | 1. Peupler BlockHalf et BlockQuarter.<br/>2. Composer les ombres. | Vérifie que `recorder.size()` vaut `2u`.<br/>Vérifie que `recorder.quads()[0].sprite.width` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `recorder.quads()[0].sprite.height` vaut `0.5f` (comparaison flottante).<br/>Vérifie que `recorder.quads()[1].sprite.width` vaut `0.25f` (comparaison flottante).<br/>Vérifie que `recorder.quads()[1].sprite.height` vaut `0.25f` (comparaison flottante). |
| **ShadowRenderTest.BlocPoussableEnMouvementOmbreInterpolee** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_shadow_render.cpp:193`</sub> | Bloc poussable en mouvement : ombre interpolee. | 1. Peupler un Block avec une PreviousPosition distincte.<br/>2. Composer a interpolationAlpha = 0.5. | Vérifie que `recorder.size()` vaut `1u`.<br/>Vérifie que `recorder.quads().front().sprite.x` vaut `4.5f + hmi::SHADOW_OFFSET_X` (comparaison flottante).<br/>Vérifie que `recorder.quads().front().sprite.y` vaut `5.0f + hmi::SHADOW_OFFSET_Y` (comparaison flottante). |
| **ShadowRenderTest.PorteSuitLEtatCourantDeLaGrilleDeCollision** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_shadow_render.cpp:220`</sub> | Porte : ombre selon l'etat courant de la grille de collision. | 1. Peupler une porte.<br/>2. Composer sans grille, puis avec une grille ou elle est fermee, puis ouverte. | Vérifie que `scene.size()` vaut `0u`.<br/>Vérifie que `scene.size()` vaut `1u`.<br/>Vérifie que `scene.size()` vaut `0u`. |

**`test_skin_catalog.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **SkinCatalogTest.AllerRetourDuNomDeMode** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_skin_catalog.cpp:82`</sub> | Le nom persiste d'un mode de skin fait l'aller-retour sans perte. | 1. Convertir chaque mode en nom, puis le nom en mode. | Vérifie que `parsed.has_value()` est vrai.<br/>Vérifie que `*parsed` vaut `mode`.<br/>Vérifie que `hmi::skinModeFromName("").has_value()` est faux.<br/>Vérifie que `hmi::skinModeFromName("bitmask47").has_value()` est faux. |
| **SkinCatalogTest.AllerRetourLectureEcriture** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_skin_catalog.cpp:102`</sub> | Un catalogue fait l'aller-retour lecture -> ecriture -> lecture sans perte. | 1. Lire le catalogue de reference (deux jeux).<br/> 2. Le reserialiser puis le relire. | Vérifie que `reread.ok()` est vrai.<br/>Vérifie que `reread.catalog->setNames()` vaut `original.setNames()`.<br/>Vérifie que `reread.catalog->defaultSetName()` vaut `original.defaultSetName()`.<br/>Vérifie que `reread.catalog->assignments(setName)` vaut `original.assignments(setName)`. |
| **SkinCatalogTest.ResolutionDansUnJeuExistant** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_skin_catalog.cpp:126`</sub> | La resolution rend l'asset et le mode assignes dans le jeu demande. | 1. Resoudre Solid et Block dans le jeu « foret », puis Solid dans « grotte ». | Vérifie que `solid.has_value()` est vrai.<br/>Vérifie que `solid->asset` vaut `"stone.png"`.<br/>Vérifie que `solid->mode` vaut `hmi::SkinMode::Bitmask16`.<br/>Vérifie que `block.has_value()` est vrai.<br/>Vérifie que `block->asset` vaut `"crate.png"`.<br/>Vérifie que `block->mode` vaut `hmi::SkinMode::Single`.<br/>Vérifie que `cave.has_value()` est vrai.<br/>Vérifie que `cave->asset` vaut `"rock.png"`. |
| **SkinCatalogTest.JeuInexistantRetombeSurLeDefaut** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_skin_catalog.cpp:154`</sub> | Un jeu inexistant retombe sur le jeu par defaut plutot que de ne rien rendre. | 1. Resoudre Solid dans un jeu qui n'existe pas.<br/> 2. Resoudre Solid en demandant explicitement le defaut (nom vide). | Vérifie que `missing.has_value()` est vrai.<br/>Vérifie que `missing->asset` vaut `"stone.png"`.<br/>Vérifie que `byDefault.has_value()` est vrai.<br/>Vérifie que `byDefault->asset` vaut `"stone.png"`. |
| **SkinCatalogTest.TypeNonSkinneNeResoudRien** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_skin_catalog.cpp:176`</sub> | Un type de tuile sans skin assigne ne resout aucune entree. | 1. Resoudre un type absent du jeu « foret », puis un type absent de « grotte ». | Vérifie que `catalog.resolve("foret", core::TileType::Danger).has_value()` est faux.<br/>Vérifie que `catalog.resolve("grotte", core::TileType::Block).has_value()` est faux. |
| **SkinCatalogTest.JsonMalformeSignale** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_skin_catalog.cpp:194`</sub> | Un fichier de skins malforme est signale sans lever d'exception. | 1. Lire une chaine qui n'est pas du JSON, puis un JSON dont la racine est un tableau. | Vérifie que `broken.ok()` est faux.<br/>Vérifie que `broken.errorCode` vaut `hmi::SkinCatalogError::ParseError`.<br/>Vérifie que `broken.error.empty()` est faux.<br/>Vérifie que `notObject.ok()` est faux.<br/>Vérifie que `notObject.errorCode` vaut `hmi::SkinCatalogError::ParseError`. |
| **SkinCatalogTest.VersionInconnueRefusee** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_skin_catalog.cpp:214`</sub> | Une version de format superieure a celle geree est refusee explicitement. | 1. Lire un catalogue dont la version vaut celle geree, puis une superieure. | Vérifie que `current.ok()` est vrai.<br/>Vérifie que `future.ok()` est faux.<br/>Vérifie que `future.errorCode` vaut `hmi::SkinCatalogError::UnsupportedVersion`. |
| **SkinCatalogTest.TypeOuModeInconnuSignale** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_skin_catalog.cpp:236`</sub> | Un type de tuile ou un mode de skin inconnu est signale a la lecture. | 1. Lire un catalogue citant un type de tuile inexistant.<br/> 2. Lire un catalogue citant un mode inexistant, puis une entree sans asset. | Vérifie que `badType.ok()` est faux.<br/>Vérifie que `badType.errorCode` vaut `hmi::SkinCatalogError::MalformedStructure`.<br/>Vérifie que `badMode.ok()` est faux.<br/>Vérifie que `badMode.errorCode` vaut `hmi::SkinCatalogError::MalformedStructure`.<br/>Vérifie que `noAsset.ok()` est faux.<br/>Vérifie que `noAsset.errorCode` vaut `hmi::SkinCatalogError::MalformedStructure`. |
| **SkinCatalogTest.DefautInexistantSignale** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_skin_catalog.cpp:263`</sub> | Un jeu par defaut qui ne designe aucun jeu existant est signale. | 1. Lire un catalogue dont le champ « defaut » cite un jeu absent de « jeux ». | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `hmi::SkinCatalogError::MalformedStructure`. |
| **SkinCatalogTest.ModeAbsentVautSingle** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_skin_catalog.cpp:280`</sub> | Une entree sans champ « mode » est lue en mode image unique. | 1. Lire une entree ne portant que le champ « asset ». | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `entry.has_value()` est vrai.<br/>Vérifie que `entry->mode` vaut `hmi::SkinMode::Single`. |
| **SkinCatalogTest.FichierAbsentSignale** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_skin_catalog.cpp:300`</sub> | Un fichier de skins absent est signale par un code dedie, sans exception. | 1. Lire un chemin qui n'existe pas. | Vérifie que `result.ok()` est faux.<br/>Vérifie que `result.errorCode` vaut `hmi::SkinCatalogError::FileNotFound`. |
| **SkinCatalogTest.EcritureSurDisqueRelisible** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_skin_catalog.cpp:317`</sub> | Le catalogue ecrit sur disque est relu a l'identique. | 1. Ecrire le catalogue de reference dans un fichier temporaire.<br/> 2. Le relire depuis ce fichier. | Vérifie que `catalog.saveToFile(path)` est vrai.<br/>Vérifie que `reread.ok()` est vrai.<br/>Vérifie que `reread.catalog->assignments("foret")` vaut `catalog.assignments("foret")`.<br/>Vérifie que `reread.catalog->defaultSetName()` vaut `catalog.defaultSetName()`. |
| **SkinCatalogTest.AssignationPuisRetrait** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_skin_catalog.cpp:342`</sub> | Assigner puis retirer un skin change la resolution du type concerne. | 1. Assigner un asset a Danger dans un catalogue vide, puis resoudre.<br/> 2. Retirer l'assignation, puis resoudre a nouveau. | Vérifie que `catalog.defaultSetName()` vaut `"foret"`.<br/>Vérifie que `assigned.has_value()` est vrai.<br/>Vérifie que `assigned->asset` vaut `"spikes.png"`.<br/>Vérifie que `catalog.resolve("foret", core::TileType::Danger).has_value()` est faux. |
| **SkinCatalogTest.OrdreDesJeuxStable** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_skin_catalog.cpp:369`</sub> | Les noms de jeux sont rendus dans un ordre alphabetique stable. | 1. Creer trois jeux dans un ordre quelconque. | Vérifie que `catalog.setNames()` vaut `expected`. |
| **SkinCatalogTest.CatalogueLivreValide** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_skin_catalog.cpp:390`</sub> | Le fichier de skins livre avec le jeu se lit sans erreur. | 1. Lire Source/Elements/Assets/skins.json depuis les sources. | Vérifie que `std::filesystem::exists(path)` est vrai.<br/>Vérifie que `result.ok()` est vrai.<br/>Vérifie que `sets.empty()` est faux.<br/>Vérifie que `std::find(sets.begin(), sets.end(), result.catalog->defaultSetName())` diffère de `sets.end()`. |
| **SkinCatalogTest.AssetsDuCatalogueLivreConformes** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_skin_catalog.cpp:413`</sub> | Chaque asset reference par le catalogue livre existe aux bonnes dimensions. | 1. Lire le catalogue livre et parcourir toutes les assignations de tous les jeux.<br/> 2. Pour chaque asset, verifier sa presence dans Assets/Skins puis ses dimensions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `std::filesystem::exists(path)` est vrai.<br/>Vérifie que `size.has_value()` est vrai.<br/>Vérifie que `size->first` vaut `TILE * hmi::AUTOTILE_SHEET_SIDE`.<br/>Vérifie que `size->second` vaut `TILE * hmi::AUTOTILE_SHEET_SIDE`.<br/>Vérifie que `size->second` vaut `TILE`.<br/>Vérifie que `size->first` est strictement supérieur à `0`.<br/>Vérifie que `size->first % TILE` vaut `0`. |

**`test_slope_mask.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **SlopeMaskTest.DouzeTypesOntUneSilhouette** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_slope_mask.cpp:38`</sub> | Seuls les douze types de pentes et arrondis ont une silhouette. | 1. Interroger hasSilhouette pour les douze types listes.<br/> 2. L'interroger pour des types carres representatifs. | Vérifie que `hmi::hasSilhouette(type)` est vrai.<br/>Vérifie que `hmi::SILHOUETTE_TILE_TYPE_COUNT` vaut `12`.<br/>Vérifie que `hmi::hasSilhouette(core::TileType::Solid)` est faux.<br/>Vérifie que `hmi::hasSilhouette(core::TileType::Block)` est faux.<br/>Vérifie que `hmi::hasSilhouette(core::TileType::Danger)` est faux.<br/>Vérifie que `hmi::hasSilhouette(core::TileType::Empty)` est faux. |
| **SlopeMaskTest.MasqueIdentiqueALAtlasProcedural** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_slope_mask.cpp:61`</sub> | Le masque coincide pixel a pixel avec la silhouette de l'atlas procedural. | 1. Generer l'atlas procedural.<br/> 2. Pour chacun des douze types, comparer chaque pixel de sa case a isInsideSilhouette. | Vérifie que `position.has_value()` est vrai.<br/>Vérifie que `alphaOf(pixel) != 0` vaut `inside`. |
| **SlopeMaskTest.PenteMontantePleineEnBas** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_slope_mask.cpp:96`</sub> | Une pente montante vers la droite est pleine sous sa surface, vide au-dessus. | 1. Tester les quatre coins de la case d'une pente montante vers la droite. | Vérifie que `hmi::isInsideSilhouette(core::TileType::SlopeUpRight, LAST, LAST, TILE)` est vrai.<br/>Vérifie que `hmi::isInsideSilhouette(core::TileType::SlopeUpRight, 0, LAST, TILE)` est vrai.<br/>Vérifie que `hmi::isInsideSilhouette(core::TileType::SlopeUpRight, LAST, 0, TILE)` est vrai.<br/>Vérifie que `hmi::isInsideSilhouette(core::TileType::SlopeUpRight, 0, 0, TILE)` est faux. |
| **SlopeMaskTest.PenteDePlafondMiroirVertical** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_slope_mask.cpp:115`</sub> | Chaque silhouette de plafond est le miroir vertical de celle de sol. | 1. Pour les six paires sol/plafond, comparer chaque pixel du plafond au pixel verticalement symetrique de la variante de sol. | Vérifie que `surface.has_value()` est vrai.<br/>Vérifie que `hmi::isInsideSilhouette(pair.ceiling, x, y, TILE)` vaut `hmi::isInsideSilhouette(pair.floor, x, LAST - y, TILE)`. |
| **SlopeMaskTest.DetourageTransparenceFranche** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_slope_mask.cpp:168`</sub> | Le detourage rend transparents les pixels hors silhouette, et eux seuls. | 1. Detourer une image entierement opaque a la silhouette d'une pente.<br/> 2. Comparer l'alpha de chaque pixel a l'appartenance a la silhouette. | Vérifie que `alpha == 0 \|\| alpha == 255` est vrai.<br/>Vérifie que `alpha != 0` vaut `hmi::isInsideSilhouette(core::TileType::RoundedUpLeft, x, y, TILE)`. |
| **SlopeMaskTest.TypeCarreNonDetoure** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_slope_mask.cpp:195`</sub> | Un type de tuile carre n'est pas detoure. | 1. Detourer une image opaque a la silhouette de Solid, puis de Block et Danger. | Vérifie que `alphaOf(pixel)` vaut `255u`. |
| **SlopeMaskTest.ImageNonCarreeInchangee** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_slope_mask.cpp:216`</sub> | Une image non carree ou de dimensions incoherentes n'est pas detouree. | 1. Detourer une image rectangulaire, puis une image dont la taille ne correspond pas aux dimensions annoncees. | Vérifie que `rectangle` vaut `rectangleBefore`.<br/>Vérifie que `tooSmall` vaut `tooSmallBefore`. |

**`test_text_renderer.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **TextRendererTest.ChaineVideNeProduitAucunQuad** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_text_renderer.cpp:41`</sub> | Une chaîne vide ne produit aucun quad. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `scene.size()` vaut `0u`. |
| **TextRendererTest.ChaineDeuxCaracteresProduitDeuxQuadsAuxPositionsAttendues** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_text_renderer.cpp:59`</sub> | Une chaîne de N caractères produit N quads, aux positions attendues. | 1. Composer « AB » en ancrage haut-gauche.<br/>2. Capturer la scène. | Vérifie que `recorder.size()` vaut `2u`.<br/>Vérifie que `recorder.containsSpriteAt(0.0f, 0.0f)` est vrai.<br/>Vérifie que `recorder.containsSpriteAt(6.0f, 0.0f)` est vrai. |
| **TextRendererTest.EspaceNEmetAucunQuadMaisAvanceLeStylo** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_text_renderer.cpp:81`</sub> | L'espace n'émet aucun quad mais fait avancer le stylo. | 1. Composer « A B » (A, espace, B).<br/>2. Capturer la scène. | Vérifie que `recorder.size()` vaut `2u`.<br/>Vérifie que `recorder.containsSpriteAt(0.0f, 0.0f)` est vrai.<br/>Vérifie que `recorder.containsSpriteAt(12.0f, 0.0f)` est vrai. |
| **TextRendererTest.ChaineAccentueeCompteLesPointsDeCode** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_text_renderer.cpp:104`</sub> | Une chaîne accentuée produit un quad par point de code, pas par octet. | 1. Composer « É » (deux octets UTF-8, un point de code).<br/>2. Capturer la scène. | Vérifie que `scene.size()` vaut `1u`. |
| **TextRendererTest.AncrageCentreDecaleLaBoiteDeSaMoitie** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_text_renderer.cpp:122`</sub> | L'ancrage centré décale la boîte du texte de sa moitié. | 1. Composer « AB » avec un ancrage centré aux deux axes.<br/>2. Capturer la scène. | Vérifie que `recorder.size()` vaut `2u`.<br/>Vérifie que `recorder.containsSpriteAt(-6.0f, -5.0f)` est vrai. |
| **TextRendererTest.AncrageDroiteBasPositionneLaFinDeLaBoite** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_text_renderer.cpp:144`</sub> | L'ancrage droite/bas positionne la fin de la boîte sur le point d'ancrage. | 1. Composer « AB » avec un ancrage droite/bas à (100, 100).<br/>2. Capturer la scène. | Vérifie que `recorder.size()` vaut `2u`.<br/>Vérifie que `recorder.containsSpriteAt(88.0f, 90.0f)` est vrai. |
| **TextRendererTest.PrimitivesSurLeCalqueUi** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_text_renderer.cpp:166`</sub> | Toutes les primitives composées le sont sur le calque UI. | 1. Composer un texte de plusieurs caractères.<br/>2. Capturer la scène. | Vérifie que `recorder.countOnLayer(hmi::RenderLayer::UI)` vaut `2`.<br/>Vérifie que `recorder.countOnLayer(hmi::RenderLayer::Tile)` vaut `0`. |
| **TextRendererTest.CaractereAbsentEstSubstitueEtComposeUnQuad** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_text_renderer.cpp:188`</sub> | Un point de code non couvert est substitué par le glyphe de remplacement. | 1. Composer un caractère absent de `testMetrics()` ('#').<br/>2. Capturer la scène. | Vérifie que `scene.size()` vaut `1u`. |
| **TextRendererTest.TexteNestJamaisCulle** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_text_renderer.cpp:207`</sub> | Le texte n'est jamais soumis au culling par cadrage. | 1. Composer un texte à une position écran éloignée (10000, 10000), sans cadrage actif sur la scène.<br/>2. Vérifier la scène. | Vérifie que `scene.isCullingEnabled()` est faux.<br/>Vérifie que `scene.size()` vaut `1u`. |

**`test_texture_atlas.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **TextureAtlasTest.TileRenvoieLeRectangleAttendu** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_texture_atlas.cpp:16`</sub> | tile(colonne, ligne) renvoie un rectangle de 16x16 pixels à l'origine attendue. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `origin.x` vaut `0`.<br/>Vérifie que `origin.y` vaut `0`.<br/>Vérifie que `origin.width` vaut `hmi::TextureAtlas::TILE_SIZE`.<br/>Vérifie que `origin.height` vaut `hmi::TextureAtlas::TILE_SIZE`.<br/>Vérifie que `secondRow.x` vaut `2 * hmi::TextureAtlas::TILE_SIZE`.<br/>Vérifie que `secondRow.y` vaut `1 * hmi::TextureAtlas::TILE_SIZE`.<br/>Vérifie que `secondRow.width` vaut `hmi::TextureAtlas::TILE_SIZE`.<br/>Vérifie que `secondRow.height` vaut `hmi::TextureAtlas::TILE_SIZE`. |
| **TextureAtlasTest.PlayerFrameRegionSousLaGrilleDeTuiles** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_texture_atlas.cpp:42`</sub> | playerFrameRegion place les images du personnage sous la grille de tuiles, dans l'ordre Idle, Run, Jump. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `idleFirst.x` vaut `0`.<br/>Vérifie que `idleFirst.y` vaut `gridSide`.<br/>Vérifie que `idleFirst.width` vaut `hmi::TextureAtlas::PLAYER_FRAME_SIZE`.<br/>Vérifie que `idleFirst.height` vaut `hmi::TextureAtlas::PLAYER_FRAME_SIZE`.<br/>Vérifie que `runFirst.x` vaut `(runFlatIndex % hmi::TextureAtlas::PLAYER_FRAME_COLUMNS) * hmi::TextureAtlas::PLAYER_FRAME_SIZE`.<br/>Vérifie que `runFirst.y` vaut `gridSide + (runFlatIndex / hmi::TextureAtlas::PLAYER_FRAME_COLUMNS) * hmi::TextureAtlas::PLAYER_FRAME_SIZE`.<br/>Vérifie que `jump.x` vaut `(jumpFlatIndex % hmi::TextureAtlas::PLAYER_FRAME_COLUMNS) * hmi::TextureAtlas::PLAYER_FRAME_SIZE`.<br/>Vérifie que `jump.y` vaut `gridSide + (jumpFlatIndex / hmi::TextureAtlas::PLAYER_FRAME_COLUMNS) * hmi::TextureAtlas::PLAYER_FRAME_SIZE`. |

**`test_texture_resolution.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **TextureResolutionTest.SurchargeChargeePrioritaireSurLeSkin** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_texture_resolution.cpp:64`</sub> | Une surcharge chargée est prioritaire sur le skin du type. | 1. Résoudre une tuile Solid (skinnée) portant une surcharge chargée, en mode Texture. | Vérifie que `appearance.source` vaut `hmi::AppearanceSource::Override`.<br/>Vérifie que `appearance.region.width` vaut `TILE`.<br/>Vérifie que `appearance.region.height` vaut `TILE`.<br/>Vérifie que `textures.textureFor(appearance)` vaut `&doorRedStorage`. |
| **TextureResolutionTest.SurchargeIntrouvableRetombeSurLeDamierPasSurLeSkin** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_texture_resolution.cpp:90`</sub> | Une surcharge introuvable retombe sur le damier, pas sur le skin. | 1. Résoudre une tuile Solid skinnée portant une surcharge dont l'asset n'est pas chargé. | Vérifie que `appearance.source` vaut `hmi::AppearanceSource::MissingTexture`.<br/>Vérifie que `textures.textureFor(appearance)` vaut `&missingStorage`. |
| **TextureResolutionTest.SansSurchargeLeSkinSAppliqueNormalement** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_texture_resolution.cpp:114`</sub> | Sans surcharge, le skin du type s'applique normalement. | 1. Résoudre une tuile Solid skinnée, sans surcharge. | Vérifie que `appearance.source` vaut `hmi::AppearanceSource::Skin`.<br/>Vérifie que `textures.textureFor(appearance)` vaut `&stoneStorage`. |
| **TextureResolutionTest.SurchargeSAppliqueMemeSansSkinDuType** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_texture_resolution.cpp:138`</sub> | Une surcharge s'affiche même sur un type sans skin. | 1. Résoudre une tuile Danger (non skinnée) portant une surcharge chargée. | Vérifie que `appearance.source` vaut `hmi::AppearanceSource::Override`. |
| **TextureResolutionTest.SurchargeSAppliqueMemeSansCatalogue** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_texture_resolution.cpp:160`</sub> | Une surcharge s'affiche même sans catalogue de skins. | 1. Résoudre une tuile portant une surcharge, catalogue absent. | Vérifie que `appearance.source` vaut `hmi::AppearanceSource::Override`.<br/>Vérifie que `textures.textureFor(appearance)` vaut `&doorRedStorage`. |
| **TextureResolutionTest.ModePhysiqueIgnoreLesSurcharges** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_texture_resolution.cpp:184`</sub> | Le mode Physique ignore les surcharges. | 1. Résoudre une tuile portant une surcharge, en mode Physique. | Vérifie que `appearance.source` vaut `hmi::AppearanceSource::Atlas`.<br/>Vérifie que `appearance.region.x` vaut `physical.x`. |
| **TextureResolutionTest.IndexDObjetInvalideBorne** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_texture_resolution.cpp:206`</sub> | Un index d'objet invalide retombe sur le damier sans lecture hors bornes. | 1. Interroger les textures avec une apparence Override portant un index hors bornes. | Vérifie que `textures.textureFor(broken)` vaut `&missingStorage`.<br/>Vérifie que `textures.widthFor(broken)` vaut `hmi::MISSING_TEXTURE_SIZE`.<br/>Vérifie que `textures.heightFor(broken)` vaut `hmi::MISSING_TEXTURE_SIZE`. |
| **TextureResolutionTest.ObjetsSeulsSurchargeChargeeSAffiche** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_texture_resolution.cpp:228`</sub> | Objets interactifs seuls : une surcharge chargee s'affiche normalement. | 1. Résoudre une tuile Solid skinnée portant une surcharge chargée, axe skin masqué. | Vérifie que `appearance.has_value()` est vrai.<br/>Vérifie que `appearance->source` vaut `hmi::AppearanceSource::Override`. |
| **TextureResolutionTest.ObjetsSeulsSansSurchargeNAfficheRien** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_texture_resolution.cpp:251`</sub> | Objets interactifs seuls : une case sans surcharge n'affiche rien. | 1. Résoudre une tuile Solid skinnée sans surcharge, axe skin masqué. | Vérifie que `appearance.has_value()` est faux. |
| **TextureResolutionTest.ObjetsSeulsSansSurchargeNiSkinNAfficheRien** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_texture_resolution.cpp:273`</sub> | Objets interactifs seuls : une case sans surcharge ni skin n'affiche pas le damier. | 1. Résoudre une tuile Danger (non skinnée) sans surcharge, axe skin masqué. | Vérifie que `appearance.has_value()` est faux. |
| **TextureResolutionTest.SkinSeulTypeSkinneSAfficheNormalement** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_texture_resolution.cpp:296`</sub> | Skin des tuiles seul : un type skinne s'affiche normalement. | 1. Résoudre une tuile Solid skinnée sans surcharge, axe surcharge masqué. | Vérifie que `appearance.has_value()` est vrai.<br/>Vérifie que `appearance->source` vaut `hmi::AppearanceSource::Skin`. |
| **TextureResolutionTest.SkinSeulTypeNonSkinneNAfficheRien** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_texture_resolution.cpp:319`</sub> | Skin des tuiles seul : un type non skinne n'affiche rien. | 1. Résoudre une tuile Danger (non skinnée) sans surcharge, axe surcharge masqué. | Vérifie que `appearance.has_value()` est faux. |
| **TextureResolutionTest.SkinSeulCaseAvecSurchargeNAfficheRien** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_texture_resolution.cpp:341`</sub> | Skin des tuiles seul : une case avec surcharge n'affiche rien. | 1. Résoudre une tuile Solid portant une surcharge chargée, axe surcharge masqué. | Vérifie que `appearance.has_value()` est faux. |
| **TextureResolutionTest.DeuxAxesMasquesNAfficheJamaisRien** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_texture_resolution.cpp:363`</sub> | Les deux axes masques n'affichent jamais rien. | 1. Résoudre une tuile Solid skinnée avec surcharge, les deux axes masqués. | Vérifie que `appearance.has_value()` est faux. |
| **TextureResolutionTest.ModeComposeSurchargeIntrouvableRetombeSurLeDamier** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_texture_resolution.cpp:386`</sub> | Mode compose : une surcharge introuvable retombe toujours sur le damier. | 1. Résoudre une tuile Solid skinnée portant une surcharge introuvable, les deux axes visibles (valeurs par défaut). | Vérifie que `appearance.has_value()` est vrai.<br/>Vérifie que `appearance->source` vaut `hmi::AppearanceSource::MissingTexture`. |
| **TextureResolutionTest.TextureOverrideAtTrouveLaSurchargeDeLaBonneCase** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_texture_resolution.cpp:409`</sub> | textureOverrideAt trouve la surcharge d'une case precise. | 1. Chercher une position avec et sans surcharge dans une liste de surcharges. | Vérifie que `found.has_value()` est vrai.<br/>Vérifie que `*found` vaut `"door_red.png"`.<br/>Vérifie que `hmi::textureOverrideAt(overrides, core::GridPosition{0, 0}).has_value()` est faux. |

**`test_tile_autotile.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **TileAutotileTest.SeizeConfigurationsSeizeCasesDistinctes** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_autotile.cpp:28`</sub> | Les seize configurations de voisinage donnent seize cases distinctes. | 1. Calculer la case de chacun des seize masques possibles. | Vérifie que `cell.column` est supérieur ou égal à `0`.<br/>Vérifie que `cell.column` est strictement inférieur à `hmi::AUTOTILE_SHEET_SIDE`.<br/>Vérifie que `cell.row` est supérieur ou égal à `0`.<br/>Vérifie que `cell.row` est strictement inférieur à `hmi::AUTOTILE_SHEET_SIDE`.<br/>Vérifie que `cells.size()` vaut `static_cast<std::size_t>(hmi::AUTOTILE_CONFIGURATION_COUNT)`. |
| **TileAutotileTest.TableDeCorrespondanceExhaustive** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_autotile.cpp:54`</sub> | Chaque masque de voisinage designe la case attendue de la planche. | 1. Verifier la case des seize masques, un a un. | Vérifie que `cell.column` vaut `expected.column`.<br/>Vérifie que `cell.row` vaut `expected.row`. |
| **TileAutotileTest.ExterieurDeLaGrilleCompteSolide** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_autotile.cpp:96`</sub> | L'exterieur de la grille compte comme solide. | 1. Placer un solide isole dans le coin haut-gauche d'une grille vide. | Vérifie que `(mask & hmi::NEIGHBOR_UP) != 0` est vrai.<br/>Vérifie que `(mask & hmi::NEIGHBOR_LEFT) != 0` est vrai.<br/>Vérifie que `(mask & hmi::NEIGHBOR_RIGHT) != 0` est faux.<br/>Vérifie que `(mask & hmi::NEIGHBOR_DOWN) != 0` est faux. |
| **TileAutotileTest.TuileIsoleeAuCentre** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_autotile.cpp:118`</sub> | Une tuile solide entouree de vide donne la configuration isolee. | 1. Placer un solide au centre d'une grille vide, loin des bords. | Vérifie que `hmi::solidNeighborMask(tiles, 2, 2)` vaut `0`.<br/>Vérifie que `hmi::autotileCell(0)` vaut `(hmi::AutotileCell{0, 0})`. |
| **TileAutotileTest.DessusDePlateformeDistinctDeLInterieur** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_autotile.cpp:135`</sub> | Le dessus d'une plateforme pleine differe de son interieur. | 1. Remplir de solide les lignes 2 a 4 d'une grille vide.<br/> 2. Comparer la case de la ligne de surface et celle d'une case enfouie. | Vérifie que `surface` vaut `(hmi::AutotileCell{2, 3})`.<br/>Vérifie que `inside` vaut `(hmi::AutotileCell{3, 3})`.<br/>Vérifie que `surface == inside` est faux. |
| **TileAutotileTest.RaccordEntreTypesSolidesDifferents** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_autotile.cpp:163`</sub> | Le voisinage est celui de la solidite, pas du type de tuile. | 1. Placer un solide et, a sa droite, un bloc poussable. | Vérifie que `(hmi::solidNeighborMask(tiles, 2, 2) & hmi::NEIGHBOR_RIGHT) != 0` est vrai. |
| **TileAutotileTest.PenteNonCompteeCommeVoisinSolide** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_autotile.cpp:182`</sub> | Une pente n'est pas comptee comme voisin solide. | 1. Placer un solide et, a sa droite, une pente montante. | Vérifie que `(hmi::solidNeighborMask(tiles, 2, 2) & hmi::NEIGHBOR_RIGHT) != 0` est faux. |
| **TileAutotileTest.CaseRepresentativeEstLInterieurPlein** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_autotile.cpp:201`</sub> | La case representative d'une planche est son interieur plein. | 1. Demander la case representative de la planche. | Vérifie que `hmi::autotileRepresentativeCell()` vaut `full`.<br/>Vérifie que `hmi::autotileRepresentativeCell() == (hmi::AutotileCell{0, 0})` est faux. |
| **TileAutotileTest.BitsParasitesIgnores** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_autotile.cpp:218`</sub> | Un masque portant des bits parasites reste dans la planche. | 1. Demander la case d'un masque dont les bits de poids fort sont a 1. | Vérifie que `hmi::autotileCell(noisy)` vaut `hmi::autotileCell(hmi::NEIGHBOR_UP)`. |
| **TileAutotileTest.SeizeMasquesProduisentSeizeClesDeLibelleDistinctes** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_autotile.cpp:234`</sub> | Les seize masques produisent seize cles de libelle distinctes. | 1. Produire la cle de libelle de chacun des seize masques. | Vérifie que `keys.size()` vaut `static_cast<std::size_t>(hmi::AUTOTILE_CONFIGURATION_COUNT)`. |
| **TileAutotileTest.LibelleIgnoreLesBitsParasites** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_autotile.cpp:251`</sub> | Un masque de libelle portant des bits parasites reste dans la table. | 1. Demander le libelle d'un masque dont les bits de poids fort sont a 1. | Vérifie que `hmi::autotileConfigurationLabelKey(noisy)` vaut `hmi::autotileConfigurationLabelKey(hmi::NEIGHBOR_UP)`. |
| **TileAutotileTest.AssemblageTroisParTroisPlaceLInterieurPleinAuCentre** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_autotile.cpp:268`</sub> | L'assemblage 3x3 place l'interieur plein au centre. | 1. Produire les neuf masques de l'assemblage.<br/>2. Verifier la case centrale et la diversite des masques. | Vérifie que `masks[4]` vaut `fullMask`.<br/>Vérifie que `distinctMasks.size()` est strictement supérieur à `1U`. |
| **TileAutotileTest.AssemblageResoutDesCasesValidesViaLaTableCanonique** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_autotile.cpp:291`</sub> | Chaque masque de l'assemblage resout une case valide de la planche. | 1. Resoudre la case de chacun des neuf masques via autotileCell. | Vérifie que `cell.column` est supérieur ou égal à `0`.<br/>Vérifie que `cell.column` est strictement inférieur à `hmi::AUTOTILE_SHEET_SIDE`.<br/>Vérifie que `cell.row` est supérieur ou égal à `0`.<br/>Vérifie que `cell.row` est strictement inférieur à `hmi::AUTOTILE_SHEET_SIDE`. |

**`test_tile_skin_resolution.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **TileSkinResolutionTest.ModePhysiqueIgnoreLesSkins** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_skin_resolution.cpp:64`</sub> | Le mode Physique rend la region d'atlas meme quand un skin est assigne. | 1. Resoudre une tuile Solid skinnee, en mode Physique. | Vérifie que `appearance.source` vaut `hmi::AppearanceSource::Atlas`.<br/>Vérifie que `appearance.region.x` vaut `physical.x`.<br/>Vérifie que `appearance.region.y` vaut `physical.y`. |
| **TileSkinResolutionTest.SkinSingleRendLImageEntiere** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_skin_resolution.cpp:89`</sub> | Un skin en mode image unique rend l'image entiere. | 1. Resoudre une tuile Block, assignee a un skin en mode single, en mode Texture. | Vérifie que `appearance.source` vaut `hmi::AppearanceSource::Skin`.<br/>Vérifie que `appearance.region.x` vaut `0`.<br/>Vérifie que `appearance.region.y` vaut `0`.<br/>Vérifie que `appearance.region.width` vaut `TILE`.<br/>Vérifie que `appearance.region.height` vaut `TILE`.<br/>Vérifie que `textures.textureFor(appearance)` vaut `&crateStorage`. |
| **TileSkinResolutionTest.SkinBitmaskChoisitSaCase** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_skin_resolution.cpp:118`</sub> | Un skin a raccords choisit sa case selon le voisinage solide. | 1. Resoudre une tuile Solid entouree de solide, puis la meme isolee. | Vérifie que `insideAppearance.source` vaut `hmi::AppearanceSource::Skin`.<br/>Vérifie que `aloneAppearance.source` vaut `hmi::AppearanceSource::Skin`.<br/>Vérifie que `textures.textureFor(insideAppearance)` vaut `&stoneStorage`.<br/>Vérifie que `insideAppearance.region.x` vaut `fullCell.column * TILE`.<br/>Vérifie que `insideAppearance.region.y` vaut `fullCell.row * TILE`.<br/>Vérifie que `aloneAppearance.region.x` vaut `0`.<br/>Vérifie que `aloneAppearance.region.y` vaut `0`.<br/>Vérifie que `insideAppearance.region.x` diffère de `aloneAppearance.region.x`. |
| **TileSkinResolutionTest.TypeNonSkinneRetombeSurLeDamier** (Critique)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_skin_resolution.cpp:160`</sub> | Un type de tuile sans skin assigne retombe sur le damier. | 1. Resoudre une tuile Danger, absente du catalogue, en mode Texture. | Vérifie que `appearance.source` vaut `hmi::AppearanceSource::MissingTexture`.<br/>Vérifie que `appearance.region.width` vaut `hmi::MISSING_TEXTURE_SIZE`.<br/>Vérifie que `textures.textureFor(appearance)` vaut `&missingStorage`. |
| **TileSkinResolutionTest.SkinNonChargeRetombeSurLeDamier** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_skin_resolution.cpp:186`</sub> | Un skin assigne dont la texture n'est pas chargee retombe sur le damier. | 1. Assigner un asset absent des textures chargees, puis resoudre en mode Texture. | Vérifie que `appearance.source` vaut `hmi::AppearanceSource::MissingTexture`. |
| **TileSkinResolutionTest.SansCatalogueToutRetombeSurLeDamier** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_skin_resolution.cpp:212`</sub> | En l'absence de catalogue de skins, tout retombe sur le damier. | 1. Resoudre une tuile Solid avec des textures sans catalogue. | Vérifie que `appearance.source` vaut `hmi::AppearanceSource::MissingTexture`. |
| **TileSkinResolutionTest.IndexDeSkinInvalideBorne** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Graphics/test_tile_skin_resolution.cpp:235`</sub> | Un index de skin invalide retombe sur le damier sans lecture hors bornes. | 1. Interroger les textures avec une apparence portant un index hors bornes. | Vérifie que `textures.textureFor(broken)` vaut `&missingStorage`.<br/>Vérifie que `textures.widthFor(broken)` vaut `hmi::MISSING_TEXTURE_SIZE`.<br/>Vérifie que `textures.heightFor(broken)` vaut `hmi::MISSING_TEXTURE_SIZE`. |

#### Input (65)

**`test_editor_key_bindings.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **EditorKeyBindingsTest.ValeursParDefautALaConstruction** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_editor_key_bindings.cpp:16`</sub> | Les dix actions ont leurs valeurs par défaut (S/Z/Y/C/V/P/F10/F1/F2/T) à la construction. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::EditorAction::Save)` vaut `hmi::Key::S`.<br/>Vérifie que `bindings.key(hmi::EditorAction::Undo)` vaut `hmi::Key::Z`.<br/>Vérifie que `bindings.key(hmi::EditorAction::Redo)` vaut `hmi::Key::Y`.<br/>Vérifie que `bindings.key(hmi::EditorAction::Copy)` vaut `hmi::Key::C`.<br/>Vérifie que `bindings.key(hmi::EditorAction::Paste)` vaut `hmi::Key::V`.<br/>Vérifie que `bindings.key(hmi::EditorAction::Playtest)` vaut `hmi::Key::P`.<br/>Vérifie que `bindings.key(hmi::EditorAction::ToggleGrid)` vaut `hmi::Key::F10`.<br/>Vérifie que `bindings.key(hmi::EditorAction::ToggleHelp)` vaut `hmi::Key::F1`.<br/>Vérifie que `bindings.key(hmi::EditorAction::Rename)` vaut `hmi::Key::F2`.<br/>Vérifie que `bindings.key(hmi::EditorAction::TextureAssignTool)` vaut `hmi::Key::T`. |
| **EditorKeyBindingsTest.SetKeyEchangeSurConflit** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_editor_key_bindings.cpp:42`</sub> | `setKey` sur une touche déjà liée à une autre action échange les deux touches. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::EditorAction::Rename)` vaut `hmi::Key::S`.<br/>Vérifie que `bindings.key(hmi::EditorAction::Save)` vaut `hmi::Key::F2`. |
| **EditorKeyBindingsTest.SetKeyEchangeAvecTextureAssignTool** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_editor_key_bindings.cpp:61`</sub> | `setKey` echange correctement quand TextureAssignTool est impliquee. | 1. Remapper TextureAssignTool sur la touche par defaut de Playtest.<br/>2. Verifier l'echange. | Vérifie que `bindings.key(hmi::EditorAction::TextureAssignTool)` vaut `hmi::Key::P`.<br/>Vérifie que `bindings.key(hmi::EditorAction::Playtest)` vaut `hmi::Key::T`. |
| **EditorKeyBindingsTest.ResetToDefaultsRestaureLesDefauts** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_editor_key_bindings.cpp:78`</sub> | `resetToDefaults` restaure les valeurs par défaut après un ou plusieurs remaps. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::EditorAction::Playtest)` vaut `hmi::Key::P`. |
| **EditorKeyBindingsTest.SaveEtLoadAllerRetour** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_editor_key_bindings.cpp:96`</sub> | Sauvegarder puis recharger préserve un jeu de bindings personnalisé (aller-retour JSON). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `original.save(path)` est vrai.<br/>Vérifie que `reloaded.key(hmi::EditorAction::Playtest)` vaut `hmi::Key::F1`.<br/>Vérifie que `reloaded.key(hmi::EditorAction::Save)` vaut `hmi::Key::S`. |
| **EditorKeyBindingsTest.LoadFichierAbsentRenvoieLesDefauts** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_editor_key_bindings.cpp:124`</sub> | Charger depuis un fichier absent renvoie les valeurs par défaut, sans erreur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::EditorAction::Save)` vaut `hmi::Key::S`. |
| **EditorKeyBindingsTest.SavePreserveLaSectionJeu** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_editor_key_bindings.cpp:144`</sub> | Sauvegarder les touches d'éditeur préserve une section « jeu » déjà présente. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.save(path)` est vrai.<br/>Vérifie que `content.find("\\"jeu\\"")` diffère de `std::string::npos`.<br/>Vérifie que `content.find("sauter")` diffère de `std::string::npos`. |
| **EditorKeyBindingsTest.SavePreserveLaSectionManette** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_editor_key_bindings.cpp:178`</sub> | Sauvegarder les touches d'editeur preserve une section manette deja presente. | 1. Ecrire un fichier avec une section manette.<br/>2. Sauvegarder des touches d'editeur dessus.<br/>3. Relire le fichier. | Vérifie que `bindings.save(path)` est vrai.<br/>Vérifie que `content.find("\\"manette\\"")` diffère de `std::string::npos`.<br/>Vérifie que `content.find("sauter")` diffère de `std::string::npos`. |

**`test_game_key_bindings.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **GameKeyBindingsTest.ValeursParDefautALaConstruction** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_game_key_bindings.cpp:15`</sub> | Les six actions ont leurs valeurs par défaut (flèches/Espace/Maj) à la construction. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::GameAction::MoveLeft)` vaut `hmi::Key::Left`.<br/>Vérifie que `bindings.key(hmi::GameAction::MoveRight)` vaut `hmi::Key::Right`.<br/>Vérifie que `bindings.key(hmi::GameAction::AimUp)` vaut `hmi::Key::Up`.<br/>Vérifie que `bindings.key(hmi::GameAction::AimDown)` vaut `hmi::Key::Down`.<br/>Vérifie que `bindings.key(hmi::GameAction::Jump)` vaut `hmi::Key::Space`.<br/>Vérifie que `bindings.key(hmi::GameAction::Dash)` vaut `hmi::Key::Shift`. |
| **GameKeyBindingsTest.SetKeySurToucheLibre** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_game_key_bindings.cpp:36`</sub> | `setKey` sur une touche libre lie l'action sans toucher aux autres. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::GameAction::Jump)` vaut `hmi::Key::F1`.<br/>Vérifie que `bindings.key(hmi::GameAction::MoveLeft)` vaut `hmi::Key::Left`. |
| **GameKeyBindingsTest.SetKeyEchangeSurConflit** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_game_key_bindings.cpp:53`</sub> | `setKey` sur une touche déjà liée à une autre action échange les deux touches. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::GameAction::Jump)` vaut `hmi::Key::Left`.<br/>Vérifie que `bindings.key(hmi::GameAction::MoveLeft)` vaut `hmi::Key::Space`. |
| **GameKeyBindingsTest.ResetToDefaultsRestaureLesDefauts** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_game_key_bindings.cpp:72`</sub> | `resetToDefaults` restaure les valeurs par défaut après un ou plusieurs remaps. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::GameAction::Jump)` vaut `hmi::Key::Space`.<br/>Vérifie que `bindings.key(hmi::GameAction::Dash)` vaut `hmi::Key::Shift`. |
| **GameKeyBindingsTest.SaveEtLoadAllerRetour** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_game_key_bindings.cpp:92`</sub> | Sauvegarder puis recharger préserve un jeu de bindings personnalisé (aller-retour JSON). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `original.save(path)` est vrai.<br/>Vérifie que `reloaded.key(hmi::GameAction::Jump)` vaut `hmi::Key::F1`.<br/>Vérifie que `reloaded.key(hmi::GameAction::Dash)` vaut `hmi::Key::F2`.<br/>Vérifie que `reloaded.key(hmi::GameAction::MoveLeft)` vaut `hmi::Key::Left`. |
| **GameKeyBindingsTest.LoadFichierAbsentRenvoieLesDefauts** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_game_key_bindings.cpp:122`</sub> | Charger depuis un fichier absent renvoie les valeurs par défaut, sans erreur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::GameAction::MoveLeft)` vaut `hmi::Key::Left`.<br/>Vérifie que `bindings.key(hmi::GameAction::Jump)` vaut `hmi::Key::Space`. |
| **GameKeyBindingsTest.LoadJsonCorrompuRenvoieLesDefauts** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_game_key_bindings.cpp:143`</sub> | Charger un fichier JSON corrompu renvoie les valeurs par défaut, sans exception. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::GameAction::MoveLeft)` vaut `hmi::Key::Left`. |
| **GameKeyBindingsTest.LoadValeurHorsBornesIgnoree** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Input/test_game_key_bindings.cpp:168`</sub> | Une valeur hors bornes dans le fichier est ignorée : l'action garde sa valeur par défaut. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.key(hmi::GameAction::Jump)` vaut `hmi::Key::Space`. |
| **GameKeyBindingsTest.SavePreserveLaSectionEditeur** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_game_key_bindings.cpp:194`</sub> | Sauvegarder les touches de jeu préserve une section « editeur » déjà présente. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.save(path)` est vrai.<br/>Vérifie que `content.find("editeur")` diffère de `std::string::npos`.<br/>Vérifie que `content.find("sauvegarder")` diffère de `std::string::npos`. |

**`test_gamepad_bindings.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **GamepadBindingsTest.ValeursParDefautALaConstruction** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_bindings.cpp:16`</sub> | Les six actions ont leurs boutons par défaut à la construction. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.button(hmi::GameAction::MoveLeft)` vaut `hmi::GamepadButton::Left`.<br/>Vérifie que `bindings.button(hmi::GameAction::MoveRight)` vaut `hmi::GamepadButton::Right`.<br/>Vérifie que `bindings.button(hmi::GameAction::AimUp)` vaut `hmi::GamepadButton::Up`.<br/>Vérifie que `bindings.button(hmi::GameAction::AimDown)` vaut `hmi::GamepadButton::Down`.<br/>Vérifie que `bindings.button(hmi::GameAction::Jump)` vaut `hmi::GamepadButton::A`.<br/>Vérifie que `bindings.button(hmi::GameAction::Dash)` vaut `hmi::GamepadButton::RightShoulder`. |
| **GamepadBindingsTest.SetKeySurBoutonLibre** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_bindings.cpp:36`</sub> | `setKey` sur un bouton libre lie l'action sans toucher aux autres. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.button(hmi::GameAction::Jump)` vaut `hmi::GamepadButton::X`.<br/>Vérifie que `bindings.button(hmi::GameAction::MoveLeft)` vaut `hmi::GamepadButton::Left`. |
| **GamepadBindingsTest.SetKeyEchangeSurConflit** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_bindings.cpp:53`</sub> | `setKey` sur un bouton déjà lié à une autre action échange les deux boutons. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.button(hmi::GameAction::Jump)` vaut `hmi::GamepadButton::RightShoulder`.<br/>Vérifie que `bindings.button(hmi::GameAction::Dash)` vaut `hmi::GamepadButton::A`. |
| **GamepadBindingsTest.ResetToDefaultsRestaureLesDefauts** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_bindings.cpp:71`</sub> | `resetToDefaults` restaure les valeurs par défaut après un ou plusieurs remaps. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.button(hmi::GameAction::Jump)` vaut `hmi::GamepadButton::A`.<br/>Vérifie que `bindings.button(hmi::GameAction::Dash)` vaut `hmi::GamepadButton::RightShoulder`. |
| **GamepadBindingsTest.SaveEtLoadAllerRetour** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_bindings.cpp:91`</sub> | Sauvegarder puis recharger préserve un jeu de bindings personnalisé (aller-retour JSON). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `original.save(path)` est vrai.<br/>Vérifie que `reloaded.button(hmi::GameAction::Jump)` vaut `hmi::GamepadButton::X`.<br/>Vérifie que `reloaded.button(hmi::GameAction::Dash)` vaut `hmi::GamepadButton::Y`.<br/>Vérifie que `reloaded.button(hmi::GameAction::MoveLeft)` vaut `hmi::GamepadButton::Left`. |
| **GamepadBindingsTest.LoadFichierAbsentRenvoieLesDefauts** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_bindings.cpp:121`</sub> | Charger depuis un fichier absent renvoie les valeurs par défaut, sans erreur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.button(hmi::GameAction::Jump)` vaut `hmi::GamepadButton::A`. |
| **GamepadBindingsTest.LoadNomBoutonInconnuIgnore** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_bindings.cpp:142`</sub> | Un nom de bouton inconnu est ignoré : l'action garde sa valeur par défaut. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.button(hmi::GameAction::Jump)` vaut `hmi::GamepadButton::A`. |
| **GamepadBindingsTest.SavePreserveLesAutresSections** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_bindings.cpp:167`</sub> | Sauvegarder les bindings manette préserve les sections « jeu »/« editeur » déjà présentes. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bindings.save(path)` est vrai.<br/>Vérifie que `content.find("\\"jeu\\"")` diffère de `std::string::npos`.<br/>Vérifie que `content.find("\\"editeur\\"")` diffère de `std::string::npos`.<br/>Vérifie que `content.find("\\"manette\\"")` diffère de `std::string::npos`. |

**`test_gamepad_button_name.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **GamepadButtonNameTest.ChaqueBoutonALibelleDedie** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_button_name.cpp:14`</sub> | Chaque bouton manette a un libellé lisible dédié. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::gamepadButtonDisplayName(hmi::GamepadButton::A)` vaut `"A"`.<br/>Vérifie que `hmi::gamepadButtonDisplayName(hmi::GamepadButton::Left)` vaut `"Gauche"`.<br/>Vérifie que `hmi::gamepadButtonDisplayName(hmi::GamepadButton::RightShoulder)` vaut `"Epaule droite"`. |
| **GamepadButtonNameTest.CapturedGamepadButtonRenvoieLeBoutonEnfonce** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_button_name.cpp:30`</sub> | `capturedGamepadButton` renvoie le bouton pressé à la frame courante. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `captured.has_value()` est vrai.<br/>Vérifie que `*captured` vaut `hmi::GamepadButton::X`. |
| **GamepadButtonNameTest.CapturedGamepadButtonVideSansBouton** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_gamepad_button_name.cpp:48`</sub> | `capturedGamepadButton` renvoie vide quand aucun bouton n'est pressé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::capturedGamepadButton(input).has_value()` est faux. |

**`test_input_state.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **InputStateTest.FrontMontantClavier** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:12`</sub> | Une touche passée d'« absente » à « présente » est « pressée » exactement une frame. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.keyDown(hmi::Key::Enter)` est vrai.<br/>Vérifie que `input.keyPressed(hmi::Key::Enter)` est vrai.<br/>Vérifie que `input.keyReleased(hmi::Key::Enter)` est faux.<br/>Vérifie que `input.keyDown(hmi::Key::Enter)` est vrai.<br/>Vérifie que `input.keyPressed(hmi::Key::Enter)` est faux. |
| **InputStateTest.MaintienClavier** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:38`</sub> | Une touche restée enfoncée n'est « pressée » qu'à la première frame. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.keyPressed(hmi::Key::Down)` est vrai.<br/>Vérifie que `input.keyDown(hmi::Key::Down)` est vrai.<br/>Vérifie que `input.keyPressed(hmi::Key::Down)` est faux.<br/>Vérifie que `input.keyReleased(hmi::Key::Down)` est faux. |
| **InputStateTest.FrontDescendantClavier** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:63`</sub> | Le relâchement d'une touche est détecté « relâchée » pendant exactement une frame. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.keyDown(hmi::Key::Space)` est faux.<br/>Vérifie que `input.keyReleased(hmi::Key::Space)` est vrai.<br/>Vérifie que `input.keyReleased(hmi::Key::Space)` est faux. |
| **InputStateTest.TouchesIndependantes** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:90`</sub> | Les touches sont indépendantes : un front sur l'une n'affecte pas les autres. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.keyPressed(hmi::Key::Up)` est vrai.<br/>Vérifie que `input.keyDown(hmi::Key::Down)` est faux.<br/>Vérifie que `input.keyPressed(hmi::Key::Down)` est faux. |
| **InputStateTest.BoutonSouris** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:111`</sub> | Un bouton de souris suit la même logique pressé/cliqué/relâché que les touches. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.mouseButtonDown(hmi::MouseButton::Left)` est vrai.<br/>Vérifie que `input.mouseButtonPressed(hmi::MouseButton::Left)` est vrai.<br/>Vérifie que `input.mouseButtonDown(hmi::MouseButton::Left)` est vrai.<br/>Vérifie que `input.mouseButtonPressed(hmi::MouseButton::Left)` est faux.<br/>Vérifie que `input.mouseButtonDown(hmi::MouseButton::Left)` est faux.<br/>Vérifie que `input.mouseButtonReleased(hmi::MouseButton::Left)` est vrai. |
| **InputStateTest.PositionSouris** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:140`</sub> | La position de la souris reflète le dernier déplacement injecté. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.mouseX()` vaut `42`.<br/>Vérifie que `input.mouseY()` vaut `99`.<br/>Vérifie que `input.mouseX()` vaut `-3`.<br/>Vérifie que `input.mouseY()` vaut `7`. |
| **InputStateTest.MoletteAccumuleEtSeReinitialise** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:162`</sub> | Les incréments de molette d'une frame s'additionnent et repartent de zéro ensuite. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.wheelDelta()` vaut `0`.<br/>Vérifie que `input.wheelDelta()` vaut `80`.<br/>Vérifie que `input.wheelDelta()` vaut `0`. |
| **InputStateTest.CaracteresTapesAccumulesEtVides** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:186`</sub> | Les caractères tapés s'accumulent dans l'ordre puis sont vidés à la frame suivante. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.typedCharacters().empty()` est vrai.<br/>Vérifie que `input.typedCharacters().size()` vaut `3u`.<br/>Vérifie que `input.typedCharacters()[0]` vaut `L'N'`.<br/>Vérifie que `input.typedCharacters()[1]` vaut `L'1'`.<br/>Vérifie que `input.typedCharacters()[2]` vaut `L'\\xE9'`.<br/>Vérifie que `input.typedCharacters().empty()` est vrai. |
| **InputStateTest.ManetteSeuleActiveLaTouche** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:215`</sub> | Un bouton manette seul rend `keyDown`/`keyPressed` vrais, comme au clavier. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.keyDown(hmi::Key::Enter)` est vrai.<br/>Vérifie que `input.keyPressed(hmi::Key::Enter)` est vrai.<br/>Vérifie que `input.keyDown(hmi::Key::Enter)` est vrai.<br/>Vérifie que `input.keyPressed(hmi::Key::Enter)` est faux.<br/>Vérifie que `input.keyDown(hmi::Key::Enter)` est faux.<br/>Vérifie que `input.keyReleased(hmi::Key::Enter)` est vrai. |
| **InputStateTest.ClavierEtManetteMemeToucheUnSeulFront** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:243`</sub> | Clavier et manette combinés sur la même touche ne produisent pas de double front. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.keyPressed(hmi::Key::Space)` est vrai.<br/>Vérifie que `input.keyPressed(hmi::Key::Space)` est faux.<br/>Vérifie que `input.keyDown(hmi::Key::Space)` est vrai.<br/>Vérifie que `input.keyReleased(hmi::Key::Space)` est faux.<br/>Vérifie que `input.keyDown(hmi::Key::Space)` est faux.<br/>Vérifie que `input.keyReleased(hmi::Key::Space)` est vrai. |
| **InputStateTest.ManetteRelacheeNeMasquePasLeClavier** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:280`</sub> | La manette relâchée ne masque jamais une touche clavier réellement maintenue. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.keyDown(hmi::Key::Left)` est vrai.<br/>Vérifie que `input.keyDown(hmi::Key::Left)` est vrai.<br/>Vérifie que `input.keyReleased(hmi::Key::Left)` est faux. |
| **InputStateTest.GamepadConnecteReecrasable** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:308`</sub> | `gamepadConnected` reflète le dernier `setGamepadConnected` appelé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.gamepadConnected()` est faux.<br/>Vérifie que `input.gamepadConnected()` est vrai.<br/>Vérifie que `input.gamepadConnected()` est faux. |
| **InputStateTest.FrontMontantBoutonManetteBrut** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:330`</sub> | Un bouton manette (piste brute) est « pressé » exactement une frame. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.gamepadButtonDown(hmi::GamepadButton::A)` est vrai.<br/>Vérifie que `input.gamepadButtonPressed(hmi::GamepadButton::A)` est vrai.<br/>Vérifie que `input.gamepadButtonDown(hmi::GamepadButton::A)` est vrai.<br/>Vérifie que `input.gamepadButtonPressed(hmi::GamepadButton::A)` est faux.<br/>Vérifie que `input.gamepadButtonDown(hmi::GamepadButton::A)` est faux. |
| **InputStateTest.PisteBrutIndependanteDeLaFusionKey** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:357`</sub> | La piste manette brute est indépendante de la fusion clavier/manette sur Key. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.keyDown(hmi::Key::Enter)` est faux.<br/>Vérifie que `input.gamepadButtonDown(hmi::GamepadButton::B)` est faux. |
| **InputStateTest.RelacheToutSansFront** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_input_state.cpp:380`</sub> | releaseAll relâche tout sans produire de front « relâchée ». | 1. Maintenir des entrées clavier/manette/souris.<br/>2. Appeler releaseAll et verifier qu'aucune n'est plus enfoncee ni signalee « relâchée ». | Vérifie que `input.keyDown(hmi::Key::Right)` est faux.<br/>Vérifie que `input.keyReleased(hmi::Key::Right)` est faux.<br/>Vérifie que `input.keyDown(hmi::Key::Space)` est faux.<br/>Vérifie que `input.keyReleased(hmi::Key::Space)` est faux.<br/>Vérifie que `input.gamepadButtonDown(hmi::GamepadButton::A)` est faux.<br/>Vérifie que `input.mouseButtonDown(hmi::MouseButton::Left)` est faux.<br/>Vérifie que `input.mouseButtonReleased(hmi::MouseButton::Left)` est faux.<br/>Vérifie que `input.keyReleased(hmi::Key::Right)` est faux.<br/>Vérifie que `input.keyPressed(hmi::Key::Right)` est faux. |

**`test_key_name.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **KeyNameTest.ToucheNommeeLibelleDedie** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_key_name.cpp:13`</sub> | Une touche nommée de l'énumération a un libellé lisible dédié. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::keyDisplayName(hmi::Key::Left)` vaut `"Fleche gauche"`.<br/>Vérifie que `hmi::keyDisplayName(hmi::Key::Space)` vaut `"Espace"`.<br/>Vérifie que `hmi::keyDisplayName(hmi::Key::Shift)` vaut `"Maj"`.<br/>Vérifie que `hmi::keyDisplayName(hmi::Key::F10)` vaut `"F10"`. |
| **KeyNameTest.LettreNonNommeeAfficheSonCaractere** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Input/test_key_name.cpp:30`</sub> | Une lettre ou un chiffre non nommé explicitement s'affiche par son propre caractère. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::keyDisplayName(static_cast<hmi::Key>('B'))` vaut `"B"`.<br/>Vérifie que `hmi::keyDisplayName(static_cast<hmi::Key>('5'))` vaut `"5"`. |
| **KeyNameTest.ToucheInconnueRepliHexadecimal** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Input/test_key_name.cpp:47`</sub> | Une touche totalement inconnue s'affiche via un repli hexadécimal. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::keyDisplayName(static_cast<hmi::Key>(0x12))` vaut `"Touche 0x12"`. |
| **KeyNameTest.CapturedKeyRenvoieLaToucheEnfoncee** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_key_name.cpp:61`</sub> | `capturedKey` renvoie la touche pressée à la frame courante. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `captured.has_value()` est vrai.<br/>Vérifie que `*captured` vaut `hmi::Key::F1`. |
| **KeyNameTest.CapturedKeyVideSansTouche** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_key_name.cpp:79`</sub> | `capturedKey` renvoie vide quand aucune touche n'est pressée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::capturedKey(input).has_value()` est faux. |
| **KeyNameTest.EchapEtEntreeReserveesNonCapturees** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_key_name.cpp:94`</sub> | `Échap` et `Entrée` sont réservées : leur pression seule ne capture rien. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::capturedKey(escapeOnly).has_value()` est faux.<br/>Vérifie que `hmi::capturedKey(enterOnly).has_value()` est faux. |

**`test_player_input_mapper.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **PlayerInputMapperTest.FlecheGauche** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:43`</sub> | Flèche gauche seule → intention vers la gauche (-1). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mapWithDefaults(withKeys({hmi::Key::Left})).moveX` vaut `-1.0f` (comparaison flottante). |
| **PlayerInputMapperTest.FlecheDroite** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:57`</sub> | Flèche droite seule → intention vers la droite (+1). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mapWithDefaults(withKeys({hmi::Key::Right})).moveX` vaut `1.0f` (comparaison flottante). |
| **PlayerInputMapperTest.SansRemapQDWNeDeclenchentRien** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:71`</sub> | Sans remap, Q/D/W ne déclenchent rien (aucun alias fixe). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mapWithDefaults(withKeys({hmi::Key::Q})).moveX` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `mapWithDefaults(withKeys({hmi::Key::D})).moveX` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `mapWithDefaults(withKeys({hmi::Key::W})).jumpPressed` est faux. |
| **PlayerInputMapperTest.AucuneTouche** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:87`</sub> | Aucune touche → intention nulle (immobile). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mapWithDefaults(hmi::InputState{}).moveX` vaut `0.0f` (comparaison flottante). |
| **PlayerInputMapperTest.GaucheEtDroiteSeNeutralisent** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:101`</sub> | Gauche et droite simultanées → neutralisation (0). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mapWithDefaults(withKeys({hmi::Key::Left, hmi::Key::Right})).moveX` vaut `0.0f` (comparaison flottante). |
| **PlayerInputMapperTest.EspacePresseeDeclencheLeSaut** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:115`</sub> | Espace fraîchement enfoncée → saut **pressé** (front) et **maintenu**. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.jumpPressed` est vrai.<br/>Vérifie que `input.jumpHeld` est vrai. |
| **PlayerInputMapperTest.SautMaintenuN_estPasUnFront** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:131`</sub> | Saut **maintenu** sans nouveau front → `jumpHeld` vrai mais `jumpPressed` faux. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mapped.jumpPressed` est faux.<br/>Vérifie que `mapped.jumpHeld` est vrai. |
| **PlayerInputMapperTest.PasDeSaut** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:153`</sub> | Aucune touche de saut → ni pressé ni maintenu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.jumpPressed` est faux.<br/>Vérifie que `input.jumpHeld` est faux. |
| **PlayerInputMapperTest.DeplacementEtSautIndependants** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:169`</sub> | Déplacement et saut sont indépendants (axes distincts). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.moveX` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `input.jumpPressed` est vrai. |
| **PlayerInputMapperTest.MajDeclencheLeDash** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:185`</sub> | Maj fraîchement enfoncée → dash **pressé** (front). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mapWithDefaults(withKeys({hmi::Key::Shift})).dashPressed` est vrai.<br/>Vérifie que `mapWithDefaults(hmi::InputState{}).dashPressed` est faux. |
| **PlayerInputMapperTest.ViseeVerticale** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:200`</sub> | Visée verticale (y vers le bas) : Bas → +1, Haut → -1, les deux → 0. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `mapWithDefaults(withKeys({hmi::Key::Down})).moveY` vaut `1.0f` (comparaison flottante).<br/>Vérifie que `mapWithDefaults(withKeys({hmi::Key::Up})).moveY` vaut `-1.0f` (comparaison flottante).<br/>Vérifie que `mapWithDefaults(withKeys({hmi::Key::Up, hmi::Key::Down})).moveY` vaut `0.0f` (comparaison flottante). |
| **PlayerInputMapperTest.RemapperUneActionClavierUtiliseLaNouvelleTouche** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:216`</sub> | Une action remappée au clavier réagit à sa nouvelle touche. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::toPlayerInput(withKeys({hmi::Key::F1}), gameKeyBindings, hmi::GamepadBindings{}) .dashPressed` est vrai. |
| **PlayerInputMapperTest.RemapperGaucheEtDroiteSurDesTouchesDistinctesNeLesAnnulePas** (Bloquant)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:236`</sub> | Remapper Gauche et Droite sur des touches distinctes ne les neutralise pas l'une l'autre. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::toPlayerInput(withKeys({hmi::Key::D}), bindings, gamepadBindings).moveX` vaut `-1.0f` (comparaison flottante).<br/>Vérifie que `hmi::toPlayerInput(withKeys({hmi::Key::Q}), bindings, gamepadBindings).moveX` vaut `1.0f` (comparaison flottante). |
| **PlayerInputMapperTest.ActionDeclencheeParSonBoutonManetteParDefaut** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:263`</sub> | Une action est déclenchée par son bouton manette lié. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `input.jumpPressed` est vrai. |
| **PlayerInputMapperTest.RemapperUneActionManetteUtiliseLeNouveauBouton** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:279`</sub> | Une action remappée manette réagit à son nouveau bouton, le clavier reste indépendant. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::toPlayerInput(withGamepadButton(hmi::GamepadButton::X), hmi::GameKeyBindings{}, gamepadBindings) .jumpPressed` est vrai.<br/>Vérifie que `mapWithDefaults(withKeys({hmi::Key::Space})).jumpPressed` est vrai. |
| **PlayerInputMapperTest.RemapperGaucheEtDroiteManetteSurDesBoutonsDistinctsNeLesAnnulePas** (Bloquant)<br/><sub>`Source/Test/Unit/HMI/Input/test_player_input_mapper.cpp:304`</sub> | Remapper deux actions manette sur des boutons distincts ne les neutralise pas l'une l'autre. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `hmi::toPlayerInput(withGamepadButton(hmi::GamepadButton::X), gameKeyBindings, gamepadBindings) .moveX` vaut `-1.0f` (comparaison flottante).<br/>Vérifie que `hmi::toPlayerInput(withGamepadButton(hmi::GamepadButton::Y), gameKeyBindings, gamepadBindings) .moveX` vaut `1.0f` (comparaison flottante). |

#### Interface (28)

**`test_application_theme.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **ApplicationThemeTest.SubstitutionRemplaceTousLesMarqueurs** (Critique)<br/><sub>`Source/Test/Unit/HMI/Interface/test_application_theme.cpp:35`</sub> | La substitution remplace tous les marqueurs du modele. | 1. Substituer un modele avec deux marqueurs connus. | Vérifie que `result.ok` est vrai.<br/>Vérifie que `result.text` vaut `"x: 1; y: 2;"`. |
| **ApplicationThemeTest.MarqueurInconnuEstSignale** (Critique)<br/><sub>`Source/Test/Unit/HMI/Interface/test_application_theme.cpp:53`</sub> | Un marqueur inconnu est signale plutot que produit avec un trou. | 1. Substituer un modele dont un marqueur n'a pas de valeur. | Vérifie que `result.ok` est faux.<br/>Vérifie que `result.error.find("inconnu")` diffère de `std::string::npos`. |
| **ApplicationThemeTest.AucuneCouleurLitteraleDansLeModeleReel** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_application_theme.cpp:70`</sub> | Le modele de theme livre ne contient aucune couleur litterale. | 1. Lire le fichier theme.qss livre.<br/>2. Chercher un motif de couleur hexadecimale. | Vérifie que `themeText.empty()` est faux.<br/>Vérifie que `std::regex_search(withoutComments, hexColorPattern)` est faux. |
| **ApplicationThemeTest.EtancheiteDesPortees** (Critique)<br/><sub>`Source/Test/Unit/HMI/Interface/test_application_theme.cpp:96`</sub> | Les regles d'identite sont etanches au theme de l'editeur. | 1. Substituer le modele reel avec deux jeux de valeurs 'editor.*' distincts, memes valeurs 'identity.*'.<br/>2. Extraire les blocs `#MainMenu`/`#OptionsPage` des deux resultats. | Vérifie que `themeText.empty()` est faux.<br/>Vérifie que `dark.ok` est vrai.<br/>Vérifie que `light.ok` est vrai.<br/>Vérifie que `darkEnd` diffère de `std::string::npos`.<br/>Vérifie que `lightEnd` diffère de `std::string::npos`.<br/>Vérifie que `dark.text.substr(0, darkEnd)` vaut `light.text.substr(0, lightEnd)`. |
| **ApplicationThemeTest.ResolutionDePoliceSansNomDeRepliCodeEnDur** (Critique)<br/><sub>`Source/Test/Unit/HMI/Interface/test_application_theme.cpp:157`</sub> | La resolution de police retombe sur une famille generique sans nom code en dur. | 1. Resoudre la police embarquee enregistree, puis non enregistree. | Vérifie que `registered.useEmbeddedFamily` est vrai.<br/>Vérifie que `registered.embeddedFamily` vaut `"Inter"`.<br/>Vérifie que `missing.useEmbeddedFamily` est faux.<br/>Vérifie que `missing.embeddedFamily.empty()` est vrai. |
| **ApplicationThemeTest.EchelleTypographiquePositiveEtOrdonnee** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_application_theme.cpp:177`</sub> | L'echelle typographique est positive et ordonnee du plus grand au plus petit. | 1. Lire les tailles de l'echelle typographique des jetons. | Vérifie que `typography.screenTitle.pointSize` est strictement supérieur à `0`.<br/>Vérifie que `typography.sectionTitle.pointSize` est strictement supérieur à `0`.<br/>Vérifie que `typography.body.pointSize` est strictement supérieur à `0`.<br/>Vérifie que `typography.caption.pointSize` est strictement supérieur à `0`.<br/>Vérifie que `typography.monospaceBody.pointSize` est strictement supérieur à `0`.<br/>Vérifie que `typography.screenTitle.pointSize` est strictement supérieur à `typography.sectionTitle.pointSize`.<br/>Vérifie que `typography.sectionTitle.pointSize` est strictement supérieur à `typography.body.pointSize`.<br/>Vérifie que `typography.body.pointSize` est strictement supérieur à `typography.caption.pointSize`. |
| **ApplicationThemeTest.AucuneTailleDePoliceResiduelleDansLesFichiersUi** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_application_theme.cpp:200`</sub> | Aucune taille de police ni marge figee ne subsiste dans les fichiers .ui. | 1. Lire MainMenu.ui et OptionsPage.ui.<br/>2. Chercher une propriete font/margin figee au niveau du widget racine. | Vérifie que `text.empty()` est faux.<br/>Vérifie que `text.find("<property name=\\"font\\">")` vaut `std::string::npos`.<br/>Vérifie que `text.find("Margin")` vaut `std::string::npos`. |
| **ApplicationThemeTest.ResolutionDuThemeEffectifSuitLeReglageEtLeSysteme** (Critique)<br/><sub>`Source/Test/Unit/HMI/Interface/test_application_theme.cpp:225`</sub> | La resolution du theme effectif suit le reglage et, si Systeme, le systeme. | 1. Resoudre les quatre combinaisons reglage/systeme. | Vérifie que `hmi::resolveEffectiveEditorTheme(EditorThemeSetting::System, /*systemPrefersDark=*/true)` vaut `EditorThemeMode::Dark`.<br/>Vérifie que `hmi::resolveEffectiveEditorTheme(EditorThemeSetting::System, /*systemPrefersDark=*/false)` vaut `EditorThemeMode::Light`.<br/>Vérifie que `hmi::resolveEffectiveEditorTheme(EditorThemeSetting::Light, /*systemPrefersDark=*/true)` vaut `EditorThemeMode::Light`.<br/>Vérifie que `hmi::resolveEffectiveEditorTheme(EditorThemeSetting::Dark, /*systemPrefersDark=*/false)` vaut `EditorThemeMode::Dark`. |
| **ApplicationThemeTest.EtancheiteDesPorteesAvecLesVraisThemes** (Critique)<br/><sub>`Source/Test/Unit/HMI/Interface/test_application_theme.cpp:255`</sub> | L'etancheite des portees tient avec les vrais themes sombre et clair. | 1. Produire la feuille de style avec editorDarkTokens() puis editorLightTokens().<br/> 2. Comparer les blocs `#MainMenu`/`#OptionsPage` des deux resultats. | Vérifie que `themeText.empty()` est faux.<br/>Vérifie que `dark.ok` est vrai.<br/>Vérifie que `light.ok` est vrai.<br/>Vérifie que `darkEnd` diffère de `std::string::npos`.<br/>Vérifie que `lightEnd` diffère de `std::string::npos`.<br/>Vérifie que `dark.text.substr(0, darkEnd)` vaut `light.text.substr(0, lightEnd)`.<br/>Vérifie que `dark.text.substr(darkEnd)` diffère de `light.text.substr(lightEnd)`. |

**`test_design_tokens.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **DesignTokensTest.ConversionHexadecimaleStableEtBienFormee** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_design_tokens.cpp:14`</sub> | La conversion en couleur CSS hexadecimale est stable et bien formee. | 1. Convertir une couleur connue en chaine hexadecimale.<br/>2. Reconvertir la meme couleur. | Vérifie que `first` vaut `second`.<br/>Vérifie que `first.size()` vaut `7u`.<br/>Vérifie que `first` vaut `"#1a2b3c"`. |
| **DesignTokensTest.ConversionRgbaPorteLesQuatreComposantes** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_design_tokens.cpp:35`</sub> | La conversion en couleur CSS rgba() porte les quatre composantes. | 1. Convertir une couleur avec alpha partiel. | Vérifie que `hmi::toCssRgba(color)` vaut `"rgba(0, 128, 255, 64)"`. |
| **DesignTokensTest.CouleurViewportSuitLaPorteeDuMode** (Critique)<br/><sub>`Source/Test/Unit/HMI/Interface/test_design_tokens.cpp:51`</sub> | La couleur d'effacement du viewport suit le jeton de fond de la bonne portee. | 1. Demander la couleur d'effacement en mode edition, puis en mode jeu. | Vérifie que `hmi::viewportClearColor(/*editorMode=*/true, hmi::editorDarkTokens())` vaut `hmi::editorDarkTokens().color.background`.<br/>Vérifie que `hmi::viewportClearColor(/*editorMode=*/true, hmi::editorLightTokens())` vaut `hmi::editorLightTokens().color.background`.<br/>Vérifie que `hmi::viewportClearColor(/*editorMode=*/false, hmi::editorDarkTokens())` vaut `hmi::identityTokens().color.background`. |
| **DesignTokensTest.LesDeuxPorteesPartagentLesMemesEchelles** (Critique)<br/><sub>`Source/Test/Unit/HMI/Interface/test_design_tokens.cpp:70`</sub> | Les deux portees partagent les memes echelles et tailles. | 1. Comparer espacement, typographie et tailles des deux jeux de jetons. | Vérifie que `hmi::identityTokens().spacing` vaut `hmi::editorDarkTokens().spacing`.<br/>Vérifie que `hmi::identityTokens().typography` vaut `hmi::editorDarkTokens().typography`.<br/>Vérifie que `hmi::identityTokens().size` vaut `hmi::editorDarkTokens().size`.<br/>Vérifie que `hmi::editorDarkTokens().spacing` vaut `hmi::editorLightTokens().spacing`.<br/>Vérifie que `hmi::editorDarkTokens().typography` vaut `hmi::editorLightTokens().typography`.<br/>Vérifie que `hmi::editorDarkTokens().size` vaut `hmi::editorLightTokens().size`. |
| **DesignTokensTest.CouleursDesDeuxPorteesDistinctesEtCoherentes** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_design_tokens.cpp:90`</sub> | Les couleurs des deux portees sont definies et distinctes. | 1. Comparer le fond des deux portees.<br/>2. Verifier que le texte contraste avec le fond dans chaque portee. | Vérifie que `hmi::identityTokens().color.background == hmi::editorDarkTokens().color.background` est faux.<br/>Vérifie que `hmi::identityTokens().color.text == hmi::identityTokens().color.background` est faux.<br/>Vérifie que `hmi::editorDarkTokens().color.text == hmi::editorDarkTokens().color.background` est faux. |
| **DesignTokensTest.AucunDoublonDeLargeurMinimale** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_design_tokens.cpp:108`</sub> | Une seule grandeur de largeur minimale existe pour les deux widgets de remappage. | 1. Lire `controlMinWidth` dans les jetons du chassis d'edition. | Vérifie que `hmi::editorDarkTokens().size.controlMinWidth` est strictement supérieur à `0`. |
| **DesignTokensTest.ChaqueThemeSatisfaitLeSeuilDeContraste** (Critique)<br/><sub>`Source/Test/Unit/HMI/Interface/test_design_tokens.cpp:122`</sub> | Chaque theme d'editeur satisfait un seuil de contraste texte/fond. | 1. Calculer le contraste texte/fond et texte-attenue/fond pour les deux themes. | Vérifie que `hmi::contrastRatio(tokens->color.text, tokens->color.background)` est supérieur ou égal à `MIN_TEXT_CONTRAST`.<br/>Vérifie que `hmi::contrastRatio(tokens->color.textMuted, tokens->color.background)` est supérieur ou égal à `MIN_MUTED_TEXT_CONTRAST`. |
| **DesignTokensTest.ContrasteSymetriqueEtUnitairePourUneMemeCouleur** (Mineur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_design_tokens.cpp:143`</sub> | Le rapport de contraste est symetrique et vaut 1 sans difference de couleur. | 1. Calculer le contraste d'une couleur avec elle-meme, puis dans les deux sens entre deux couleurs distinctes. | Vérifie que `hmi::contrastRatio(color, color)` vaut `1.0`, à `1e-9` près.<br/>Vérifie que `hmi::contrastRatio(hmi::DesignColor{0, 0, 0}, hmi::DesignColor{255, 255, 255})` vaut `hmi::contrastRatio(hmi::DesignColor{255, 255, 255}, hmi::DesignColor{0, 0, 0})`, à `1e-9` près. |

**`test_editor_actions.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **EditorActionsTest.LesSixOutilsFormentUneBijectionAvecEditorTool** (Critique)<br/><sub>`Source/Test/Unit/HMI/Interface/test_editor_actions.cpp:35`</sub> | Les six outils du catalogue forment une bijection avec EditorTool. | 1. Pour chaque hmi::EditorTool, resoudre l'action puis reconvertir vers l'outil. | Vérifie que `seen.insert(id).second` est vrai.<br/>Vérifie que `roundTrip.has_value()` est vrai.<br/>Vérifie que `*roundTrip` vaut `tool`.<br/>Vérifie que `seen.size()` vaut `6u`. |
| **EditorActionsTest.LesCinqOutilsDeCanevasFormentUneBijectionAvecPixelTool** (Critique)<br/><sub>`Source/Test/Unit/HMI/Interface/test_editor_actions.cpp:62`</sub> | Les quatre outils de canevas forment une bijection avec PixelTool. | 1. Pour chaque hmi::PixelTool, resoudre l'action puis reconvertir vers l'outil.<br/> 2. Verifier que l'action appartient au groupe PixelTools. | Vérifie que `seen.insert(id).second` est vrai.<br/>Vérifie que `hmi::editorActionSpec(id).group` vaut `hmi::EditorActionGroup::PixelTools`.<br/>Vérifie que `hmi::editorActionTool(id).has_value()` est faux.<br/>Vérifie que `roundTrip.has_value()` est vrai.<br/>Vérifie que `*roundTrip` vaut `tool`.<br/>Vérifie que `seen.size()` vaut `5u`. |
| **EditorActionsTest.DefinitionUniqueDeRaccourci** (Critique)<br/><sub>`Source/Test/Unit/HMI/Interface/test_editor_actions.cpp:92`</sub> | Aucun raccourci n'est attribue a deux actions. | 1. Parcourir le catalogue, ignorer les raccourcis vides.<br/>2. Verifier qu'aucune valeur n'apparait deux fois. | Vérifie que `shortcuts.insert(shortcut).second` est vrai. |
| **EditorActionsTest.GeometrieDesIconesNonVideEtDansLeCadre** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_editor_actions.cpp:115`</sub> | La geometrie de chaque icone est non vide et dans le cadre normalise. | 1. Pour chaque action du catalogue, produire sa geometrie.<br/>2. Verifier qu'elle a au moins un trait, et que chaque point est dans [0,1]. | Vérifie que `geometry.strokes.empty()` est faux.<br/>Vérifie que `stroke.points.empty()` est faux.<br/>Vérifie que `point.x` est supérieur ou égal à `0.0f`.<br/>Vérifie que `point.x` est inférieur ou égal à `1.0f`.<br/>Vérifie que `point.y` est supérieur ou égal à `0.0f`.<br/>Vérifie que `point.y` est inférieur ou égal à `1.0f`. |
| **EditorActionsTest.ChaqueLibelleExisteDansLesDeuxLangues** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_editor_actions.cpp:142`</sub> | Chaque cle de libelle du catalogue d'actions existe en francais et en anglais. | 1. Lire fr.lang et en.lang.<br/>2. Verifier que chaque labelKey du catalogue y figure. | Vérifie que `fr.empty()` est faux.<br/>Vérifie que `en.empty()` est faux.<br/>Vérifie que `fr.count(spec.labelKey) > 0` est vrai.<br/>Vérifie que `en.count(spec.labelKey) > 0` est vrai. |
| **EditorActionsTest.AucuneActionRemappableOrpheline** (Critique)<br/><sub>`Source/Test/Unit/HMI/Interface/test_editor_actions.cpp:165`</sub> | Chaque action d'editeur remappable a une commande effective. | 1. Comparer le nombre d'actions remappables (hors outil) au nombre d'entrees de la table de correspondance.<br/>2. Verifier que chaque entree pointe vers une commande reelle du catalogue et que l'aller-retour restitue l'action d'origine. | Vérifie que `hmi::KEY_BINDING_ICON_COUNT` vaut `hmi::EDITOR_ACTION_COUNT - 1`.<br/>`EXPECT_NO_THROW(static_cast<void>(hmi::editorActionSpec(entry.id)))`<br/>Vérifie que `seen.insert(entry.id).second` est vrai.<br/>Vérifie que `roundTrip.has_value()` est vrai.<br/>Vérifie que `*roundTrip` vaut `entry.action`. |

**`test_screen_flow.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **ScreenFlowTest.TransitionsAutoriseesMenentALEcranAttendu** (Critique)<br/><sub>`Source/Test/Unit/HMI/Interface/test_screen_flow.cpp:24`</sub> | Chaque transition autorisée mène à l'écran attendu. | 1. Résoudre chacune des transitions autorisées listées.<br/>2. Vérifier l'écran résultant. | Vérifie que `resolveTransition(menu, ScreenEvent::OpenEditor)->screen` vaut `ScreenId::Editor`.<br/>Vérifie que `resolveTransition(menu, ScreenEvent::OpenGame)->screen` vaut `ScreenId::Game`.<br/>Vérifie que `resolveTransition(menu, ScreenEvent::OpenOptions)->screen` vaut `ScreenId::Options`.<br/>Vérifie que `resolveTransition(editor, ScreenEvent::OpenMenu)->screen` vaut `ScreenId::Menu`.<br/>Vérifie que `resolveTransition(game, ScreenEvent::OpenMenu)->screen` vaut `ScreenId::Menu`.<br/>Vérifie que `resolveTransition(game, ScreenEvent::OpenPause)->screen` vaut `ScreenId::Pause`.<br/>Vérifie que `resolveTransition(game, ScreenEvent::LevelSucceeded)->screen` vaut `ScreenId::NiveauTermine`.<br/>Vérifie que `resolveTransition(pause, ScreenEvent::ResumePause)->screen` vaut `ScreenId::Game`.<br/>Vérifie que `resolveTransition(pause, ScreenEvent::RestartFromPause)->screen` vaut `ScreenId::Game`.<br/>Vérifie que `resolveTransition(pause, ScreenEvent::QuitPauseToMenu)->screen` vaut `ScreenId::Menu`.<br/>Vérifie que `resolveTransition(pause, ScreenEvent::OpenOptions)->screen` vaut `ScreenId::Options`.<br/>Vérifie que `resolveTransition(optionsFromMenu, ScreenEvent::CloseOptions)->screen` vaut `ScreenId::Menu`.<br/>Vérifie que `resolveTransition(optionsFromPause, ScreenEvent::CloseOptions)->screen` vaut `ScreenId::Pause`.<br/>Vérifie que `resolveTransition(niveauTermine, ScreenEvent::ContinueAfterLevel)->screen` vaut `ScreenId::Game`.<br/>Vérifie que `resolveTransition(niveauTermine, ScreenEvent::ReplayLevel)->screen` vaut `ScreenId::Game`.<br/>Vérifie que `resolveTransition(niveauTermine, ScreenEvent::ReturnToMenuFromLevelComplete)->screen` vaut `ScreenId::Menu`.<br/>Vérifie que `resolveTransition(menu, ScreenEvent::OpenLevelSelect)->screen` vaut `ScreenId::LevelSelect`.<br/>Vérifie que `resolveTransition(levelSelect, ScreenEvent::LevelChosen)->screen` vaut `ScreenId::Game`.<br/>Vérifie que `resolveTransition(levelSelect, ScreenEvent::CloseLevelSelect)->screen` vaut `ScreenId::Menu`. |
| **ScreenFlowTest.OptionsRevientVersSonEcranDOrigine** (Critique)<br/><sub>`Source/Test/Unit/HMI/Interface/test_screen_flow.cpp:76`</sub> | Options revient vers son écran d'origine (Menu ou Pause). | 1. Ouvrir Options depuis Menu, puis le fermer : vérifier le retour au Menu.<br/>2. Ouvrir Options depuis Pause, puis le fermer : vérifier le retour à Pause. | Vérifie que `openedFromMenu.has_value()` est vrai.<br/>Vérifie que `resolveTransition(*openedFromMenu, ScreenEvent::CloseOptions)->screen` vaut `ScreenId::Menu`.<br/>Vérifie que `openedFromPause.has_value()` est vrai.<br/>Vérifie que `resolveTransition(*openedFromPause, ScreenEvent::CloseOptions)->screen` vaut `ScreenId::Pause`. |
| **ScreenFlowTest.TransitionInterditeEstRefusee** (Critique)<br/><sub>`Source/Test/Unit/HMI/Interface/test_screen_flow.cpp:104`</sub> | Une transition interdite est refusée. | 1. Tenter Editor -> Pause (via OpenPause).<br/>2. Tenter Menu -> NiveauTermine (via LevelSucceeded).<br/>3. Tenter Menu -> Pause directement. | Vérifie que `resolveTransition(editor, ScreenEvent::OpenPause)` vaut `std::nullopt`.<br/>Vérifie que `resolveTransition(menu, ScreenEvent::LevelSucceeded)` vaut `std::nullopt`.<br/>Vérifie que `resolveTransition(menu, ScreenEvent::OpenPause)` vaut `std::nullopt`.<br/>Vérifie que `resolveTransition(menu, ScreenEvent::ResumePause)` vaut `std::nullopt`.<br/>Vérifie que `resolveTransition(editor, ScreenEvent::OpenOptions)` vaut `std::nullopt`.<br/>Vérifie que `resolveTransition(editor, ScreenEvent::OpenLevelSelect)` vaut `std::nullopt`.<br/>Vérifie que `resolveTransition(game, ScreenEvent::OpenLevelSelect)` vaut `std::nullopt`. |
| **ScreenFlowTest.HabillageDeFenetreEstCeluiAttenduParEcran** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_screen_flow.cpp:132`</sub> | L'habillage de fenêtre est celui attendu pour chaque écran. | 1. Interroger l'habillage de chacun des sept écrans.<br/>2. Vérifier chaque champ. | Vérifie que `menu.docksVisible` est faux.<br/>Vérifie que `menu.menuBarVisible` est faux.<br/>Vérifie que `menu.toolBarVisible` est faux.<br/>Vérifie que `menu.pixelToolBarVisible` est faux.<br/>Vérifie que `menu.editingCommandsEnabled` est faux.<br/>Vérifie que `menu.gamepadNavigationActive` est vrai.<br/>Vérifie que `menu.overlayVisible` est faux.<br/>Vérifie que `editor.docksVisible` est vrai.<br/>Vérifie que `editor.menuBarVisible` est vrai.<br/>Vérifie que `editor.toolBarVisible` est vrai.<br/>Vérifie que `editor.pixelToolBarVisible` est vrai.<br/>Vérifie que `editor.editingCommandsEnabled` est vrai.<br/>Vérifie que `editor.gamepadNavigationActive` est faux.<br/>Vérifie que `editor.overlayVisible` est faux.<br/>Vérifie que `game.docksVisible` est faux.<br/>Vérifie que `game.menuBarVisible` est faux.<br/>Vérifie que `game.toolBarVisible` est faux.<br/>Vérifie que `game.pixelToolBarVisible` est faux.<br/>Vérifie que `game.editingCommandsEnabled` est faux.<br/>Vérifie que `game.gamepadNavigationActive` est faux.<br/>Vérifie que `game.overlayVisible` est faux.<br/>Vérifie que `options.docksVisible` est faux.<br/>Vérifie que `options.menuBarVisible` est faux.<br/>Vérifie que `options.toolBarVisible` est faux.<br/>Vérifie que `options.pixelToolBarVisible` est faux.<br/>Vérifie que `options.editingCommandsEnabled` est faux.<br/>Vérifie que `options.gamepadNavigationActive` est vrai.<br/>Vérifie que `options.overlayVisible` est faux.<br/>Vérifie que `levelSelect.docksVisible` est faux.<br/>Vérifie que `levelSelect.menuBarVisible` est faux.<br/>Vérifie que `levelSelect.toolBarVisible` est faux.<br/>Vérifie que `levelSelect.pixelToolBarVisible` est faux.<br/>Vérifie que `levelSelect.editingCommandsEnabled` est faux.<br/>Vérifie que `levelSelect.gamepadNavigationActive` est vrai.<br/>Vérifie que `levelSelect.overlayVisible` est faux.<br/>Vérifie que `overlay.docksVisible` est faux.<br/>Vérifie que `overlay.menuBarVisible` est faux.<br/>Vérifie que `overlay.toolBarVisible` est faux.<br/>Vérifie que `overlay.pixelToolBarVisible` est faux.<br/>Vérifie que `overlay.editingCommandsEnabled` est faux.<br/>Vérifie que `overlay.gamepadNavigationActive` est vrai.<br/>Vérifie que `overlay.overlayVisible` est vrai. |
| **ScreenFlowTest.ContinuerOuRejouerDepuisNiveauTermineRameneAGame** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Interface/test_screen_flow.cpp:201`</sub> | Continuer/Rejouer depuis NiveauTermine ramène à Game. | 1. Résoudre ContinueAfterLevel depuis NiveauTermine.<br/>2. Résoudre ReplayLevel depuis NiveauTermine. | Vérifie que `resolveTransition(niveauTermine, ScreenEvent::ContinueAfterLevel)->screen` vaut `ScreenId::Game`.<br/>Vérifie que `resolveTransition(niveauTermine, ScreenEvent::ReplayLevel)->screen` vaut `ScreenId::Game`. |

#### Localization (8)

**`test_localization.cpp`**

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **LocalizationTest.AnalyseIgnoreCommentairesEtEspaces** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Localization/test_localization.cpp:17`</sub> | L'analyse ignore les lignes vides et les commentaires, et retire les espaces autour de '='. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `strings.size()` vaut `2u`.<br/>Vérifie que `strings.at("menu.quitter")` vaut `"Quitter"`.<br/>Vérifie que `strings.at("menu.titre")` vaut `"ProjectGaming"`. |
| **LocalizationTest.AnalyseConserveEgalDansLaValeur** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Localization/test_localization.cpp:42`</sub> | Seul le premier '=' sépare ; un '=' dans la valeur est conservé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `strings.size()` vaut `1u`.<br/>Vérifie que `strings.at("expression")` vaut `"a = b + c"`. |
| **LocalizationTest.CleExistanteResolue** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Localization/test_localization.cpp:59`</sub> | Une clé existante est résolue dans la langue active. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `localization.text("menu.quitter")` vaut `"Quitter"`.<br/>Vérifie que `localization.activeLanguage()` vaut `"fr"`. |
| **LocalizationTest.CleInconnueRenvoieLaCle** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Localization/test_localization.cpp:77`</sub> | Une clé inconnue partout est renvoyée telle quelle (repli déterministe, pas de plantage). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `localization.text("menu.inconnue")` vaut `"menu.inconnue"`. |
| **LocalizationTest.ChangementDeLangue** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Localization/test_localization.cpp:96`</sub> | Changer de langue résout les valeurs de la nouvelle langue. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `localization.activeLanguage()` vaut `"en"`.<br/>Vérifie que `localization.text("menu.quitter")` vaut `"Quit"`. |
| **LocalizationTest.RepliSurLangueParDefaut** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Localization/test_localization.cpp:115`</sub> | Une clé manquante dans la langue active retombe sur la langue par défaut. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `localization.text("menu.quitter")` vaut `"Quit"`.<br/>Vérifie que `localization.text("menu.titre")` vaut `"Jeu"`. |
| **LocalizationTest.LangueAbsenteEstRecuperable** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Localization/test_localization.cpp:134`</sub> | Charger une langue absente échoue proprement et conserve la langue active (récupérable). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `localization.loadLanguage("xx")` est faux.<br/>Vérifie que `localization.activeLanguage()` vaut `"fr"`.<br/>Vérifie que `localization.text("menu.quitter")` vaut `"Quitter"`. |
| **LocalizationTest.CatalogueFrancaisLivreSeCharge** (Majeur)<br/><sub>`Source/Test/Unit/HMI/Localization/test_localization.cpp:155`</sub> | Le catalogue français livré (Source/Elements/Localization) se charge et résout ses clés. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `localization.loadDefaultLanguage("fr")` est vrai.<br/>Vérifie que `localization.activeLanguage()` vaut `"fr"`.<br/>Vérifie que `localization.text("menu.quit")` vaut `"Quitter"`.<br/>Vérifie que `localization.text("menu.continue")` vaut `"Continuer"`. |

## Tests d'intégration (89)

### Animation Personnage — `test_animation_personnage.cpp` (13)

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **AnimationPersonnageIntegration.ImmobileAuSolEstEnRepos** (Majeur)<br/><sub>`Source/Test/Integration/test_animation_personnage.cpp:54`</sub> | Au sol et immobile, le clip est Idle et l'image alterne 0/1 après chaque durée d'image. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `animation.clipIndex` vaut `core::PLAYER_CLIP_IDLE`.<br/>Vérifie que `animation.frameIndex` vaut `0`.<br/>Vérifie que `animation.clipIndex` vaut `core::PLAYER_CLIP_IDLE`.<br/>Vérifie que `animation.frameIndex` vaut `1`. |
| **AnimationPersonnageIntegration.EnMouvementAuSolCourt** (Majeur)<br/><sub>`Source/Test/Integration/test_animation_personnage.cpp:85`</sub> | Au sol et en mouvement, le clip est Run et l'image boucle sur les 4 images dans l'ordre. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `animation.clipIndex` vaut `core::PLAYER_CLIP_RUN`.<br/>Vérifie que `animation.frameIndex` vaut `0`.<br/>Vérifie que `animation.clipIndex` vaut `core::PLAYER_CLIP_RUN`.<br/>Vérifie que `animation.frameIndex` vaut `expectedFrame`. |
| **AnimationPersonnageIntegration.EnLAirEstEnSaut** (Majeur)<br/><sub>`Source/Test/Integration/test_animation_personnage.cpp:120`</sub> | En l'air, le clip est Jump et l'image reste figée sur 0, quelle que soit la durée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `animation.clipIndex` vaut `core::PLAYER_CLIP_JUMP`.<br/>Vérifie que `animation.frameIndex` vaut `0`. |
| **AnimationPersonnageIntegration.ChangementDeClipReinitialiseLImage** (Majeur)<br/><sub>`Source/Test/Integration/test_animation_personnage.cpp:145`</sub> | Un changement de clip réinitialise immédiatement l'image et le chronomètre à zéro. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `animation.clipIndex` vaut `core::PLAYER_CLIP_RUN`.<br/>Vérifie que `animation.frameIndex` vaut `1`.<br/>Vérifie que `animation.clipIndex` vaut `core::PLAYER_CLIP_JUMP`.<br/>Vérifie que `animation.frameIndex` vaut `0`.<br/>Vérifie que `animation.elapsed` vaut `0.0f` (comparaison flottante). |
| **AnimationPersonnageIntegration.EntiteSansAnimationIgnoree** (Mineur)<br/><sub>`Source/Test/Integration/test_animation_personnage.cpp:182`</sub> | Une entité sans composant Animation n'est pas affectée par le système. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.hasComponent<core::Animation>(entity)` est faux. |
| **AnimationPersonnageIntegration.AnimationSansJeuDeClipsResteInerte** (Mineur)<br/><sub>`Source/Test/Integration/test_animation_personnage.cpp:207`</sub> | Une entité Animation sans jeu de clips assigné reste inerte, sans planter. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `animation.clipIndex` vaut `0`.<br/>Vérifie que `animation.frameIndex` vaut `0`. |
| **AnimationPersonnageIntegration.EnChuteEstDistinctDuSaut** (Majeur)<br/><sub>`Source/Test/Integration/test_animation_personnage.cpp:235`</sub> | En l'air et en train de descendre, le clip est Fall. | 1. Placer le personnage en l'air, vitesse verticale positive (chute).<br/>2. Executer un pas. | Vérifie que `animation.clipIndex` vaut `core::PLAYER_CLIP_FALL`. |
| **AnimationPersonnageIntegration.ApexSuspenduResteEnSaut** (Majeur)<br/><sub>`Source/Test/Integration/test_animation_personnage.cpp:260`</sub> | En l'air en montee ou a l'apex, le clip est Jump. | 1. Placer le personnage en l'air, vitesse verticale nulle (apex).<br/>2. Executer un pas. | Vérifie que `animation.clipIndex` vaut `core::PLAYER_CLIP_JUMP`. |
| **AnimationPersonnageIntegration.ContactMuralEstEnGlissade** (Majeur)<br/><sub>`Source/Test/Integration/test_animation_personnage.cpp:285`</sub> | Contact mural en l'air : le clip est WallSlide. | 1. Placer le personnage en l'air, au contact d'un mur.<br/>2. Executer un pas. | Vérifie que `animation.clipIndex` vaut `core::PLAYER_CLIP_WALLSLIDE`. |
| **AnimationPersonnageIntegration.DashDomineLaChute** (Critique)<br/><sub>`Source/Test/Integration/test_animation_personnage.cpp:309`</sub> | Le dash domine la chute (combinaison ambigue). | 1. Placer le personnage en chute, dashTimer actif.<br/>2. Executer un pas. | Vérifie que `animation.clipIndex` vaut `core::PLAYER_CLIP_DASH`. |
| **AnimationPersonnageIntegration.DashInterrompLAtterrissage** (Majeur)<br/><sub>`Source/Test/Integration/test_animation_personnage.cpp:333`</sub> | Le dash domine un atterrissage en cours. | 1. Faire atterrir le personnage (transition Land engagee).<br/>2. Declencher un dash au pas suivant. | Vérifie que `world.getComponent<core::Animation>(entity).clipIndex` vaut `core::PLAYER_CLIP_FALL`.<br/>Vérifie que `world.getComponent<core::Animation>(entity).clipIndex` vaut `core::PLAYER_CLIP_LAND`.<br/>Vérifie que `world.getComponent<core::Animation>(entity).clipIndex` vaut `core::PLAYER_CLIP_DASH`. |
| **AnimationPersonnageIntegration.AtterrissageEnchaineSurRepos** (Critique)<br/><sub>`Source/Test/Integration/test_animation_personnage.cpp:366`</sub> | Un contact au sol depuis un clip aerien declenche Land, qui enchaine sur Idle. | 1. Faire chuter le personnage (Fall).<br/>2. Le poser au sol, immobile.<br/>3. Executer jusqu'a la fin de la transition. | Vérifie que `world.getComponent<core::Animation>(entity).clipIndex` vaut `core::PLAYER_CLIP_FALL`.<br/>Vérifie que `world.getComponent<core::Animation>(entity).clipIndex` vaut `core::PLAYER_CLIP_LAND`.<br/>Vérifie que `world.getComponent<core::Animation>(entity).frameIndex` vaut `0`.<br/>Vérifie que `world.getComponent<core::Animation>(entity).clipIndex` vaut `core::PLAYER_CLIP_IDLE`. |
| **AnimationPersonnageIntegration.GlissadeMuraleAuContactDeclencheAtterrissage** (Majeur)<br/><sub>`Source/Test/Integration/test_animation_personnage.cpp:399`</sub> | Glissade murale au moment du contact au sol : atterrissage. | 1. Placer le personnage en glissade murale.<br/>2. Le poser au sol.<br/>3. Executer un pas. | Vérifie que `world.getComponent<core::Animation>(entity).clipIndex` vaut `core::PLAYER_CLIP_WALLSLIDE`.<br/>Vérifie que `world.getComponent<core::Animation>(entity).clipIndex` vaut `core::PLAYER_CLIP_LAND`. |

### Bloc Réduit — `test_bloc_reduit.cpp` (2)

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **BlocReduitIntegration.ArreteAuBordDeSaBoiteReelle** (Majeur)<br/><sub>`Source/Test/Integration/test_bloc_reduit.cpp:112`</sub> | Un bloc réduit coincé contre un mur arrête le personnage au bord de sa boîte réelle, pas de la case entière. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `blocks.positions()[0]` vaut `(core::GridPosition{2, 2})`.<br/>Vérifie que `world.getComponent<core::Transform>(player).position.x` vaut `1.25f`, à `1e-3f` près. |
| **BlocReduitIntegration.EspaceAutourResteFranchissable** (Majeur)<br/><sub>`Source/Test/Integration/test_bloc_reduit.cpp:154`</sub> | L'espace autour d'un bloc réduit, hors de sa boîte réelle, reste franchissable. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `endX - startX` est strictement supérieur à `8.0f`. |

### Boucle Simulation — `test_boucle_simulation.cpp` (2)

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **BoucleSimulationIntegration.CadenceurPiloteLaSimulation** (Majeur)<br/><sub>`Source/Test/Integration/test_boucle_simulation.cpp:50`</sub> | Le cadenceur découpe un temps réel variable en pas fixes, et la position finale correspond exactement au nombre de pas réellement exécutés (aucune dérive). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `totalSteps` vaut `4`.<br/>Vérifie que `position.x` vaut `6.0f * totalSteps * STEP`, à `TOLERANCE` près. |
| **BoucleSimulationIntegration.MemeEntreeMemeResultat** (Majeur)<br/><sub>`Source/Test/Integration/test_boucle_simulation.cpp:76`</sub> | Déterminisme : même séquence de frames et même état initial -> même résultat. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `firstPosition.x` vaut `secondPosition.x`, à `TOLERANCE` près.<br/>Vérifie que `firstPosition.y` vaut `secondPosition.y`, à `TOLERANCE` près. |

### Dangers avancés — `test_danger_avance.cpp` (4)

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **DangerAvanceIntegrationTest.DangerDirectionnelMortelSeulementSurSaBande** (Majeur)<br/><sub>`Source/Test/Integration/test_danger_avance.cpp:77`</sub> | Un danger directionnel n'est mortel que sur la bande de son bord désigné. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `outcomeAt(5.0f)` vaut `core::LevelOutcome::Playing`.<br/>Vérifie que `outcomeAt(5.85f)` vaut `core::LevelOutcome::Lost`. |
| **DangerAvanceIntegrationTest.DangerMobileRattrapeLePersonnageImmobile** (Majeur)<br/><sub>`Source/Test/Integration/test_danger_avance.cpp:116`</sub> | Un danger mobile finit par rattraper un personnage sur sa trajectoire. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `outcome` vaut `core::LevelOutcome::Playing`.<br/>Vérifie que `outcome` vaut `core::LevelOutcome::Lost`. |
| **DangerAvanceIntegrationTest.DangerCommuteMortelSeulementApresDeclenchement** (Majeur)<br/><sub>`Source/Test/Integration/test_danger_avance.cpp:163`</sub> | Un danger commuté ne devient mortel qu'une fois son déclencheur actionné. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `walkAndCheckIfEverLost(false)` est faux.<br/>Vérifie que `walkAndCheckIfEverLost(true)` est vrai. |
| **DangerAvanceIntegrationTest.DangerTemporiseMortelSeulementPendantSaFenetreActive** (Majeur)<br/><sub>`Source/Test/Integration/test_danger_avance.cpp:221`</sub> | Un danger temporisé n'est mortel que pendant sa fenêtre active. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `outcomeAtSpawn(0)` vaut `core::LevelOutcome::Lost`.<br/>Vérifie que `outcomeAtSpawn(15)` vaut `core::LevelOutcome::Playing`. |

### Ecs Mouvement — `test_ecs_mouvement.cpp` (4)

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **EcsMouvementIntegration.EntiteMobileAvanceDeVitesseFoisPas** (Majeur)<br/><sub>`Source/Test/Integration/test_ecs_mouvement.cpp:25`</sub> | Une entité Transform + Velocity avance de `velocity * fixedDelta` par pas ; après N pas, la position attendue est déterministe. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `position.x` vaut `2.0f * steps * STEP`, à `TOLERANCE` près.<br/>Vérifie que `position.y` vaut `-3.0f * steps * STEP`, à `TOLERANCE` près. |
| **EcsMouvementIntegration.EntiteSansVelociteNeBougePas** (Majeur)<br/><sub>`Source/Test/Integration/test_ecs_mouvement.cpp:55`</sub> | Une entité sans Velocity ne bouge pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `position.x` vaut `5.0f`, à `TOLERANCE` près.<br/>Vérifie que `position.y` vaut `5.0f`, à `TOLERANCE` près. |
| **EcsMouvementIntegration.DeuxEntitesEvoluentIndependamment** (Majeur)<br/><sub>`Source/Test/Integration/test_ecs_mouvement.cpp:81`</sub> | Deux entités de vitesses différentes évoluent indépendamment. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `slowPosition.x` vaut `1.0f`, à `TOLERANCE` près.<br/>Vérifie que `slowPosition.y` vaut `0.0f`, à `TOLERANCE` près.<br/>Vérifie que `fastPosition.x` vaut `0.0f`, à `TOLERANCE` près.<br/>Vérifie que `fastPosition.y` vaut `4.0f`, à `TOLERANCE` près. |
| **EcsMouvementIntegration.IntegrationViaWorldUpdate** (Majeur)<br/><sub>`Source/Test/Integration/test_ecs_mouvement.cpp:114`</sub> | Enregistré dans le World, le système s'exécute via World::update (chaîne complète). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `position.x` vaut `10.0f * steps * STEP`, à `TOLERANCE` près. |

### Niveau Ecs — `test_niveau_ecs.cpp` (2)

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **NiveauEcsIntegration.DuJsonAuxEntites** (Majeur)<br/><sub>`Source/Test/Integration/test_niveau_ecs.cpp:49`</sub> | Le niveau chargé peuple le monde d'une entité par tuile non vide, bien placée et typée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `tilesByPosition.size()` vaut `3u`.<br/>Vérifie que `tilesByPosition.count({0, 1})` vaut `0u`.<br/>Vérifie que `tilesByPosition.at({0, 0})` vaut `static_cast<int>(core::TileType::Entry)`.<br/>Vérifie que `tilesByPosition.at({1, 0})` vaut `static_cast<int>(core::TileType::Solid)`.<br/>Vérifie que `tilesByPosition.at({1, 1})` vaut `static_cast<int>(core::TileType::Exit)`. |
| **NiveauEcsIntegration.FichierDemoVersMonde** (Majeur)<br/><sub>`Source/Test/Integration/test_niveau_ecs.cpp:85`</sub> | Le niveau de démonstration livré, chargé depuis le fichier, peuple bien le monde. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `result.ok()` est vrai.<br/>Vérifie que `nonEmptyTiles` est strictement supérieur à `0`.<br/>Vérifie que `entities` vaut `nonEmptyTiles`. |

### Physique Personnage — `test_physique_personnage.cpp` (62)

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **PhysiquePersonnageIntegration.TombeSousGraviteVitesseCroissante** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:390`</sub> | Sans sol, le personnage tombe et sa vitesse verticale croît (gravité continue). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `vy1` est strictement supérieur à `0.0f`.<br/>Vérifie que `vy2` est strictement supérieur à `vy1`.<br/>Vérifie que `y2` est strictement supérieur à `y1`.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est faux. |
| **PhysiquePersonnageIntegration.ChuteConvergeVersUneVitesseTerminale** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:422`</sub> | En chute prolongée, la vitesse verticale converge vers une vitesse terminale sans la dépasser. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `lastVy` est inférieur ou égal à `expectedTerminal + TOLERANCE`.<br/>Vérifie que `lastVy` vaut `expectedTerminal`, à `0.05f` près. |
| **PhysiquePersonnageIntegration.AccelerationDeChuteDecroitVersLeRegimePermanent** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:457`</sub> | L'accélération verticale décroît à mesure que la vitesse approche le régime permanent. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `delta` est inférieur ou égal à `previousDelta + TOLERANCE`. |
| **PhysiquePersonnageIntegration.MasseSuperieureTombePlusVite** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:498`</sub> | Une masse plus grande produit une vitesse terminale plus élevée, à traînée égale. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `heavyVy` est strictement supérieur à `lightVy`. |
| **PhysiquePersonnageIntegration.AtterritSurLeSolEtEstAuSol** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:527`</sub> | Le personnage se pose sur le sol : vitesse verticale annulée, état « au sol » vrai. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `transform.position.y` vaut `5.0f`, à `TOLERANCE` près.<br/>Vérifie que `velocity.value.y` vaut `0.0f`, à `TOLERANCE` près.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai. |
| **PhysiquePersonnageIntegration.BloqueParUnMurADroite** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:557`</sub> | Poussé contre un mur, le personnage s'arrête au ras du mur (blocage horizontal). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `transform.position.x` vaut `3.0f`, à `TOLERANCE` près. |
| **PhysiquePersonnageIntegration.AvanceAVitesseConstante** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:586`</sub> | Sur terrain libre, l'avancée horizontale vaut vitesse × temps (vitesse constante). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `endX - startX` vaut `config.moveSpeed * steps * STEP`, à `1e-3f` près. |
| **PhysiquePersonnageIntegration.NeTraversePasLeSolEnChuteRapide** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:617`</sub> | Même en chute très rapide (grand pas), le personnage ne traverse pas le sol (balayage continu). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `transform.position.y` vaut `49.0f`, à `TOLERANCE` près.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai. |
| **PhysiquePersonnageIntegration.Deterministe** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:650`</sub> | Mêmes entrées → même résultat : la simulation est déterministe (pas fixe). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `first.x` vaut `second.x` (comparaison flottante).<br/>Vérifie que `first.y` vaut `second.y` (comparaison flottante). |
| **PhysiquePersonnageIntegration.SauteDepuisLeSol** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:680`</sub> | Au sol, une pression de saut fait décoller le personnage (vitesse ascendante, il s'élève). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai.<br/>Vérifie que `world.getComponent<core::Velocity>(player).value.y` est strictement inférieur à `0.0f`.<br/>Vérifie que `world.getComponent<core::Transform>(player).position.y` est strictement inférieur à `groundY`. |
| **PhysiquePersonnageIntegration.PasDeSautEnLAir** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:720`</sub> | En l'air, une pression de saut n'a aucun effet : pas de double saut (`EX-GP-013`). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Player>(player).grounded` est faux.<br/>Vérifie que `world.getComponent<core::Velocity>(player).value.y` est strictement supérieur à `0.0f`. |
| **PhysiquePersonnageIntegration.HauteurDeSautVariable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:749`</sub> | Hauteur de saut variable : maintenir le bouton saute plus haut que le relâcher tôt. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `fullJump` est strictement supérieur à `shortHop + 0.3f`.<br/>Vérifie que `fullJump` est strictement supérieur à `2.0f`.<br/>Vérifie que `fullJump` est strictement inférieur à `3.0f`. |
| **PhysiquePersonnageIntegration.DoubleSautNombreParametrable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:769`</sub> | Double saut : 1 saut au sol + N sauts aériens (paramétrable), rechargés au contact du sol. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `successfulJumps(1)` vaut `2`.<br/>Vérifie que `successfulJumps(2)` vaut `3`.<br/>Vérifie que `successfulJumps(0)` vaut `1`. |
| **PhysiquePersonnageIntegration.ChutePlusRapideQueLaMontee** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:787`</sub> | Gravité asymétrique : la chute accélère plus vite que la montée ne décélère (EX-GP-018). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `riseDelta` est strictement supérieur à `0.0f`.<br/>Vérifie que `fallDelta` est strictement supérieur à `0.0f`.<br/>Vérifie que `fallDelta` est strictement supérieur à `riseDelta * 1.3f`. |
| **PhysiquePersonnageIntegration.ApexHangReduitLaGravite** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:836`</sub> | Apex hang : près du sommet du saut, la gravité est réduite (contrôle flottant). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `earlyRise` est strictement supérieur à `0.0f`.<br/>Vérifie que `atApex` est strictement supérieur à `0.0f`.<br/>Vérifie que `atApex` est strictement inférieur à `earlyRise * 0.8f`. |
| **PhysiquePersonnageIntegration.FastFallAccelereLaChute** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:883`</sub> | Fast-fall : maintenir « bas » en l'air fait tomber plus loin qu'une chute libre normale. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `fallDistance(true)` est strictement supérieur à `fallDistance(false)`. |
| **PhysiquePersonnageIntegration.DashHorizontalRapide** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:911`</sub> | Dash horizontal : une ruée rapide (≫ vitesse normale) fait parcourir une grande distance. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Velocity>(player).value.x` est strictement supérieur à `config.moveSpeed * 2.0f`.<br/>Vérifie que `world.getComponent<core::Transform>(player).position.x - startX` est strictement supérieur à `config.dashSpeed * config.dashDuration * 0.7f`. |
| **PhysiquePersonnageIntegration.DashDiagonalHautDroite** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:948`</sub> | Dash diagonal (8 directions) : viser haut-droite envoie en +X et −Y. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `velocity.value.x` est strictement supérieur à `0.0f`.<br/>Vérifie que `velocity.value.y` est strictement inférieur à `0.0f`. |
| **PhysiquePersonnageIntegration.GraviteSuspenduePendantDash** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:979`</sub> | Pendant un dash horizontal, la gravité est suspendue (la composante verticale reste nulle). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Velocity>(player).value.y` vaut `0.0f`, à `0.1f` près. |
| **PhysiquePersonnageIntegration.DashUneSeuleFoisEnLAir** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1010`</sub> | Une seule ruée par phase aérienne : le second dash en l'air est refusé jusqu'au retour au sol. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Player>(player).grounded` est faux.<br/>Vérifie que `world.getComponent<core::Player>(player).dashAvailable` est vrai.<br/>Vérifie que `world.getComponent<core::Velocity>(player).value.x` est strictement supérieur à `config.dashSpeed * 0.9f`.<br/>Vérifie que `world.getComponent<core::Player>(player).dashAvailable` est faux.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est faux.<br/>Vérifie que `world.getComponent<core::Velocity>(player).value.x` est strictement inférieur à `config.dashSpeed * 0.9f`. |
| **PhysiquePersonnageIntegration.DashNeTraversePasLeMur** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1065`</sub> | Un dash vers un mur ne le traverse pas (résolu par le balayage). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Transform>(player).position.x` est inférieur ou égal à `4.0f + 0.01f`. |
| **PhysiquePersonnageIntegration.BudgetDeSautsRefuseAuDela** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1093`</sub> | Budget de sauts (EX-GP-024) : avec 1 saut, le premier fonctionne, le suivant est refusé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Velocity>(player).value.y` est strictement inférieur à `0.0f`.<br/>Vérifie que `world.getComponent<core::Player>(player).jumpsRemaining` vaut `0`.<br/>Vérifie que `world.getComponent<core::Velocity>(player).value.y` est strictement supérieur à `-1.0f`. |
| **PhysiquePersonnageIntegration.BudgetDeDashsRefuseAuDela** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1130`</sub> | Budget de dashs (EX-GP-024) : avec 1 dash, le premier fonctionne, le suivant est refusé. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Velocity>(player).value.x` est strictement supérieur à `10.0f`.<br/>Vérifie que `world.getComponent<core::Player>(player).dashesRemaining` vaut `0`.<br/>Vérifie que `world.getComponent<core::Velocity>(player).value.x` est strictement inférieur à `10.0f`. |
| **PhysiquePersonnageIntegration.WallSlideRalentitLaChute** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1167`</sub> | Wall slide : collé à un mur en l'air, la vitesse de chute est plafonnée (descente ralentie). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Player>(player).wallDirection` diffère de `0.0f`.<br/>Vérifie que `velocity.value.y` est strictement supérieur à `0.0f`.<br/>Vérifie que `velocity.value.y` est inférieur ou égal à `config.wallSlideSpeed + 0.5f`. |
| **PhysiquePersonnageIntegration.WallJumpEjecteAlOpposeDuMur** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1199`</sub> | Wall jump : contre un mur à droite, un saut éjecte vers la gauche et le haut. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Player>(player).wallDirection` diffère de `0.0f`.<br/>Vérifie que `velocity.value.x` est strictement inférieur à `0.0f`.<br/>Vérifie que `velocity.value.y` est strictement inférieur à `0.0f`. |
| **PhysiquePersonnageIntegration.PasDeWallJumpSansMur** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1235`</sub> | Sans mur, la logique de wall jump ne s'active pas (et sans saut aérien, aucun saut en l'air). | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Player>(player).wallDirection` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `velocity.value.x` vaut `0.0f` (comparaison flottante).<br/>Vérifie que `velocity.value.y` est strictement supérieur à `before`. |
| **PhysiquePersonnageIntegration.Niveau2FranchissableAvecSaut** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1266`</sub> | Le niveau saut est franchissable **avec le saut** : en avançant et sautant, on atteint la sortie. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playLevelFile("demo-saut.json", rightAndJump)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.Niveau2RequiertLeSaut** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1287`</sub> | Le niveau saut **exige** le saut : en avançant seulement (sans sauter), la marche bloque. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playLevelFile("demo-saut.json", rightOnly)` diffère de `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.Niveau3FranchissableAvecDash** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1310`</sub> | Le niveau dash (couloir bas + fosse) est franchissable **au dash** : avancer + dasher franchit. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playLevelFile("demo-dash.json", rightAndDash)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.Niveau3RequiertLeDash** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1332`</sub> | Le niveau dash **exige** le dash : en avançant seulement, on tombe dans la fosse de danger. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playLevelFile("demo-dash.json", rightOnly)` diffère de `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.Niveau4FranchissableAvecInterrupteur** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1352`</sub> | Le niveau interrupteur : toucher l'interrupteur ouvre la porte → la sortie devient atteignable. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playPuzzleFile("demo-interrupteur.json", right)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.PorteFermeeBloque** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1370`</sub> | Une porte **fermée** (interrupteur non touché) **bloque** : la sortie reste inatteignable. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `simulatePuzzle(level, right, 600)` diffère de `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.CoyoteTimeAutoriseUnSautJusteApresLeBord** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1403`</sub> | Coyote time : sauter juste après avoir quitté un bord fonctionne ; trop tard, non. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `jumpAfterLeavingLedge(0)` est vrai.<br/>Vérifie que `jumpAfterLeavingLedge(15)` est faux. |
| **PhysiquePersonnageIntegration.JumpBufferingHonoreUnSautPreAppuye** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1420`</sub> | Jump buffering : un saut pré-appuyé peu avant l'atterrissage s'exécute à la pose ; trop tôt, non. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bufferedJump(2)` est vrai.<br/>Vérifie que `bufferedJump(30)` est faux. |
| **PhysiquePersonnageIntegration.NiveauDemoEstFranchissableEnAllantADroite** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1438`</sub> | Le niveau de démonstration livré est **franchissable** sans saut : en maintenant « droite », le personnage descend l'escalier de paliers et atteint la sortie sans jamais mourir. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `outcome` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.SuitUnePenteAscendanteEnMarchant** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1477`</sub> | Marcher sur une pente ascendante fait monter le personnage progressivement, sans traverser la surface. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bottom` est inférieur ou égal à `6.0f + TOLERANCE`.<br/>Vérifie que `bottom` est supérieur ou égal à `5.0f - TOLERANCE`.<br/>Vérifie que `sawSlopeSample` est vrai.<br/>Vérifie que `yExitSlope` est strictement inférieur à `yEnterSlope - 0.5f`.<br/>Vérifie que `guard` est strictement inférieur à `600`.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai.<br/>Vérifie que `world.getComponent<core::Transform>(player).position.y` vaut `4.0f`, à `0.05f` près. |
| **PhysiquePersonnageIntegration.SuitUnePenteDescendanteEnMarchant** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1540`</sub> | Marcher sur une pente descendante fait descendre le personnage progressivement, symétrique de la montée. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bottom` est inférieur ou égal à `6.0f + TOLERANCE`.<br/>Vérifie que `bottom` est supérieur ou égal à `5.0f - TOLERANCE`.<br/>Vérifie que `sawSlopeSample` est vrai.<br/>Vérifie que `yExitSlope` est strictement supérieur à `yEnterSlope + 0.5f`.<br/>Vérifie que `guard` est strictement inférieur à `600`.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai.<br/>Vérifie que `world.getComponent<core::Transform>(player).position.y` vaut `5.0f`, à `0.05f` près. |
| **PhysiquePersonnageIntegration.ChuteRapideSurUnePenteSansLaTraverser** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1606`</sub> | Même en chute très rapide, le personnage tombant sur une pente se pose sur sa surface au premier contact, sans la traverser. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `transform.position.y` vaut `49.2f`, à `TOLERANCE` près.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai. |
| **PhysiquePersonnageIntegration.SauteDepuisUnePenteImpulsionNormale** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1639`</sub> | Sauter depuis une pente produit une impulsion identique à un saut depuis un sol plat : le suivi de pente ne l'absorbe pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai.<br/>Vérifie que `world.getComponent<core::Velocity>(player).value.y` est strictement inférieur à `-10.0f`.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est faux.<br/>Vérifie que `world.getComponent<core::Transform>(player).position.y` est strictement inférieur à `landedY`. |
| **PhysiquePersonnageIntegration.TransitionPenteSolPlatSansAACoup** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1684`</sub> | La transition entre une pente et le sol plat se fait sans à-coup ni blocage : la position verticale varie continûment. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `maxStep` est strictement inférieur à `0.5f`. |
| **PhysiquePersonnageIntegration.SuitUnArrondiAscendantEnMarchant** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1737`</sub> | Marcher sur un arrondi ascendant fait monter le personnage en suivant une courbe distincte d'une pente linéaire, sans le traverser. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bottom` est inférieur ou égal à `6.0f + TOLERANCE`.<br/>Vérifie que `bottom` est supérieur ou égal à `5.0f - TOLERANCE`.<br/>Vérifie que `sawMidSample` est vrai.<br/>Vérifie que `midBottom` est strictement inférieur à `5.3f`.<br/>Vérifie que `guard` est strictement inférieur à `600`.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai.<br/>Vérifie que `world.getComponent<core::Transform>(player).position.y` vaut `4.0f`, à `0.05f` près. |
| **PhysiquePersonnageIntegration.NiveauPenteFranchissable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1798`</sub> | Le niveau pente est franchissable en marchant, sans saut. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playLevelFile("demo-pente.json", rightOnly)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauPlaquePressionFranchissable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1814`</sub> | Le niveau plaque de pression est franchissable : la plaque ouvre la porte, un saut par-dessus le mur permet de rejoindre la sortie. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playReactiveFile("demo-plaque-pression.json", script)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauPlaquePressionExigeUnSaut** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1846`</sub> | Le niveau plaque de pression exige un saut : marcher sans sauter reste bloqué contre le mur. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playLevelFile("demo-plaque-pression.json", rightOnly)` diffère de `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauArrondiFranchissable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1864`</sub> | Le niveau arrondi est franchissable en marchant, sans saut. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playLevelFile("demo-arrondi.json", rightOnly)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.PlafondInclineBloqueSelonSaSilhouette** (Bloquant)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1881`</sub> | Une pente de plafond bloque un saut selon sa silhouette réelle, pas comme un carré plein uniforme. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `thinSideMinY` est strictement inférieur à `thickSideMinY - 0.3f`. |
| **PhysiquePersonnageIntegration.PlafondInclineSupportePersonnageParLeDessus** (Bloquant)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1939`</sub> | La face du haut d'une pente de plafond supporte le personnage qui tombe dessus par le dessus. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `player_.grounded` est vrai.<br/>Vérifie que `transform.position.y` vaut `static_cast<float>(CEILING_ROW) - core::kPlayerHeight`, à `0.05f` près. |
| **PhysiquePersonnageIntegration.SuitUnArrondiConcaveAscendantEnMarchant** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:1980`</sub> | Marcher sur un arrondi concave ascendant suit une courbe inversée par rapport à l'arrondi convexe, sans le traverser. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `bottom` est inférieur ou égal à `6.0f + TOLERANCE`.<br/>Vérifie que `bottom` est supérieur ou égal à `5.0f - TOLERANCE`.<br/>Vérifie que `sawMidSample` est vrai.<br/>Vérifie que `midBottom` est strictement supérieur à `5.7f`.<br/>Vérifie que `guard` est strictement inférieur à `600`.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai.<br/>Vérifie que `world.getComponent<core::Transform>(player).position.y` vaut `5.0f - size.y`, à `0.05f` près. |
| **PhysiquePersonnageIntegration.ConcaveDePlafondBloqueSelonSaSilhouette** (Bloquant)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2058`</sub> | Un arrondi concave de plafond bloque un saut selon sa silhouette réelle, pas comme un carré plein uniforme. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `thinSideMinY` est strictement inférieur à `thickSideMinY - 0.3f`. |
| **PhysiquePersonnageIntegration.ConcaveDePlafondSupportePersonnageParLeDessus** (Bloquant)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2122`</sub> | La face du haut d'un arrondi concave de plafond supporte le personnage qui tombe dessus par le dessus. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `player_.grounded` est vrai.<br/>Vérifie que `transform.position.y` vaut `static_cast<float>(CEILING_ROW) - core::kPlayerHeight`, à `0.05f` près. |
| **PhysiquePersonnageIntegration.ArrondisConcavesDeSolAdjacentsSansChuteALaJointure** (Bloquant)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2166`</sub> | Deux arrondis concaves de sol adjacents restent praticables à leur jointure, sans chute jusqu'au fond de la case voisine. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `guard` est strictement inférieur à `600`.<br/>Vérifie que `maxBottomNearPeak` est strictement inférieur à `5.4f`.<br/>Vérifie que `world.getComponent<core::Player>(player).grounded` est vrai.<br/>Vérifie que `world.getComponent<core::Transform>(player).position.y` vaut `6.0f - size.y`, à `0.05f` près. |
| **PhysiquePersonnageIntegration.ConcaveDePlafondBordFinResteBloqueSansTeleportation** (Bloquant)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2238`</sub> | Un saut bloqué près du bord fin d'un arrondi concave de plafond reste bloqué durablement, sans téléportation au-dessus du plafond au pas suivant. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `y` est strictement supérieur à `static_cast<float>(CEILING_ROW) - 0.5f`. |
| **PhysiquePersonnageIntegration.ConcaveDePlafondBloqueMemeEnMarchantPendantLeSaut** (Bloquant)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2305`</sub> | Sauter tout en marchant vers le bord épais d'un arrondi concave de plafond bloque toujours le saut, en combinant les deux mouvements. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `minTop` est strictement supérieur à `2.9f`.<br/>Vérifie que `minTop` est strictement supérieur à `2.9f`.<br/>Vérifie que `minTop` est strictement supérieur à `2.9f`.<br/>Vérifie que `minTop` est strictement supérieur à `2.9f`. |
| **PhysiquePersonnageIntegration.ConcaveDePlafondBloqueMemePresDuBordDeSaPropreCase** (Bloquant)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2397`</sub> | Un arrondi concave de plafond bloque un saut visant son bord plein, même tout près du bord de sa propre case. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `minY` est strictement supérieur à `3.0f`. |
| **PhysiquePersonnageIntegration.NiveauDoubleSautFranchissable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2456`</sub> | Le niveau double saut est franchissable en enchaînant saut au sol et saut aérien. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playReactivePhysicsOnly("demo-double-saut.json", script)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauDoubleSautRequiertLeSautAerien** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2488`</sub> | Le niveau double saut exige le saut aérien : un seul saut ne suffit pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playReactivePhysicsOnly("demo-double-saut.json", script)` diffère de `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauWallJumpFranchissable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2510`</sub> | Le niveau wall jump est franchissable en enchaînant wall slide et wall jump. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playReactivePhysicsOnly("demo-wall-jump.json", script)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauBlocFranchissable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2536`</sub> | Le niveau bloc poussable est franchissable en poussant le bloc contre le mur pour s'en servir de marche. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playReactiveFile("demo-bloc.json", script)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauBlocReduitFranchissable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2568`</sub> | Le niveau bloc à taille réduite est franchissable, le bloc comblant la fosse à la hauteur du chemin. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playReactiveFile("demo-bloc-reduit.json", script)` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauBudgetFranchissable** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2592`</sub> | Le niveau budget est franchissable avec exactement le budget de sauts prévu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `outcome` vaut `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauBudgetRequiertLesDeuxSauts** (Majeur)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2630`</sub> | Le niveau budget exige ses deux sauts : un seul saut autorisé ne suffit pas. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `loaded.ok()` est vrai.<br/>Vérifie que `outcome` diffère de `core::LevelOutcome::Won`. |
| **PhysiquePersonnageIntegration.NiveauFinalFranchissable** (Bloquant)<br/><sub>`Source/Test/Integration/test_physique_personnage.cpp:2668`</sub> | Le niveau final combine plusieurs mécaniques en un seul parcours cohérent et reste franchissable. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `playReactiveFile("demo-final.json", script)` vaut `core::LevelOutcome::Won`. |

## Tests système (3)

### Parcours Complet — `test_parcours_complet.cpp` (1)

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **ParcoursCompletSysteme.FranchitTouteLaSequence** (Bloquant)<br/><sub>`Source/Test/Systeme/test_parcours_complet.cpp:201`</sub> | Parcours complet : chaque niveau de la séquence est franchi (`Won`) dans l'ordre, puis « retour au titre ». Reproduit la boucle titre → niveau 1 → niveau 2 → titre du jeu. | 1. Mettre en place le contexte du test (arrangement).<br/>2. Executer le scenario et verifier les assertions. | Vérifie que `sequence.empty()` est faux.<br/>Vérifie que `playLevel(level)` vaut `core::LevelOutcome::Won`. |

### Éditeur de niveaux — `test_parcours_edition.cpp` (2)

| Titre (criticité) | Brief | Étapes | Résultat attendu |
|---|---|---|---|
| **ParcoursEditionSysteme.CreerEditerEnregistrerRechargerEtJouer** (Critique)<br/><sub>`Source/Test/Systeme/test_parcours_edition.cpp:26`</sub> | Parcours complet d'édition : peindre, lier un mécanisme, annuler une erreur, redimensionner, enregistrer, recharger, et confirmer que le niveau produit est directement jouable. | 1. Peindre un niveau (entrée, sortie, interrupteur/porte liés) dans un `LevelDraft`.<br/>2. Peindre une tuile par erreur puis l'annuler (`undo`).<br/>3. Redimensionner la grille.<br/>4. Valider, enregistrer sur disque, recharger.<br/>5. Vérifier le contenu rechargé et l'issue (`evaluateOutcome`) à l'entrée et à la sortie. | Vérifie que `draft.undo()` est vrai.<br/>Vérifie que `draft.tileMap().tile(1, 1)` vaut `core::TileType::Empty`.<br/>Vérifie que `draft.tileMap().width()` vaut `6`.<br/>Vérifie que `draft.tileMap().tile(4, 1)` vaut `core::TileType::Exit`.<br/>Vérifie que `validated.ok()` est vrai.<br/>Vérifie que `core::LevelWriter::saveToFile(*validated.level, path)` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `level.entry()` vaut `(core::GridPosition{0, 1})`.<br/>Vérifie que `level.exit()` vaut `(core::GridPosition{4, 1})`.<br/>Vérifie que `level.jumpBudget()` vaut `2`.<br/>Vérifie que `level.tileMap().tile(1, 1)` vaut `core::TileType::Empty`.<br/>Vérifie que `level.mechanisms().size()` vaut `1u`.<br/>Vérifie que `level.mechanisms().front().switchPosition` vaut `(core::GridPosition{2, 0})`.<br/>Vérifie que `level.mechanisms().front().doorPosition` vaut `(core::GridPosition{2, 2})`.<br/>Vérifie que `core::evaluateOutcome(atEntry, level)` vaut `core::LevelOutcome::Playing`.<br/>Vérifie que `core::evaluateOutcome(atExit, level)` vaut `core::LevelOutcome::Won`. |
| **ParcoursEditionSysteme.EditeConfigureEnregistreEtRechargeLesDangersAvances** (Critique)<br/><sub>`Source/Test/Systeme/test_parcours_edition.cpp:95`</sub> | Parcours d'édition des dangers avancés : peindre, lier, configurer, enregistrer, recharger. | 1. Peindre les sept nouveaux types de danger dans un `LevelDraft`.<br/>2. Lier un interrupteur à un danger commuté (même geste qu'une porte).<br/>3. Configurer l'axe/la portée d'un danger mobile et la période/le déphasage d'un danger temporisé.<br/>4. Enregistrer sur disque, recharger. | Vérifie que `validated.ok()` est vrai.<br/>Vérifie que `core::LevelWriter::saveToFile(*validated.level, path)` est vrai.<br/>Vérifie que `reloaded.ok()` est vrai.<br/>Vérifie que `level.tileMap().tile(1, 0)` vaut `core::TileType::DangerUp`.<br/>Vérifie que `level.tileMap().tile(2, 0)` vaut `core::TileType::DangerRight`.<br/>Vérifie que `level.tileMap().tile(4, 0)` vaut `core::TileType::DangerSwitched`.<br/>Vérifie que `level.tileMap().tile(5, 0)` vaut `core::TileType::DangerMover`.<br/>Vérifie que `level.tileMap().tile(6, 0)` vaut `core::TileType::DangerBlink`.<br/>Vérifie que `level.dangerLinks().size()` vaut `1u`.<br/>Vérifie que `level.dangerLinks().front().triggerPosition` vaut `(core::GridPosition{3, 0})`.<br/>Vérifie que `level.dangerLinks().front().dangerPosition` vaut `(core::GridPosition{4, 0})`.<br/>Vérifie que `level.moverConfigs().size()` vaut `1u`.<br/>Vérifie que `level.moverConfigs().front().axis` vaut `core::DangerMoverAxis::Vertical`.<br/>Vérifie que `level.moverConfigs().front().range` vaut `2`.<br/>Vérifie que `level.blinkConfigs().size()` vaut `1u`.<br/>Vérifie que `level.blinkConfigs().front().period` vaut `90`.<br/>Vérifie que `level.blinkConfigs().front().phase` vaut `15`.<br/>Vérifie que `level.blinkConfigs().front().activeDuration` vaut `30`. |
